# Facet public-tip allowlist — one pattern per line. A tracked path must match one
# of these to be part of the public branch tip; anything else must be archived by
# tools/archive_private.py (or explicitly deleted) before the tree is public.
# Patterns: a trailing "/" allows the whole directory; otherwise an exact path or a
# shell glob (fnmatch) against the repo-relative path. "!" negates a pattern that a
# broader allow would otherwise admit (evaluated after allows).
README.md
LICENSE
THIRD_PARTY_NOTICES.md
CHANGELOG.md
CONTRIBUTING.md
SECURITY.md
AGENTS.md
.gitignore
.gitattributes
# ^ marks src/vendor/compose/** and skills/compose/** as generated, so a diff
#   view collapses the vendored pin instead of reading as hand-written code.
rokit.toml
stylua.toml
.styluaignore
# ^ Luau formatting configuration and the exclusion preserving pinned vendor bytes.
run-tests.sh
requirements.json
src/
assets/
examples/
docs/guide/
docs/reference/api.md
docs/reference/constitution.md
docs/extending/
docs/MAINTAINERS.md
tests/
bench/
tools/
skills/
.github/
package/
artifacts/theme-packages-and-skinning/final-neutral-dump.json
artifacts/distribution-readiness/
# ^ the OPEN stage's own ledgers: allowed while the stage runs, archived at close.
docs/superpowers/
# ^ the OPEN campaign's own plan and spec drafts (FacetBench + the perf fix
#   campaign): internal planning, not shipped surface. Same rule as the ledgers
#   above — allowed while the campaign runs, archived by tools/archive_private.py
#   at close, so the public tip never carries a plan for work that is finished.
docs/plans/
# ^ the wicked-fast campaign's goal + operating-rules docs: the same class as
#   docs/superpowers/ above, archived by the same tool at close.
!tools/lune/gate_manifest.luau
!tools/lune/gate_legacy.luau
!tools/lune/verify/convert_manifest.py
# ^ the retired gate path and its one-shot converter: archived with phases.json;
#   the graph is the living system and check_manifest_integrity audits it.
# --- never public, even inside an allowed directory ---
!tools/lune/_probe_*.luau
!tools/lune/_tmp/
!tools/__pycache__/
!examples/table_phaseb/
!src/core/fusion_adapter.luau
!tests/fusion_vendor.spec.luau
!tests/lib/fusion_*.luau
# --- secret-shaped files are never public, even inside an allowed tree ---
!*API_KEYS*
!*.env
!*/.env
!*.key
!*.pem
!*id_rsa*
!*.keychain

# Native-cutover evidence linked by PR22; retained for verification review.
artifacts/compose-simplification/final-full-report.json
artifacts/compose-simplification/final-performance.json
artifacts/compose-simplification/final-readiness.json
artifacts/compose-simplification/gap-closure-live.json
artifacts/compose-simplification/verification.md
artifacts/studio-live/
artifacts/captures/
artifacts/gamepad/
artifacts/performance-stress-places/studio/
artifacts/compose-simplification/gallery-parity-audit.md
artifacts/compose-simplification/live-comparison.json
artifacts/compose-simplification/virtual-monitors-parity-audit.md
# ^ live Studio evidence that tools/lune/verification_parity.json and the guides cite; removing it breaks the citations.
