--!strict
return {
	sensory = require(script.sensory),
	dataset = require(script.dataset),
	rows = require(script.rows),
	capture = require(script.capture),
	workloads = require(script.workloads),
	screen = require(script.perf_lab),
}
