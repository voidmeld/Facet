# Facet — provenance ledger

Every item in the repository that Facet did not author, with origin, licence, the notice it
requires, and whether redistributing it inside a public MIT-licensed repo is fine.

**Audited at `27c0afd`, which is exactly `origin/main`** — the commit the private GitHub repo holds.
Verdicts marked `[VERIFY]` are not confirmed; each says what would confirm it.

> **Drift during the audit.** 11 unpushed commits landed locally (`27c0afd..17ae422`). Two matter here:
> a root `LICENSE` (MIT, `Copyright (c) 2026 Josh Anon`) and a 15,410-byte `THIRD_PARTY_NOTICES.md` now
> exist — see item 24 — and `vendor/Fusion/` plus the two comparison docs are deleted in the working tree,
> which takes the "drop it" branch of item 1. Item 18, the rights blocker, is **unaffected**: all 11
> Roblox-app captures are still present at `17ae422`.

## Ledger

| # | Item | Origin | Licence | Redistribution verdict | Notice required |
|---|---|---|---|---|---|
| 1 | `vendor/Fusion/` (69 files) | github.com/dphfox/Fusion, tag `v0.3-beta`, `src/` copied verbatim then patched | MIT | **Fine as-is.** MIT inside MIT; `vendor/Fusion/LICENSE` already travels with the code, which satisfies MIT's notice clause | Full MIT text + `Copyright (c) 2024 Daniel P H Fox` — see notices §1 |
| 2 | `assets/icons/` (13 icons) | Original. Generated by `assets/icons/source/generate_icons.py` (Pillow, flat geometry drawn at 8× and Lanczos-downsampled) | Facet's own | **Fine as-is.** Not third-party | None |
| 3 | `assets/themes/compact-pointer/` | Original. `generate_art.py`, seed `0xC0DE`, Pillow + numpy | Facet's own | **Fine as-is** | None |
| 4 | `assets/themes/fantasy-ornate/` | Original. `generate_art.py`, seed `0x60D1` | Facet's own | **Fine as-is** | None |
| 5 | `assets/themes/fantasy-parchment/` | Original. `generate_art.py`, seed `0x5EED` | Facet's own | **Fine as-is** | None |
| 6 | `assets/themes/glossy-touch/` | Original. `generate_art.py`, seed `0x6E43` | Facet's own | **Fine as-is** | None |
| 7 | `assets/themes/ornate-gauge/` | Original. `generate_art.py`, seed `0x9A17`; PNG SHA-256s recorded in `provenance.md` | Facet's own | **Fine as-is** | None |
| 8 | `assets/themes/pixel-quest/` | Original. `generate_art.py`, seed `0x8B17` | Facet's own | **Fine as-is** | None |
| 9 | `examples/gallery/examples/words/*.luau` (word data) | **Derived from SCOWL 2020.12.07.** Archive `downloads.sourceforge.net/project/wordlist/SCOWL/2020.12.07/scowl-2020.12.07.tar.gz`, SHA-256 `5587667c…b859cc` pinned in both `PROVENANCE.md:14` and `tools/build_word_lists.py:72`. The archive itself is gitignored (`tools/.wordlist-cache/`) and never ships — only generated output does | Composite permissive: Kevin Atkinson's collective-work notice over 12dicts (Alan Beale), Moby (Grady Ward, public domain), Brian Kelk's wordlist (public domain), WordNet (Princeton), ENABLE (public domain), UKACD (J Ross Beresford), VarCon (Atkinson/Titze/Kuenning, BSD-style) | **Fine as-is.** SCOWL explicitly grants distribution of "the output created from the scripts" provided the notices travel with the copies — and they already do | **Yes, and it is already present verbatim** at `examples/gallery/examples/words/PROVENANCE.md` lines 92–341. See notices §2 |
| 10 | Fonts | Referenced only as `rbxasset://fonts/families/{Fondamento,Michroma,Nunito,BuilderSans}.json` — the Roblox client's own catalog | N/A — nothing embedded | **Fine as-is.** A runtime reference, like `font-family:` in CSS. No font file exists in this repo on any ref | None |
| 11 | Audio | None. No `.mp3`/`.wav`/`.ogg` on any ref | — | **N/A** | None |
| 12 | Example copy — `02_playlist_table.luau` song/artist rows | Invented (`Neon Straightaway / Chrome Coyote`, `Drift City Nights / Nine Volt`, …) | Facet's own | **Fine as-is.** No real songs or artists | None |
| 13 | Example copy — `01_temperature_converter`, `06_tile_game`, `07_match3` | Original invented copy | Facet's own | **Fine as-is** | None |
| 14 | `examples/gallery/examples/05_word_game.luau:721` — on-screen string `"Facet Wordle"` | Uses a third-party **trademark** as a product name for Facet's own screen | N/A — trademark, not copyright | **Needs a fix before shipping.** "Wordle" is a live NYT mark; naming Facet's screen after it is use-as-a-mark, not nominative comparison. Also pinned in 3 lines of `tools/lune/check_flat_baseline.luau` | Rename the string; no notice fixes this |
| 15 | `docs/reference/swiftui-parity.md` + the 151 `SW-###` citation rows | Short single/double-sentence quotations from Apple developer docs, HIG and WWDC, each with a live link and access date | Quotation, not licence | **Fine as written.** Attributed, comparative, transformative; no code fences; a scan for quoted blocks >150 chars found no real Apple quote. The existing distribution plan removes the file anyway, which is a stronger position | None |
| 16 | `artifacts/swiftui-reference-app-validation/` (Food Truck, Fruta, Backyard Birds notes) | Clean-room behavioral paraphrase of Apple sample apps. `sources.md` states the IP boundary; the sample archives were inspected outside the repo and never committed | N/A | **Fine as-is.** Verified: no sample `.zip` and no sample `.swift` is tracked. The only two tracked `.swift` files are Facet's own `tools/studio/capture/{crop,window_id}.swift` | None |
| 17 | React-Lua / Roact / Vide references in `docs/reference/` | Same short-quote-with-link pattern | Quotation | **Fine as-is** | None |
| 18 | **`docs/plans/reference-media/2026-08-16-roblox-app-navigation/{f1,r1,r2,r3}-*` (11 files, 5.91 MB)** | **Screen captures and recordings of the official Roblox mobile app**, taken as UX reference. Described as such in `docs/plans/navigation-and-menus-brief.md`: the avatar editor with its "Post Avatars to Marketplace" coach-mark, the "Top Trending" popup list, the "For you / Charts" tab bar, the segmented control | **None. Roblox Corporation's UI and trade dress** | **MUST REMOVE — from the working tree *and* from git history.** There is no licence to redistribute another company's app UI, and no notice that makes it permissible. This is the single hard rights blocker in the repo | No notice can cure this |
| 19 | `docs/plans/reference-media/.../hud-*` (4 files, 0.89 MB) | Facet's own HUD showcase captures (`examples/gallery/scenarios/hud.luau`) | Facet's own | **Fine as-is** | None |
| 20 | `artifacts/.../roblox-home-narrow-observation.png` | A Roblox app screenshot on disk | N/A | **Non-issue.** Verified never committed: covered by `.gitignore`'s `artifacts/**/*.png` and absent from `git log --all --full-history` | None — but never force-add it |
| 21 | `rbxassetid://…` content ids in `src/`, `examples/`, `assets/` | Facet's own Roblox Open Cloud uploads, recorded in `assets/icons/upload-manifest.json` and `assets/themes/*/upload-manifest.json` (all `Approved`, type `Image`). Sentinels `1`, `0`, `999999999999999` are test fixtures | Facet's own | **Fine as-is.** Public content ids for assets this account published | None |
| 22 | `rbxthumb://type=AvatarHeadShot&id=…` | Live calls to Roblox's thumbnail service | N/A — nothing embedded | **Fine as-is** | None |
| 23 | `examples/places/*.rbxl` (14 at tip) | Built by `rojo build` from the audited Luau source (`tools/build_places.sh`) | Presumed Facet's own | **`[VERIFY]`** — should be a strict subset of items 2, 3–8 and 21, but `.rbxl` is LZ4-chunk-compressed, so a `strings` pass cannot rule out an asset dragged in by hand during a Studio session. **What would confirm it:** diff a fresh `rojo build` against each tracked binary, or parse them with an rbxl format tool | None pending verification |
| 24 | **Root `LICENSE`** | Absent at `27c0afd`; **added at `17ae422`** (unpushed) | MIT, `Copyright (c) 2026 Josh Anon` | **Resolved after the audit began.** A 15,410-byte `THIRD_PARTY_NOTICES.md` landed with it, covering Facet's own art, SCOWL (full verbatim block, UKACD clause included — verified), and the toolchain | None outstanding, provided those commits are pushed before the repo is made public |

## Verification notes

- **Fusion LICENSE integrity.** `git log --oneline -- vendor/Fusion/LICENSE` returns exactly one
  commit (`6a4b59c`, the initial import). The file has never been modified. Its first three lines
  read `MIT License` / blank / `Copyright (c) 2024 Daniel P H Fox`.
- **Fusion local patches.** `vendor/Fusion/VENDOR.md` records the upstream as tag `v0.3-beta` with
  `src/` copied verbatim. Patches are mechanical: `require(Package.X)` → `require("../X")` string-require
  form across 54 files, plus one scheduler guard in `init.luau` (`doTaskImmediate` runs callbacks
  synchronously, because the upstream queue-and-yield variant kills Lune's main thread). No logic changed.
- **SCOWL notice location.** `examples/gallery/examples/words/PROVENANCE.md` is 16,073 bytes / 341 lines.
  The "Required notices" section starts at line 85; the verbatim `Copyright` block is lines 92–341.
  Copyright lines confirmed present at 93, 96, 186 (WordNet/Princeton), 265 (UKACD/Beresford),
  287 (VarCon/Atkinson), 297 (Titze), 309 (Kuenning).
- **The UKACD clause is the strict one.** It requires that "the copyright notice must be prominently
  displayed and the text of this document must be included verbatim." Today that is satisfied only
  because the whole SCOWL `Copyright` file is reproduced. **Never truncate or summarise that block.**
- **No font, no audio.** Searched all refs for `.ttf`, `.otf`, `.woff`, `.woff2`, `.mp3`, `.wav`,
  `.ogg`: zero blobs. The 673 binary blobs in history are 530 `.rbxl`, 135 `.png`, 4 `.jpeg`,
  3 `.mov`, 1 `.mp4`.

## What has to happen before the repo is public

1. **Purge the 11 Roblox-app captures from history** (item 18). This is the only rights blocker.
2. ~~Add a root `LICENSE` and a `THIRD_PARTY_NOTICES.md`~~ (item 24) — **done at `17ae422`**, unpushed.
   Push it.
3. **Rename the `"Facet Wordle"` on-screen string** (item 14) and re-pin the flat baseline.
4. **Finish the `vendor/Fusion/` removal** (item 1). The working tree already deletes it and the new
   `THIRD_PARTY_NOTICES.md` has no Fusion section, which is consistent. What is not clean is landing
   the notices file without the deletion: that would ship Fusion's code with no notice for it.
5. **Optionally verify the `.rbxl` binaries** by rebuild-diff (item 23), if they are going to ship.


## 2026-09-17 addendum: official Roblox Signals

The migration vendors Signals 0.9.0 from Roblox/signals commit
`7ef2ff7db01f6955cf7d9e5a0becb3129f7f8d60`. Four runtime modules retain the
upstream algorithm; only requires/extensions are adapted. Source blob IDs and
the adapted byte hashes are recorded in `src/vendor/signals/README.md` and
`UPSTREAM.lock`. The MIT copyright and full permission notice are preserved
in `LICENSE`, the package-carried `license.luau`, and `THIRD_PARTY_NOTICES.md`.
The dependency inventory gate and offline package verification passed against
this explicit vendor allowlist. This addendum records the new dependency; it
does not re-adjudicate the historical audit's unrelated assets or release gates.


## 2026-09-20 addendum: current Compose dependency

The earlier audit and Signals addendum above are historical. The current runtime
ships Compose from `https://github.com/voidmeld/compose.git` at commit
`5f6bf7d081bab39971b116e47d6eb9ccf5877282`. The committed, generated snapshot is
read-only: `patches` is empty, and `UPSTREAM.lock` inventories 68 files by
SHA-256. `python3 tools/sync_compose.py --check` verifies the snapshot without
rewriting it. The complete upstream MIT notice remains in the vendored LICENSE,
package-carried license module and THIRD_PARTY_NOTICES.md. This entry records the
current dependency facts; it does not re-adjudicate the historical asset audit
or authorize publication.


## 2026-09-21 addendum: local Compose recovery candidate

The 2026-09-20 dependency entry records the previous official pin. The current
generated snapshot uses LOCAL/UNPUBLISHED commit
`974b7527709a4db61024cc8994cdce30adc49790`, based on official
`5f6bf7d081bab39971b116e47d6eb9ccf5877282`. It adds two calls to the existing
dependency-rearming operation in failed reactive-consumer paths; successful
evaluation and the MIT license/notice bytes are unchanged. This is a committed
source repair, not a post-generation vendor patch. UPSTREAM.lock records its
exact commit and file hashes with an empty patches list; sync_compose.py --check
verifies the checked-in offline snapshot. The source candidate is not claimed
to be publicly fetchable from the official repository. THIRD_PARTY_NOTICES.md
now distinguishes the official base from this local candidate. These inspected
provenance facts do not assert publication approval or new Studio/device results.

## 2026-09-24 addendum: official Compose pin

The generated snapshot now uses commit
`cbab6e0f828b68faaf676ee718cb0036c20ed935` on the official `main` branch of
voidmeld/compose, a descendant of `5f6bf7d081bab39971b116e47d6eb9ccf5877282`.
It supersedes the local candidate `974b7527709a4db61024cc8994cdce30adc49790`
and does not carry that candidate's rearming calls. UPSTREAM.lock records the
commit and file hashes with an empty patches list; sync_compose.py --check
verifies the checked-in offline snapshot. The MIT license and notice bytes are
unchanged. These provenance facts do not assert publication approval or new
Studio/device results.
