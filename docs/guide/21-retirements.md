# Retired promises

This page lists each behavior that `main` promised and that the native cutover no longer promises. Use it to review the retirements before the merge. The data comes from these files:

- [`tools/lune/verification_parity.json`](../../tools/lune/verification_parity.json), class b and class c contracts.
- The strengthening results of the parity work (weaker contracts A, B and C). They retire parts of covered contracts.
- The `tools/lune/coverage_*.json` retirement reasons.

The baseline is `main` at `a8c88956`. The branch is `codex/compose-ui-simplification` at `a358bed6`. Current `main` is `bc9a56a6`. It has the same public model as the baseline and adds to it.

## Summary

| Kind | Contracts | Main cases |
|---|---:|---:|
| Class b: the Roblox Engine or Compose owns the mechanism | 255 | 1301 |
| Class b with no candidate test and no live record | 123 | 625 |
| Class c: the feature or the code is deleted | 842 | 6015 |
| Restored before the merge: moved from class b or c to class a | 11 | 92 |
| Parts of covered contracts that the strengthening work retired | 117 | - |
| Owner decisions (next section) | 21 | - |

Most class c contracts tested the deleted solver, renderer, focus graph, input system, presenter and paint layer. Their retirement is correct when the public API no longer makes the promise. The next section lists the retirements that a player or a game author can see.

## Owner decisions

Each item below removed a behavior that a player or a game author uses. The owner decided each item. The "Decision" line gives the result:

- Restored: Facet has the behavior again. [Restored before the merge](#restored-before-the-merge) names the change and its tests.
- Restored by another change: a separate branch restores the behavior.
- Accepted: the retirement stays. The line names the native replacement that a game uses.

### 1. Screen-level entry focus

- Main promised: When a screen or a surface appeared, Facet put the focus on a declared or first control (`UI.focusSection` `focusOnAppear`/`returnFocus`, presenter `initialFocus`), and the initial focus showed its ring.
- Main evidence: 4 contracts, 33 main cases: `apps-153` (7, class c, `focus_section_appear`); `apps-170` (22, class c, `keyboard_navigation`); `inputs-04` (3, class b, `auto_input`); `apps-187` (1, class b, `navigation_groups`).
- Strengthening notes that retire part of a covered contract: `apps-159`, `mech2-104`.
- PR status: The PR gives entry selection only to modal controls (Alert, Sheet, Menu) and to collections that you give a focus option. On an ordinary screen, a gamepad or keyboard player starts with no selection until the engine picks one. `docs/guide/07-input.md` promises entry only for modals.
- Current main `bc9a56a6` still ships this.
- Decision: Restored. The first D-pad press selects the first control of the active screen. [Restored before the merge](#restored-before-the-merge) names the tests.

### 2. Nearest-survivor focus after a removal

- Main promised: When the focused row or control was removed, focus moved to the nearest surviving neighbour (`focusPolicy`, index policy).
- Main evidence: 2 contracts, 18 main cases: `collections-260` (17, class c, `virtual_list_focus_policy`); `collections-306` (1, class c, `virtualization`).
- Strengthening notes that retire part of a covered contract: `apps-147`, `apps-185`.
- PR status: Compose `createFocusScope` clears the focus or moves it to the first row. A gamepad player loses the position in a long list after a delete.
- Current main `bc9a56a6` still ships this.
- Decision: Restored. A removed control gives the selection to its nearest neighbour, and a collection keeps it on the next row by key. [Restored before the merge](#restored-before-the-merge) names the tests.

### 3. Tab and Shift+Tab traversal

- Main promised: Tab and Shift+Tab walked all focus stops in order, with entry, group crossing, wrap and skip rules, also inside the radial ring.
- Main evidence: 4 contracts, 22 main cases: `apps-158` (15, class b, `keyboard_navigation`); `apps2-169` (1, class b, `playlist_columns`); `apps2-222` (1, class b, `recipes_common`); `apps-151` (5, class b, `focus_chrome`).
- Strengthening notes that retire part of a covered contract: `apps2-210`.
- PR status: The PR binds no Tab key (`Button` refuses Tab as a shortcut). Roblox engine selection moves by direction only. The b-class record says `nativeEvidence: none`. The live run `artifacts/studio-live/needs_live_input-power-off-traversal-portrait-largest.json` records the check `Tab reaches After` as failed: Tab selected no control, while DPadDown did.
- Current main `bc9a56a6` still ships this.
- Decision: Restored. Tab and Shift+Tab walk the controls in layout order inside the active modal or screen. [Restored before the merge](#restored-before-the-merge) names the tests.

### 4. Ten-foot (viewing distance) profile, including the ten-foot focus ring

- Main promised: A distant-TV profile changed the type ladder, the hit floor, the overscan margins, the 66 px focus floor and the focus ring, stacked Alert actions and centred Sheet panels.
- Main evidence: 6 contracts, 83 main cases: `mech2-93` (25, class c, `paradigm_tenfoot`); `mech3-62` (48, class c, `ten_foot_metrics`); `navigation-2-21` (1, class c, `distance_gamepad`); `navigation-1-15` (1, class c, `alert`); `navigation-4-46` (1, class c, `sheet`); `apps2-96` (7, class b, `gallery_chrome`).
- Strengthening notes that retire part of a covered contract: `navigation-1-13`, `navigation-1-01`, `navigation-4-41`, `apps-190`, `paint-85`, `apps2-134`, `inputs-53`.
- PR status: The PR has no environment distance fact. The gallery keeps only a `UIScale` 1.5 preview. Console players get desktop density unless each game scales its own UI.
- Current main `bc9a56a6` still ships this.
- Decision: Accepted. The engine selection highlight and `GuiObject.SelectionImageObject` replace the ten-foot focus ring. A game that needs a distance profile scales its own UI, for example with `UIScale`.

### 5. Theme-owned focus indicator

- Main promised: A theme package set the focus ring or glow, its colour, its corners and its fit around skinned art.
- Main evidence: 6 contracts, 23 main cases: `paint-73` (5, class b, `native_style_scenario`); `themes-P2-31` (8, class c, `theme_chrome`); `themes-P5-20` (2, class b, `theme_reference_packages`); `themes-P5-43` (2, class b, `review_theme_corners`); `themes-P4-15` (3, class b, `theme_layer_application`); `apps-166` (3, class b, `keyboard_navigation`).
- Strengthening notes that retire part of a covered contract: `mech1-91`.
- PR status: The PR uses the engine default selection image (`SelectionImageObject` is not set). `theme_types.SkinLayer.kind` still lists `glow`, and two shipped packages still declare focus art that nothing reads (`themes-P2-31` removal text).
- Current main `bc9a56a6` still ships this.
- Decision: Accepted. The engine selection highlight is the focus indicator. A game that needs its own indicator sets `SelectionImageObject` on the control.

### 6. The application handle

- Main promised: `Facet.new()` made one application with `controls`, `mount`, `environment`, `presenter`, `onFrame`, `presentModal`, `presentAnchored`, `presentToast`, `installTheme` and `dispose`, and shut them down in a fixed order.
- Main evidence: 4 contracts, 39 main cases: `mech2-13` (10, class c, `host_core_lifetime`); `mech1-64` (21, class c, `client_host`); `mech2-94` (3, class c, `parentless_build_owner`); `apps2-06` (5, class c, `consumer_standalone`).
- Strengthening notes that retire part of a covered contract: `apps-125`.
- PR status: The caller now makes the runtime, the ScreenGui, the StyleSheet and the StyleLink, and calls `runtime.mount`, `stop` and `runtime:dispose`. Every main screen must change (ergonomics audit, section 5).
- Current main `bc9a56a6` still ships this.
- Decision: Accepted. The game uses the Compose runtime, `runtime.mount` (the mount helper), `RunService` for frame work and Compose animation for motion.

### 7. Adaptive environment facts

- Main promised: `app.environment` and `Facet.adaptive` published size class, orientation, interaction classes, display size, safe areas, locale, preview profiles and a theme choice by input class (`selectBy`).
- Main evidence: 9 contracts, 82 main cases: `mech1-02` (36, class c, `adaptive`); `mech3-11` (7, class c, `renderer`); `apps-236` (8, class c, `adaptive_defaults`); `apps2-233` (4, class c, `spatial`); `apps-132` (6, class c, `preview`); `apps2-04` (2, class c, `consumer_standalone`); `apps2-75` (1, class c, `examples_games`); `themes-P3-36` (13, class c, `theme_selectby`); `apps2-151` (5, class c, `perf_lab`).
- Strengthening notes that retire part of a covered contract: `mech1-121`.
- PR status: Controls read `UserInputService.PreferredInput` and native `AbsoluteSize` directly. A screen that must choose between two arrangements observes `AbsoluteSize` itself (`docs/guide/15-adaptive-recipes.md`). AGENTS.md on main still tells authors to read `app.environment`.
- Current main `bc9a56a6` still ships this.
- Decision: Accepted. Native layout replaces the environment facts, the `ViewThatFits` choice and the environment preview and reset: `AbsoluteSize`, `AutomaticSize`, `UIListLayout.Wraps` and `UserInputService.PreferredInput`.

### 8. Presenter toasts

- Main promised: `presentToast` queued, stacked, timed and dismissed toasts in a display band, with reduced-motion transitions and HUD reservations.
- Main evidence: 5 contracts, 34 main cases: `mech2-82` (2, class c, `native_toast`); `mech3-86` (13, class c, `toast_layer_seam`); `mech3-88` (13, class c, `toast_presentation`); `mech3-90` (1, class c, `toast_schedule`); `apps-220` (5, class c, `hud_reservations`).
- Strengthening notes that retire part of a covered contract: `mech3-87`, `mech3-89`, `apps2-226`, `mech2-12`.
- PR status: The toast is now a gallery scenario (`examples/gallery/scenarios/sponsor_toast.luau`). Each game writes its own queue and timers.
- Current main `bc9a56a6` still ships this.
- Decision: Restored by another change. The overlays port restores toasts.

### 9. Custom and anchored presentation

- Main promised: `app.presentModal` showed any component as a modal with a scrim and `outsideTapCancel`, and `presentAnchored` placed a surface at a source path or rectangle.
- Main evidence: 4 contracts, 41 main cases: `navigation-1-46` (7, class c, `anchored_surface`); `mech2-105` (21, class c, `presenter`); `mech1-84` (11, class c, `compose_scene`); `navigation-2-62` (2, class c, `modal_dismissal`).
- PR status: `UI.Alert` and `UI.Sheet` own presentation. A game that needs a custom modal body must fit it in a Sheet or build it on native objects.
- Current main `bc9a56a6` still ships this.
- Decision: Restored by another change. The overlays port restores custom and anchored presentation.

### 10. Server replication helpers

- Main promised: `Facet.replication` kept revisioned snapshots and collections, repaired gaps and ran optimistic mutations with confirm, reject and reset.
- Main evidence: 3 contracts, 24 main cases: `apps2-81` (17, class c, `replication`, `fuzz_replication`); `apps2-162` (6, class c, `platform_adapters_recovery`); `apps2-223` (1, class c, `replication`).
- PR status: `docs/guide/06-client-server.md` teaches a hand-written request model. The settings-sync example writes its own loopback.
- Current main `bc9a56a6` still ships this.
- Decision: Accepted. Server state is game domain. [Client and server](06-client-server.md) shows the request model.

### 11. Generic drag and drop

- Main promised: `UI.draggable` and `UI.dropTarget` gave hover, legality, drop, cancel, gamepad pickup and a drag proxy for any control.
- Main evidence: 3 contracts, 81 main cases: `inputs-84` (53, class c, `drag_public`); `inputs-85` (11, class c, `drag_session`); `apps2-246` (17, class c, `sponsor_scenarios`).
- PR status: Only table and list reorder keep a drag. A game uses `UIDragDetector` directly (the match-3 example does).
- Current main `bc9a56a6` still ships this.
- Decision: Accepted. A game uses the native `UIDragDetector`.

### 12. On-screen keyboard avoidance

- Main promised: A focused TextInput moved up out from under the on-screen keyboard, and `keyboardType` and `submitLabel` mapped to the keyboard.
- Main evidence: 3 contracts, 16 main cases: `apps-244` (1, class c, `adaptive_defaults`); `inputs-08` (1, class c, `auto_input`); `inputs-189` (14, class c, `text_input`).
- Strengthening notes that retire part of a covered contract: `inputs-10`.
- PR status: The PR leaves this to the engine TextBox. No test or live record shows that the engine keeps a field visible on a phone.
- Current main `bc9a56a6` still ships this.
- Decision: Accepted. The engine TextBox owns the on-screen keyboard.

### 13. Haptic and sensory feedback system

- Main promised: A twelve-verb feedback taxonomy, `UI.sensoryFeedback`, a feedback bus and a haptics adapter with rate limits and profiles.
- Main evidence: 4 contracts, 209 main cases: `inputs-75` (64, class c, `control_feedback`); `inputs-89` (104, class c, `haptics`); `inputs-169` (14, class c, `sensory_feedback`); `inputs-170` (27, class c, `sensory_profile`).
- Strengthening notes that retire part of a covered contract: `inputs-76`.
- PR status: The PR keeps one `pressHaptic` option that plays only on a value change. The motor output on a phone and a gamepad has no physical evidence (`tools/lune/parity_blockers.json` `pendingLiveRisks`).
- Current main `bc9a56a6` still ships this.
- Decision: Accepted. A game plays a native `HapticEffect`. The controls play the factory `pressHaptic` option for a change of state or value.

### 14. Table selection and editing model

- Main promised: A click moved keyboard focus, a plain click replaced a multi selection, Ctrl-click toggled, Ctrl or Shift with an arrow extended the selection, a double click ran `onPrimaryAction`, and the table owned its Edit/Done toggle.
- Main evidence: 7 contracts, 35 main cases: `collections-148` (2, class c, `table`); `collections-150` (2, class c, `table`); `collections-198` (4, class c, `table_input`); `collections-185` (19, class c, `table_input`); `collections-105` (1, class c, `row_actions_scenario`); `collections-41` (3, class c, `paradigm_table`); `collections-38` (4, class c, `paradigm_table`).
- Strengthening notes that retire part of a covered contract: `apps2-179`.
- PR status: In multi mode every activation toggles. Arrows do not select. The caller owns `editing`. Desktop players lose the usual list selection keys.
- Current main `bc9a56a6` still ships this.
- Decision: Restored in part. The multi-select keys are restored: a plain click replaces, Ctrl-click or Cmd-click toggles, Shift-click and Shift with an arrow extend. A plain arrow still does not select. The decision does not cover `onPrimaryAction` and the table-owned Edit and Done toggle. They stay retired.

### 15. Row tray dismissal

- Main promised: A tap outside an open row tray closed it without swallowing the tap.
- Main evidence: 3 contracts, 13 main cases: `collections-78` (1, class c, `row_actions_input`); `collections-90` (2, class c, `row_actions_input`); `collections-285` (10, class c, `virtual_list_row_actions`).
- PR status: The PR RowActions has no outside catcher. A tray stays open until the row closes it.
- Current main `bc9a56a6` still ships this.
- Decision: Restored.

### 16. Error boundary control

- Main promised: `UI.ErrorBoundary` contained a failing region, showed a fallback and retried a failing row factory.
- Main evidence: 1 contracts, 8 main cases: `mech1-115` (8, class c, `error_boundary`).
- PR status: The PR contains failures only for Alert content and NavigationStack destinations. Main `bc9a56a6` extended this control (commit `e09f648c`, G1), so the port to main must decide.
- Current main `bc9a56a6` still ships this.
- Decision: Restored. The presenter critical screen stays retired.

### 17. Declarative motion API

- Main promised: `UI.withAnimation`, region `transition` on `UI.When` and `UI.ForEach`, the marquee reveal of truncated text, the typewriter stream and the value count-up.
- Main evidence: 5 contracts, 68 main cases: `mech3-99` (29, class c, `with_animation`); `mech1-105` (10, class c, `declarative_animation`); `mech3-78` (14, class c, `text_reveal`); `mech3-79` (5, class c, `text_reveal_stream`); `mech3-96` (10, class c, `value_reveal`).
- Strengthening notes that retire part of a covered contract: `apps2-155`, `mech3-98`.
- PR status: Authors compose `runtime.spring` and `runtime.tween`. Controls animate by default since `4b9ab031`. AGENTS.md on main still teaches `transition` on `UI.When` and `UI.ForEach`.
- Current main `bc9a56a6` still ships this.
- Decision: Accepted. Controls animate by default, and a game uses Compose animation (`runtime.spring`, `runtime.tween`) for its own motion.

### 18. Text fit and middle truncation

- Main promised: `textSize = "fit"` chose the largest size that fits, and long values could truncate in the middle.
- Main evidence: 2 contracts, 32 main cases: `mech3-83` (18, class c, `text_size_fit`); `mech2-46` (14, class c, `leaf_capabilities`).
- PR status: Only native end truncation and `TextScaled` remain.
- Current main `bc9a56a6` still ships this.
- Decision: Restored.

### 19. Programmatic scrolling and reorder autoscroll

- Main promised: `scrollTo`, `scrollToVisible`, the shared keep-visible clamp, and autoscroll with dwell, bands and a nested chain during a reorder.
- Main evidence: 4 contracts, 44 main cases: `apps-228` (20, class c, `native_scroll`); `collections-01` (10, class c, `autoscroll`); `collections-02` (7, class c, `autoscroll`); `collections-181` (7, class c, `table_input`).
- Strengthening notes that retire part of a covered contract: `mech1-42`.
- PR status: VirtualList keeps `indexOfKey`, `placementOf` and `offsetOf`. Reorder autoscroll starts at once and scrolls only its own body.
- Current main `bc9a56a6` still ships this.
- Decision: Accepted. A game sets the native `ScrollingFrame.CanvasPosition`.

### 20. World anchor projection

- Main promised: `client.world_anchor` projected a world object to a screen anchor with padding, tracking and near-plane rejection.
- Main evidence: 1 contracts, 6 main cases: `apps-223` (6, class c, `world_anchor`).
- PR status: `docs/guide/14-choosing-controls.md` tells the game to project with the camera. AGENTS.md on main still names `client.world_anchor`.
- Current main `bc9a56a6` still ships this.
- Decision: Accepted. A game puts the interface in a `BillboardGui` or a `SurfaceGui` target.

### 21. Engine-owned mechanisms with no proof

- Main promised: 123 class b contracts (625 main cases) that now depend on an engine or Compose mechanism.
- Evidence: their `nativeEvidence` is `none`. No candidate test and no live record shows that Facet uses the mechanism correctly. Examples: `inputs-79` (D-pad and thumbstick repeat cadence), `inputs-134` (thumbstick latch and dead zone), `mech2-35` (no focus ring after touch). The class b tests in [Engine mechanism tests](20-verification-parity.md#engine-mechanism-tests) closed 81 more of these contracts.
- Decision: accept these on the engine's word, or ask for live checks (`docs/guide/11-device-verification.md` harness).

## Restored before the merge

These contracts moved from class c to class a. The cases are in `tests/native_parity_restore.spec.luau`. Each contract in `tools/lune/verification_parity.json` has `restoredBy` set to `parity/restore`.

| Contract | Main cases | Restored behavior | Native implementation | Still weaker |
|---|---:|---|---|---|
| `mech1-115` | 8 | `UI.ErrorBoundary` contains a failure, shows its fallback, reports the failure once and can retry. | `Compose.boundary` | The presenter critical screen is not restored. |
| `collections-150` | 2 | A plain mouse click replaces a multi selection. Ctrl-click or Cmd-click toggles a row. | `UserInputService:IsKeyDown` and the `Activated` input object | - |
| `collections-198` | 4 | Shift with an arrow key, Home or End extends the selection from the anchor. Ctrl with an arrow moves the focus only. | The collection arrow actions and the Compose focus scope | A plain arrow does not replace the selection. |
| `collections-78` | 1 | A tap outside an open tray closes it and is not swallowed. | `UserInputService.InputBegan`, observed only while the tray is open | - |
| `collections-90` | 2 | An outside tap during a destructive commit does not stop the commit. | The same observer | There is no passive HUD catcher. |
| `collections-285` | 10 | A press on another row closes the open row. | The same observer | A press on the content of the open row keeps its tray open. There is no shared list overlay. |
| `mech3-83` | 18 | Label `textSize = "fit"` paints the largest size that fits, between a cap and a floor. | `TextScaled` and `UITextSizeConstraint` | The engine picks the size. The headless engine does not scale text. |
| `mech2-46` | 14 | Label `truncate = "middle"` keeps the start and the end of a long value. | The engine `TextBounds` of the label | The headless engine measures with a fixed glyph width. |

The focus rules change moved three more contracts to class a. The cases are in `tests/native_selection_parity.spec.luau`. Each contract has `restoredBy` set to `parity/gamepad`. The live routes are in `artifacts/gamepad/gamepad-parity-live.json`.

| Contract | Main cases | Restored behavior | Native implementation | Still weaker |
|---|---:|---|---|---|
| `apps-158` | 15 | Tab and Shift+Tab walk the controls in layout order, wrap, skip hidden and disabled controls and stay inside a modal. | A Facet `InputAction` on Tab that sets `GuiService.SelectedObject` | Studio cannot inject Tab. The live check fires the action. There are no main navigation groups. |
| `collections-260` | 17 | A removed focused row gives the selection to the next row, or to the previous row at the end. | The Compose focus scope and `GuiService.SelectedObject` | The `focusPolicy` field and its index policy stay removed. |
| `collections-306` | 1 | The same nearest-survivor rule for a virtualized list. | The same | - |

Facet Neutral's `Light` palette (main `b1a08042`) is newer than the baseline. Its contract `post-lab-02` moves from class d to class a. Facet Neutral declares `Dark` and `Light` again. Both palettes pass the contrast gate, and the theme strength specs check both palettes where they checked one before.

## Retired contracts by area

Each line gives the contract id, the main case count and the main specs, the promise, the reason for the retirement and the replacement test. "Engine or Compose owns it" names the native mechanism. "Deleted" gives the audit evidence from `api.md`, the guides or `src`.

### Reactive core

2 class b and 6 class c contracts, 40 main cases.

- `mech1-137` (3 main cases; `fuzz_scheduler`). Main promised: Seeded scheduler fuzz invariants and replay. Engine or Compose owns it: Compose scheduler (vendored). Replacement test: yes: native_stress runs 400 replayable seeds over keyed collections, not scheduler-level invariants..
- `mech1-65` (1 main case; `compose_backend`). Main promised: Delivery order across separately created memos. Engine or Compose owns it: Compose scheduler (vendored src/vendor/compose). Replacement test: no test (none in Facet tests; relies on upstream Compose.).
- `mech1-122` (1 main case; `faults`). Main promised: Scheduler feedback converges or hits the cap loudly. Deleted: Facet scheduler feedback/settle cap deleted.. Replacement test: none; the feature is deleted.
- `mech1-70` (5 main cases; `compose_contract`). Main promised: Settle pass convergence, 100-round quarantine. Deleted: src/render/settle_pass.luau and services error boundary deleted; no settle loop exists (guide 15-adaptive-recipes: 'Do not run a second settle loop').. Replacement test: none; the feature is deleted.
- `mech1-87` (3 main cases; `conformance/conformance`). Main promised: Micro-screen conformance fixtures. Deleted: Conformance fixture harness and dumps deleted.. Replacement test: none; the feature is deleted.
- `mech2-113` (11 main cases; `reactive_recovery`). Main promised: Settle quarantine, drain cap, lone-observer snapshot guarantees. Deleted: Facet settle pass deleted; observer/drain semantics belong to vendored Compose (tested upstream, not in CAND). Replacement test: none; the feature is deleted.
- `mech3-38` (14 main cases; `settle_containment`). Main promised: Settle pass once per flush, vendored hook assertions, failure announcement throttling. Deleted: src/core/services settle pass and core.lastError deleted; api.md 'There is no ... reactor'. Replacement test: none; the feature is deleted.
- `themes-P2-61` (2 main cases; `theme_icons_applied`). Main promised: Adapter sequencing: the icon seam runs before early returns, and the snapshot arrives inside the swap transaction. Deleted: screen_chrome, screen_target and theme_controller are removed. Reactive swaps are covered by Compose cells, as in the icon swap in 'keeps native semantic icons reactive...'.. Replacement test: none; the feature is deleted.

### Lifetime and ownership

3 class b and 23 class c contracts, 174 main cases.

- `apps-123` (2 main cases; `components`). Main promised: Partially constructed component is released after a setup error; a throwing cleanup is contained and others still run. Engine or Compose owns it: Compose owner disposal/error containment (src/vendor/compose/core/ownership). Replacement test: no test (none: no Facet test injects a setup error or a throwing cleanup).
- `inputs-94` (1 main case; `help`). Main promised: Dismissing the surface takes its help. Engine or Compose owns it: Compose owner disposal of the overlay.. Replacement test: no test.
- `mech1-71` (3 main cases; `compose_lifetime`). Main promised: Compose listener detach, no resurrection after self-disposal, drain-cap wake. Engine or Compose owns it: Compose runtime ownership (vendored). Replacement test: yes: Indirect only: native_stress census returns connections to baseline over 200 modal cycles; no direct test of drain-cap wake or read-after-dispose..
- `apps2-06` (5 main cases; `consumer_standalone`). Main promised: Consumer teardown: registries baseline, nodes off target, Close/timer tear the session down, frame hook released. Deleted: The session object, timer teardown and frame hook were removed from examples/consumer; main.client.luau only stops the root on script.Destroying. The remaining 'Close removes the panel' is covered above; root stop lea.... Replacement test: none; the feature is deleted.
- `apps2-239` (7 main cases; `sponsor_scenarios`). Main promised: 20 mount/reset/dispose cycles return the core to baseline (deleted sponsor fixtures). Deleted: Seven of the eight fixtures no longer exist. Generic cycle leak coverage is native_stress 'returns all instances and connections to baseline through 200 modal cycles twice'.. Replacement test: a candidate test covers the surviving part (see reason).
- `apps2-62` (6 main cases; `examples_gallery`). Main promised: Lifecycle hooks and hidden: caption moves, kept slot stops painting/taps, hook log, disappear on teardown. Deleted: examples/gallery/scenarios/lifecycle_hidden.luau and Facet's onAppear/onDisappear/hidden lifecycle hooks were deleted; api.md documents no such hooks (Compose.cleanup owns teardown).. Replacement test: none; the feature is deleted.
- `inputs-37` (1 main case; `button_complete`). Main promised: Structural remount forgets per-path caches. Deleted: Scene path caches were deleted with the renderer.. Replacement test: none; the feature is deleted.
- `mech1-01` (3 main cases; `activate_handler_reconnect`). Main promised: activatedConn source scan on recycled instances. Deleted: screen_pointer.luau and instance recycling deleted; Compose runtime.connect owns connections. Leak freedom is asserted generically by native_stress census.. Replacement test: a candidate test covers the surviving part (see reason).
- `mech1-135` (11 main cases; `foreign_host_prop`). Main promised: Foreign onHost adoption and cleanup. Deleted: UI.Foreign deleted; Host constructors + Compose.cleanup.. Replacement test: none; the feature is deleted.
- `mech1-64` (21 main cases; `client_host`). Main promised: Client host six-step construction, frame driver and teardown. Deleted: client host/presenter/frame driver deleted; api.md: caller calls runtime.mount/runtime:dispose. Keyboard-occlusion placement deleted.. Replacement test: none; the feature is deleted.
- `mech1-84` (11 main cases; `compose_scene`). Main promised: Domain host journals, modifiers, layered layout, surface diagnostics, presenter routing, app root, scroll navigation. Deleted: Facet domain host, modifiers (background/overlay/frame), app.presentModal anchored builder, named scroll navigation and presenter deleted.. Replacement test: none; the feature is deleted.
- `mech2-13` (10 main cases; `host_core_lifetime`). Main promised: Facet.new app/settle/presenter/provider/callout shutdown ordering and error aggregation. Deleted: Facet.new, host, settle pass, presenter, provider transport deleted; api.md: caller calls runtime.mount/stop/runtime:dispose directly. Replacement test: none; the feature is deleted.
- `mech2-29` (4 main cases; `instance_park_corpse`). Main promised: Pool never holds a destroyed instance. Deleted: screen_target park/adopt deleted; Compose.createPool owns row hosts. Replacement test: none; the feature is deleted.
- `mech2-32` (8 main cases; `instance_recycling`). Main promised: Instance-host park/adopt, CanvasGroup refusal, cross-surface adoption. Deleted: screen_target instance hosts deleted. Replacement test: none; the feature is deleted.
- `mech2-33` (12 main cases; `instance_recycling_themed`). Main promised: Themed chrome park/adopt hint matching. Deleted: adapter chrome parking deleted; paint is StyleSheet rules on tags. Replacement test: none; the feature is deleted.
- `mech2-52` (16 main cases; `lifecycle_hooks`). Main promised: onAppear/onDisappear hooks and space-reserving hidden. Deleted: onAppear/onDisappear/hidden props deleted (no match in CAND src/docs); Compose.cleanup replaces; native Visible=false does not reserve list space. Replacement test: none; the feature is deleted.
- `mech2-77` (14 main cases; `mount`). Main promised: Facet mount, When/ForEach regions, dirty classes and history. Deleted: Facet mount/UI.When/UI.ForEach deleted; Compose.show/Compose.keyed own branches (vendored, tested upstream). Replacement test: none; the feature is deleted.
- `mech2-94` (3 main cases; `parentless_build_owner`). Main promised: Parentless control belongs to the application owner. Deleted: application core deleted; api.md "Make controls inside a Compose owner". Replacement test: none; the feature is deleted.
- `mech3-01` (10 main cases; `recycle_key_seam`). Main promised: Recycle-key seam source structure. Deleted: src/render/recycle_key.luau and the renderer's instance parking/recycling are deleted (CAND/src contains only src/init.luau, src/ui/*.luau and src/vendor/compose; the main src/render, src/layout, src/core, src/client .... Replacement test: none; the feature is deleted.
- `mech3-02` (13 main cases; `recycle_prop_signature`). Main promised: Prop-signature cache removal, setProp audit and style-adapter teardown. Deleted: renderer applyProp/applyStyleProp/applyShadow/applyCorners adapters deleted; paint is StyleSheet rules (api.md Themes). Replacement test: none; the feature is deleted.
- `mech3-10` (8 main cases; `renderer`). Main promised: Renderer minimal updates, path locality, dispose and diagnostic copies. Deleted: renderer.attach, solver rect writes and diagnostics() deleted; Compose owns the tree. Replacement test: none; the feature is deleted.
- `mech3-18` (1 main case; `replaced_occupant`). Main promised: Replaced occupant keeps no weak record. Deleted: renderer path records deleted. Replacement test: none; the feature is deleted.
- `navigation-2-07` (1 main case; `collapsible_view`). Main promised: Separate parents may reuse the same local control id. Deleted: Path/id-based node identity (presenter paths) deleted; candidate controls return native Instances whose identity is the Instance itself. No id option exists on CollapsibleViewSpec.. Replacement test: none; the feature is deleted.
- `navigation-3-02` (1 main case; `native_navigation_stack`). Main promised: Only the top route stays mounted; equal path keeps owner. Deleted: Hard-coded Compose.LayerStack{retention="all"} in src/ui/nav_pages.luau navigationStack keeps every covered page mounted; entries are keyed by table identity so an equal-but-new path rebuilds. api.md NavigationStack n.... Replacement test: none; the feature is deleted.
- `navigation-4-56` (1 main case; `tab_customization`). Main promised: Placement flip retains navigation option bundles and selected page. Deleted: Solver contribution bundles deleted. Page retention across placement flips (LayerStack) is not asserted by any candidate case; accessory rebuild per placement is tested.. Replacement test: none; the feature is deleted.
- `themes-P1-93` (1 main case; `skeleton`). Main promised: Held driver released when layout refuses a metric. Deleted: Solver metric resolution (fill min targetSizes.minimum) deleted.. Replacement test: none; the feature is deleted.

### Layout and geometry

46 class b and 79 class c contracts, 1028 main cases.

- `apps2-123` (4 main cases; `nested_compositing_scenario`). Main promised: Zero solver diagnostics, visible rects, pseudo-localized chrome. Engine or Compose owns it: Native layout (UIListLayout/AutomaticSize/TextBounds) replaces the Facet solver and its diagnostics.. Replacement test: no test (none (no pseudo-localization or visibility sweep of canvas_group)).
- `apps2-142` (11 main cases; `perf_lab`). Main promised: Workload paints inside its boxes at 8 viewports and after rotation; row that cannot fit fails; identity walk clean at phone widths. Engine or Compose owns it: UIListLayout/AutomaticSize/ScrollingFrame engine layout of lab rows. Replacement test: no test (none (fake engine performs no layout; no live capture of lab rows at the listed viewports in parity_blockers.json)).
- `apps2-16` (7 main cases; `example_readouts`). Main promised: Every tutorial example's declared readouts have a real rect at every viewport and text size. Engine or Compose owns it: UIListLayout/AutomaticSize and engine text measurement under PreferredTextSize. Replacement test: no test (None headless beyond mounting (native_gallery 'mounts ex02..ex07', 'mounts the additional temperature and confirmation tutorials'); no live readout sweep recorded in parity_blockers.json.).
- `apps2-166` (2 main cases; `playlist_columns`). Main promised: Header titles have real area; pinning one column leaves a sibling to absorb the remainder. Engine or Compose owns it: UIListLayout with UIFlexItem fill for unsized columns. Replacement test: live Studio evidence `classb_examples/playlist-header-titles-and-pinned-column`.
- `apps2-180` (1 main case; `playlist_columns`). Main promised: 320x640 edit mode: exact cell measurements. Engine or Compose owns it: UIListLayout/UIFlexItem cell widths. Replacement test: live Studio evidence `classb_examples/playlist-320x640-edit-mode-cells`.
- `apps2-188` (4 main cases; `radial_menu`). Main promised: Solved-position gating and shared-transform pivot exactness. Engine or Compose owns it: Native UDim2 Position/AnchorPoint plus Compose springs replace the Facet solver, its solved-rect gate and the shared Rings transform/pivot.. Replacement test: no test.
- `apps2-193` (2 main cases; `radial_menu`). Main promised: List fallback hugs rows and separates nav/preview space. Engine or Compose owns it: Native ScrollingFrame with UIListLayout, AutomaticCanvasSize and fixed Position/Size offsets replace the solver-fitted list.. Replacement test: yes: native_radial_controls::native radial interaction policies::NRAD-6 adapts into native scrolling list without losing a branch (asserts ScrollingEnabled and UIListLayout only).
- `apps2-282` (3 main cases; `virtual_monitors_layout`). Main promised: Detail Information area scrolls inside the panel at 920x640, 393x852, 1920x1080. Engine or Compose owns it: Native AutomaticSize.Y detail column inside the AlertModal panel/ScrollingFrame. Replacement test: live Studio evidence `classb_showcase/vm-detail-information-scrolls`.
- `apps2-58` (1 main case; `examples_gallery`). Main promised: Posts have different heights and none overlaps. Engine or Compose owns it: Native row Size/AutomaticSize with Compose placement offsets. Replacement test: no test (none (fake engine has no layout)).
- `apps2-60` (3 main cases; `examples_gallery`). Main promised: Tags wrap into several lines, never overlap, and survive pseudo-localization. Engine or Compose owns it: UIListLayout.Wraps. Replacement test: no test.
- `apps2-70` (1 main case; `examples_games`). Main promised: Crossword progress and goal readouts are arranged to a real rect. Engine or Compose owns it: UIListLayout/AutomaticSize. Replacement test: no test.
- `apps2-98` (1 main case; `gallery_chrome`). Main promised: Pixel Quest display picker stays contained. Engine or Compose owns it: Native Picker/Sheet layout under a skinned theme. Replacement test: no test.
- `collections-124` (32 main cases; `row_permutation`). Main promised: Incremental solver prefix/suffix/append reuse against the forced-full oracle. Engine or Compose owns it: Native layout (UIListLayout/AutomaticSize) plus Compose OrderedCollection placement. There is no Facet incremental solver.. Replacement test: yes: native_stress::native stress invariants::preserves keyed row content and bounded retention across 400 replayable mutation seeds checks keyed row content and bounded retention across 400 mutation seeds. There is no geo....
- `inputs-159` (4 main cases; `rating`). Main promised: Strip fits a 132px table cell in every package, centred. Engine or Compose owns it: Native UIListLayout with 2px padding and centred Marks frame.. Replacement test: live Studio evidence `classb_keyboard/rating-fits-a-table-cell`.
- `inputs-187` (2 main cases; `text_input`). Main promised: Clear button inset at trailing edge, vertically centred. Engine or Compose owns it: Native AnchorPoint/Position on the Clear button.. Replacement test: live Studio evidence `classb_keyboard/clear-button-trailing-and-centred`.
- `inputs-40` (6 main cases; `button_shape`). Main promised: Circle squares from one authored axis, fill width, rect unchanged. Engine or Compose owns it: UIAspectRatioConstraint(AspectRatio=1) + native Size/AutomaticSize.. Replacement test: yes: native_inputs::native input controls::uses native circle constraints and updates semantic roles without paint writes asserts the constraint exists; no test authors one axis or a fill width..
- `mech1-127` (20 main cases; `flow_wrap`). Main promised: Flow wrap line partition, cross-axis packing, lineAlign/distribute, oversized items. Engine or Compose owns it: UIListLayout.Wraps / ItemLineAlignment / HorizontalFlex. Replacement test: yes: Guide 15-adaptive-recipes teaches Host.UIListLayout{Wraps=true}; native_documentation only executes the snippet (no geometry). Facet controls themselves do not rely on wrap; no live Studio evidence cited..
- `mech2-02` (12 main cases; `grid_column_flow`). Main promised: Grid column-flow transpose (lanes, fill, minColumnWidth, align). Engine or Compose owns it: UIGridLayout FillDirection/CellSize/CellPadding (VirtualGrid horizontal placement is Compose OrderedCollection). Replacement test: yes: native_collections::Compose native collections::uses measured extents and column placements for both scroll directions (VirtualGrid only; no general Grid).
- `mech2-03` (17 main cases; `grid_measure_arrange`). Main promised: Grid measure/arrange agreement and fill lines. Engine or Compose owns it: UIGridLayout + AutomaticSize. Replacement test: no test (none (Facet no longer wraps general layout; callers author Host.* native layout objects directly)).
- `mech2-51` (4 main cases; `lifecycle_hooks`). Main promised: hidden hides paint, flips live, leaves focus and taps. Engine or Compose owns it: GuiObject.Visible (engine excludes invisible objects from selection and input). Replacement test: yes: native_public_surface::native public surface::gates native actions through retained ancestors and detaches observations (Visible=false disables actions).
- `mech2-83` (4 main cases; `nested_tree_pins`). Main promised: Sibling z order = paint order and tap target. Engine or Compose owns it: ZIndexBehavior.Sibling + engine hit testing. Replacement test: no test.
- `navigation-1-68` (1 main case; `callout_scenario`). Main promised: Safe-area inset larger than the gutter wins. Engine or Compose owns it: Native ScreenGui ScreenInsets/SafeAreaCompatibility determine the LayerCollector AbsolutePosition/AbsoluteSize that the Callout clamps against. Replacement test: no test.
- `navigation-2-06` (1 main case; `collapsible_view`). Main promised: Expanded view stays within safe edges. Engine or Compose owns it: Native centered layout: AnchorPoint 0.5/Position 0.5 with Size (1,-32) and UISizeConstraint MaxSize 640 (surfaces.centered) inside the ScreenGui safe area. Replacement test: live Studio evidence `classb_navigation/collapsible-safe-edges`.
- `navigation-2-16` (1 main case; `disclosure_group`). Main promised: Without presenter motion the displaced sibling moves immediately. Engine or Compose owns it: Native UIListLayout reflow (SortOrder LayoutOrder) on content insert/remove. Replacement test: yes: native_navigation::native navigation controls::owns disclosure content only while expanded asserts header/body LayoutOrder and UIListLayout SortOrder; no sibling position assertion..
- `navigation-2-56` (1 main case; `menu_scenario`). Main promised: Solver reports nothing at swept viewports. Engine or Compose owns it: Native UIListLayout/AutomaticSize/ScrollingFrame layout (no Facet solver). Replacement test: no test.
- `navigation-3-30` (1 main case; `navigation_visuals`). Main promised: Fill columns with aspect images solve without diagnostics. Engine or Compose owns it: UIListLayout flex / UIAspectRatioConstraint native layout (solver deleted). Replacement test: no test.
- `navigation-3-48` (1 main case; `page_view`). Main promised: Fixed-height pager bounded inside an outer vertical scroller. Engine or Compose owns it: Native ScrollingFrame/AutomaticSize. Replacement test: live Studio evidence `classb_navigation/pageview-fixed-in-scroller`.
- `navigation-4-31` (2 main cases; `selection_indicator`). Main promised: Bar width follows label width; wrapping does not collapse a fill strip. Engine or Compose owns it: UDim2 scale sizing (indicator Size 1,0,0,2) + UIListLayout Wraps/HorizontalFlex Fill. Replacement test: yes: Candidate asserts Wraps=true and AutomaticSize XY; HorizontalFlex Fill and indicator width are not asserted..
- `navigation-5-20` (1 main case; `tab_view`). Main promised: The content region stretches its page to the offer. Engine or Compose owns it: Native Size: Content frame sized per placement, each page layer Size=fromScale(1,1) with ClipsDescendants on Content.. Replacement test: live Studio evidence `classb_navigation/tab-content-stretches-page`.
- `paint-124` (1 main case; `styling`). Main promised: Shadows never enter layout. Engine or Compose owns it: UIShadow (engine) does not participate in layout. Replacement test: no test.
- `paint-145` (6 main cases; `themed_containment`). Main promised: Themed chrome insets keep content inside plates. Engine or Compose owns it: StyleRule .facet-<role>::UIPadding from chrome contentInsets + native layout. Replacement test: yes: native_themes_media::native themes and media::keeps plaques edges tiling pixel resampling and chrome insets native (button inset only; no live Studio evidence).
- `paint-146` (6 main cases; `themed_containment`). Main promised: Badge/seal clears its segment/tile carved border per axis. Engine or Compose owns it: UIPadding from chrome contentInsets on .facet-badge/.facet-button + native layout. Replacement test: yes: native_themes_media::native themes and media::keeps plaques edges tiling pixel resampling and chrome insets native (badge inset untested; no live evidence).
- `paint-15` (1 main case; `control_vocabulary`). Main promised: Composed Button content keeps theme padding. Engine or Compose owns it: StyleRule .facet-button::UIPadding (and real > UIPadding) from chrome contentInsets. Replacement test: yes: native_themes_media::native themes and media::keeps plaques edges tiling pixel resampling and chrome insets native.
- `paint-35` (2 main cases; `display_controls`). Main promised: Progress track and label centering. Engine or Compose owns it: UIListLayout VerticalAlignment/HorizontalAlignment and TextXAlignment. Replacement test: live Studio evidence `classb_navigation/progress-track-centring`.
- `paint-43` (3 main cases; `display_controls`). Main promised: Track stays visible beside long trailing copy. Engine or Compose owns it: UIListLayout row with UIFlexItem Fill on the label and AutomaticSize readout. Replacement test: live Studio evidence `classb_navigation/progress-track-beside-trailing-copy`.
- `paint-52` (2 main cases; `icon_box`). Main promised: Search-clear and checkbox glyphs centered in plates. Engine or Compose owns it: AnchorPoint/UIListLayout centering. Replacement test: yes: native_inputs::native input controls::keeps mixed checkbox geometry live and validates presentation-specific options (visibility only; centering unasserted).
- `paint-81` (8 main cases; `paint_extensions`). Main promised: zIndex paint order within siblings. Engine or Compose owns it: GuiObject.ZIndex with ZIndexBehavior.Sibling (callers set native ZIndex). Replacement test: no test.
- `themes-P1-106` (2 main cases; `stage`). Main promised: Stage solves to declared box; no intrinsic size. Engine or Compose owns it: Native Size on ViewportFrame (caller Size overrides the 240x180 default).. Replacement test: yes: Conformance preserves caller properties; Size not asserted; default is now 240x180 not 0x0..
- `themes-P1-118` (1 main case; `status_indicator`). Main promised: Ring and dash centered in a nonsquare reservation. Engine or Compose owns it: Native UIAspectRatioConstraint FitWithinMaxSize.. Replacement test: yes: ACCESS asserts AspectRatio 3 FitWithinMaxSize on an 80x12 dash..
- `themes-P1-120` (4 main cases; `status_indicator`). Main promised: Round containment: contained square, fractional offers, subpixel, reselects axis. Engine or Compose owns it: Native UIAspectRatioConstraint FitWithinMaxSize and UICorner(1,0).. Replacement test: live Studio evidence `classb_themes/status-round-containment`.
- `themes-P1-73` (2 main cases; `progress_circular`). Main promised: Lays out in every theme/viewport; label never overlaps; long label fits. Engine or Compose owns it: Native UIListLayout column (label above Indicator) with AutomaticSize XY.. Replacement test: no test.
- `themes-P1-82` (1 main case; `skeleton`). Main promised: Paragraph bars fill resolved width before final 60%. Engine or Compose owns it: Native UDim2 scale widths in a UIListLayout column.. Replacement test: yes: SKEL asserts Placeholder3 Size.X.Scale 0.6..
- `themes-P1-90` (1 main case; `skeleton`). Main promised: Obeys parent alignment. Engine or Compose owns it: Native UIListLayout alignment of the parent.. Replacement test: live Studio evidence `classb_themes/skeleton-parent-alignment`.
- `themes-P4-04` (6 main cases; `theme_layer_application`). Main promised: Backdrop z-ladder: every layer below content, strictly ordered, text lift above ladder, one ImageLabel per box with ZIndex by array order. Engine or Compose owns it: Roblox sibling ZIndex / child order: themes.skin puts every layer ImageLabel at ZIndex = options.ZIndex or 0 (ctx.skin passes 0) inside a 'Skin' Frame; PlaqueCaption TextLabel ZIndex=1 inside its image; Compose.keyed .... Replacement test: no test (none - no candidate case asserts layer ZIndex, relative order, or that content draws above the art; no parity_blockers liveEvidence for skins.).
- `themes-P4-17` (1 main case; `theme_layers`). Main promised: Stack z-order = array order. Engine or Compose owns it: Roblox sibling draw order of the Compose.keyed ImageLabels (all ZIndex 0).. Replacement test: no test (none - order across layers is not asserted by any candidate case.).
- `themes-P5-39` (4 main cases; `theme_matrix_audit`). Main promised: Matrix rows: no zero content box, text fits, no diagnostics. Engine or Compose owns it: Engine layout (UIListLayout/UIFlexItem/AutomaticSize) and TextBounds replace the Facet solver.. Replacement test: no test (none for the canonical/handheld x package matrix; pendingLiveRisks lists 'Shell measured reservation and Settings reachability under themes/largest text'.).
- `apps-124` (1 main case; `components`). Main promised: Layout modifiers apply to the component root. Deleted: Facet layout modifiers deleted with the solver; api.md 'Options and native properties' now forwards native Size/Position/AutomaticSize directly.. Replacement test: none; the feature is deleted.
- `apps-233` (15 main cases; `placement_anchor_place`). Main promised: placement.anchorPlace nine corners, fill/offset forms, innerBox. Deleted: Solver placement helpers deleted; native AnchorPoint/Position/UIPadding own placement (docs/guide/15-adaptive-recipes.md).. Replacement test: none; the feature is deleted.
- `apps-250` (11 main cases; `large_text_layout`). Main promised: ViewThatFits band, AdaptiveStack split, Composition arrangements, motionPolicy. Deleted: ViewThatFits, AdaptiveStack, Composition and motionPolicy deleted; docs/guide/15-adaptive-recipes.md tells screens to observe native AbsoluteSize and choose with Compose.show/keyed.. Replacement test: none; the feature is deleted.
- `apps2-110` (7 main cases; `gallery_demo_picker`). Main promised: Strip reserves measured chip height; reservationSlack covers chrome glow. Deleted: The chip strip reservation and reservationSlack were deleted with showcase_chrome/demo_host.. Replacement test: none; the feature is deleted.
- `apps2-120` (27 main cases; `gallery_theme_picker`). Main promised: Theme picker chips, section spacing, RS-DIR14 grid rows, hugging/scrolling panel, standalone collapsed shell, handlers. Deleted: The composed theme picker panel (chip grid, metric-spaced sections, standalone collapsed shell) was deleted; the choice is now a native UI.Picker in the Settings Sheet (screen.luau:209-235).. Replacement test: none; the feature is deleted.
- `apps2-125` (3 main cases; `nested_compositing_scenario`). Main promised: Carried marker real parent and shared carry drivers. Deleted: The carry-pair transform-inheritance demo and Facet instance-host registry (adapter.isHost/nearest host) are deleted with the renderer; canvas_group.luau has no carry pair.. Replacement test: none; the feature is deleted.
- `apps2-182` (1 main case; `playlist_columns`). Main promised: Solver files no collapse diagnostic. Deleted: Facet solver and its diagnostics deleted.. Replacement test: none; the feature is deleted.
- `apps2-273` (3 main cases; `virtual_monitors_diagnostics`). Main promised: VM mounts report no Facet layout diagnostics in any state. Deleted: Facet's layout solver and its diagnostics (controller.diagnostics, layout guard) were deleted; CAND src has no solver. Layout is native UIListLayout/AutomaticSize, which publishes no Facet findings. The mounted states.... Replacement test: none; the feature is deleted.
- `apps2-56` (8 main cases; `examples_gallery`). Main promised: Round-2 layout vocabulary: distribute, shrink tiers, lineAlign, containerRelativeFrame, GridRow/gridSpan, sensoryFeedback bus. Deleted: Facet's layout vocabulary (distribute, layout priority tiers, lineAlign, containerRelativeFrame, UI.GridRow/gridSpan) and the presenter sensory bus were deleted; api.md has none of these words. Native UIListLayout/UIF.... Replacement test: none; the feature is deleted.
- `collections-278` (4 main cases; `virtual_list_measured_extents`). Main promised: Fill main-axis refusals and the lying-extent class. Deleted: The solver fill semantics are gone. AutomaticSize is native.. Replacement test: none; the feature is deleted.
- `collections-30` (5 main cases; `collection_self_measure`). Main promised: crossExtent hug/measured options. Deleted: crossExtent is not in collection_types.luau or api.md. An unknown lower-case option raises 'Unknown Facet option'.. Replacement test: none; the feature is deleted.
- `collections-49` (2 main cases; `paradigm_table`). Main promised: Surface onGeometry is additive. Deleted: The presenter surface onGeometry hooks are gone.. Replacement test: none; the feature is deleted.
- `collections-85` (1 main case; `row_actions_input`). Main promised: The menu adds nothing to row measurement. Deleted: Solver rect identity is gone. The menu is a native overlay.. Replacement test: none; the feature is deleted.
- `inputs-193` (3 main cases; `text_input`). Main promised: Clear chip sizes per theme. Deleted: Theme-sized clear chip replaced by a fixed 44x32 native Button.. Replacement test: none; the feature is deleted.
- `inputs-207` (4 main cases; `toggle_presentations`). Main promised: Nested switch refusal, solver label column and width refusals. Deleted: Solver-assigned label columns and width-rejection passes retired.. Replacement test: none; the feature is deleted.
- `inputs-225` (3 main cases; `value_controls`). Main promised: Hit expander re-basing and 44px stepper floor. Deleted: Hit expanders removed; Stepper buttons are now 44x36 (no 44px floor on both axes).. Replacement test: none; the feature is deleted.
- `inputs-34` (8 main cases; `button_complete`). Main promised: 44px hit-target floor via hit expanders. Deleted: Hit expanders/effective hit rects removed (no expander code in CAND src/ui); api.md does not promise a Button hit floor.. Replacement test: none; the feature is deleted.
- `inputs-44` (2 main cases; `button_shape`). Main promised: Circle hit geometry full 44px square. Deleted: Hit expanders removed.. Replacement test: none; the feature is deleted.
- `inputs-59` (2 main cases; `chip`). Main promised: Disjoint effective targets for body/Remove and zero-gap reservations. Deleted: Synthetic target expansion retired.. Replacement test: none; the feature is deleted.
- `mech1-03` (10 main cases; `adaptive`). Main promised: AdaptiveStack axis flip and axis-aware Divider. Deleted: UI.AdaptiveStack and UI.Divider deleted; native UIListLayout.FillDirection bound by a cell replaces them (not a Facet promise).. Replacement test: none; the feature is deleted.
- `mech1-04` (13 main cases; `adaptive`). Main promised: ViewThatFits candidate choice, hiding and focus exclusion. Deleted: UI.ViewThatFits deleted; no doc promise. Authors use Compose.show on observed bounds (guide 15-adaptive-recipes).. Replacement test: none; the feature is deleted.
- `mech1-111` (33 main cases; `elision_recovery`). Main promised: Composition elision recovery routes and overflow sink. Deleted: Composition recovery routes deleted; HUD overflow recovery exists separately (native_hud 'recovers whole simplified zones in overflow').. Replacement test: a candidate test covers the surviving part (see reason).
- `mech1-123` (41 main cases; `fill_minimum`). Main promised: Fill minimum shares, shrink negotiation, Screen fill metrics. Deleted: fill weights/minimums deleted; native UIFlexItem/UISizeConstraint are author-owned.. Replacement test: none; the feature is deleted.
- `mech1-126` (15 main cases; `fits_ideal_size`). Main promised: ViewThatFits ideal-size probing and UI.fits. Deleted: ViewThatFits and UI.fits deleted.. Replacement test: none; the feature is deleted.
- `mech1-128` (20 main cases; `flow_wrap`). Main promised: Wrap prop authoring, stretch refusal, placement audit, gallery sweep. Deleted: VStack/HStack wrap prop, placement audit and cross-overflow diagnostics deleted.. Replacement test: none; the feature is deleted.
- `mech1-136` (12 main cases; `fractional_offsets`). Main promised: Anchor fractional offsets. Deleted: UI.Anchor deleted; native UDim2 scale+offset.. Replacement test: none; the feature is deleted.
- `mech1-37` (4 main cases; `authoring`). Main promised: overflow = clip implies clipChildren. Deleted: overflow/clipChildren props deleted; native ClipsDescendants.. Replacement test: none; the feature is deleted.
- `mech1-38` (5 main cases; `authoring`). Main promised: Divider/Path thickness metric names. Deleted: UI.Divider and UI.Path deleted.. Replacement test: none; the feature is deleted.
- `mech1-39` (4 main cases; `authoring`). Main promised: surface = badge intrinsic minimum. Deleted: Box surface floor deleted; UI.Badge sizes itself natively (StatusIndicator count pill tested in native_themes_media).. Replacement test: a candidate test covers the surviving part (see reason).
- `mech1-44` (19 main cases; `baseline_spacer`). Main promised: Text baseline alignment and spacer minimums. Deleted: Baseline alignment and UI.Spacer deleted; Roblox layouts have no baseline alignment and no doc promises it.. Replacement test: none; the feature is deleted.
- `mech1-85` (107 main cases; `composition`). Main promised: UI.Composition arrangements, step-down, floors, spans, scroll region, eligibility. Deleted: UI.Composition/Region deleted; no doc promise (guide 14-choosing-controls: use native stacks).. Replacement test: none; the feature is deleted.
- `mech1-86` (20 main cases; `composition_exclusions`). Main promised: Composition exclusions (chrome rows push lanes). Deleted: Composition exclusions deleted.. Replacement test: none; the feature is deleted.
- `mech1-93` (4 main cases; `conformance/conformance`). Main promised: Corpus text-in-box, clip containment, finite rects, deterministic dumps. Deleted: Solver rect dumps deleted.. Replacement test: none; the feature is deleted.
- `mech1-97` (4 main cases; `container_grid`). Main promised: Themed row containment in three packages; recipe formula release. Deleted: Measured container recipe and solver containment deleted.. Replacement test: none; the feature is deleted.
- `mech1-98` (19 main cases; `container_relative_frame`). Main promised: containerRelativeFrame and paging arithmetic. Deleted: UI.containerRelativeFrame deleted; native Scale UDim/UIPageLayout.. Replacement test: none; the feature is deleted.
- `mech1-99` (8 main cases; `containment_diagnostic`). Main promised: Cross-axis containment diagnostic. Deleted: Facet layout solver/renderer (src/layout, src/render) deleted; CAND/src has only src/ui + vendor. api.md: 'There is no Facet application, mounting service, render target, solver'. No doc promises it.. Replacement test: none; the feature is deleted.
- `mech2-01` (14 main cases; `geometry_solve_coalescing`). Main promised: Geometry change coalesces into one solve. Deleted: Facet layout solver/measure/renderer (src/layout, src/render, src/client adapter) deleted; CAND/src has only src/ui + vendor/compose; api.md "There is no Facet application, mounting service, render target, solver, rea.... Replacement test: none; the feature is deleted.
- `mech2-04` (4 main cases; `grid_measure_arrange`). Main promised: Grid plan single-owner source scans. Deleted: src/layout/grid.luau deleted. Replacement test: none; the feature is deleted.
- `mech2-05` (18 main cases; `grid_row`). Main promised: GridRow row mode, spans, squeeze report, D-pad grid. Deleted: UI.Grid/UI.GridRow/gridSpan deleted; no native span equivalent; api.md/guides do not mention GridRow. Replacement test: none; the feature is deleted.
- `mech2-06` (5 main cases; `hidden_roots_owner`). Main promised: Hidden ViewThatFits candidates skip disclosure/help/reveal walks. Deleted: ViewThatFits and hidden-root walks deleted; Button help promise itself is covered by native_navigation::native navigation controls::shows button help after a pointer rests on it and never on touch. Replacement test: a candidate test covers the surviving part (see reason).
- `mech2-100` (16 main cases; `premeasure_round_seam`). Main promised: Premeasure round seam. Deleted: Facet layout solver/measure/renderer (src/layout, src/render, src/client adapter) deleted; CAND/src has only src/ui + vendor/compose; api.md "There is no Facet application, mounting service, render target, solver, rea.... Replacement test: none; the feature is deleted.
- `mech2-15` (5 main cases; `host_rect_identity`). Main promised: Coordinate-space host rect bookkeeping. Deleted: Facet layout solver/measure/renderer (src/layout, src/render, src/client adapter) deleted; CAND/src has only src/ui + vendor/compose; api.md "There is no Facet application, mounting service, render target, solver, rea.... Replacement test: none; the feature is deleted.
- `mech2-16` (5 main cases; `host_space_memo`). Main promised: Coordinate-space host rect bookkeeping. Deleted: Facet layout solver/measure/renderer (src/layout, src/render, src/client adapter) deleted; CAND/src has only src/ui + vendor/compose; api.md "There is no Facet application, mounting service, render target, solver, rea.... Replacement test: none; the feature is deleted.
- `mech2-17` (8 main cases; `host_space_oracle`). Main promised: Coordinate-space host rect bookkeeping. Deleted: Facet layout solver/measure/renderer (src/layout, src/render, src/client adapter) deleted; CAND/src has only src/ui + vendor/compose; api.md "There is no Facet application, mounting service, render target, solver, rea.... Replacement test: none; the feature is deleted.
- `mech2-28` (19 main cases; `instance_hosts`). Main promised: Instance host registry and real-parent triggers. Deleted: Facet layout solver/measure/renderer (src/layout, src/render, src/client adapter) deleted; CAND/src has only src/ui + vendor/compose; api.md "There is no Facet application, mounting service, render target, solver, rea.... Replacement test: none; the feature is deleted.
- `mech2-37` (20 main cases; `layout`). Main promised: Solver diagnostics, purity, per-word slack estimator, lineLimit, ViewThatFits. Deleted: Facet layout solver/measure/renderer (src/layout, src/render, src/client adapter) deleted; CAND/src has only src/ui + vendor/compose; api.md "There is no Facet application, mounting service, render target, solver, rea.... Replacement test: none; the feature is deleted.
- `mech2-39` (7 main cases; `layout_prop_dirt`). Main promised: Layout dirt classes audit. Deleted: Facet layout solver/measure/renderer (src/layout, src/render, src/client adapter) deleted; CAND/src has only src/ui + vendor/compose; api.md "There is no Facet application, mounting service, render target, solver, rea.... Replacement test: none; the feature is deleted.
- `mech2-43` (33 main cases; `layout_vocabulary`). Main promised: Modifier API (overlay/background/order), refusals, intrinsic/uniform grid, shrink pass, meta channel. Deleted: UI modifiers, Grid itemSizing/minColumnWidth=intrinsic, shrinkWeight deleted; not in api.md. Replacement test: none; the feature is deleted.
- `mech2-84` (12 main cases; `nested_tree_pins`). Main promised: Renderer write-one-node, host-before-descendant, UIScale corpse. Deleted: Facet layout solver/measure/renderer (src/layout, src/render, src/client adapter) deleted; CAND/src has only src/ui + vendor/compose; api.md "There is no Facet application, mounting service, render target, solver, rea.... Replacement test: none; the feature is deleted.
- `mech2-88` (18 main cases; `overflow_guard`). Main promised: Overflow guard diagnostics and waivers. Deleted: overflow guard/waiver registry deleted. Replacement test: none; the feature is deleted.
- `mech3-04` (10 main cases; `redteam_closing_round`). Main promised: Red-team: measure verdicts under reuse arm, translate arm refusal, paint eraser agreement. Deleted: measure_reuse, translate arm and paint eraser modules deleted (CAND/src contains only src/init.luau, src/ui/*.luau and src/vendor/compose; the main src/render, src/layout, src/core, src/client modules are gone). Replacement test: none; the feature is deleted.
- `mech3-104` (3 main cases; `zstack_fill_diagnostic`). Main promised: ZStack fill overflow diagnostic. Deleted: UI.ZStack and solver diagnostics deleted. Replacement test: none; the feature is deleted.
- `mech3-105` (23 main cases; `zstack_shape`). Main promised: Circle ZStack/Box/Image shape. Deleted: UI.ZStack/UI.Box/UI.Image deleted; Button shape='circle' survives and is covered by native_inputs::native input controls::uses native circle constraints and updates semantic roles without paint writes. Note stale exam.... Replacement test: a candidate test covers the surviving part (see reason).
- `mech3-41` (12 main cases; `solve_ctx_seam`). Main promised: Solve-context seam. Deleted: solver solve context deleted. Replacement test: none; the feature is deleted.
- `mech3-49` (16 main cases; `stack_measure_seam`). Main promised: Stack measure seam. Deleted: solver stack measure deleted. Replacement test: none; the feature is deleted.
- `mech3-52` (15 main cases; `stack_seam`). Main promised: Stack arrange seam. Deleted: solver stack arrange deleted. Replacement test: none; the feature is deleted.
- `mech3-91` (6 main cases; `transform_footprint`). Main promised: Facet.layout.transformFootprint. Deleted: Facet.layout deleted; api.md exports no layout table. Replacement test: none; the feature is deleted.
- `navigation-1-41` (4 main cases; `anchored_surface`). Main promised: Solver refusal of unknown edge/overflow, never-worse flip, oversize pinning. Deleted: src/layout/anchor_placement.luau and its overflow policy are deleted (no anchor placement module in CAND src; api.md has no Facet.layout).. Replacement test: none; the feature is deleted.
- `navigation-1-44` (4 main cases; `anchored_surface`). Main promised: Tail suppression rules and side-edge tails. Deleted: Solver tail suppression and left/right edges are deleted. The CAND Callout edge is only 'top'/'bottom', and its arrow is always clamped inside the plate (never suppressed).. Replacement test: none; the feature is deleted.
- `navigation-3-58` (5 main cases; `picker_segments`). Main promised: Content-sized vertical pill rail, verticalPill dump, chip down the rail, rail<->column flip. Deleted: Vertical pill rail concept deleted: axis='y' segments are Size (1,0,0,44) full width in a Vertical UIListLayout; dump() removed.. Replacement test: none; the feature is deleted.
- `navigation-4-22` (7 main cases; `selection_indicator`). Main promised: Indicator rect arithmetic (pill inset, underline x/y, clamps, nil target). Deleted: Main src/controls/selection_indicator.luau (newSelectionIndicator) deleted; CAND pill = selected paint on the option, underline = fixed 2px Frame at the bottom of the selected button (nav_menu.luau/nav_pages.luau). No.... Replacement test: none; the feature is deleted.
- `navigation-4-70` (5 main cases; `tab_strip_gap`). Main promised: Tab strip leaves a theme spacing step between itself and content (s for bands, m for rail, 0 compact). Deleted: Main tab_view gap metric (gap_metric.luau / theme space steps) deleted; CAND nav_pages.luau positions Content exactly at the strip edge (topBar Position 0,44; sidebar at railWidth; bottom bars Size 1,-44) with no gap .... Replacement test: none; the feature is deleted.
- `navigation-5-03` (5 main cases; `tab_view`). Main promised: Centered hugging bands and the fill/hug sizing split. Deleted: Main's centered TopBandCenter/CompactBandCenter/BottomBandCenter homes and fill-vs-hug sizing are gone. CAND's bar is one ScrollingFrame whose UIListLayout sets no HorizontalAlignment (left-aligned) and whose buttons .... Replacement test: none; the feature is deleted.
- `navigation-5-07` (1 main case; `tab_view`). Main promised: A filling thumb-zone band has nothing to scroll. Deleted: The 'fill' sizing mode is gone: every CAND bar is a scroller of AutomaticSize.X buttons. No api.md promise of a filling bottom band.. Replacement test: none; the feature is deleted.
- `navigation-5-37` (2 main cases; `tab_view_accessories`). Main promised: Strip centred beside/without a trailing accessory. Deleted: Centering is gone (UIListLayout with no HorizontalAlignment; no TopBandBox); not promised by api.md.. Replacement test: none; the feature is deleted.
- `paint-11` (11 main cases; `control_vocabulary`). Main promised: Compact hit footprints, disjoint targets and theme target floors. Deleted: Hit-expander/target-floor solver deleted (no targetSizes consumer in src/ui); no doc promises a minimum touch target for Button.. Replacement test: none; the feature is deleted.
- `paint-28` (3 main cases; `display_controls`). Main promised: Touch target floors (Picker rows, Disclosure header, PopupButton rows). Deleted: Target-floor enforcement deleted with the solver; no doc promises it.. Replacement test: none; the feature is deleted.
- `themes-P1-127` (2 main cases; `badge_centering`). Main promised: Option badge plate takes theme badge minimum; skinned half centres. Deleted: Picker/Tab option badge seal deleted: nav_menu.luau/nav_pages.luau render badge as a text suffix ' (3)'; chrome_slots deleted.. Replacement test: none; the feature is deleted.
- `themes-P1-128` (2 main cases; `badge_centering`). Main promised: Glyph concentric with its plate across themes and text preferences. Deleted: No option-badge plate exists to centre within (text suffix); standalone Badge centering is native (UIListLayout VerticalAlignment Center asserted in ACCESS).. Replacement test: none; the feature is deleted.
- `themes-P1-38` (1 main case; `avatar_group`). Main promised: Overlap and overflow target derived from theme rungs. Deleted: Overlap now a fixed UIListLayout Padding (-10 stacked, 8 spread) in media.luau; rung-derived gap and targetSizes.minimum hit target deleted; api.md makes no overlap promise.. Replacement test: none; the feature is deleted.
- `themes-P1-81` (1 main case; `skeleton`). Main promised: Relative circle width resolved once; other axis derived. Deleted: Facet width/height layout specs deleted; Skeleton circle is fromOffset(controlSize) and callers use native Size.. Replacement test: none; the feature is deleted.
- `themes-P3-20` (19 main cases; `theme_pixel_content`, `theme_pixel`). Main promised: Pixel packages snap every resolved length up to the pixel unit (durations/overrides exempt); pixelUnit validation. Deleted: themes.snapToPixelUnit and the snapshot snap path are gone (no pixelUnit/snapToPixelUnit in CAND src). define() no longer validates identity.rendering, pixelUnit, integer SliceScale or on-grid slice borders (themes.lu.... Replacement test: none; the feature is deleted.
- `themes-P3-25` (1 main case; `theme_roles`). Main promised: Themed target-size floor raises the enforced hit rect. Deleted: Hit-rect floors belonged to the solver/focus graph; CAND hit area is the native GuiObject. Note metrics.targetSizes.minimum=44 remains in the neutral package (themes.luau:88) but nothing in CAND src reads it.. Replacement test: none; the feature is deleted.
- `themes-P3-29` (12 main cases; `theme_roles`). Main promised: Byte-parity geometry of migrated controls under Facet Neutral (Slider, ProgressView, PopupButton, TextInput, Toggle, Stepper, Chip, Label, renderer defaults, AsyncImage, Table). Deleted: Solver geometry fixtures retired with the solver; api.md does not promise these pixel values for these controls (control-specific defaults it does promise, e.g. Table 44 row minimum, belong to other batches).. Replacement test: none; the feature is deleted.
- `themes-P4-18` (6 main cases; `theme_layers`). Main promised: Plaque overhang reserved as chrome outset metadata; preload seam carries layer assets. Deleted: No overhang reservation/outset or census in CAND (solver and snapshot deleted); plaque simply overhangs via Position offset in themes.skin. No theme preload seam (ThemeTypes.Asset.preload is unread). Not promised.. Replacement test: none; the feature is deleted.
- `themes-P4-26` (6 main cases; `theme_layers`). Main promised: Clipping scroll containers reserve the package's shadow bleed. Deleted: The solver's chromeBleed reservation is deleted; shadows are native UIShadow children (themes.skin) and ScrollingFrame clipping is the engine's. No api/guide promise of bleed reservation (10-rich-skinning only lists '.... Replacement test: none; the feature is deleted.
- `themes-P4-47` (4 main cases; `theme_variants`). Main promised: State changes never relayout: per-state insets must match. Deleted: Per-state inset validation deleted; contentInsets are per recipe, so a state map cannot carry different insets. Not promised.. Replacement test: none; the feature is deleted.
- `themes-P5-26` (8 main cases; `theme_value_displays`). Main promised: Bar ornaments reserve space; unaffordable insets refused/reported. Deleted: Solver chrome_slots.barReservation and inset diagnostics deleted with the Facet solver; CAND caps/centre are overhanging ImageLabels with no reservation. Docs make no reservation promise (10-rich-skinning asks authors.... Replacement test: none; the feature is deleted.
- `themes-P5-40` (1 main case; `theme_matrix_audit`). Main promised: No chrome extent paints on a neighbour. Deleted: chromeOutsets/overhang reservation deleted with the solver; CAND plaques/caps overhang with no reservation and no docs promise.. Replacement test: none; the feature is deleted.

### Text measurement and fit

30 class b and 20 class c contracts, 402 main cases.

- `apps-130` (1 main case; `semantic_rows`). Main promised: Described toggle fits beside natural-width value in a narrow theme. Engine or Compose owns it: Native TextButton text layout with UIPadding reserved for the indicator. Replacement test: yes: native_inputs::native input controls::uses native Toggle text and reserved padding while retaining rich hints and live theme insets (asserts padding offsets, not narrow fit).
- `apps-248` (4 main cases; `large_text_hot_swap`). Main promised: Geometry returns byte-identical and files no overlap/clip finding. Engine or Compose owns it: Engine text layout (TextSize/AutomaticSize/TextWrapped); solver findings deleted. Replacement test: no test.
- `apps-249` (3 main cases; `large_text_layout`). Main promised: Form in a scroll view reflows then scrolls; content grows while viewport does not; layout identical under reduced motion. Engine or Compose owns it: ScrollingFrame AutomaticCanvasSize + AutomaticSize/TextWrapped. Replacement test: live Studio evidence `classb_keyboard/form-reflows-then-scrolls`, `classb_keyboard/form-geometry-identical-under-reduced-motion`.
- `apps-251` (68 main cases; `large_text_matrix`). Main promised: Every control at four text preferences x two themes has no overlap, undisclosed clip or sub-floor target. Engine or Compose owns it: Engine text layout (TextSize, TextWrapped, AutomaticSize, UITextSizeConstraint) in native controls. Replacement test: no test (none (no candidate sweep over controls x preferences; native_text_preference::native text preference updates::keeps editing focus row identity and scroll position across text and theme changes covers three controls)).
- `apps-255` (5 main cases; `large_text_matrix`). Main promised: Action labels wrap before truncating; fixed px widths clip; mixed CJK/emoji/latin lines grow monotonically. Engine or Compose owns it: Engine TextWrapped/AutomaticSize and text shaping. Replacement test: live Studio evidence `classb_keyboard/action-label-wraps-before-truncating`, `classb_keyboard/fixed-px-action-draws-compact-form`, `classb_keyboard/mixed-script-lines-grow-monotonically`.
- `apps2-05` (1 main case; `consumer_standalone`). Main promised: Player's preferred text size grows the untouched blurb label. Engine or Compose owns it: GuiService.PreferredTextSize applied to TextLabel with AutomaticSize.Y. Replacement test: live Studio evidence `classb_examples/consumer-blurb-grows-with-text-size`.
- `apps2-131` (1 main case; `outpost_terminal`). Main promised: Terminal holds its canvas at every text preference. Engine or Compose owns it: SurfaceGui fixed CanvasSize with engine text measurement under PreferredTextSize. Replacement test: live Studio evidence `classb_examples/outpost-terminal-holds-canvas-every-text-size`.
- `apps2-27` (1 main case; `example_word_game`). Main promised: Board holds letters and marks at every text size. Engine or Compose owns it: Engine text measurement and GuiService.PreferredTextSize scaling on UI.Label. Replacement test: live Studio evidence `classb_examples/word-board-holds-glyphs-at-every-text-size`.
- `apps2-54` (1 main case; `examples_gallery`). Main promised: Circular progress copy survives pseudo-localization. Engine or Compose owns it: Engine text measurement/TextWrapped. Replacement test: no test.
- `apps2-64` (3 main cases; `examples_gallery`). Main promised: Nothing leaves the row at any stop; lower panel wraps then cuts; pseudo-localized fits. Engine or Compose owns it: TextWrapped/TextTruncate and UIListLayout. Replacement test: no test.
- `inputs-147` (10 main cases; `preferred_text_seam`). Main promised: Async preferred-text offset cache and correction. Engine or Compose owns it: Engine text rendering applies GuiService.PreferredTextSize; native TextBounds is the measurement.. Replacement test: yes: native_text_preference::native text preference updates::keeps editing focus row identity and scroll position across text and theme changes; native_collections::Compose native collections::adapts table row and header h....
- `inputs-208` (1 main case; `toggle_presentations`). Main promised: Content-sized labels wrap and follow live width. Engine or Compose owns it: Native TextWrapped/AutomaticSize.. Replacement test: no test.
- `inputs-48` (3 main cases; `button_shape`). Main promised: Button label wraps only at legal breaks. Engine or Compose owns it: Native TextWrapped/TextTruncate line breaking.. Replacement test: yes: native_inputs::native input controls::mirrors native caption alignment and typography without a text geometry solver (mirrors TextWrapped/TextTruncate); no wrapping assertion..
- `inputs-50` (3 main cases; `button_text_centre`). Main promised: Button and menu-row labels centred in every package and text preference. Engine or Compose owns it: Native TextXAlignment/TextYAlignment from StyleSheet control rules; UIListLayout for icon+label.. Replacement test: yes: native_inputs::native input controls::mirrors native caption alignment and typography without a text geometry solver (mirrored caption copies node alignment); no per-package centring assertion..
- `inputs-64` (2 main cases; `compact_label`). Main promised: Without compact form wrap rule unchanged; unbounded buttons never compact. Engine or Compose owns it: Native AutomaticSize grows the button to its text; TextWrapped rules.. Replacement test: no test.
- `inputs-69` (2 main cases; `compact_label`). Main promised: Ladder holds under every shipped package. Engine or Compose owns it: Native TextBounds measurement per font.. Replacement test: live Studio evidence `classb_keyboard/compact-label-ladder-every-package`.
- `mech1-92` (5 main cases; `conformance/conformance`). Main promised: Pseudo-loc expansion, tall scripts, CJK, emoji, bidi. Engine or Compose owns it: TextLabel TextBounds/AutomaticSize/TextWrapped shaping. Replacement test: yes: Partial: native_collections measures localized row-action labels from TextBounds; no CJK/emoji/bidi/tall-script case and no live evidence cited..
- `mech2-44` (28 main cases; `leaf_capabilities`). Main promised: Rich text display, measurement and font weights. Engine or Compose owns it: TextLabel.RichText + engine TextBounds. Replacement test: yes: Label forwards native text properties (api.md Media "native text properties"); native_inputs::native input controls::uses native Toggle text and reserved padding while retaining rich hints and live theme insets. Facet....
- `mech2-45` (2 main cases; `leaf_capabilities`). Main promised: Text direction closed set. Engine or Compose owns it: GuiObject/TextLabel TextDirection native property. Replacement test: no test (none (forwarded native property, no Facet test)).
- `mech2-92` (1 main case; `paradigm_tenfoot`). Main promised: Explicit Toggle textSize honored. Engine or Compose owns it: TextLabel.TextSize + TextBounds. Replacement test: yes: native_inputs::native input controls::mirrors native caption alignment and typography without a text geometry solver.
- `mech3-15` (5 main cases; `renderer`). Main promised: Toggle value flip is paint-only; label is one line and ellipsizes. Engine or Compose owns it: Native TextButton text layout (TextWrapped/TextTruncate/AutomaticSize) in the Toggle root. Replacement test: yes: partial: native_inputs::native input controls::uses native Toggle text and reserved padding while retaining rich hints and live theme insets shows the label is the native TextButton Text with no Facet TextLabel/UIList....
- `mech3-17` (5 main cases; `renderer`). Main promised: Player preferred text size applied once (measure vs paint, additive offset). Engine or Compose owns it: Native Roblox preferred text size scaling of TextSize/TextBounds. Replacement test: yes: partial: native_text_preference::native text preference updates::keeps editing focus row identity and scroll position across text and theme changes (state/scroll survive a text-size change); no live Studio evidence fo....
- `mech3-64` (9 main cases; `text_calibration`). Main promised: Text metrics calibration and whitespace reservation. Engine or Compose owns it: Native TextBounds / text engine measurement. Replacement test: yes: partial: native_inputs::native input controls::chooses compact button text from native measurements and restores the full caption and native_collections::Compose native collections::adapts table row and header height ....
- `mech3-66` (22 main cases; `text_degrade_cascade`). Main promised: Degrade cascade: wrap, truncate, clip and min-content breaking. Engine or Compose owns it: Native TextWrapped/TextTruncate/TextFits and AutomaticSize/UIFlexItem. Replacement test: no test (none for cascade order; native_collections::Compose native collections::measures localized row action labels and reveals truncated visible table values uses native TextFits).
- `mech3-76` (7 main cases; `text_padding_paint`). Main promised: Padded text inset reaches paint. Engine or Compose owns it: Native UIPadding inside the text-bearing Instance. Replacement test: yes: partial: native_inputs::native input controls::uses native Toggle text and reserved padding while retaining rich hints and live theme insets asserts UIPadding offsets follow theme insets; no Label padding case.
- `mech3-77` (35 main cases; `text_premeasure`). Main promised: Engine premeasurement round and revealWhenTextExact. Engine or Compose owns it: Native asynchronous TextBounds (api.md 603-605: 'Engine geometry settles asynchronously ... Do not assume final text or layout bounds synchronously'). Replacement test: yes: partial: native_inputs::native input controls::chooses compact button text from native measurements and restores the full caption, native_collections::Compose native collections::adapts table row and header height fro....
- `navigation-3-57` (1 main case; `picker_segments`). Main promised: Icon+label survives 1.4x pseudo-localization. Engine or Compose owns it: Native text measurement/AutomaticSize. Replacement test: no test.
- `paint-134` (1 main case; `theme_authoring_scenario`). Main promised: Missing font grows boxes. Engine or Compose owns it: Engine text measurement with AutomaticSize/TextBounds. Replacement test: no test.
- `paint-149` (1 main case; `typography_weight_roles`). Main promised: Measured and painted face agree. Engine or Compose owns it: Engine TextBounds from the StyleRule-applied FontFace/TextSize. Replacement test: no test.
- `themes-P5-06` (1 main case; `theme_reference_packages`). Main promised: Unknown font family compiles and does not clip. Engine or Compose owns it: Engine text measurement (TextBounds / FontFace fallback); Facet's conservative text_metrics reservation is deleted.. Replacement test: no test (none (no candidate test builds buildBadFont; no live evidence of an unknown family).).
- `apps-252` (3 main cases; `large_text_matrix`). Main promised: Sweep coverage, clip allowlist liveness, target-floor mirror. Deleted: Registry sweep, clip allowlists and renderer hit expander deleted.. Replacement test: none; the feature is deleted.
- `apps2-104` (3 main cases; `gallery_demo_picker`). Main promised: Chip label clipping to a budget. Deleted: The chip strip and its label budget were replaced by TabView buttons; truncation is native text behaviour.. Replacement test: none; the feature is deleted.
- `collections-167` (1 main case; `table`). Main promised: No solver overflow in table chrome. Deleted: The solver findings are gone.. Replacement test: none; the feature is deleted.
- `collections-48` (3 main cases; `paradigm_table`). Main promised: Pinned rowHeight caps cells; package border insets grow rows. Deleted: Cell capping to a pinned row and the package border/card inset arithmetic are gone. Cells grow the row (collection_table.luau render watch).. Replacement test: none; the feature is deleted.
- `inputs-49` (2 main cases; `button_shape`). Main promised: Adapter and solver agree on wrap verdict. Deleted: Solver/adapter dual seams deleted.. Replacement test: none; the feature is deleted.
- `inputs-62` (1 main case; `compact_label`). Main promised: Square button draws icon glyph compact form. Deleted: Icon/image compact forms removed; compactLabel is a string or readable (api.md).. Replacement test: none; the feature is deleted.
- `inputs-65` (4 main cases; `compact_label`). Main promised: Solver reserved box, verdict channel, remount cache, zero writes. Deleted: Solver compact verdict and path caches removed.. Replacement test: none; the feature is deleted.
- `mech2-55` (6 main cases; `measure_entry_point`). Main promised: Standalone measurement engine entry. Deleted: Facet layout solver/measure/renderer (src/layout, src/render, src/client adapter) deleted; CAND/src has only src/ui + vendor/compose; api.md "There is no Facet application, mounting service, render target, solver, rea.... Replacement test: none; the feature is deleted.
- `mech3-63` (35 main cases; `text_audit`). Main promised: Text audit: truncation facts, overlaps, clippedEssential, hitFloor, movingText, focusVisibility, outOfBounds. Deleted: text_audit and facet-dump/2 deleted. Replacement test: none; the feature is deleted.
- `mech3-65` (15 main cases; `text_collect_inflight`). Main promised: Premeasure collector in-flight budget. Deleted: premeasure collector deleted. Replacement test: none; the feature is deleted.
- `mech3-71` (23 main cases; `text_disclosure`). Main promised: UI.Text disclose plates: engagement rules, single plate, chrome placement, Toggle label disclosure. Deleted: UI.Text `disclose`, the renderer disclosure plate and adapter zones deleted; api.md has no disclose option. Replacement test: none; the feature is deleted.
- `mech3-73` (16 main cases; `text_fit`). Main promised: text_fit.fit largest fitting size. Deleted: text.fit/text.measure public helpers deleted; no fit API in api.md. Replacement test: none; the feature is deleted.
- `mech3-74` (6 main cases; `text_index_seam`). Main promised: Text index seam. Deleted: per-surface word index deleted. Replacement test: none; the feature is deleted.
- `mech3-75` (20 main cases; `text_line_box`). Main promised: text.facts / text.lineBox / composition.floorPx. Deleted: text.facts, text.lineBox and composition.floorPx deleted. Replacement test: none; the feature is deleted.
- `mech3-80` (15 main cases; `text_round_reentry`). Main promised: Premeasure answer re-entry depth and deferral. Deleted: premeasure/solve re-entry deleted. Replacement test: none; the feature is deleted.
- `navigation-2-41` (1 main case; `menu`). Main promised: Over-long Back label degrades to icon. Deleted: Back row is a plain UI.Button with label only (nav_menu.luau); no expansion-ratio degradation exists or is documented.. Replacement test: none; the feature is deleted.
- `navigation-3-53` (2 main cases; `picker_segments`). Main promised: Icon rides compactLabel degrade ladder. Deleted: Picker no longer passes compactLabel to segments (nav_menu.luau); degrade ladder removed.. Replacement test: none; the feature is deleted.
- `paint-150` (2 main cases; `typography_weight_roles`). Main promised: Exact legacy weights of the eight roles. Deleted: Neutral now: heading/title/strong Bold, numeral Regular; no doc promises weights.. Replacement test: none; the feature is deleted.
- `themes-P3-08` (7 main cases; `theme_controller`). Main promised: Font preflight/calibration and its lifecycle. Deleted: MAIN src/client/text_calibration.luau, text_premeasure.luau, src/layout/text_metrics.luau removed; engine TextBounds are authoritative (api.md: 'Do not assume final text or layout bounds synchronously').. Replacement test: none; the feature is deleted.
- `themes-P3-28` (5 main cases; `theme_roles`). Main promised: text_metrics canonical font-descriptor keys, shared word widths, per-call lineHeight. Deleted: MAIN src/layout/text_metrics.luau deleted; engine text measurement. (Role LineHeight now reaches paint via .facet-type-* rules - see themes-P3-10.). Replacement test: none; the feature is deleted.

### Focus and selection

42 class b and 31 class c contracts, 377 main cases.

- `apps-144` (1 main case; `focus`). Main promised: Screen ring navigation and wrap at edges. Engine or Compose owns it: GuiService.SelectedObject + engine keyboard/gamepad GUI selection navigation (spatial, SelectionGroup/SelectionBehavior*/NextSelection*). Replacement test: no test.
- `apps-148` (1 main case; `focus`). Main promised: Focus changes are observable client-local state. Engine or Compose owns it: GuiService.SelectedObject property changes. Replacement test: yes: native_navigation::native navigation controls::takes selection into a modal only for players navigating by selection.
- `apps-154` (7 main cases; `focus_sections`). Main promised: Declared focus sections: bounds, grid exits, preferred entry, adjacent topology. Engine or Compose owns it: Native SelectionGroup + SelectionBehavior*/NextSelection* (set by src/ui/collections.luau, nav_pages.luau, nav_modal.luau). Replacement test: yes: native_virtual_monitors::native virtual monitors::NVM-2 retains native avatar controls across narrow layouts and expands live HUD (asserts SelectionGroup true); native_navigation::native navigation controls::traps all....
- `apps-155` (1 main case; `focus_structural`). Main promised: Focusable inside a conditional branch joins/leaves selection with mount. Engine or Compose owns it: Engine selection of live Selectable GuiObjects. Replacement test: yes: native_collections::Compose native collections::restores the keyed native focus target when virtualization remounts it.
- `apps-175` (7 main cases; `traversal_order`). Main promised: Grip-bearing controls (Slider/Rating) traverse in document position. Engine or Compose owns it: GuiService.SelectedObject + engine keyboard/gamepad GUI selection navigation (spatial, SelectionGroup/SelectionBehavior*/NextSelection*). Replacement test: no test.
- `apps-183` (6 main cases; `navigation_groups`). Main promised: Group axis, wrap, exits, restore entry, containment, fallthrough. Engine or Compose owns it: Native SelectionGroup/SelectionBehavior*/NextSelection*. Replacement test: yes: native_navigation::native navigation controls::traps all modal selection directions and restores surviving focus on Back (SelectionBehavior Stop only).
- `apps-187` (1 main case; `navigation_groups`). Main promised: Initial focus paints its ring. Engine or Compose owns it: Engine selection highlight. Replacement test: yes: native_navigation::native navigation controls::takes selection into a modal only for players navigating by selection.
- `apps-192` (2 main cases; `review_navigation`). Main promised: Directional reachability of every form control and a slider between controls. Engine or Compose owns it: GuiService.SelectedObject + engine keyboard/gamepad GUI selection navigation (spatial, SelectionGroup/SelectionBehavior*/NextSelection*). Replacement test: no test.
- `apps2-10` (1 main case; `example_match3_motion`). Main promised: Everything below the board is reachable without a pointer. Engine or Compose owns it: GuiService engine directional selection over Selectable controls. Replacement test: no test.
- `apps2-169` (1 main case; `playlist_columns`). Main promised: The header is reachable by a Tab walk from the first focus stop. Engine or Compose owns it: GuiService selection over Selectable Resize grips and Sort buttons. Replacement test: no test.
- `apps2-222` (1 main case; `recipes_common`). Main promised: Real Tab navigation reaches checkbox, chip, accordion and scrolls the rail tail into view. Engine or Compose owns it: GuiService selection traversal and ScrollingFrame.ScrollingEnabled auto-scroll to SelectedObject. Replacement test: live Studio evidence `classb_showcase/recipe-rail-tab-scrolls-tail`.
- `apps2-268` (1 main case; `virtual_monitors`). Main promised: Ten-foot tab strip keeps answering arrows after a coach-mark tab opens. Engine or Compose owns it: GuiService.SelectedObject directional navigation over native tab buttons. Replacement test: yes: artifacts/compose-simplification/gap-closure-live.json nativeGamepad: 'Avatar to Chat directional tab selection', 'DPadDown from tabs into message collection' (Studio virtual controller). No headless test; the coach m....
- `apps2-275` (4 main cases; `virtual_monitors_input`). Main promised: Keyboard ring reaches all Discover controls in order, never sticks, header->hero->feed in few presses, Left/Right pages hero. Engine or Compose owns it: GuiService.SelectedObject engine directional selection with SelectionGroup/SelectionBehavior and UIPageLayout paging. Replacement test: yes: Headless: NVM-2 asserts settings SelectionGroup=true and SelectionBehavior*=Stop. Live: artifacts/compose-simplification/gap-closure-live.json nativeGamepad (DPad tab/collection traversal, Return activates tab). No ev....
- `apps2-277` (4 main cases; `virtual_monitors_input`). Main promised: Gamepad reaches Avatar settings/palette/toggle/stepper/slider and whole Assistant composer by direction; A activates. Engine or Compose owns it: Native gamepad selection (GuiService.SelectedObject, SelectionGroup) and GuiButton.Activated on ButtonA. Replacement test: yes: Headless: NVM-2 SelectionGroup/SelectionBehavior Stop; NVM-Avatar drives stepper/slider via native FacetIncrease/FacetDecrease actions with SelectedObject set. Live: artifacts/compose-simplification/gap-closure-live.j....
- `apps2-281` (3 main cases; `virtual_monitors_input`). Main promised: Screen mode presented before content: ring groups late content, hero horizontal axis, grid 2D navigation. Engine or Compose owns it: Engine GuiService selection over the live Instance tree (no post-present focus graph build); UIGridLayout geometry for 2D navigation. Replacement test: no test (None specific; ledger retiredAssertions notes live gamepad routing remains an engine integration check. artifacts/compose-simplification/gap-closure-live.json nativeGamepad covers DPadDown from tabs into a collection ...).
- `apps2-32` (4 main cases; `examples_gallery`). Main promised: Playlist gamepad/keyboard routes: activate filter field, selection follows focus, d-pad past rating into tray, Left selects edit ⊖. Engine or Compose owns it: GuiService.SelectedObject directional navigation and native InputActions. Replacement test: yes: Control level: native_collections::Compose native collections::respects edge full-swipe policy, edit affordances and focused destructive input. No playlist-specific or live evidence..
- `apps2-43` (5 main cases; `examples_gallery`). Main promised: Settings sync keyboard/gamepad routes and compact phone at every text size. Engine or Compose owns it: GuiService selection with Return/ButtonA activation; UIListLayout/AutomaticSize under PreferredTextSize. Replacement test: no test (none example-specific).
- `apps2-48` (4 main cases; `examples_gallery`). Main promised: Card rail height includes scrollbar band; keyboard/gamepad step sideways only; pseudo-localized chrome. Engine or Compose owns it: Horizontal ScrollingFrame with ScrollBarThickness; GuiService directional selection; engine text measurement. Replacement test: no test.
- `apps2-68` (5 main cases; `examples_games`). Main promised: Keyboard arrows and gamepad DPad/A navigate word keyboard and crossword board; keyboard rows are navigation groups. Engine or Compose owns it: GuiService.SelectedObject engine directional selection and GuiButton.Activated on Return/ButtonA. Replacement test: no test (none for these examples (native selection geometry is not simulated by the fake engine; no live entry in parity_blockers.json or gap-closure-live.json for games)).
- `apps2-88` (16 main cases; `gallery_chrome`). Main promised: D-pad, keyboard alone and gamepad alone reach the demo list and change demo and theme through Settings. Engine or Compose owns it: GuiService.SelectedObject engine directional navigation, Return/ButtonA activation of TabView buttons and the Settings Sheet pickers. Replacement test: no test (None for the gallery headlessly. parity_blockers.json G17 resolution and gap-closure-live.json G17 ('nativeTabsWorking': true) and G18 ('pointerSelections': 12) record Studio checks, pointer-driven; no recorded d-pad/...).
- `inputs-04` (3 main cases; `auto_input`). Main promised: Auto-merged focus groups: initial field focus, D-pad field-to-rows, refresh after structural change. Engine or Compose owns it: GuiService.SelectedObject and engine directional selection over live GuiObjects (no Facet focus graph or group derivation).. Replacement test: no test (none for plain screens; initial selection is only promised for modals (NN 'takes selection into a modal only for players navigating by selection'). Collections: native_collections::Compose native collections::defers i...).
- `inputs-11` (12 main cases; `auto_input_screens`). Main promised: Grid/HStack/flat-ring automatic directional navigation and wrap. Engine or Compose owns it: Engine GuiService directional selection (NextSelection*/selection groups) over native UIGridLayout/UIListLayout geometry.. Replacement test: yes: native_collections::Compose native collections::uses new grid neighbors when responsive columns change without changing total extent (VirtualGrid neighbours only); none for plain Host grids/rows..
- `inputs-131` (2 main cases; `paradigm_carry`). Main promised: Focus and exclusive modal survive input class flips. Engine or Compose owns it: GuiService.SelectedObject persists across PreferredInput changes.. Replacement test: yes: native_navigation::native navigation controls::traps all modal selection directions and restores surviving focus on Back; native_navigation::native navigation controls::takes selection into a modal only for players na....
- `inputs-134` (9 main cases; `paradigm_input_axis`). Main promised: Thumbstick latch, deadzone, arbitration and navigation binding. Engine or Compose owns it: GuiService thumbstick navigation and InputAction axis handling.. Replacement test: no test.
- `inputs-79` (11 main cases; `dpad_repeat`). Main promised: D-pad/arrow/thumbstick navigation auto-repeat cadence, catch-up and ownership loss. Engine or Compose owns it: Engine GuiService directional navigation repeat.. Replacement test: no test (none for navigation repeat (Facet's own value repeat is covered by native_inputs::native input controls::repeats focused value adjustments with bounded catch-up and no idle frame subscription).).
- `inputs-99` (1 main case; `input`). Main promised: Navigate/Activate drive focus through actions. Engine or Compose owns it: GuiService selection + Facet Activate InputAction.. Replacement test: yes: native_inputs::native input controls::retains native keyboard activation after first focus and gates it on live selection.
- `mech1-129` (24 main cases; `focus_grid_axis`). Main promised: 2-D grid focus: perpendicular line moves, ragged clamp, entry, eligibility, rectangle invariant, corpus. Engine or Compose owns it: Compose.createFocusScope (collection focus) + GuiService.SelectedObject. Replacement test: yes: Facet passes columns/position to Compose focus scope; native_collections asserts one 'down' move after a column change and disabled skipping. Probe confirmed ragged-last-row clamp and disabled skip down a lane work, b....
- `mech1-131` (15 main cases; `focus_skip`). Main promised: Focus-skip live predicate and grouped traversal. Engine or Compose owns it: Compose.createFocusScope disabled predicate + GuiService.SelectedObject. Replacement test: yes: native_collections asserts disabled rows are skipped by the Down action and after source churn; predicate re-read per navigation and 'nearest/restore' entry are not asserted..
- `mech2-21` (32 main cases; `inherited_subtree`). Main promised: Disabled ancestor blocks pointer/touch/keyboard/gamepad/focus for its subtree, live, and selection leaves it. Engine or Compose owns it: GuiObject.Interactable/Visible inherited by descendants + GuiService selection eligibility; Facet ctx.action walks ancestor Interactable/Visible/LayerCollector.Enabled (src/ui/context.luau) to gate InputActions. Replacement test: yes: native_public_surface::native public surface::gates native actions through retained ancestors and detaches observations; native_gallery_parity::restored native gallery parity::retains nested color overrides and contro....
- `mech2-35` (4 main cases; `late_contributions`). Main promised: Focus ring appears for navigation, never for touch. Engine or Compose owns it: GuiService.SelectedObject selection image is only shown in gamepad/keyboard selection mode. Replacement test: yes: native_navigation::native navigation controls::takes selection into a modal only for players navigating by selection (modal entry only); no live evidence of ring suppression after touch.
- `navigation-2-19` (5 main cases; `distance_gamepad`). Main promised: Geometric directional navigation (nested columns, beams, ties, wrap/grid precedence, live axis). Engine or Compose owns it: GuiService.SelectedObject directional navigation by the engine, SelectionGroup/SelectionBehavior*, native UIListLayout FillDirection. Replacement test: yes: native_navigation::native navigation controls::traps all modal selection directions and restores surviving focus on Back (SelectionBehavior Stop); native_navigation::native navigation controls::adapts automatic picker....
- `navigation-3-28` (1 main case; `navigation_stack`). Main promised: DPad crosses sibling chrome, Back and page controls without traps. Engine or Compose owns it: GuiService.SelectedObject engine directional navigation. Replacement test: no test.
- `navigation-3-60` (1 main case; `picker_segments`). Main promised: Rail options reachable by pointer, keyboard and gamepad. Engine or Compose owns it: GuiService selection and GuiButton activation. Replacement test: no test.
- `navigation-3-68` (3 main cases; `picker_segments`). Main promised: Segmented strip navigates across on a pad; vertical rail vertical; live flip. Engine or Compose owns it: GuiService engine directional selection over UIListLayout-arranged buttons. Replacement test: yes: native_navigation::native navigation controls::adapts automatic pickers and updates native segment axis and badges without replacing options (FillDirection follows axis).
- `navigation-4-05` (2 main cases; `popup_button`). Main promised: Keyboard / gamepad open, navigate and Activate select in the popup, focus returns to trigger. Engine or Compose owns it: GuiService.SelectedObject engine navigation + native GuiButton.Activated on Return/ButtonA; Menu modal SelectionGroup + SelectionBehavior Stop. Replacement test: yes: Modal selection trap/restore is tested for Alert only; Menu panel initial-row selection (menuPanel focusIn watch picking the first enabled row) and return to the Trigger are not tested for Menu/Picker..
- `navigation-4-30` (2 main cases; `selection_indicator`). Main promised: Indicator is never a focus stop; indicated picker has exactly its option stops. Engine or Compose owns it: Native GuiService selection over Selectable GuiButtons; indicator Frame has Selectable=false. Replacement test: no test (none (no candidate assertion of Selectable on SelectionIndicator or stop count)).
- `navigation-5-14` (7 main cases; `tab_view`). Main promised: Directional, keyboard, gamepad and touch activation of tabs. Engine or Compose owns it: Tabs are native TextButtons (UI.Button); GuiService directional selection, Return/ButtonA activation and touch taps are engine behavior that all end in Activated. PreferredInput hot-switch carries no Facet state.. Replacement test: yes: native_gallery_shell fires tab TextButton.Activated and asserts selection; live parity_blockers G18 (12 native pointer selections). No gamepad/keyboard live evidence cited for TabView..
- `paint-27` (2 main cases; `display_controls`). Main promised: Keyboard/gamepad activate through the same path (Picker, DisclosureGroup). Engine or Compose owns it: GuiService.SelectedObject + native GuiButton.Activated for every input kind. Replacement test: yes: native_inputs::native input controls::activates programmatically selected buttons through the native GuiService property.
- `paint-73` (5 main cases; `native_style_scenario`). Main promised: Themed focus ring and glow. Retired by the owner: Facet has no theme focus ring or glow. api.md Selection says that the controls use GuiService.SelectedObject. The engine selection highlight is the focus indicator, and a game sets SelectionImageObject for its own. See owner decision 5.
- `themes-P1-29` (3 main cases; `avatar`). Main promised: Avatar hover/Tab rank/gamepad A-B affordances. Engine or Compose owns it: Native TextButton hover, GuiService.SelectedObject focus, engine-owned ButtonB.. Replacement test: yes: Selectable true/false asserted; no live evidence..
- `themes-P1-42` (3 main cases; `avatar_group`). Main promised: Overflow hover/Tab rank/gamepad affordances. Engine or Compose owns it: Native TextButton hover and GuiService selection; faces non-selectable.. Replacement test: no test (none beyond Activated firing).
- `themes-P4-15` (3 main cases; `theme_layer_application`). Main promised: Focus ring/glow hug the art plate; hit geometry keeps the 47px floor. Engine or Compose owns it: Roblox native selection (GuiService.SelectedObject with the engine selection image) on the control's own GuiObject; Facet no longer parents a ring/glow.. Replacement test: live Studio evidence `classb_themes/skinned-selection-hugs-art`.
- `themes-P5-20` (2 main cases; `theme_reference_packages`). Main promised: Theme focus chrome (glow vs ring) per package. Engine or Compose owns it: GuiService.SelectedObject native selection highlight; chrome.focus is declared by 5 packages but consumed by no CAND src.. Replacement test: no test (none for theme-specific focus visuals; the ornate two-theme contrast half is covered by report.ok in the cited case.).
- `themes-P5-43` (2 main cases; `review_theme_corners`). Main promised: Focus ring corners track selection, theme and square skin art. Retired by the owner: Facet has no focus ring. The engine draws the selection highlight, and no file in src reads radii.selection. api.md Selection says only that the controls use GuiService.SelectedObject. See owner decision 5.
- `apps-149` (5 main cases; `focus`). Main promised: Focus graph never writes caller tables; navigation publication ordering. Deleted: Facet focus graph (newFocusGraph, NavigationGroup, replaceGroups, traverse/rank, presenter present() focus opts) deleted; api.md has no newFocusGraph/navigationGroups/traversalPriority/focusOrder; docs/guide/07-input..... Replacement test: none; the feature is deleted.
- `apps-150` (10 main cases; `focus_chrome`). Main promised: focusChrome adoption declarations: composed ring membership, placements, entry, modal suspension, dismissal, edge refusal. Deleted: focusChrome option and composed focus maps deleted with the focus graph; api.md has no focusChrome.. Replacement test: none; the feature is deleted.
- `apps-153` (7 main cases; `focus_section_appear`). Main promised: UI.focusSection focusOnAppear/returnFocus. Deleted: UI.focusSection removed (not in CAND UI registry or api.md). Modal controls own initial selection and restoration (docs/guide/07-input.md).. Replacement test: none; the feature is deleted.
- `apps-174` (2 main cases; `keyboard_navigation`). Main promised: present({traversalWrap}) per surface and unknown opt refusal. Deleted: present() and traversalWrap deleted.. Replacement test: none; the feature is deleted.
- `apps-176` (9 main cases; `traversal_order`). Main promised: traversalPriority tiers and validation. Deleted: Facet focus graph (newFocusGraph, NavigationGroup, replaceGroups, traverse/rank, presenter present() focus opts) deleted; api.md has no newFocusGraph/navigationGroups/traversalPriority/focusOrder; docs/guide/07-input..... Replacement test: none; the feature is deleted.
- `apps-177` (3 main cases; `traversal_order`). Main promised: Explicit navigationGroups map order. Deleted: Facet focus graph (newFocusGraph, NavigationGroup, replaceGroups, traverse/rank, presenter present() focus opts) deleted; api.md has no newFocusGraph/navigationGroups/traversalPriority/focusOrder; docs/guide/07-input..... Replacement test: none; the feature is deleted.
- `apps-178` (5 main cases; `traversal_order`). Main promised: handle.focusOrder() inspector. Deleted: Facet focus graph (newFocusGraph, NavigationGroup, replaceGroups, traverse/rank, presenter present() focus opts) deleted; api.md has no newFocusGraph/navigationGroups/traversalPriority/focusOrder; docs/guide/07-input..... Replacement test: none; the feature is deleted.
- `apps-182` (6 main cases; `traversal_order`). Main promised: graph.traverse rank semantics. Deleted: Facet focus graph (newFocusGraph, NavigationGroup, replaceGroups, traverse/rank, presenter present() focus opts) deleted; api.md has no newFocusGraph/navigationGroups/traversalPriority/focusOrder; docs/guide/07-input..... Replacement test: none; the feature is deleted.
- `apps-186` (4 main cases; `navigation_groups`). Main promised: Presenter navigationGroups opt, replaceGroups, groups function, flat legacy ring. Deleted: Facet focus graph (newFocusGraph, NavigationGroup, replaceGroups, traverse/rank, presenter present() focus opts) deleted; api.md has no newFocusGraph/navigationGroups/traversalPriority/focusOrder; docs/guide/07-input..... Replacement test: none; the feature is deleted.
- `apps2-87` (7 main cases; `gallery_chrome`). Main promised: Chrome closed leaves the demo's input alone; exactly one engaged surface; no stolen keys. Deleted: examples/gallery/client/showcase_chrome.luau (engagement model, global chrome keys) was deleted. The new chrome (client/screen.luau) is a TabView plus a Settings button with no global bindings; TabView shoulder action.... Replacement test: none; the feature is deleted.
- `collections-148` (2 main cases; `table`). Main promised: Click moves keyboard focus; arrows move the selection. Deleted: Behavior changed. Focus and selection are separate. Arrows move native selection only.. Replacement test: none; the feature is deleted.
- `collections-159` (2 main cases; `table`). Main promised: buildFocusGroups; focus inside a row re-selects. Deleted: There is no focus graph.. Replacement test: none; the feature is deleted.
- `collections-18` (2 main cases; `collection_list`). Main promised: Live predicate flip and mid-gesture focusability protection. Deleted: The permissive focus-skip predicate and the rule that keeps a session target focusable are gone. disabled(item) is one non-reactive predicate that Compose createFocusScope evaluates on source change.. Replacement test: none; the feature is deleted.
- `collections-180` (1 main case; `table_input`). Main promised: Selection follows D-pad focus. Deleted: Behavior changed. Focus does not select.. Replacement test: none; the feature is deleted.
- `collections-195` (1 main case; `table_input`). Main promised: A table with no selection/reorder/actions has no row stops. Deleted: Behavior changed. Table rows always have a Selectable RowHit.. Replacement test: none; the feature is deleted.
- `collections-236` (2 main cases; `virtual_grid_input`). Main promised: Right off the end of a line wraps into the next line; the focus dump. Deleted: Compose spatial moveDirection has no line wrap, and the dump is gone.. Replacement test: none; the feature is deleted.
- `collections-246` (1 main case; `virtual_hgrid`). Main promised: Down off the end of a line crosses into the next. Deleted: There is no line wrap in Compose spatial moves.. Replacement test: none; the feature is deleted.
- `collections-269` (3 main cases; `virtual_list_input`). Main promised: Navigation group name and exit, unknown navigation field. Deleted: navigationGroups are gone. The native SelectionGroup=true root is used.. Replacement test: none; the feature is deleted.
- `collections-33` (3 main cases; `native_scroll_autobind`). Main promised: navigationGroups/focusGroups presenter discriminator. Deleted: The presenter, focusGroups and navigationGroups are gone. There is no Facet focus graph (guide 02-architecture).. Replacement test: none; the feature is deleted.
- `collections-45` (1 main case; `paradigm_table`). Main promised: A selection-none, non-reorderable table keeps rows out of the focus ring. Deleted: Behavior changed. The Table always passes onActivate to its body, so every row has a Selectable RowHit (collection_table.luau bodySpec.onActivate).. Replacement test: none; the feature is deleted.
- `mech1-132` (4 main cases; `focus_skip`). Main promised: Active-interaction focus exemption. Deleted: FocusGraph exemption API deleted.. Replacement test: none; the feature is deleted.
- `mech1-133` (19 main cases; `focus_lift_travel`). Main promised: Focus lift reset, float ring travel and held Toggle knob tweens. Deleted: Focus lift/float ring and adapter tweens deleted; native selection image and Compose motion.. Replacement test: none; the feature is deleted.
- `mech1-134` (12 main cases; `focus_ring_float_geometry`). Main promised: Float focus ring geometry. Deleted: Float focus ring deleted.. Replacement test: none; the feature is deleted.
- `mech1-95` (4 main cases; `conn_reconnect_sweep`). Main promised: Focus lift caps to nearest scroll/clip host window. Deleted: Focus lift/float ring deleted; native SelectionImageObject.. Replacement test: none; the feature is deleted.
- `mech2-34` (6 main cases; `late_contributions`). Main promised: Late contributions wired into focus scope. Deleted: presenter contribution discovery / focus groups deleted. Replacement test: none; the feature is deleted.
- `mech3-26` (12 main cases; `screen_focus_visual_seam`). Main promised: screen_focus_visual seam structure. Deleted: screen_focus_visual module deleted. Replacement test: none; the feature is deleted.
- `navigation-1-04` (6 main cases; `adaptive_presentation`). Main promised: Sidebar expand/collapse commands, focus handoff, Back collapse and distance observers. Deleted: Main TabView expandSidebar()/collapseSidebar() handle API, RailHome focus handoff, handleCancel contribution and env viewingDistance no longer exist: CAND src/ui/nav_pages.luau has no expand/collapse commands and no v.... Replacement test: none; the feature is deleted.
- `navigation-3-42` (1 main case; `page_view`). Main promised: Carousel is one stop; one Down leaves it from any page. Deleted: Main focus-map carousel grouping deleted; CAND indicators are a selectable segmented UI.Picker plus Previous/Next buttons; api.md makes no one-stop promise.. Replacement test: none; the feature is deleted.
- `themes-P2-31` (8 main cases; `theme_chrome`). Main promised: Themed focus treatment: ring or glow, per-theme colour, distance profiles, census. Deleted: chrome_slots.focusRecipeOf, resolveFocusColor, focusShadowSpec and FOCUS_TEN_FOOT are removed. No src reads chrome.focus, but theme_types.SkinLayer.kind still lists 'glow', and shipped scifi_hud and fantasy_parchment .... Replacement test: none; the feature is deleted.

### Input actions

30 class b and 18 class c contracts, 209 main cases.

- `apps-152` (3 main cases; `focus_chrome`). Main promised: A press on chrome activates its own handler exactly once. Engine or Compose owns it: Native GuiButton.Activated on the selected object. Replacement test: yes: native_inputs::native input controls::activates programmatically selected buttons through the native GuiService property.
- `apps2-183` (2 main cases; `radial_hold_resolve`). Main promised: Held stick/d-pad cost no re-solves or repeat walks. Engine or Compose owns it: Native InputAction Pressed fires once per press and Direction2D StateChanged fires only on value change; the Facet presenter repeat clock (DPAD_REPEAT_*) and solver solve counts no longer exist.. Replacement test: no test (none (NRAD-1 exercises hold/aim hysteresis but not held-repeat or a held d-pad)).
- `apps2-203` (3 main cases; `radial_menu`). Main promised: Pointer/keyboard/gamepad activate the selected wedge. Engine or Compose owns it: Native GuiButton.Activated (mouse click, touch tap, Return/ButtonA on GuiService.SelectedObject) replaces the presenter dispatch.. Replacement test: yes: Candidate fires Activated directly (native_navigation radial case, NRAD-7); keyboard/gamepad selection activation is engine-owned, no live evidence recorded.
- `apps2-211` (1 main case; `radial_menu`). Main promised: Input-class hot switch keeps the menu open. Engine or Compose owns it: Engine PreferredInput changes no longer route through a Facet interaction-class state machine; nothing in nav_radial reacts to preferred input.. Replacement test: no test.
- `apps2-280` (2 main cases; `virtual_monitors_input`). Main promised: Affordance follows input: tap hides ring, navigation restores; resolved input fact follows device. Engine or Compose owns it: Engine selection-ring visibility (GuiService selection image shown only for gamepad/keyboard navigation) and UserInputService.PreferredInput. Replacement test: yes: Facet reads PreferredInput via ctx.observe(input,'PreferredInput') in src/ui/nav_pages.luau:150, nav_surfaces.luau:149, collection_table.luau:85; headless tests grep PreferredInput in native_inputs/native_navigation/n....
- `apps2-30` (3 main cases; `examples_gallery`). Main promised: Temperature field enters edit mode by pointer, touch, Return and gamepad A; B cancels to snapshot. Engine or Compose owns it: Native TextBox focus (CaptureFocus on activation) and GuiService selection/Return/ButtonA. Replacement test: yes: Control level: native_inputs::native input controls::retains native keyboard activation after first focus and gates it on live selection; native_inputs::native input controls::limits UTF-8 characters, rejects validati....
- `apps2-52` (2 main cases; `examples_gallery`). Main promised: withAnimation reachable by four inputs; copy survives 1.4x pseudo-localization. Engine or Compose owns it: GuiButton.Activated for all input types; engine text measurement. Replacement test: no test.
- `collections-179` (2 main cases; `table_input`). Main promised: ButtonA/Return select the focused row. Engine or Compose owns it: Native GuiButton activation of the Selectable RowHit. Replacement test: no test.
- `collections-237` (2 main cases; `virtual_grid_input`). Main promised: Return/ButtonA activate the focused cell. Engine or Compose owns it: Native GuiButton activation of the selected cell or RowHit. Replacement test: no test.
- `collections-247` (1 main case; `virtual_hgrid`). Main promised: Return/ButtonA activate. Engine or Compose owns it: Native activation. Replacement test: no test.
- `collections-265` (2 main cases; `virtual_list_input`). Main promised: Return/ButtonA activate the focused row. Engine or Compose owns it: Native GuiButton activation. Replacement test: no test.
- `collections-288` (1 main case; `virtual_list_row_actions`). Main promised: A multi-action tray navigates horizontally. Engine or Compose owns it: Native spatial selection in a horizontal UIListLayout tray. Replacement test: no test.
- `collections-59` (4 main cases; `row_actions`). Main promised: Keyboard and gamepad activate a revealed tray action or the edit minus. Engine or Compose owns it: GuiService.SelectedObject plus native GuiButton activation (Return/ButtonA). Opening a tray moves SelectedObject into it (collection_row_actions.luau watch).. Replacement test: yes: native_collections::Compose native collections::respects edge full-swipe policy, edit affordances and focused destructive input asserts the focused RowActionsDelete context. No test asserts that selection moves into t....
- `inputs-100` (5 main cases; `input`). Main promised: Modifier state tracking, key-up ownership and stuck-modifier reset. Engine or Compose owns it: InputBinding PrimaryModifier/SecondaryModifier evaluation by the engine.. Replacement test: no test.
- `inputs-145` (1 main case; `pointer`). Main promised: sinkNavigation stops nav keys reaching lower contexts. Engine or Compose owns it: InputContext.Sink.. Replacement test: yes: native_inputs::native input controls::gives native dialog contexts ordered sink priority and live eligibility; native_inputs::native input controls::honors every clear-button mode and ends disabled edits without a commit.
- `inputs-165` (7 main cases; `review_native_input`). Main promised: Preferred binding selection per class, naming revisions and modifier siblings. Engine or Compose owns it: InputAction.PreferredBinding.. Replacement test: yes: native_inputs::native input controls::borrows native preferred bindings and releases replaced hint observers.
- `inputs-82` (1 main case; `dpad_repeat`). Main promised: Higher-priority gameplay binding cancels value repeat. Engine or Compose owns it: InputContext Priority/Sink arbitration.. Replacement test: no test.
- `inputs-97` (6 main cases; `input`). Main promised: Context arbitration: priority sink, release on disable, dedup, destroyed context. Engine or Compose owns it: InputContext Priority/Sink/Enabled and InputAction state.. Replacement test: yes: native_inputs::native input controls::gives native dialog contexts ordered sink priority and live eligibility; native_public_surface::native public surface::gates native actions through retained ancestors and detaches....
- `inputs-98` (1 main case; `input`). Main promised: Preferred binding follows preferred input kind. Engine or Compose owns it: InputAction.PreferredBinding.. Replacement test: yes: native_inputs::native input controls::borrows native preferred bindings and releases replaced hint observers.
- `mech1-08` (6 main cases; `adaptive`). Main promised: Primary input class from device capability and expressed preference. Engine or Compose owns it: UserInputService.PreferredInput (engine resolves the primary input). Replacement test: yes: Facet observes PreferredInput and switches policy: native_collections 'adapts table row and header height from native text bounds and preferred input' (Touch -> 44 min row), native_navigation help-on-touch, modal sele....
- `mech3-36` (3 main cases; `session_lifetime`). Main promised: Action system lifetime disposes its contexts. Engine or Compose owns it: Native InputContext/InputAction owned by Compose. Replacement test: yes: native_public_surface::native public surface::gates native actions through retained ancestors and detaches observations.
- `mech3-40` (1 main case; `slice_mutation`). Main promised: One action carries keyboard, touch and gamepad bindings. Engine or Compose owns it: Native InputAction with multiple InputBinding children. Replacement test: yes: partial: native_inputs::native input controls::deduplicates shortcut/default bindings and yields shortcuts during native text editing.
- `navigation-1-12` (6 main cases; `alert`). Main promised: Every input class activates actions; hover and touch target affordances. Engine or Compose owns it: Native GuiButton.Activated (mouse, touch, keyboard Return and gamepad A through GuiService selection), plus GuiState hover.. Replacement test: yes: native_navigation 'captures alert payload and closes before invoking its action' fires Activated on the action. No per-input-class or 44px-height assertion..
- `navigation-1-58` (5 main cases; `callout`). Main promised: Dismiss affordance works for pointer, touch, keyboard and gamepad; one arrow press reaches it; 44px floor. Engine or Compose owns it: Native GuiButton.Activated and GuiService directional selection (the callout panel is registered through ctx.modalPortal so it stays available). Replacement test: yes: The candidate fires Dismiss.Activated only. There is no directional-reach or 44px-height assertion and no live evidence..
- `navigation-2-03` (8 main cases; `collapsible_view`). Main promised: CollapsibleView per-input activation and affordances (pointer/touch/keyboard/gamepad, hover, wheel, 44px, focus ring, B closes). Engine or Compose owns it: GuiButton.Activated for all input classes, GuiService selection/SelectionGroup + SelectionBehavior Stop trap, native ScrollingFrame, ModalBack InputAction (Escape/ButtonB) from nav_modal.luau. Replacement test: yes: Candidate fires Summary.Activated and switches PreferredInput to Gamepad (Close appears); modal trap/Back asserted generically for Alert in native_navigation::native navigation controls::traps all modal selection dire....
- `navigation-3-20` (1 main case; `navigation_stack`). Main promised: Keyboard/gamepad Activate and Back pop the stack. Engine or Compose owns it: GuiService selection activation plus an InputContext/InputAction 'NavigationBack' bound to Escape and ButtonB. Replacement test: yes: native_navigation::native navigation controls::routes nested Back through the focused native context before its parent (fires NavigationBack.Pressed and asserts pop).
- `navigation-3-41` (4 main cases; `page_view`). Main promised: Keyboard/gamepad page by direction; Activate reaches the showing page. Engine or Compose owns it: UIPageLayout GamepadInputEnabled + GuiService selection. Replacement test: yes: native_navigation::native navigation controls::restores surviving navigation focus and moves paging focus into the visible page (Facet moves selection into the visible page); none for keyboard Right paging.
- `navigation-4-42` (3 main cases; `sheet`). Main promised: Directional Adjust on the size control (Right/DPadRight), yield at last detent, no adjust for caller content named Size. Engine or Compose owns it: GuiService selection navigation; the Facet Adjust action is deleted and the grabber only cycles on Activated. Replacement test: yes: Candidate asserts no node named "Size" exists and the Grabber is Selectable; directional keys are not tested..
- `paint-102` (1 main case; `sheet_model`). Main promised: Hover never matches touch. Engine or Compose owns it: StyleRule :Hover pseudo-class driven by engine GuiState. Replacement test: no test.
- `paint-57` (1 main case; `native_regions_feedback`). Main promised: Activation feedback reaches the device. Engine or Compose owns it: GuiButton.PressHapticEffect (factory pressHaptic). Replacement test: yes: native_inputs::native input controls::plays the application press haptic only for controls that change a state or a value; native_gallery::native gallery::requests native haptics and releases effects when the screen closes; physical moto....
- `apps-157` (12 main cases; `responder`). Main promised: HUD responder engage/resign, gameplay guard, present() default, setSink seam, priority bands. Deleted: Responder/engage/resign/gameplayGuard API deleted. CAND src/ui/nav_modal.luau binds Back with priority 2000 (same as ordinary actions, src/ui/context.luau default), so the main 'modal >= 3000 above gameplay 2000' orde.... Replacement test: none; the feature is deleted.
- `apps-165` (1 main case; `keyboard_navigation`). Main promised: Keyboard navigation OFF binds and sinks nothing. Deleted: Keyboard navigation opt-in flag deleted.. Replacement test: none; the feature is deleted.
- `apps-170` (22 main cases; `keyboard_navigation`). Main promised: Responder/passive surfaces, initialFocus keywords/ids, focus-ring ownership, hot-plug, gameplayGuard. Deleted: Facet focus graph (newFocusGraph, NavigationGroup, replaceGroups, traverse/rank, presenter present() focus opts) deleted; api.md has no newFocusGraph/navigationGroups/traversalPriority/focusOrder; docs/guide/07-input..... Replacement test: none; the feature is deleted.
- `apps-172` (5 main cases; `keyboard_navigation`). Main promised: Keyboard navigation opt-in globally and per surface. Deleted: Presenter/surface keyboard opt-in deleted.. Replacement test: none; the feature is deleted.
- `apps2-164` (11 main cases; `platform_adapters_recovery`). Main promised: Legacy controls unbind, TouchControlsEnabled refcount, PlayerScripts wait. Deleted: src/client/gamepad_contention, responder_effects and roblox_input are deleted; CAND src/ui has no UnbindAction, TouchControlsEnabled or PlayerScripts wait, and api.md makes no such promise (guides require PlayerScript.... Replacement test: none; the feature is deleted.
- `apps2-234` (6 main cases; `spatial`). Main promised: Normalized spatial event data. Deleted: src/input/spatial.luau (normalize/extend/isFlat/describe) and the renderer pointer path are deleted; no API replaces them.. Replacement test: none; the feature is deleted.
- `apps2-95` (19 main cases; `gallery_chrome`). Main promised: Shoulder buttons open sections, chips carry shoulder marks, one context per press, ButtonY for menus, raise layer, raise-listener errors. Deleted: All chrome-owned bindings (ButtonL1/R1 section opening, shoulder marks, ButtonY, raise layer and listener) were in showcase_chrome.luau, now deleted. TabView keeps its own PreviousTab/NextTab shoulders (untested; see .... Replacement test: none; the feature is deleted.
- `collections-175` (2 main cases; `table_input`). Main promised: Header group initial focus and select-then-stick sort. Deleted: The header focus group and the stick sort model are gone.. Replacement test: none; the feature is deleted.
- `collections-186` (4 main cases; `table_input`). Main promised: A swipe release Activate does not eat the next press. Deleted: The activation-artifact arming is gone.. Replacement test: none; the feature is deleted.
- `collections-190` (2 main cases; `table_input`). Main promised: Horizontal press row-change rule; closed-row byte identity. Deleted: The focus graph and path identity are gone.. Replacement test: none; the feature is deleted.
- `inputs-15` (1 main case; `auto_input_screens`). Main promised: inputHint style='phrase'. Deleted: inputHint/newActionSystem hint styles removed; ShortcutHint (api.md) has no style option.. Replacement test: none; the feature is deleted.
- `inputs-168` (5 main cases; `review_native_input`). Main promised: Provider stack lifetime and restoration. Deleted: Adapter provider stack removed; no provider registry in CAND src/ui input code.. Replacement test: none; the feature is deleted.
- `inputs-174` (2 main cases; `shortcut_hint`). Main promised: Names its own surface before competing chrome. Deleted: Surface naming via presenter chrome removed.. Replacement test: none; the feature is deleted.
- `inputs-78` (5 main cases; `dpad_repeat`). Main promised: Gamepad-source classification of keyCodes. Deleted: Facet action-system source classification removed; InputAction bindings are native.. Replacement test: none; the feature is deleted.
- `inputs-96` (4 main cases; `hint_no_remount`). Main promised: inputHint scope ownership and style validation. Deleted: newActionSystem inputHint memo/scope/style options removed.. Replacement test: none; the feature is deleted.
- `mech1-138` (14 main cases; `gamepad_contention`). Main promised: Gamepad/legacy control-module contention probes. Deleted: Client contention module deleted; no doc promise.. Replacement test: none; the feature is deleted.
- `navigation-2-68` (5 main cases; `modal_dismissal`). Main promised: cancelPolicy option. Deleted: cancelPolicy deleted; no reference in CAND src/docs; Alert/Sheet expose only interactiveDismissDisabled/canDismiss internally.. Replacement test: none; the feature is deleted.
- `navigation-5-16` (1 main case; `tab_view`). Main promised: Keyboard Period/Comma page the tabs. Deleted: Main's keyboard Adjust paging (Comma/Period) is not bound in CAND (only ButtonL1/ButtonR1); api.md promises only 'shoulder navigation'.. Replacement test: none; the feature is deleted.

### Pointer, touch and drag

16 class b and 20 class c contracts, 298 main cases.

- `collections-75` (8 main cases; `row_actions_input`). Main promised: Touch drag parity and mouse/touch hot switch. Engine or Compose owns it: UIDragDetector gives one drag stream for mouse and touch and owns a single capture. Replacement test: yes: native_collections::Compose native collections::cancels native row gestures and keeps destructive opt-outs behind every action route drives the detector after a Touch InputBegan. No hot-switch test. No live evidence..
- `collections-91` (8 main cases; `row_actions_input`). Main promised: A swipe over a content Button opens the tray and a tap still reaches the Button. Engine or Compose owns it: A UIDragDetector on the RowActions root, with the content Button beneath getting native Activated. Replacement test: yes: native_gallery_collections::native gallery collections::shows seeded mail state and applies idempotent read visible flag and chosen deletion swipes mail rows whose content is Button-based. No live evidence of tap-thro....
- `inputs-133` (4 main cases; `paradigm_hover`). Main promised: Hover wiring by pointer presence; nothing essential behind hover. Engine or Compose owns it: GuiObject.GuiState / StyleRule :Hover driven by the engine.. Replacement test: yes: native_navigation::native navigation controls::shows button help after a pointer rests on it and never on touch.
- `inputs-144` (3 main cases; `pointer`). Main promised: Pointer capture lifecycle on root destroy. Engine or Compose owns it: UIDragDetector capture owned by the engine and destroyed with its Compose owner.. Replacement test: yes: native_inputs::native input controls::lets the native drag detector drive one bounded slider model and cancel; native_inputs::native input controls::cancels slider pointer ownership on native gamepad handoff and repor....
- `inputs-209` (15 main cases; `touch_drag_arm`). Main promised: Touch hold arms a drag and stops the scroller. Engine or Compose owns it: UIDragDetector and ScrollingFrame gesture arbitration.. Replacement test: yes: native_collections::Compose native collections::locks vertical gestures, opens short velocity flicks and cancels disabled drags; native_inputs::native input controls::lets the native drag detector drive one bounded sl....
- `inputs-211` (7 main cases; `touch_gestures`). Main promised: Gesture arbiter (pinch/rotate over pan, long-press over tap). Engine or Compose owns it: Engine Touch* gesture events.. Replacement test: yes: native_navigation::native navigation controls::keeps split primary activation separate from touch long-press alternatives.
- `inputs-223` (1 main case; `value_controls`). Main promised: Touch drag drives the Slider identically. Engine or Compose owns it: UIDragDetector handles touch and mouse.. Replacement test: yes: native_inputs::native input controls::lets the native drag detector drive one bounded slider model and cancel.
- `inputs-83` (10 main cases; `drag_public`). Main promised: Drag acquisition thresholds, touch vs pointer promotion, armed presses, declineTouch. Engine or Compose owns it: UIDragDetector drag start and ScrollingFrame gesture arbitration.. Replacement test: yes: native_inputs::native input controls::lets the native drag detector drive one bounded slider model and cancel; native_collections::Compose native collections::reorders selected blocks with native drag and cancels stal....
- `mech1-108` (8 main cases; `drag_bridge_seam`). Main promised: Drag registry, held source, z tie-break, live source enablement, detector attach/dispose. Engine or Compose owns it: UIDragDetector. Replacement test: yes: Facet's use is exercised on fake-engine detectors: reorder, row-action swipe, disabled drag cancel, slider. Stacked-target z tie-break has no analog. No live Studio drag evidence cited in parity_blockers..
- `mech2-106` (3 main cases; `presenter_drag_integration`). Main promised: Detector taps activate exactly once and are never eaten. Engine or Compose owns it: UIDragDetector. Replacement test: yes: native_games::examples/07 native Match3 parity::selects detector taps once and swaps adjacent tiles with mouse and touch; native_virtual_monitors::native virtual monitors::opens native detector taps once while drags a....
- `mech2-97` (5 main cases; `pointer_seam_ownership`). Main promised: Touch capture belongs to originating finger; scrolling passes through pointer zones. Engine or Compose owns it: UIDragDetector per-input ownership; engine scroll pass-through. Replacement test: yes: native_radial_controls::native radial interaction policies::NRAD-2 ignores foreign touch release and cancels captured touch (radial custom capture only).
- `navigation-3-19` (1 main case; `navigation_stack`). Main promised: Back hover for pointer and 44px touch target. Engine or Compose owns it: GuiButton hover/touch hit testing on the internal UI.Button 'Back'. Replacement test: live Studio evidence `classb_navigation/navigation-back-pointer`.
- `navigation-3-38` (4 main cases; `page_view`). Main promised: One swipe advances one page; pointer-down origin; unchanged offset cannot swallow reversal. Engine or Compose owns it: UIPageLayout TouchInputEnabled swipe paging and JumpTo. Replacement test: yes: native_navigation::native navigation controls::uses native page layout for model navigation and engine swipes (engine JumpTo -> selection).
- `navigation-3-43` (3 main cases; `page_view`). Main promised: Pointer/touch/keyboard affordances (hover, 44px dots, focus ring). Engine or Compose owns it: Native GuiButton hover/selection; UISizeConstraint 44x44 on picker segments. Replacement test: yes: native_navigation::native navigation controls::preserves icon-only names, indicator choices and native target floors.
- `navigation-4-43` (2 main cases; `sheet`). Main promised: In-flight drag cancels on an input-class flip; stationary grab cancel resumes interrupted motion. Retired by the owner: api.md Sheet promises only the release rule: the sheet goes to the detent nearest to the released height plus 0.15 s of velocity, and a detent change from outside ends the drag. It does not promise a cancel when the input class changes, or a resume of an interrupted detent motion. The engine UIDragDetector owns the gesture, and Facet treats DragEnd as a release.
- `themes-P1-28` (4 main cases; `avatar`). Main promised: Avatar activation on pointer/touch/keyboard/gamepad reaches one surface. Engine or Compose owns it: Native TextButton.Activated with Selectable=true, GuiService selection for keyboard/gamepad.. Replacement test: yes: ACCESS fires Activated and asserts Selectable=true; per-device routing is engine-owned, no live Studio evidence in parity_blockers.json..
- `themes-P1-41` (4 main cases; `avatar_group`). Main promised: Overflow activates on pointer/touch/keyboard/gamepad. Engine or Compose owns it: Native TextButton.Activated (Selectable when onOverflow supplied).. Replacement test: yes: AVATAR fires Overflow.Activated and counts one call; no live evidence..
- `apps2-246` (17 main cases; `sponsor_scenarios`). Main promised: Sponsor drop: drag session, ghost, verdicts, edge autoscroll, device-driven armed flow, cancel affordances. Deleted: The generic drag session/drop API and sponsor_drop fixture were deleted (api.md mentions drag only for Slider, Sheet detents and Table/list reorder). Native reorder drag and offscreen autoscroll are tested in native_c.... Replacement test: a candidate test covers the surviving part (see reason).
- `collections-113` (3 main cases; `row_actions_state`). Main promised: Rubber-banding past the tray. Deleted: CAND clamps the drag to [-rowWidth, rowWidth] (move() in collection_row_actions.luau). There is no rubber band.. Replacement test: none; the feature is deleted.
- `inputs-102` (18 main cases; `interaction_tokens`). Main promised: Promotion tokens, responder bands and drag velocity window. Deleted: interaction_tokens facade deleted (ledger retiredMechanisms); no module in CAND src/ui.. Replacement test: none; the feature is deleted.
- `inputs-143` (6 main cases; `pointer`). Main promised: Grip, cursor hints, rectOf and anchored overlay offsets. Deleted: Grip/cursorHint/controller.rectOf removed; no such API in CAND src/api.md.. Replacement test: none; the feature is deleted.
- `inputs-178` (15 main cases; `tap_arm_seam`). Main promised: Renderer tap-arm seam. Deleted: Renderer tap-arm module deleted (ledger retiredMechanisms).. Replacement test: none; the feature is deleted.
- `inputs-210` (14 main cases; `touch_gestures`). Main promised: Gesture argument normalizer. Deleted: touch_gestures normalizer deleted.. Replacement test: none; the feature is deleted.
- `inputs-222` (1 main case; `value_controls`). Main promised: Fallback grip capture. Deleted: Fallback Grip capture transport removed.. Replacement test: none; the feature is deleted.
- `inputs-84` (53 main cases; `drag_public`). Main promised: UI.draggable/UI.dropTarget hover, legality, terminals, geometry, pad pickup, proxy, authoring errors, enabled gate and structural refusal. Deleted: UI.draggable/UI.dropTarget and the drag session/proxy facade are absent from CAND src and api.md (grep draggable/dropTarget: 0 in src/ui and docs).. Replacement test: none; the feature is deleted.
- `inputs-85` (11 main cases; `drag_session`). Main promised: Generic drag session hover/predict/drop/cancel/retarget. Deleted: drag_session module removed; no generic session in CAND src/ui.. Replacement test: none; the feature is deleted.
- `mech1-107` (13 main cases; `drag_bridge_seam`). Main promised: Drag bridge seam source scans. Deleted: render drag bridge module deleted.. Replacement test: none; the feature is deleted.
- `mech2-07` (6 main cases; `hit_expander_overhang`). Main promised: Sub-floor hit expander placement/z/clamp/refusal. Deleted: Hit-expander instances deleted with screen_target adapter; controls now size themselves to 44px minimums (e.g. inputs.luau, nav_surfaces.luau) and api.md promises a 44 floor only for Table rows (covered by native_coll.... Replacement test: a candidate test covers the surviving part (see reason).
- `mech2-08` (4 main cases; `hit_expander_z_band`). Main promised: Sub-floor hit expander placement/z/clamp/refusal. Deleted: Hit-expander instances deleted with screen_target adapter; controls now size themselves to 44px minimums (e.g. inputs.luau, nav_surfaces.luau) and api.md promises a 44 floor only for Table rows (covered by native_coll.... Replacement test: a candidate test covers the surviving part (see reason).
- `mech2-09` (14 main cases; `hit_floor_region_clamp`). Main promised: Sub-floor hit expander placement/z/clamp/refusal. Deleted: Hit-expander instances deleted with screen_target adapter; controls now size themselves to 44px minimums (e.g. inputs.luau, nav_surfaces.luau) and api.md promises a 44 floor only for Table rows (covered by native_coll.... Replacement test: a candidate test covers the surviving part (see reason).
- `mech2-10` (5 main cases; `hit_rect_refusal`). Main promised: Sub-floor hit expander placement/z/clamp/refusal. Deleted: Hit-expander instances deleted with screen_target adapter; controls now size themselves to 44px minimums (e.g. inputs.luau, nav_surfaces.luau) and api.md promises a 44 floor only for Table rows (covered by native_coll.... Replacement test: a candidate test covers the surviving part (see reason).
- `mech2-108` (13 main cases; `presenter_drag_integration`). Main promised: Presenter drag bus, ghost layer, held source, retiring targets, onTick. Deleted: generic drag session/presenter bus/onTick deleted. Replacement test: none; the feature is deleted.
- `mech2-98` (10 main cases; `pointer_seam_ownership`). Main promised: Cursor borrowing, menu connection split, connection reuse. Deleted: pointer seam / cursor hint deleted (no MouseIcon in CAND src). Replacement test: none; the feature is deleted.
- `navigation-2-45` (3 main cases; `menu`). Main promised: Long-press ownership outside menus (disclosure plate / help). Deleted: The plate long-press help route was deleted; help is hover/selection only and suppressed on touch (nav_modal.luau help.register). Touch suppression is covered by native_navigation::native navigation controls::shows bu.... Replacement test: a candidate test covers the surviving part (see reason).
- `themes-P1-30` (1 main case; `avatar`). Main promised: Compact avatars reserve disjoint 44px touch targets. Deleted: Facet hit-target expansion ('+target' wrapper) deleted; api.md only promises a 44 minimum row height for collections, not Avatar targets.. Replacement test: none; the feature is deleted.
- `themes-P1-43` (1 main case; `avatar_group`). Main promised: Compact overflow keeps one whole 44px target. Deleted: Hit-target expansion wrapper deleted; not promised in api.md.. Replacement test: none; the feature is deleted.

### Scrolling

16 class b and 20 class c contracts, 227 main cases.

- `apps-188` (2 main cases; `navigation_groups`). Main promised: Horizontal ScrollView is a horizontal run. Engine or Compose owns it: Engine gamepad navigation within ScrollingFrame. Replacement test: no test.
- `apps-224` (4 main cases; `native_scroll`). Main promised: Scroll host declares content extent (including gaps) and updates it on change. Engine or Compose owns it: ScrollingFrame AutomaticCanvasSize / CanvasSize with UIListLayout Padding. Replacement test: yes: native_collections::Compose native collections::uses measured extents and column placements for both scroll directions.
- `apps-226` (4 main cases; `native_scroll`). Main promised: Horizontal scroll axis layout and canvas. Engine or Compose owns it: ScrollingFrame ScrollingDirection + horizontal UIListLayout. Replacement test: yes: native_collections::Compose native collections::uses measured extents and column placements for both scroll directions.
- `apps-227` (2 main cases; `native_scroll`). Main promised: Focus keep-visible scrolls the host. Engine or Compose owns it: Engine scroll-into-view of GuiService.SelectedObject. Replacement test: yes: native_collections::Compose native collections::enters native focus after unfocused collection churn and reaches offscreen keys.
- `apps-230` (2 main cases; `scroll_indicators`). Main promised: Hug height stable across session flip; single-axis canvas never exceeds window on the other axis. Engine or Compose owns it: AutomaticSize / AutomaticCanvasSize per axis. Replacement test: no test.
- `apps2-109` (2 main cases; `gallery_demo_picker`). Main promised: The list box never exceeds its room and its canvas exceeds the box when it does not fit. Engine or Compose owns it: ScrollingFrame with AutomaticCanvasSize inside TabView. Replacement test: no test.
- `apps2-228` (1 main case; `showcase_tabs`). Main promised: Keyboard-focused recipe rail tail stays inside every nested clipping ancestor. Engine or Compose owns it: Engine ScrollingFrame scroll-to-SelectedObject within nested ScrollingFrames (ClipsDescendants). Replacement test: live Studio evidence `classb_showcase/showcase-rail-tail-inside-clips`.
- `apps2-229` (1 main case; `showcase_tabs`). Main promised: Every selected tab stays within its nearest scroll host on a 390px phone at Largest text. Engine or Compose owns it: TabView ScrollingFrame bar with engine AutomaticSize/PreferredTextSize. Replacement test: yes: parity_blockers.json pendingLiveRisks: 'Shell measured reservation and Settings reachability under themes/largest text' (unresolved)..
- `mech2-48` (23 main cases; `leaf_capabilities`). Main promised: Two-axis scrolling, freeze, indicators, nested scroll arbitration. Engine or Compose owns it: ScrollingFrame ScrollingEnabled/ScrollingDirection/ScrollBarThickness/CanvasSize and engine nested-scroll handoff. Replacement test: yes: native_collections::Compose native collections::uses measured extents and column placements for both scroll directions; native_virtual_monitors::native virtual monitors::NVM-16 permits only the active presentation to ....
- `mech2-78` (2 main cases; `mouse_scroll`). Main promised: Mouse drag scroll arbitration and paged drag bounds. Engine or Compose owns it: ScrollingFrame + UIPageLayout engine swipes. Replacement test: yes: native_navigation::native navigation controls::uses native page layout for model navigation and engine swipes.
- `mech3-21` (1 main case; `review_cards`). Main promised: Shelf settles when theme/text change crosses the scrollbar threshold. Engine or Compose owns it: Native ScrollingFrame ScrollBarInset + AutomaticSize. Replacement test: no test.
- `mech3-29` (28 main cases; `scroll_bar_measure`). Main promised: Scrollbar gutter reservation: measure includes bar, persistent vs auto, nested canvas, chrome bleed. Engine or Compose owns it: Native ScrollingFrame VerticalScrollBarInset/HorizontalScrollBarInset = ScrollBar and ScrollBarThickness (set by the theme rule in src/ui/themes.luau:403-409). Replacement test: no test (none: no candidate test asserts the ScrollingFrame StyleRule insets; collections/menus also override ScrollBarThickness per instance (collections.luau:347 = 6 vs sheet 4)).
- `mech3-32` (10 main cases; `scroll_window_clip`). Main promised: Scroll window clip and bar policy. Engine or Compose owns it: Native ScrollingFrame AbsoluteWindowSize and ScrollBarInset. Replacement test: no test (none (same as scroll_bar_measure)).
- `navigation-3-37` (1 main case; `page_view`). Main promised: Paging without a horizontal scrollbar. Engine or Compose owns it: UIPageLayout in a clipped Frame. Replacement test: yes: native_navigation::native navigation controls::uses native page layout for model navigation and engine swipes (PagesLayout present, CurrentPage).
- `navigation-5-04` (3 main cases; `tab_view`). Main promised: An overflowing strip scrolls inside its band. Engine or Compose owns it: Native ScrollingFrame 'Tabs' with AutomaticCanvasSize X/Y and ScrollingDirection per placement; wheel/drag scrolling and hover are engine behavior.. Replacement test: yes: native_navigation 'fades native tab layers...' sets a tab beyond the bar's AbsoluteSize and asserts CanvasPosition.X=140 (Facet writes the canvas); ScrollingDirection/AutomaticCanvasSize values themselves are not asse....
- `paint-86` (4 main cases; `path`). Main promised: Path culled outside a clipping scroll viewport. Engine or Compose owns it: Engine clipping (ClipsDescendants/ScrollingFrame) of Path2D. Replacement test: live Studio evidence `classb_navigation/path-clipped-in-scroll`.
- `apps-228` (20 main cases; `native_scroll`). Main promised: controller.scrollTo, scrollToVisible, keep-visible clamp, adapter fallbacks, fake/screen target scroll seams. Deleted: ScrollView controller, scrollToVisible, shared keep-visible clamp and render-target scroll seam deleted; api.md VirtualList 'controls' exposes only indexOfKey/placementOf/offsetOf.. Replacement test: none; the feature is deleted.
- `apps-229` (12 main cases; `scroll_indicators`). Main promised: Input-derived scroll indicator policy, reserve seam, indicators='none', live reserve writes. Deleted: Scroll indicator policy and reserve seam deleted; CAND collections.luau sets ScrollBarThickness = 6 statically.. Replacement test: none; the feature is deleted.
- `apps-232` (20 main cases; `scroll_navigation`). Main promised: Named scroll navigation, visibility crossings, progress, arrival focus handoff and interruptions. Deleted: Named scroll destination API deleted; not in api.md.. Replacement test: none; the feature is deleted.
- `apps2-34` (3 main cases; `examples_gallery`). Main promised: Bootstrap auto-bind of bindNativeScroll (both spellings); edit mode diagnostic-clean at narrowest viewport. Deleted: Table api.bindNativeScroll and the gallery bootstrap auto-bind were removed (native ScrollingFrame is the scroller); solver diagnostics no longer exist.. Replacement test: none; the feature is deleted.
- `collections-01` (10 main cases; `autoscroll`). Main promised: Autoscroll bands, dwell and flick-through safety. Deleted: Main's autoscroll state machine (top/bottom bands, dwell before arming, justArmed, band-cross restart) is gone. CAND src/ui/collection_reorder.luau startFrames() starts edge scrolling at once inside an edge of min(40,.... Replacement test: none; the feature is deleted.
- `collections-02` (7 main cases; `autoscroll`). Main promised: Autoscroll speed ramp, exit ease and coast. Deleted: CAND speed is clamp(penetration)*600 px/s. It has no start ramp, exit ease or D-11 coast (collection_reorder.luau startFrames). Nothing is promised publicly.. Replacement test: none; the feature is deleted.
- `collections-04` (3 main cases; `autoscroll`). Main promised: Autoscroll published defaults, portrait band and garbage options. Deleted: The autoscroll options and their ratified defaults are gone. Nothing public can be tuned (collection_reorder.luau uses constants).. Replacement test: none; the feature is deleted.
- `collections-126` (3 main cases; `scroll_snap`). Main promised: Uniform/variable index parity and keep-visible outward rounding. Deleted: Facet's extent index is gone. Keep-visible uses controls.offsetOf with start/end and no snap rounding.. Replacement test: none; the feature is deleted.
- `collections-130` (3 main cases; `scroll_snap`). Main promised: Keep-visible and programmatic scrollTo honour snap; a flick after a settle measured in seconds. Deleted: There is no scrollTo API. Framework writes cancel snap (frameworkWrite). The flick clock is internal.. Replacement test: none; the feature is deleted.
- `collections-181` (7 main cases; `table_input`). Main promised: Reorder inside a scrolling page, nested autoscroll chain, autoscroll=false. Deleted: There is no page or nested autoscroll chain. CAND autoscroll scrolls only its own body.. Replacement test: none; the feature is deleted.
- `collections-215` (1 main case; `table_virtualized`). Main promised: The no-anchor negative control. Deleted: The Facet anchor algorithm is gone (Compose).. Replacement test: none; the feature is deleted.
- `collections-251` (1 main case; `virtual_list_axis`). Main promised: scrollTo along the axis. Deleted: There is no scrollTo API.. Replacement test: none; the feature is deleted.
- `collections-257` (1 main case; `virtual_list_axis`). Main promised: Band naming per axis. Deleted: There are no named bands.. Replacement test: none; the feature is deleted.
- `collections-268` (1 main case; `virtual_list_input`). Main promised: unbindNativeScroll. Deleted: bindNativeScroll and unbindNativeScroll are gone.. Replacement test: none; the feature is deleted.
- `collections-34` (3 main cases; `native_scroll_autobind`). Main promised: bindNativeScroll de-duplication and unbind. Deleted: bindNativeScroll is not in CAND src or api.md. The control observes its own CanvasPosition.. Replacement test: none; the feature is deleted.
- `collections-35` (2 main cases; `native_scroll_autobind`). Main promised: Block table and bare mount publish no scroll path. Deleted: Published scroll paths are gone. A block table sets ScrollingEnabled=false (NC15).. Replacement test: none; the feature is deleted.
- `mech1-41` (12 main cases; `autoscroll_bridge_seam`). Main promised: Autoscroll bridge seam source scans. Deleted: render autoscroll bridge module deleted.. Replacement test: none; the feature is deleted.
- `mech3-28` (9 main cases; `scroll_arrange_seam`). Main promised: scroll-arrange seam structure. Deleted: solver scroll arrange branch deleted. Replacement test: none; the feature is deleted.
- `mech3-30` (15 main cases; `scroll_into_view_seam`). Main promised: scroll-into-view seam structure. Deleted: scroll_into_view module and controller.scrollToVisible/scrollHostFor deleted. Replacement test: none; the feature is deleted.
- `themes-P2-36` (4 main cases; `theme_chrome`). Main promised: Scroll canvas grows by the theme's decoration bleed. Deleted: snapshot.chromeBleed and focusBlurWithin are removed along with the solver. A native ScrollingFrame clips UIShadow and glow bleed, and there is no promise.. Replacement test: none; the feature is deleted.

### Motion

3 class b and 42 class c contracts, 422 main cases.

- `apps-260` (9 main cases; `transitions`). Main promised: Retire-until-exit model: re-entry reuses identity, dispose exactly once, rows hold slot, churn returns to baseline. Engine or Compose owns it: Compose presence/LayerStack exit ownership (src/vendor/compose); UI.When/UI.ForEach removed. Replacement test: yes: native_navigation::native navigation controls::animates alerts from native sources and reclaims interrupted Compose presence; native_radial_controls::native radial interaction policies::NRAD-9 Compose exit presence re....
- `mech2-101` (4 main cases; `presentation_channel`). Main promised: Declared fade group uses CanvasGroup GroupTransparency. Engine or Compose owns it: CanvasGroup.GroupTransparency. Replacement test: yes: native_gallery_workflows::native gallery workflows::applies shared and per-plate CanvasGroup opacity and owns the arrival tween; native_navigation::native navigation controls::fades native tab layers with Compose and ....
- `mech2-70` (5 main cases; `motion_paint_only`). Main promised: Feedback motion is paint-only (pop/shake/caret/radial lift). Engine or Compose owns it: UIScale / Rotation / Position offsets on native instances animated by Compose. Replacement test: yes: native_inputs::native input controls::restarts Compose pop feedback and obeys live reduced motion (Button pop drives UIScale); shake/caret/radial lift not asserted to be layout-neutral.
- `apps-259` (1 main case; `shared_element`). Main promised: Origin named twice or not at all is refused. Deleted: Named transition.source across UI.When/ForEach removed; source is now a direct node/readable.. Replacement test: none; the feature is deleted.
- `apps-265` (52 main cases; `transitions`). Main promised: Transition coordinator forms, exit caps/classes, event-sampled slides, arrive events, presenter surface ids, exit asymmetry, stagger, nested fade groups, materialize band. Deleted: Facet transition coordinator and presets deleted; api.md TabView: 'Named Facet transition presets do not exist'; motion is runtime.tween/spring/timeline (docs/guide/15-components.md).. Replacement test: none; the feature is deleted.
- `apps2-13` (3 main cases; `example_match3_motion`). Main promised: Example has no reduced-motion branch and builds no animation system of its own (source scan). Deleted: The presenter.onTick/ctx.app seams the scan required are deleted. 07_match3.luau now reads GuiService.ReducedMotionEnabled itself (line 94, branch at 391) and drives its state machine from RunService.Heartbeat (line 7.... Replacement test: none; the feature is deleted.
- `apps2-187` (5 main cases; `radial_menu`). Main promised: Launcher morph: launcher/ring crossfade and handoff. Deleted: The launcher/ring morph (LauncherVisual CanvasGroup whose opacity complements the ring, ListArea handoff, Back occupying the launcher while closing) is gone: nav_radial.luau attaches the launcher as an ordinary UI.But.... Replacement test: none; the feature is deleted.
- `apps2-218` (6 main cases; `radial_menu`). Main promised: Bloom stagger, candidate lift and commit pop. Deleted: nav_radial.luau has no per-wedge stagger, lift or pop (only a 0.16 s entry timeline and 0.12 s exit fade); api.md/guides make no bloom/lift/pop promise.. Replacement test: none; the feature is deleted.
- `apps2-241` (8 main cases; `sponsor_scenarios`). Main promised: Sponsor motion flights, chase, overshoot, wash re-set, panel reversal, rest cost, reduced-motion parity. Deleted: sponsor_motion fixture deleted; the Compose spring runtime (runtime.spring) remains and reversal/reduced motion are exercised generically in native_navigation ('retargets native sheet motion and honors live reduced mo.... Replacement test: none; the feature is deleted.
- `apps2-242` (6 main cases; `sponsor_scenarios`). Main promised: Celebration beats, interrupts, skip, replay, reduced motion. Deleted: sponsor_celebration fixture deleted; no celebration API in api.md.. Replacement test: none; the feature is deleted.
- `apps2-51` (4 main cases; `examples_gallery`). Main promised: withAnimation: solver exact while paint travels; panel installs no record; segmented Full/Reduced; environment restored on dismiss. Deleted: The solver, animation-record system and Facet environment were deleted; with_animation.luau now uses a UI.Toggle 'Reduce decorative motion' (line 69), so the Full/Reduced segmented contract was deliberately changed.. Replacement test: none; the feature is deleted.
- `apps2-93` (4 main cases; `gallery_chrome`). Main promised: Settings and with_animation edit one reduced-motion fact. Deleted: Now two facts: with_animation.luau keeps its own 'reduced' cell and ORs it with ctx.reducedMotion (line 8); its Toggle does not reflect or write the gallery setting.. Replacement test: none; the feature is deleted.
- `collections-69` (2 main cases; `row_actions_hosted`). Main promised: Hosted departure clipping without host translation. Deleted: The host translation is gone. The RowActions root has ClipsDescendants=true.. Replacement test: none; the feature is deleted.
- `collections-93` (1 main case; `row_actions_motion`). Main promised: The tray unmounts within a frame of arrival. Deleted: The hand-rolled spring epsilon is gone. Visibility is the sign of paintedOffset.. Replacement test: none; the feature is deleted.
- `collections-96` (2 main cases; `row_actions_motion`). Main promised: Collapse timing against slide arrival and fast reversal. Deleted: Replaced by a runtime.timeline split at a fixed 0.7 fraction. No assertion.. Replacement test: none; the feature is deleted.
- `inputs-163` (4 main cases; `reduced_motion_seam`). Main promised: Target setReducedMotion seam and ScreenTarget resolver. Deleted: Render target/presenter push seam removed.. Replacement test: none; the feature is deleted.
- `mech1-105` (10 main cases; `declarative_animation`). Main promised: Declarative layout animation presets and region transitions. Deleted: Facet declarative animation/withAnimation and UI.When/ForEach transitions deleted; Compose runtime.tween/spring and control transition options (TabView/Alert) are tested in native_navigation.. Replacement test: a candidate test covers the surviving part (see reason).
- `mech1-13` (5 main cases; `animation_precedence`). Main promised: Keep-visible (keyboard) writer composes with animation records. Deleted: present(bp,{keepVisibleOffset}) and presentation records deleted; no keyboard-avoidance promise in CAND api.md.. Replacement test: none; the feature is deleted.
- `mech2-102` (25 main cases; `presentation_channel`). Main promised: Presentation transforms and keep-visible root shift. Deleted: presentation channel/screen_target applyRect deleted. Replacement test: none; the feature is deleted.
- `mech2-103` (2 main cases; `presentation_transform_subtree`). Main promised: Transform subtree recompose. Deleted: presentation channel deleted. Replacement test: none; the feature is deleted.
- `mech2-64` (3 main cases; `motion_animate`). Main promised: State-driven motion observer, retarget, validation. Deleted: Facet motion.animate deleted; Compose animation APIs (api.md "Use the Compose animation APIs for motion"). Replacement test: none; the feature is deleted.
- `mech2-65` (10 main cases; `motion_chase`). Main promised: Perceptual-arrival chase. Deleted: motion chase deleted. Replacement test: none; the feature is deleted.
- `mech2-68` (33 main cases; `motion_clock`). Main promised: Motion clock stepping, timers, glides, counters, class facade. Deleted: src/motion clock/classes deleted; Compose animation replaces. Replacement test: none; the feature is deleted.
- `mech2-71` (17 main cases; `motion_spring`). Main promised: Spring classes and solver. Deleted: Facet spring solver/classes deleted. Replacement test: none; the feature is deleted.
- `mech2-72` (14 main cases; `motion_timeline`). Main promised: Timeline beats. Deleted: Facet timeline deleted. Replacement test: none; the feature is deleted.
- `mech2-75` (23 main cases; `motion_tween`). Main promised: Curve registry, engine curve twin, clock:tween timing, setEasing. Deleted: Facet curve registry/clock deleted; Compose tween with Compose.easing (api.md TabView/Alert transition options). Replacement test: none; the feature is deleted.
- `mech3-22` (16 main cases; `review_motion_pixels`). Main promised: Focus-ring motion pixel settling and stale tween cancel. Deleted: Facet focus ring and its tweened lift deleted; selection visuals are native GuiService selection. Replacement test: none; the feature is deleted.
- `mech3-78` (14 main cases; `text_reveal`). Main promised: Auto marquee reveal of truncated text. Deleted: reveal='auto' strip deleted; api.md promises only native truncation. Replacement test: none; the feature is deleted.
- `mech3-79` (5 main cases; `text_reveal_stream`). Main promised: motion.newTextReveal typewriter stream. Deleted: motion.newTextReveal deleted. Replacement test: none; the feature is deleted.
- `mech3-96` (10 main cases; `value_reveal`). Main promised: value_reveal count-up hold rules. Deleted: value_reveal motion helper deleted; ProgressView trail (covered in native_themes_media) is the nearest surviving value motion. Replacement test: a candidate test covers the surviving part (see reason).
- `mech3-99` (29 main cases; `with_animation`). Main promised: withAnimation transaction: exact solve with painted travel, relative records, refusals, curves, size travel, commits. Deleted: UI.withAnimation and registered motion classes/curves deleted; api.md 'Named Facet transition presets do not exist' (line 291). Replacement test: none; the feature is deleted.
- `navigation-1-34` (8 main cases; `alert`). Main promised: Named transition forms, materialize band, readable/bound refusal recording, text-raster-safe settle. Deleted: Main transitions.luau named forms (enter='materialize'/'instant', band, class) are deleted. api.md: 'Named Facet transition presets do not exist'; the transition is {source, seconds, ease}. Without a transition the pr.... Replacement test: none; the feature is deleted.
- `navigation-1-52` (27 main cases; `backdrop_fade`). Main promised: Backdrop-before-content structural finder, coordinator split write, Glade modals clean, surface acknowledgements. Deleted: src/render/transitions.luau (findBackdropFindings, coordinator split GroupTransparency routing, acknowledgement schema) and the presenter's per-modal coordinator are deleted. CAND uses Compose presence + direct GroupT.... Replacement test: none; the feature is deleted.
- `navigation-2-01` (5 main cases; `collapsible_view`). Main promised: CollapsibleView morph from compact view (plate transform, scroll-live endpoints, one CanvasGroup, in-flight hot switch). Deleted: Candidate UI.CollapsibleView (src/ui/nav_surfaces.luau) is a Button that opens surfaces.modal(surfaces.centered(...)) through surfaces.overlay with no presence/transition option: no morph from the trigger rect, no com.... Replacement test: none; the feature is deleted.
- `navigation-2-15` (5 main cases; `disclosure_group`). Main promised: Disclosure motion: sibling glide, reduced-motion snap, fade-group exclusion, exit clipping, no overshoot. Deleted: Candidate DisclosureGroup (nav_surfaces.luau) mounts content with Compose.show and no transition/presence; there is no caret rotation, content exit clip, fade group or sibling glide. api.md DisclosureGroup section pro.... Replacement test: none; the feature is deleted.
- `navigation-2-49` (3 main cases; `menu`). Main promised: Menu panel materialize and sheet submenu slide motion. Deleted: Menu overlay uses surfaces.overlay without presence options (nav_menu.luau); no menu motion API remains.. Replacement test: none; the feature is deleted.
- `navigation-3-03` (6 main cases; `native_navigation_stack`). Main promised: Named slide/materialize/transform/reveal forms, interruption reclaim and rapid push-push-pop. Deleted: Main navigation transition forms (slide-left/right, materialize, transform fromRect, reveal) and presentationOffset slot motion are gone; CAND layer() in src/ui/nav_pages.luau asserts transition keys are only seconds/.... Replacement test: none; the feature is deleted.
- `navigation-3-07` (1 main case; `native_selection_indicator`). Main promised: Sliding selection chip reverses continuously and settles under reduced motion. Deleted: Main newSelectionIndicator / Layer/Bar chip motion deleted; CAND Picker marks the current option with StyleSheet tags (facet-segment-current, facet-selected) and an optional static 'SelectionIndicator' underline Frame.... Replacement test: none; the feature is deleted.
- `navigation-3-29` (4 main cases; `navigation_stack`). Main promised: Slide geometry, interruption containment, nested viewport and first-presentation slide width. Deleted: Slide transition and clipped slide viewport deleted (layer() accepts only seconds/ease fade); api.md: no named transition presets.. Replacement test: none; the feature is deleted.
- `navigation-4-23` (14 main cases; `selection_indicator`). Main promised: Indicator slides between solved rects; place-vs-slide policy, reduced-motion snap, flight guards, timing with layout. Deleted: Sliding shared indicator and its motion policy deleted; the CAND indicator is a per-option child shown by Compose.show, so there is no travel to animate or snap. api.md promises no indicator motion.. Replacement test: none; the feature is deleted.
- `navigation-5-49` (3 main cases; `tab_view_transition`). Main promised: Legacy accepted spellings ({fade=true}, enter/exit fade, instant). Deleted: UI.When transition schema and transitions.resolve are gone; api.md: 'Native fades accept direct Compose tween options ... Named Facet transition presets do not exist.'. Replacement test: none; the feature is deleted.
- `navigation-5-51` (8 main cases; `tab_view_transition`). Main promised: Refuse transform/slide/materialize/reveal/asymmetric/no-form/unknown-form. Deleted: Named forms no longer exist; CAND validates only { seconds, ease } keys (api.md 'Named Facet transition presets do not exist').. Replacement test: none; the feature is deleted.
- `paint-18` (3 main cases; `control_vocabulary`). Main promised: Plate animation forwarding and passive Text over-media tag. Deleted: Facet animation modifiers and UI.Text over-media classification deleted; motion is Compose timelines per api.md.. Replacement test: none; the feature is deleted.
- `paint-83` (2 main cases; `path`). Main promised: Needle helper and 100-point limit. Deleted: No public Path; nav_path_shapes.needle is private and unused by src/ui (dead code); the 100-point assert is internal.. Replacement test: none; the feature is deleted.
- `themes-P1-86` (1 main case; `skeleton`). Main promised: Reducing motion before render drain never restarts recursively. Deleted: Facet motionClock (lastError, recursive restart guard) deleted; Compose shared timeline pause/play replaces it.. Replacement test: none; the feature is deleted.

### Paint and theming

17 class b and 82 class c contracts, 474 main cases.

- `apps-200` (2 main cases; `selection_radius`). Main promised: Segmented fill and focused row paint follow theme corner radius. Engine or Compose owns it: StyleSheet StyleRule '.facet-segmented::UICorner' from metrics.radii.control; engine selection highlight. Replacement test: yes: native_themes_media::native themes and media::keeps native theme rules reactive without writing instance paint.
- `apps-202` (3 main cases; `selection_shape`). Main promised: Square themes get square selection corners; authored corner wins. Engine or Compose owns it: StyleRule UICorner from package metrics.radii. Replacement test: yes: native_themes_media::native themes and media::keeps native theme rules reactive without writing instance paint.
- `apps-205` (2 main cases; `selection_shape`). Main promised: Bar LevelPicker plates a track; glyph picker plates nothing. Engine or Compose owns it: StyleRule tags on level marks. Replacement test: yes: native_inputs::native input controls::marks filled bar levels with a tag and keeps the shortest bar visible.
- `inputs-110` (1 main case; `level_picker`). Main promised: Bar filled/empty halves carry different theme tints. Engine or Compose owns it: StyleSheet rules on .facet-level-on vs .facet-level-segment.. Replacement test: yes: native_inputs::native input controls::marks filled bar levels with a tag and keeps the shortest bar visible (tags only).
- `inputs-132` (1 main case; `paradigm_hover`). Main promised: Hover role distinct from rest fill. Engine or Compose owns it: StyleRule :Hover selectors.. Replacement test: yes: native_themes_media::native themes and media::uses Button interaction state for semantic icon variants.
- `inputs-205` (1 main case; `toggle_presentations`). Main promised: Theme-owned checkbox outline in each state. Engine or Compose owns it: StyleSheet rules on .facet-toggle-checkbox/.facet-toggle-on and skin slots.. Replacement test: yes: native_themes_media::native themes and media::applies rich native skin slots to editable fields toggles steppers and sliders.
- `navigation-1-37` (2 main cases; `alert`). Main promised: Title (and mark+title pair) centred in the card. Engine or Compose owns it: Native TextXAlignment through StyleSheet rules on the facet-type-heading/alert tags. Replacement test: no test.
- `navigation-2-48` (6 main cases; `menu`). Main promised: Anchored panel is one card (hairlines, radii, selection fill, selected label/icon paint, sheet shape). Engine or Compose owns it: Native StyleSheet/StyleRule paint on facet-menu/facet-menu-item/facet-selected tags, UICorner on MenuRows. Replacement test: yes: native_themes_media::native themes and media::shares selected contrast across rich disclosure titles and chevrons proves the '.facet-selected >> .facet-text/.facet-icon' rule for DisclosureGroup, not for menu rows; no....
- `navigation-3-35` (2 main cases; `navigation_visuals`). Main promised: Capsule paint order after compact-to-distant resize. Engine or Compose owns it: Native ZIndex/sibling paint order. Replacement test: yes: native_navigation::native navigation controls::rehomes tab accessories with native measured space and owner cleanup (asserts Tabs.ZIndex > Content.ZIndex).
- `navigation-3-83` (1 main case; `picker_style`). Main promised: Chosen menu row readable on its selection fill. Engine or Compose owns it: StyleSheet .facet-selected rule. Replacement test: yes: native_themes_media::native themes and media::uses authored selected contrast for native buttons and their mirrored captions (colors only, no 4.5:1 check for controlSelected/onSelected).
- `navigation-4-17` (1 main case; `popup_catcher_paint`). Main promised: Synthesized scrim root shares the screen StyleSheet. Engine or Compose owns it: StyleLink inheritance: overlays are descendants of the caller ScreenGui, so the same StyleSheet applies. Replacement test: yes: Candidate asserts MenuPanel.Parent.Parent == screen and ModalBack context under screen (in-tree); no paint assertion..
- `paint-113` (3 main cases; `sheet_model`). Main promised: Phantom ::UIStroke strokes the border. Engine or Compose owns it: UIStroke ApplyStrokeMode (Contextual on Frames = border; avatar-stack rules set Border explicitly). Replacement test: no test (none (all other ::UIStroke rules target Frames/ScrollingFrames)).
- `paint-72` (1 main case; `native_style_scenario`). Main promised: Per-view explicit override wins over the sheet. Engine or Compose owns it: Engine precedence of explicit Instance properties over StyleRule (api.md: 'Explicit Instance paint still overrides stylesheet paint'). Replacement test: yes: native_themes_media::native themes and media::keeps native theme rules reactive without writing instance paint (shows Facet writes no instance paint); precedence itself engine-owned, no live evidence.
- `paint-76` (4 main cases; `native_style_scenario`). Main promised: Toggle follows the live palette without touching its value. Engine or Compose owns it: StyleRules .facet-toggle-indicator/.facet-toggle-on with native StyleRule transitions (retarget in flight). Replacement test: yes: native_themes_media::native themes and media::retargets native theme paint with shared timing and disables motion without replacing rules; native_inputs::native input controls::keeps Toggle native eligibility semantic....
- `themes-P1-112` (1 main case; `status_indicator`). Main promised: Cutout spent within the reserved footprint. Engine or Compose owns it: Native UIStroke with BorderStrokePosition.Inner painted by .facet-cutout.. Replacement test: yes: ACCESS asserts the Cutout stroke is Inner..
- `themes-P1-121` (1 main case; `status_indicator`). Main promised: Small cutout gutter vs hairline. Retired by the owner: api.md does not document the StatusIndicator cutout or a theme hairline gutter. The cutout is a fixed 1 px inner UIStroke, and no file in src reads metrics.strokes. The live case classb_themes/status-cutout-hairline measures the 1 px cutout.
- `themes-P3-11` (1 main case; `theme_controller`). Main promised: Per-view explicit gradient wins over the theme's gradient on that node across a swap. Engine or Compose owns it: Roblox StyleSheet precedence: an explicitly set Instance property / explicit UIGradient child overrides StyleRule-applied values.. Replacement test: live Studio evidence `classb_themes/explicit-gradient-wins-across-swap`.
- `themes-P3-16` (1 main case; `theme_paint_repaint`). Main promised: Mounted Badge role paint follows a native palette switch. Engine or Compose owns it: StyleSheet/StyleRule resolution by the engine: Badge carries facet-status-*/facet-badge-* tags and themes.luau:562-574 rules supply the colors.. Replacement test: yes: Facet's half (rule properties re-resolve on palette switch, same rule instances) is covered by the cited cases for .facet-text/.facet-surface; no case reads a .facet-status-* or .facet-badge-* rule after a switch, and....
- `apps-199` (5 main cases; `selection_radius`). Main promised: radii.selection token and menu-card row corners/floating shade/hairline. Deleted: radii.selection token, grouped border-corner solver and popup shadeInset paint deleted; CAND themes.luau metrics.radii has only control/panel/pill.. Replacement test: none; the feature is deleted.
- `apps-201` (10 main cases; `selection_shape`). Main promised: Segmented one-strip seams, sliding inset fill geometry. Deleted: Seam/strip/sliding-fill paint geometry deleted; segmented picker is native TextButtons in a UIListLayout with an 'indicator' option.. Replacement test: none; the feature is deleted.
- `apps-203` (6 main cases; `selection_shape`). Main promised: Ten-foot focused row lift. Deleted: Distance-profile row lift deleted (no distanceProfile in CAND).. Replacement test: none; the feature is deleted.
- `apps2-14` (3 main cases; `example_match3_motion`). Main promised: Thirty-six match-3 tiles painted above the floor in every shipped theme. Deleted: Tile art is fixed image assets (assets.match3Tiles) independent of theme; the per-theme floor sweep was dropped with theme-role tints.. Replacement test: none; the feature is deleted.
- `apps2-18` (3 main cases; `example_tile_game`). Main promised: Forty-nine crossword cells painted above the floor in every theme. Deleted: 06_tile_game.luau paints cells with a hard-coded Color3 palette (line ~817); CELL_TINT theme roles are no longer rendered, so the per-theme floor sweep has no subject.. Replacement test: none; the feature is deleted.
- `apps2-245` (4 main cases; `sponsor_scenarios`). Main promised: Sponsor list theme-derived row height, role-blend wash, stroke pulse, armed-row focusability. Deleted: sponsor_list fixture deleted.. Replacement test: none; the feature is deleted.
- `apps2-25` (3 main cases; `example_word_game`). Main promised: Thirty word-game cells painted and above the visibility floor in every shipped theme. Deleted: The example no longer paints tiles from theme roles: 05_word_game.luau hard-codes tileColors Color3 values (line ~294), so the per-theme contrast floor sweep has nothing to vary. TILE_TINT roles remain only as data ex.... Replacement test: none; the feature is deleted.
- `apps2-272` (2 main cases; `virtual_monitors`). Main promised: Label/styleGroup/padding/containerRelativeFrame props land on nodes; styleGroup stroke on chips. Deleted: styleGroup, containerRelativeFrame and the Facet node-prop scene are gone: no match for styleGroup/containerRelativeFrame in CAND src/, examples/virtual_monitors or docs/reference/api.md. Paint is native StyleSheet ta.... Replacement test: none; the feature is deleted.
- `collections-05` (4 main cases; `collection_gap_tokens`). Main promised: Ambiguous environment core refusal for named gutters. Deleted: There is no environment core or named-metric resolution. `gap`/`crossGap` are plain Types.Value<number> (collection_types.luau). api.md: 'gap, crossGap / 0'.. Replacement test: none; the feature is deleted.
- `collections-06` (4 main cases; `collection_gap_tokens`). Main promised: Theme metric names and dotted paths as gutters, live on theme swap. Deleted: CAND gap accepts numbers, readables and bodies only. A theme metric string is not resolved (collections.luau layout()). api.md documents only numbers.. Replacement test: none; the feature is deleted.
- `collections-165` (3 main cases; `table`). Main promised: Named rowGap tokens. Deleted: There is no metric-name gap.. Replacement test: none; the feature is deleted.
- `inputs-32` (1 main case; `button_complete`). Main promised: Role+selected recorded limitation. Deleted: Verifier limitation record belonged to the deleted paint adapter.. Replacement test: none; the feature is deleted.
- `mech1-10` (1 main case; `adaptive`). Main promised: Adaptive materialization replays latest z before deferred paint. Deleted: Materialization adapter deleted.. Replacement test: none; the feature is deleted.
- `mech1-106` (5 main cases; `divider_hairline`). Main promised: Divider hairline opacity. Deleted: UI.Divider deleted.. Replacement test: none; the feature is deleted.
- `mech1-24` (37 main cases; `authored_presentation`). Main promised: Authored opacity/scale/rotation/offset props, refusals, composition and withAnimation. Deleted: Facet presentation props and withAnimation deleted; native GroupTransparency/UIScale/Rotation and Compose runtime.tween/spring are used directly; no api.md promise.. Replacement test: none; the feature is deleted.
- `mech1-62` (22 main cases; `chrome_inset_yield`). Main promised: Chrome inset yield, lift boxes and cross-axis reach. Deleted: Chrome inset/lift recipe arithmetic in screen_chrome deleted; skins use native UIPadding.. Replacement test: none; the feature is deleted.
- `mech2-22` (30 main cases; `inherited_subtree`). Main promised: Container enabled/tint props, tint cascade, grid-row wall, initialFocus refusal, recycle keys. Deleted: Facet containers (VStack etc.), tint cascade, focus graph groups, initialFocus refusal and recycle keys deleted; api.md has no container tint/enabled. Replacement test: none; the feature is deleted.
- `mech2-81` (23 main cases; `native_style_default`). Main promised: Sheet vs explicit-write paint path resolution and gallery A/B driver. Deleted: explicit-write paint path and sheet opt deleted; StyleSheet is the only paint path (api.md Themes). Replacement test: none; the feature is deleted.
- `mech3-101` (5 main cases; `z_order_seam`). Main promised: Z-order seam. Deleted: renderer z-order walk deleted. Replacement test: none; the feature is deleted.
- `mech3-102` (5 main cases; `z_order_visit_constant`). Main promised: Z-order per-visit constant. Deleted: renderer z-order walk deleted. Replacement test: none; the feature is deleted.
- `mech3-23` (8 main cases; `row_hit_paint`). Main promised: Row hit targets and plates take no package decoration/shadow slot. Deleted: Facet decoration-slot classification (surface tags, package shadow slots) deleted; paint is StyleSheet selectors. Replacement test: none; the feature is deleted.
- `mech3-24` (10 main cases; `row_plate_paint`). Main promised: List row plate declaration and band-dependent carving. Deleted: panel carving / plate slot resolution deleted. Replacement test: none; the feature is deleted.
- `mech3-34` (13 main cases; `selection_shape_container`). Main promised: Selection-fill container vocabulary refusals and fallbacks. Deleted: UI.corners/stripCorner container vocabulary deleted. Replacement test: none; the feature is deleted.
- `mech3-58` (13 main cases; `surface_slot_precedence`). Main promised: Surface slot precedence. Deleted: decoration slot classification deleted. Replacement test: none; the feature is deleted.
- `mech3-61` (3 main cases; `tag_sync`). Main promised: Fake adapter interactivity bookkeeping for plain/scrim surfaces. Deleted: surface=plain/scrim and facet-interactive bookkeeping deleted. Replacement test: none; the feature is deleted.
- `navigation-1-48` (11 main cases; `anchored_surface`). Main promised: Seam band, zero-radius wedge and rotated-node NO_SLOT chrome rules. Deleted: Seam band and chrome_slots NO_SLOT classification are deleted. The CAND Arrow is a 10x10 Frame rotated 45 and tagged facet-panel/facet-callout (styled, not skinned).. Replacement test: none; the feature is deleted.
- `navigation-2-59` (1 main case; `modal_dismissal`). Main promised: Catcher is never a skin target. Deleted: chrome_slots/NO_SLOT deleted; DismissOutside is a plain TextButton never passed to ctx.skin (nav_modal.luau).. Replacement test: none; the feature is deleted.
- `navigation-3-34` (1 main case; `navigation_visuals`). Main promised: Tab capsule uses the navigation chrome slot. Deleted: No 'navigation' chrome slot in CAND (docs/guide/10-rich-skinning.md lists control, field, panel, badge, barTrack, barFill); TabView bar has no decoration slot.. Replacement test: none; the feature is deleted.
- `navigation-3-49` (6 main cases; `picker_segments`). Main promised: Sliding pill chip default, chip slide, bar surface overrides. Deleted: Selection indicator chip (Indicator/Layer/Bar, barSurfaceFor, requestedIndicator dump) deleted; CAND paints via facet-segment-current StyleRule tag.. Replacement test: none; the feature is deleted.
- `navigation-4-11` (1 main case; `popup_catcher_paint`). Main promised: Row-actions tray outside-dismiss catcher is never painted. Deleted: Main presenter-synthesized __popupcatcher__ root (mountPopupCatcher, outsideDismiss contribution) is deleted; CAND collection_row_actions.luau has no full-viewport catcher at all (closes via RowActionsClose action / d.... Replacement test: none; the feature is deleted.
- `navigation-4-14` (2 main cases; `popup_catcher_paint`). Main promised: StyleRule oracle distinguishes role vs class default and skips state rules. Deleted: chrome_slots.classify / sheet_model paint oracle deleted (no chrome_slots in CAND src); paint is native StyleSheet rules on tags.. Replacement test: none; the feature is deleted.
- `navigation-4-15` (1 main case; `popup_catcher_paint`). Main promised: Region EXPAND plate barrier wears the theme scrim role. Deleted: Composition regions/expand plates deleted. Note: CAND modal scrim is hardcoded black 0.35 and the theme .facet-scrim rule / scrimOpacity metric are never applied (see bugs).. Replacement test: none; the feature is deleted.
- `navigation-4-54` (1 main case; `split_button`). Main promised: Outer ends rounded, seam squared. Deleted: Per-corner seam normalization deleted; CAND pointer form is ctx.row{Button, Menu} with each button carrying its own corner. No api.md promise.. Replacement test: none; the feature is deleted.
- `paint-03` (3 main cases; `control_content`). Main promised: Content ownership hints, pooled content rebinding, hint key identity. Deleted: src/client/screen_control_content.luau, sheet_model decorationHint.contentOwnerDepth and instance pooling are gone; CAND/src has only src/ui + vendor. Compose owns each caption; no doc promises pooling or hint keys.. Replacement test: none; the feature is deleted.
- `paint-10` (2 main cases; `control_vocabulary`). Main promised: Two-builder prefix declarations and shape-before-role ordering. Deleted: sheet_model two builders (build/buildPackage) and prefix registry deleted; one rulesFor in src/ui/themes.luau.. Replacement test: none; the feature is deleted.
- `paint-107` (3 main cases; `sheet_model`). Main promised: Bespoke-path token parity, single rule names, legacy shape. Deleted: Bespoke paint path and two builders deleted.. Replacement test: none; the feature is deleted.
- `paint-108` (5 main cases; `sheet_model`). Main promised: buildPackage rule-name identity. Deleted: buildPackage deleted; one compiler.. Replacement test: none; the feature is deleted.
- `paint-118` (1 main case; `sheet_model`). Main promised: Compiled rule carries a theme role. Deleted: No token indirection; rules carry resolved Color3.. Replacement test: none; the feature is deleted.
- `paint-121` (1 main case; `sheet_model`). Main promised: Skinned lifted onIndicator twin. Deleted: Two-builder twin rule deleted.. Replacement test: none; the feature is deleted.
- `paint-122` (27 main cases; `styling`). Main promised: Per-view shadow, corners, styleGroup, gradient, lint and closed grammars. Deleted: src/tokens/styling, src/render/style_lint, UI.shadow/UI.gradient/UI.styleGroup deleted; no api.md/guide mention (grep styleGroup/UI.gradient = 0 in src/docs).. Replacement test: none; the feature is deleted.
- `paint-155` (1 main case; `typography_weight_roles`). Main promised: Package-less built-in model weight faces. Deleted: Built-in sheet model deleted.. Replacement test: none; the feature is deleted.
- `paint-63` (2 main cases; `native_style_scenario`). Main promised: Enum never a theme-sheet attribute. Deleted: Theme-sheet attribute tokens deleted; StyleRule:SetProperties carries enums directly (e.g. ScrollBarInset).. Replacement test: none; the feature is deleted.
- `paint-67` (1 main case; `native_style_scenario`). Main promised: Sheet relinked across every live root. Deleted: api.md Mounting: the caller parents createStyleSheet + StyleLink; no Facet render target relinks.. Replacement test: none; the feature is deleted.
- `paint-69` (3 main cases; `native_style_scenario`). Main promised: Fallback explicit suppressions and decoration-hint plumbing. Deleted: screen_target fallback writes/applySurface/decoration hints deleted; StyleRule .facet-skinned owns suppression.. Replacement test: none; the feature is deleted.
- `paint-77` (30 main cases; `paint_extensions`). Main promised: Tint modifier: authoring, resolution, emission, sheet claims. Deleted: UI tint modifier, src/blueprint_schema, src/render/authority and src/tokens/styling deleted; explicit native TextColor3/ImageColor3 replace it (gallery inherited_subtree recomposed: native_gallery_parity::restored nat.... Replacement test: none; the feature is deleted.
- `paint-80` (8 main cases; `paint_extensions`). Main promised: UI.stroke / shadowData / gradientData / cornersData family. Deleted: Per-view stroke family deleted; theme chrome strokes are StyleRule ::UIStroke rules; native UIStroke children otherwise.. Replacement test: none; the feature is deleted.
- `paint-89` (2 main cases; `path_stroke_authority`). Main promised: Live ScreenTarget path rules. Deleted: screen_target/screen_presentation deleted.. Replacement test: none; the feature is deleted.
- `paint-90` (1 main case; `sheet_model`). Main promised: Canvas groups default transparent. Deleted: src/tokens/sheet_model deleted; rulesFor in src/ui/themes.luau compiles literal Color3/Font values from the palette (no tokens, stamps or tag registry).. Replacement test: none; the feature is deleted.
- `paint-91` (1 main case; `sheet_model`). Main promised: Deterministic sheet stamp. Deleted: src/tokens/sheet_model deleted; rulesFor in src/ui/themes.luau compiles literal Color3/Font values from the palette (no tokens, stamps or tag registry).. Replacement test: none; the feature is deleted.
- `paint-92` (1 main case; `sheet_model`). Main promised: Accent interaction token lerp. Deleted: src/tokens/sheet_model deleted; rulesFor in src/ui/themes.luau compiles literal Color3/Font values from the palette (no tokens, stamps or tag registry).. Replacement test: none; the feature is deleted.
- `paint-93` (1 main case; `sheet_model`). Main promised: Every $token resolves. Deleted: src/tokens/sheet_model deleted; rulesFor in src/ui/themes.luau compiles literal Color3/Font values from the palette (no tokens, stamps or tag registry).. Replacement test: none; the feature is deleted.
- `paint-94` (1 main case; `sheet_model`). Main promised: Human rule/token names. Deleted: src/tokens/sheet_model deleted; rulesFor in src/ui/themes.luau compiles literal Color3/Font values from the palette (no tokens, stamps or tag registry).. Replacement test: none; the feature is deleted.
- `paint-95` (1 main case; `sheet_model`). Main promised: Layout token mirror. Deleted: src/tokens/sheet_model deleted; rulesFor in src/ui/themes.luau compiles literal Color3/Font values from the palette (no tokens, stamps or tag registry).. Replacement test: none; the feature is deleted.
- `paint-96` (1 main case; `sheet_model`). Main promised: classifyTags pure mapping. Deleted: src/tokens/sheet_model deleted; rulesFor in src/ui/themes.luau compiles literal Color3/Font values from the palette (no tokens, stamps or tag registry).. Replacement test: none; the feature is deleted.
- `paint-97` (1 main case; `sheet_model`). Main promised: Tint fill rides a tag rule. Deleted: src/tokens/sheet_model deleted; rulesFor in src/ui/themes.luau compiles literal Color3/Font values from the palette (no tokens, stamps or tag registry).. Replacement test: none; the feature is deleted.
- `paint-98` (1 main case; `sheet_model`). Main promised: Frozen tag vocabulary. Deleted: src/tokens/sheet_model deleted; rulesFor in src/ui/themes.luau compiles literal Color3/Font values from the palette (no tokens, stamps or tag registry).. Replacement test: none; the feature is deleted.
- `themes-P1-104` (4 main cases; `stage`). Main promised: Stage property authority and seam-owned writes. Deleted: render/authority gate and seam-owned manifest deleted.. Replacement test: none; the feature is deleted.
- `themes-P1-108` (1 main case; `stage`). Main promised: Stage never classified into a decoration slot. Deleted: chrome_slots classification deleted.. Replacement test: none; the feature is deleted.
- `themes-P1-126` (1 main case; `async_completeness`). Main promised: No dimmed declaration claims no continuous colour. Deleted: Tint/paint-claim model deleted; CAND always binds ImageTransparency (0 when not dimmed).. Replacement test: none; the feature is deleted.
- `themes-P1-56` (1 main case; `badge`). Main promised: Status silhouette page-color cutout. Deleted: Badge status mark is now a StatusIndicator diameter 8 without cutout; silhouette cutout on equal-role plate deleted.. Replacement test: none; the feature is deleted.
- `themes-P2-03` (3 main cases; `theme_assets`). Main promised: Object-sourced Content is a compile error, not a silent blank. Deleted: sheet_model.checkContentValue and the sheet_model/buildPackage compile path are gone (no src/tokens in CAND). Skin art is written directly to ImageLabel.Image as a string by themes.skin, so there is no Content-object .... Replacement test: none; the feature is deleted.
- `themes-P2-07` (6 main cases; `theme_chrome`). Main promised: State and role feedback by tinting one base image (ChromeTint and ChromeTintDanger tokens). Deleted: The tint-modulation model (ChromeTint* tokens, Chrome role rules and chrome_slots.TINT) is gone. CAND selects per-state art via AssetVariants {default,hover,pressed,selected,disabled} in themes.skin. The pressed swap .... Replacement test: none; the feature is deleted.
- `themes-P2-08` (8 main cases; `theme_chrome`). Main promised: Asset-failure fallback paints native chrome and stays hidden in every state. Deleted: The fallback ledger (chrome_slots.newState, setAssetLoaded, fallback and mute tags) and the fallback rules are removed. `fallback` survives only as an unused field in theme_types.Asset and SkinLayer. Guide 10 now says.... Replacement test: none; the feature is deleted.
- `themes-P2-10` (5 main cases; `theme_chrome`). Main promised: Pseudo-selector allowlist and author-rule gate, including rejection of ::UIShadow. Deleted: sheet_model.checkSelector and the section-4 property-authority linter are removed. CAND rulesFor appends package.rules selectors verbatim (themes.luau, the `for _, definition in package.rules` loop). UIShadow is now a.... Replacement test: none; the feature is deleted.
- `themes-P2-13` (3 main cases; `theme_chrome`). Main promised: chrome_props rect and sequence kinds with headless materialize. Deleted: src/tokens/chrome_props.luau no longer exists. CAND builds native Rect, ColorSequence and NumberSequence datatypes directly with injected types.. Replacement test: none; the feature is deleted.
- `themes-P2-15` (6 main cases; `theme_chrome`). Main promised: Classifier internals: backdrop, scrim and plain nodes get no decoration; hints ride the meta channel; surface=plain opts out. Deleted: chrome_slots.classify, attachHint and readHint and the `surface` prop are removed. Controls call ctx.skin explicitly. Note: CAND UI.Button calls ctx.skin(node,'control') for every appearance, so a probe showed that ap.... Replacement test: none; the feature is deleted.
- `themes-P2-24` (1 main case; `theme_chrome`). Main promised: Whole-image rendering is inferred by default for thumbs and seals. Deleted: The chrome_slots whole-image default inference is removed. CAND slices whenever the asset has a sliceCenter unless the recipe sets sliced=false. There is no doc promise.. Replacement test: none; the feature is deleted.
- `themes-P2-27` (3 main cases; `theme_chrome`). Main promised: Badge text-lift class, badge centering, and the narrow UI.Text{surface='badge'} path. Deleted: UI.Text, the `surface` prop and chrome_slots.LIFTED_TEXT_CLASSES and centresLiftedText are removed. CAND Badge is a native TextLabel or pill with engine-centred text.. Replacement test: none; the feature is deleted.
- `themes-P2-33` (3 main cases; `theme_chrome`). Main promised: Value-control slot hint tags, and no gradient on thumb or rail (compile error). Deleted: classifyTags, slotTagFor, isGradientable and the define-time chromeGradient.sliderThumb error are removed. The CAND gradient keys are free-form role names.. Replacement test: none; the feature is deleted.
- `themes-P2-35` (3 main cases; `theme_chrome`). Main promised: Toggle OFF track is recessed, keeps a 3:1 knob contrast, and is derived when absent. Deleted: sheet_model.togglePalette is removed. The CAND OFF track is plain extra.control and define no longer checks knob-versus-track non-text contrast (define checks only six text pairs at 4.5:1). This is a lost accessibilit.... Replacement test: none; the feature is deleted.
- `themes-P2-59` (3 main cases; `theme_icons_applied`). Main promised: Glyph suppression tag, its cascade position, and the lift dying with the glyph. Deleted: The iconArt suppression tag, the 'Semantic icon replaces the glyph' rule and syncChromeText are removed. Icons are always ImageLabels.. Replacement test: none; the feature is deleted.
- `themes-P2-64` (4 main cases; `theme_icons_applied`). Main promised: Image-property sheet authority, no Image in icon rules, a declared instance-family registry, tag before parent. Deleted: render/authority and bespokeInstances are removed. CAND deliberately writes Image, ScaleType, SliceCenter, TileSize and ResampleMode on skin and icon instances (themes.skin and ctx.icon). This is a reversal of main's .... Replacement test: none; the feature is deleted.
- `themes-P2-67` (1 main case; `theme_icons_applied`). Main promised: Releasing fallback lettering restores the art once. Deleted: screen_paint releaseTint and the letterRole machinery are removed.. Replacement test: none; the feature is deleted.
- `themes-P3-18` (4 main cases; `theme_paint_repaint`). Main promised: tint channel and role-less UI.Path repaint on a theme commit. Deleted: UI.Box `tint` ({role, from, blend, direct}) and generic UI.Path with `role` were removed; CAND api.md has no tint prop (only Rating tint Color3 pairs) and no Path control; Path2D in CAND is internal to ProgressView/ra.... Replacement test: none; the feature is deleted.
- `themes-P3-19` (3 main cases; `theme_paint_repaint`). Main promised: Live adapter source contract: setActiveTheme -> refreshThemedPaint ordering. Deleted: MAIN src/client screen_paint/adapter (setActiveTheme, refreshThemedPaint, applyPathRole, applyTint) deleted; no adapter in CAND.. Replacement test: none; the feature is deleted.
- `themes-P4-02` (6 main cases; `theme_layer_application`). Main promised: Rule-owned layer paint: tag-before-parent, per-corner second tag, no adapter paint writes, class-rule wrapper, GetStyled addressable. Deleted: The screen_target adapter, chrome_slots.layerBoxes tags and rule-owned layer paint are gone (no src/client, src/tokens in CAND). CAND themes.skin writes Image/ScaleType/SliceCenter/TileSize directly onto Compose-owned.... Replacement test: none; the feature is deleted.
- `themes-P4-12` (4 main cases; `theme_layer_application`). Main promised: Node tag bookkeeping: stack yield, error state, classifyTags single builder, yield child-combinator rule. Deleted: syncTags/classifyTags, the caret stack-yield rule and the 'error' tag state are gone; SkinState in src/ui/theme_types.luau is default/hover/pressed/selected/disabled only. Not promised.. Replacement test: none; the feature is deleted.
- `themes-P4-21` (3 main cases; `theme_layers`). Main promised: Per-corner override tag, per-layer tint/failover/caret yield, semantic role tints on every layer. Deleted: Layer ImageLabels get no tags and no ImageColor3 tint rules; failover/yield deleted. Not promised.. Replacement test: none; the feature is deleted.
- `themes-P4-52` (4 main cases; `theme_variants`). Main promised: Per-state tint rules, disabled treatment kept with disabled art, error tag state, extra-art cost. Deleted: No tint rules on skin art, no 'error' SkinState, no census (src/ui/themes.luau, theme_types.luau). Not promised.. Replacement test: none; the feature is deleted.
- `themes-P5-09` (2 main cases; `theme_reference_packages`). Main promised: Skin cost census and 4px pixel-grid snapping. Deleted: sheet_model census and solver-wide pixel snapping deleted (ledger theme_pixel_content). CAND docs promise only 'Pixel artwork must declare its pixel rendering and resampling deliberately.' pixel_quest still declares i.... Replacement test: none; the feature is deleted.
- `themes-P5-31` (13 main cases; `theme_value_displays`). Main promised: Sheet-model/renderer internals for value displays. Deleted: sheet_model rule dedup, pre-armed rules, census, preload seam, spinner plate/wash refusal, screen_target path/tag ordering, native-mode paint writes, paint-then-suppress assembly, zero-size load status all belonged to.... Replacement test: none; the feature is deleted.

### Theme packages

0 class b and 45 class c contracts, 230 main cases.

- `apps2-03` (1 main case; `consumer_standalone`). Main promised: Consumer repaints its accent when a theme package is committed. Deleted: The consumer no longer accepts a theme package; main.client.luau installs the default Facet.themes.createStyleSheet(runtime) once. Theme hot-swap is covered generally in native_themes_media.. Replacement test: a candidate test covers the surviving part (see reason).
- `apps2-119` (4 main cases; `gallery_theme_picker`). Main promised: First module claiming an id wins; isPickable; undeclared theme refused; picker stays out of the library. Deleted: examples/gallery/client/theme_picker.luau (ids, isPickable, request refusal) was deleted; packages are keyed by ModuleScript name and the Appearance picker only offers declared themes.. Replacement test: none; the feature is deleted.
- `apps2-151` (5 main cases; `perf_lab`). Main promised: Installing a theme publishes metrics to the environment and solver reserves insets. Deleted: Environment metrics and the Facet solver's contentInsets reservation are deleted; the lab row is theme 'facet-neutral' only (perf_lab.luau capture()).. Replacement test: none; the feature is deleted.
- `apps2-165` (3 main cases; `platform_adapters_recovery`). Main promised: StyleSheet installer: theme child ensure, persistent host, stale sheet cleanup. Deleted: src/client/native_style.luau installer is deleted; api.md: themes.createStyleSheet(runtime, package) returns a Compose-owned StyleSheet the caller parents and links. No stamp/repair/cleanup or FacetStyleHost.. Replacement test: a candidate test covers the surviving part (see reason).
- `collections-111` (1 main case; `row_actions_scenario`). Main promised: Glossy Mobile row surfaces keep geometry across remounts. Deleted: The package-specific remount check is gone. No glossy row-surface test exists.. Replacement test: none; the feature is deleted.
- `collections-160` (3 main cases; `table`). Main promised: Rows inside the theme selection card. Deleted: Package card inset arithmetic is gone.. Replacement test: none; the feature is deleted.
- `collections-206` (8 main cases; `table_themed_header`). Main promised: Sort mark reserve and carved border under packages; toolbar glow clearance. Deleted: The sort mark is a text suffix. There is no toolbar in the Table and no glow gutter arithmetic.. Replacement test: none; the feature is deleted.
- `inputs-226` (4 main cases; `value_controls`). Main promised: Internal meta-channel slot hints. Deleted: Meta-channel skin hints removed; skins use ctx.skin slots.. Replacement test: none; the feature is deleted.
- `mech1-15` (29 main cases; `app_metrics`). Main promised: App-declared metrics namespace, ten-foot ladder, theme overrides, token dump. Deleted: App metric declarations, resolveNumber, metric-path grammar and ten-foot ladder deleted; themes.define validates only facet-theme/2 packages.. Replacement test: none; the feature is deleted.
- `mech3-43` (8 main cases; `space_tight_step`). Main promised: space.tight step resolution through props. Deleted: metric-name resolution in blueprint gap/px/gap_metric deleted; guide 09 says spacing is package data only. neutral package still carries space.tight = 6 (themes.luau:85) but nothing derives or resolves it. Replacement test: none; the feature is deleted.
- `navigation-1-21` (8 main cases; `alert`). Main promised: Bleed lane, carved-frame padding, themeMetrics degradation, exported rowFits. Deleted: Main chrome insets/chromeBleed/themeMetrics rung and alert policy exports (rowFits, resolveStacked) are deleted; CAND alert uses fixed padding (spec.padding or 20) and surfaces.skin(panel) outside native layout. api.m.... Replacement test: none; the feature is deleted.
- `navigation-2-22` (1 main case; `distance_gamepad`). Main promised: Pixel Quest selection ornaments outside label box. Deleted: chrome_slots layerPlanFor/liftGeometry deleted; candidate themes.skin places 'corners' layers inside the control rect (src/ui/themes.luau). No lift promise in api.md/guides.. Replacement test: none; the feature is deleted.
- `paint-112` (3 main cases; `sheet_model`). Main promised: Author rule chrome scope by selector. Deleted: Rule linting by selector deleted; api.md: rules = { { selector, properties } } accepted as data (callbacks rejected, tested in 'keeps compiled theme data immutable').. Replacement test: none; the feature is deleted.
- `paint-153` (1 main case; `typography_weight_roles`). Main promised: Package content stamps unchanged. Deleted: Content stamps deleted.. Replacement test: none; the feature is deleted.
- `paint-154` (1 main case; `typography_weight_roles`). Main promised: numeral prefers control over heading. Deleted: tokens.compile derivation deleted.. Replacement test: none; the feature is deleted.
- `themes-P2-02` (2 main cases; `theme_assets`). Main promised: define rejects an asset that declares both content fields or neither. Deleted: themes.define (CAND src/ui/themes.luau:147) validates only data shape, identity, palettes, RGB and contrast. Asset declarations are not inspected. api.md `define` promises only 'Callbacks, cycles and malformed definit.... Replacement test: none; the feature is deleted.
- `themes-P2-05` (5 main cases; `theme_assets`). Main promised: define rejects degenerate, oversized, out-of-bounds, inverted or malformed slice centres. Deleted: Slice geometry validation (invalidSlice class) was removed with the old themes/package define. CAND themes.skin passes asset.sliceCenter straight to Rect.new, so an empty-centre slice now paints the silent blank middl.... Replacement test: none; the feature is deleted.
- `themes-P2-12` (2 main cases; `theme_chrome`). Main promised: Deterministic package stamp and the untouched legacy build() path. Deleted: sheet_model.build/buildPackage and the stamp are removed. CAND recompiles and diffs StyleRules reactively in createStyleSheet. No stamp is promised in api.md.. Replacement test: none; the feature is deleted.
- `themes-P2-18` (2 main cases; `theme_chrome`). Main promised: Theme asset preload policy, and a nineSlice recipe without sliceCenter as an authoring error. Deleted: chrome_slots.assetsToPreload and planFor are removed. `preload` remains only as an unused Asset type field. CAND preloads only AsyncImage sources (media.luau PreloadAsync). A nineSlice recipe whose asset has no sliceC.... Replacement test: none; the feature is deleted.
- `themes-P2-29` (2 main cases; `theme_chrome`). Main promised: define validates shadows, and a census counts them. Deleted: styling.normalizeShadow and the define-time shadow validation are removed, and so is the census. Note: an unknown preset now passes define and throws at mount (see bugs).. Replacement test: none; the feature is deleted.
- `themes-P2-30` (1 main case; `theme_chrome`). Main promised: An unknown chrome slot name is rejected by name. Deleted: themes.define no longer validates chrome keys, and an unknown slot is silently unused. No doc promise.. Replacement test: none; the feature is deleted.
- `themes-P2-41` (6 main cases; `theme_icons`). Main promised: define-time icon validation: names, undeclared art, no-default maps, iconSizes roles, stamp, preload. Deleted: themes.define no longer inspects icons or metrics.iconSizes. There is no stamp and no preload seam. api.md promises only 'resolveIcon(package, name, state?) resolves real image content'.. Replacement test: none; the feature is deleted.
- `themes-P2-52` (2 main cases; `theme_icons_applied`). Main promised: iconSizes as a resolvable metric path, and stamp stability. Deleted: snapshot.isMetricPath, resolveNumber and the stamp are removed.. Replacement test: none; the feature is deleted.
- `themes-P3-02` (11 main cases; `theme_controller`). Main promised: Per-target install mechanics, capability gating, schema/env refusal, transitions opt-in, multi-root sheet following. Deleted: MAIN src/client/theme_controller.luau (install/swapPackage/uninstall/onChange/inspect/dumpTokens) has no CAND counterpart; CAND src contains only src/ui/*.luau and grep for installTheme/swapPackage/onChange/dumpTokens.... Replacement test: none; the feature is deleted.
- `themes-P3-04` (4 main cases; `theme_controller`). Main promised: Palette selection on swap: new package default, unknown theme name refused, all-or-nothing swap, per-theme authored overrides. Deleted: MAIN src/client/theme_controller.luau (install/swapPackage/uninstall/onChange/inspect/dumpTokens) has no CAND counterpart; CAND src contains only src/ui/*.luau and grep for installTheme/swapPackage/onChange/dumpTokens.... Replacement test: none; the feature is deleted.
- `themes-P3-05` (10 main cases; `theme_controller`). Main promised: Controller lifecycle: onChange observers, one controller per environment, validate-before-mutate, disposed controller. Deleted: MAIN src/client/theme_controller.luau (install/swapPackage/uninstall/onChange/inspect/dumpTokens) has no CAND counterpart; CAND src contains only src/ui/*.luau and grep for installTheme/swapPackage/onChange/dumpTokens.... Replacement test: none; the feature is deleted.
- `themes-P3-06` (9 main cases; `theme_controller`). Main promised: Studio StyleSheet-attribute live authoring: token edits re-solve, unknown attribute warns, sanitization, renamed-rule warning. Deleted: Attribute-token authoring on the theme sheet (MAIN theme_controller watch + src/tokens sheet_model) is deleted; CAND StyleSheet is compiled from the package table (themes.luau rulesFor) and api.md/guides describe chan.... Replacement test: none; the feature is deleted.
- `themes-P3-23` (2 main cases; `theme_pixel_content`). Main promised: Declaring both content and contentId is an error; export round-trip keeps plain strings. Deleted: Alias validation in MAIN buildPackage and the theme_sync export path removed; CAND define() accepts both silently and api.md does not promise rejection (see themes-P3-22 for the precedence inconsistency).. Replacement test: none; the feature is deleted.
- `themes-P3-30` (4 main cases; `theme_snapshot`). Main promised: Neutral package values: default_style literals, stage roles, toggle mark width, identity stamp. Deleted: MAIN src/tokens/default_style.luau and snapshot.neutral() removed; neutral is a literal table in themes.luau:47-117; toggle markWidth metric and identity stamp no longer exist.. Replacement test: none; the feature is deleted.
- `themes-P3-32` (9 main cases; `theme_snapshot`). Main promised: Snapshot resolution order: authored base, ten-foot floor, preferred text, accessibility floor, overrides, determinism, row gutter. Deleted: themes.resolve/snapshot pipeline removed; viewing distance and preferred text are native/app concerns (UIScale, TextSize readables). Note metrics.space.gutter remains in neutral (themes.luau:85) but is unread.. Replacement test: none; the feature is deleted.
- `themes-P3-35` (6 main cases; `theme_snapshot`). Main promised: Metric-name resolution helpers and the themeMetrics environment fact. Deleted: resolveSize/resolveSides/resolveDim and env 'themeMetrics' removed; controls read the package readable passed as controls{theme} (api.md Factory options).. Replacement test: none; the feature is deleted.
- `themes-P4-06` (3 main cases; `theme_layer_application`). Main promised: Masked layer wraps art in an untagged CanvasGroup, falls with its layer, focus ring hugs the wrapper. Deleted: `mask` and layer `inset` no longer exist in ThemeTypes.SkinLayer or themes.skin; glossy_touch.luau still declares mask='pill'/inset but define() silently accepts and skin ignores them. Not promised in api.md/guides.. Replacement test: none; the feature is deleted.
- `themes-P4-14` (5 main cases; `theme_layer_application`). Main promised: Rung-2 per-state override grammar (bare string or state map), precedence, fallback, bad-map error, shared disabled floor. Deleted: normalizeVariant and per-state override maps are gone: Slider thumbImage/trackImage take a single Value<string> (src/ui/inputs.luau paintedPart). No disabled-floor rule. api.md promises only the props, not maps.. Replacement test: none; the feature is deleted.
- `themes-P4-19` (19 main cases; `theme_layers`). Main promised: Layer recipe validation: unknown kind, borrowed geometry, inset/mask grammar, cap/empty/hole, slice centre, undeclared asset, per-kind geometry, layered scrollbar/barFill, required fallback. Deleted: themes.define (src/ui/themes.luau) validates only finiteness, callbacks, cycles, base type, identity, palettes, color channels and contrast. All recipe validators, inset and mask are deleted; api.md promises only 'Cal.... Replacement test: none; the feature is deleted.
- `themes-P4-23` (2 main cases; `theme_layers`). Main promised: One slot vocabulary list; gradient wash refused on value-control chrome. Deleted: themes.SLOTS / chrome_slots no longer exist; extra.chromeGradient compiles a UIGradient rule for any role without a slot guard (rulesFor). Not promised.. Replacement test: none; the feature is deleted.
- `themes-P4-28` (1 main case; `theme_package`). Main promised: Deterministic content stamp. Deleted: No content stamp in themes.define; api.md does not promise one.. Replacement test: none; the feature is deleted.
- `themes-P4-30` (1 main case; `theme_package`). Main promised: Base must be a compiled package. Deleted: CAND rejects only a non-table base (`Theme base must be a package`); an unfrozen table is accepted (probe). api.md does not require a compiled base.. Replacement test: none; the feature is deleted.
- `themes-P4-31` (16 main cases; `theme_package`). Main promised: Removed define validators: required roles, unknown fields, target floor, nineSlice fallback/asset/insets, schema version, partner/variant pair rules, font family, gradient alpha, scrollbar nineSlice. Deleted: themes.define now merges every section over neutral (missing roles inherit), overwrites package.schema, and has none of these checks (src/ui/themes.luau define). Probe: bad schema, unknown fields, undeclared asset, un.... Replacement test: none; the feature is deleted.
- `themes-P4-36` (8 main cases; `theme_package`). Main promised: lintProperty legal-property boundary and measured image-chrome props. Deleted: themes.lintProperty is gone; custom rules pass straight to native StyleRule:SetProperties. Not promised in api.md.. Replacement test: none; the feature is deleted.
- `themes-P4-39` (2 main cases; `theme_package`). Main promised: Coverage: field-level naming of partial sizes, rejection of unnamespaced/unknown-kind needs. Deleted: checkCoverage reports a need as a whole and does not validate need names/kinds (src/ui/themes.luau checkCoverage).. Replacement test: none; the feature is deleted.
- `themes-P4-45` (5 main cases; `theme_variants`). Main promised: normalizeVariant grammar: one shape, shared with rung 2, unknown state/missing default/malformed refs rejected. Deleted: themes.normalizeVariant is gone; variantAsset (src/ui/themes.luau) reads a string or map without validation. Not promised.. Replacement test: none; the feature is deleted.
- `themes-P4-49` (3 main cases; `theme_variants`). Main promised: Canonical sibling, undeclared-state art rejection, per-state preload. Deleted: No normalization, asset-declaration check or preload seam in CAND define/skin.. Replacement test: none; the feature is deleted.
- `themes-P5-07` (1 main case; `theme_reference_packages`). Main promised: Incompatible schema rejected with field and fix. Deleted: Main src/themes/package schemaVersion/compatibility check is gone: CAND themes.define overwrites package.schema = SCHEMA and never reads compatibility.requiresSchema. api.md only states SCHEMA is facet-theme/2; no com.... Replacement test: none; the feature is deleted.
- `themes-P5-10` (2 main cases; `theme_reference_packages`). Main promised: Selection chrome slot and gallery theme picker selection. Deleted: No CAND control consumes chrome.selection (skin() callers use control/field/panel/toggleTrack/toggleKnob/stepperPlate/sliderTrack/sliderThumb/barTrack/barFill/barCap/barCenter only); examples/gallery/client/theme_pick.... Replacement test: none; the feature is deleted.
- `themes-P5-25` (5 main cases; `theme_value_displays`). Main promised: define() refuses malformed bar-family geometry. Deleted: Main chrome_slots bar validation (direction vocabulary, per-slot legal fields, ImageRect crop refusal, ornament px box, undeclared per-end cap) is deleted; CAND define() validates only finiteness, callbacks, cycles, i.... Replacement test: none; the feature is deleted.

### Icons and media

7 class b and 24 class c contracts, 135 main cases.

- `apps2-198` (6 main cases; `radial_menu`). Main promised: Icon/label centering, sizing and focus ring on painted surface. Engine or Compose owns it: Native AnchorPoint centering, UIAspectRatioConstraint/UICorner circles, ImageLabel ScaleType and GuiService selection highlight replace solver-placed Disc/Glyph/Compact nodes and Facet focus paint.. Replacement test: yes: native_radial_controls::native radial interaction policies::NRAD-16 keeps round button swatches circular (UICorner scale 1, square >=44); no evidence for icon squares, ten-foot sizing or selection-highlight target.
- `mech2-47` (19 main cases; `leaf_capabilities`). Main promised: Image tile/slice/resample options and validation. Engine or Compose owns it: ImageLabel ScaleType Tile/Slice, TileSize, SliceCenter, SliceScale, ResampleMode. Replacement test: yes: native_themes_media::native themes and media::keeps plaques edges tiling pixel resampling and chrome insets native; native_themes_media::native themes and media::resets retained native image properties to valid defaul....
- `paint-51` (5 main cases; `icon_box`). Main promised: Paint seam clamps pictures to the content box. Engine or Compose owns it: ImageLabel draws inside its own Size (engine scaling); no Facet rect/paint seam. Replacement test: no test.
- `paint-78` (3 main cases; `paint_extensions`). Main promised: Image scale mode. Engine or Compose owns it: ImageLabel.ScaleType set directly (AsyncImage imageProperties). Replacement test: yes: native_themes_media::native themes and media::uses native image framing and rounded avatar images.
- `themes-P1-19` (1 main case; `avatar`). Main promised: Every image layer covers the same face in both forms and all rungs. Engine or Compose owns it: Portrait AsyncImage sized UDim2.fromScale(1,1) inside the TextButton, clipped by native UICorner(1,0).. Replacement test: yes: native_themes_media 'uses native image framing and rounded avatar images' asserts the image UICorner scale 1; icon form and rung sweep not asserted..
- `themes-P1-27` (2 main cases; `avatar`). Main promised: Icon form stays square; faces and round presence use true circle paint. Engine or Compose owns it: Native UICorner CornerRadius (1,0) on the TextButton and image; circle-rim classification (sheet_model) removed.. Replacement test: yes: FRAMING asserts image UICorner scale 1; icon form and large-diameter presence shapes not asserted..
- `themes-P1-59` (1 main case; `image_framing`). Main promised: Contain/stretch/crop scale modes. Engine or Compose owns it: Native ImageLabel.ScaleType (Fit/Crop/Stretch/Slice/Tile) via AsyncImage imageProperties.. Replacement test: yes: FRAMING asserts ScaleType Crop and ResampleMode Pixelated forwarded to the inner ImageLabel..
- `apps2-249` (5 main cases; `sponsor_scenarios`). Main promised: Staggered avatar load, retry budget, preload of a declared set, themable dim tint, live-transport degrade. Deleted: sponsor_avatars fixture and the retry-budget/preload-set/dim-channel behaviors were deleted; api.md AsyncImage/Avatar promise loader/cancellation/status only.. Replacement test: none; the feature is deleted.
- `inputs-194` (1 main case; `text_input`). Main promised: Themed search art. Deleted: Search presentation has no magnifier art in CAND (only the Clear button).. Replacement test: none; the feature is deleted.
- `inputs-68` (5 main cases; `compact_label`). Main promised: Icon form glyph fallback and standard_icons. Deleted: standard_icons ASCII glyph catalogue removed.. Replacement test: none; the feature is deleted.
- `mech2-18` (5 main cases; `icon_ns_glyph`). Main promised: Namespaced icon ASCII glyph fallback. Deleted: Glyph derivation deleted; guides/05-styling.md: "Icons are images, not substitute text glyphs"; themes.resolveIcon resolves image content only. Replacement test: none; the feature is deleted.
- `mech3-84` (15 main cases; `theme_asset_judgement`). Main promised: Theme asset judgement and recovery ledger. Deleted: skin asset failure ledger deleted; AsyncImage keeps its own failure state (covered elsewhere). Replacement test: none; the feature is deleted.
- `paint-13` (1 main case; `control_vocabulary`). Main promised: Per-Button rung sizes its icons. Deleted: Button icons use ctx.icon sized by the factory controlSize (api.md: 'controlSize: the control-size step ... for theme icons'); probe: controlSize='large' Button icons stay 20px. Per-button icon rung floors retired.. Replacement test: none; the feature is deleted.
- `paint-133` (1 main case; `theme_authoring_scenario`). Main promised: Missing asset fallback. Deleted: guide 10: 'A valid recipe with unavailable art still fails visually' — no fallback promised.. Replacement test: none; the feature is deleted.
- `paint-22` (8 main cases; `display_controls`). Main promised: Label presentation, semanticText, required title and title refusals. Deleted: Label presentation (iconOnly/titleOnly), semanticText and title refusals removed; api.md Label: 'text or label, icon and iconPosition, textRole and role'.. Replacement test: none; the feature is deleted.
- `paint-49` (3 main cases; `icon_box`). Main promised: Art-free reservation, explicit-size override and snapshot art names. Deleted: themes/snapshot and solver icon reservation deleted; ctx.icon always sizes from iconSize.. Replacement test: none; the feature is deleted.
- `paint-55` (9 main cases; `icon_coverage`). Main promised: Mounted controls never paint a fallback character. Deleted: ASCII icon floor deleted: ctx.icon asserts an image asset (src/ui/context.luau:298), so a text fallback cannot be painted; gallery mounts would error instead.. Replacement test: none; the feature is deleted.
- `themes-P1-122` (19 main cases; `async_completeness`). Main promised: Resource provider bounded retry, give-up, construction and preload. Deleted: Facet.newResourceProvider / src/async/resources deleted (no ResourceProvider, maxConcurrent, cacheBudget, retry, preload in CAND src or api.md). Replaced by caller loader/Compose shared resources and ContentProvider:P.... Replacement test: none; the feature is deleted.
- `themes-P1-124` (1 main case; `async_completeness`). Main promised: Per-key retry option reaches provider. Deleted: retry option deleted; a source change re-requests (FAIL).. Replacement test: none; the feature is deleted.
- `themes-P1-22` (2 main cases; `avatar`). Main promised: Invalid presence quarantined and recovered. Deleted: Quarantine/last-valid-seal and record.dump() are gone: candidate Avatar asserts presence inside a Compose formula (media.luau UI.Avatar facts) so an invalid value errors. api.md promises only the four presence values.. Replacement test: none; the feature is deleted.
- `themes-P1-26` (1 main case; `avatar`). Main promised: Authored frame input separate from Avatar activation. Deleted: Avatar `frame` option deleted (not in media_types AvatarProps, not in api.md Avatar row).. Replacement test: none; the feature is deleted.
- `themes-P1-34` (1 main case; `avatar_group`). Main promised: Keeps last valid roster and recovers. Deleted: Quarantine and record.dump() removed; candidate asserts items inside a Compose formula so invalid rosters error.. Replacement test: none; the feature is deleted.
- `themes-P1-57` (2 main cases; `image_framing`). Main promised: Circle shape refuses nine-slice and framing. Deleted: UI.Image with shape/scaleMode/imageFraming deleted (no UI.Image in CAND src/ui; api.md has no Image control).. Replacement test: none; the feature is deleted.
- `themes-P1-58` (4 main cases; `image_framing`). Main promised: Focus-point crop, natural pixels, malformed framing, bounds. Deleted: src/render/image_framing (framing.resolve) deleted; focusX/focusY and mode 'none' have no CAND equivalent or api.md promise.. Replacement test: none; the feature is deleted.
- `themes-P1-60` (1 main case; `image_framing`). Main promised: Reactive background reframes without solve or focus change. Deleted: Background framing through the solver deleted with UI.Image/UI.background.. Replacement test: none; the feature is deleted.
- `themes-P1-61` (1 main case; `image_framing`). Main promised: Native screen_image framing module. Deleted: src/client/screen_image.luau deleted.. Replacement test: none; the feature is deleted.
- `themes-P2-42` (4 main cases; `theme_icons`). Main promised: ASCII glyph fallback table and glyph mode when a package declines standard icons. Deleted: ICON_FALLBACK_GLYPHS, isSafeGlyph, identity.standardIcons and the screen_chrome paint path are removed. CAND ctx.icon asserts that the name resolves to an image asset. Guide 09 says 'Do not use arbitrary text glyphs a.... Replacement test: none; the feature is deleted.
- `themes-P2-55` (2 main cases; `theme_icons_applied`). Main promised: Control glyphs are ASCII by construction. Deleted: The glyph floor (iconGlyph, ICON_FALLBACK_GLYPHS) is removed. Note: CAND controls again render non-ASCII text (Stepper '−', Rating '★☆', table sort '↓↑').. Replacement test: none; the feature is deleted.
- `themes-P2-57` (3 main cases; `theme_icons_applied`). Main promised: Per-icon tint-role rules on a sanitized tag, with dotted names. Deleted: Per-name `.facet-chrome-icon-*` rules and iconTagFor are removed. The Asset.tintRole field is still typed but ignored, and every icon is tinted by the `.facet-icon` rules.. Replacement test: none; the feature is deleted.
- `themes-P2-65` (1 main case; `theme_icons_applied`). Main promised: Art-mode and glyph-mode packages invalidate icons in both directions. Deleted: Glyph mode (identity.standardIcons=false) and the chrome epoch are removed.. Replacement test: none; the feature is deleted.
- `themes-P3-13` (3 main cases; `theme_controller`). Main promised: Install-policy preload list requested through a content provider. Deleted: Install-time preload step deleted with the controller; asset field `preload` is no longer read anywhere in CAND src (grep preload in themes.luau: none; only media.luau AsyncImage uses ContentProvider per image). No ap.... Replacement test: none; the feature is deleted.

### Action controls

0 class b and 15 class c contracts, 297 main cases.

- `collections-185` (19 main cases; `table_input`). Main promised: onPrimaryAction double-click, touch cost and auto edit-mode semantics. Deleted: onPrimaryAction and the auto edit toggle are gone (not in collection_types.luau or api.md).. Replacement test: none; the feature is deleted.
- `inputs-01` (5 main cases; `activation_gate`). Main promised: UI.activationGate wake-then-pass gating. Deleted: UI.activationGate and its nested gate traversal are absent from CAND/src (grep activationGate: 0 hits in src/ui, src/init.luau) and api.md/guides do not mention it.. Replacement test: none; the feature is deleted.
- `inputs-169` (14 main cases; `sensory_feedback`). Main promised: UI.sensoryFeedback declarations and bus routing. Deleted: UI.sensoryFeedback absent from CAND src/api.md.. Replacement test: none; the feature is deleted.
- `inputs-170` (27 main cases; `sensory_profile`). Main promised: Sensory profile defaults, resolve, fallback and keys. Deleted: Sensory profile module deleted; api.md only accepts a caller HapticEffect (pressHaptic).. Replacement test: none; the feature is deleted.
- `inputs-27` (5 main cases; `button_complete`). Main promised: Content mode: label as semantics only, mandatory label, single-focusable content rule. Deleted: Closed blueprint content validation and mandatory label were removed (Button accepts row buttons without label, NI 'fills the width with a row button'); api.md no longer promises a content mode or focusable rejection.. Replacement test: none; the feature is deleted.
- `inputs-42` (19 main cases; `button_shape`). Main promised: Circle label/axis/icon grammar, schema, pixel snap, picture floor and ASCII glyph fallback. Deleted: Closed shape grammar (<=3 char labels, both-axes refusal, icon-on-rect and asset-id refusal), schema parity, pixel grid snapping and icon-font ASCII glyphs are gone; CAND accepts asset ids and icons on rect buttons (L.... Replacement test: none; the feature is deleted.
- `inputs-75` (64 main cases; `control_feedback`). Main promised: Feedback verb declarations, cascade, bus emission, haptic adapter verb map and tri-state demo report. Deleted: The twelve-verb feedback taxonomy, UI.feedback/sensoryFeedback declarations, presenter feedback bus and haptics adapter are absent from CAND src (grep feedback/haptics in src/ui: 0); api.md documents only the pressHap.... Replacement test: none; the feature is deleted.
- `inputs-86` (12 main cases; `feedback`). Main promised: Feedback bus taxonomy, subscriptions and presenter emission. Deleted: Feedback bus and presenter.onFeedback/emitFeedback removed; no 'feedback' symbol in CAND src/ui or api.md.. Replacement test: none; the feature is deleted.
- `inputs-89` (104 main cases; `haptics`). Main promised: Haptics adapter: enable/disable, verb map, rate limiting, capability probe, pooling, phases, profiles and static isolation. Deleted: src/client haptics adapter and sensory profile removed; CAND src/ui has no haptics module (only engine_types) and api.md promises only the pressHaptic factory option.. Replacement test: none; the feature is deleted.
- `navigation-1-05` (5 main cases; `adaptive_presentation`). Main promised: Image button focus lift / hover scale animation and its observers. Deleted: Main image-button focus lift (Artwork transform scale 1.05, stroke, hover observers counted in adapter handler census) is deleted; CAND inputs.luau image card is a plain ImageLabel inside the TextButton with no lift. .... Replacement test: none; the feature is deleted.
- `navigation-1-30` (1 main case; `alert`). Main promised: Action compactLabel and emphasis preserved with a shortcut. Deleted: The AlertAction type in CAND navigation_types.luau has no compactLabel; api.md Alert actions list id,label,role,enabled,shortcut,onActivate only.. Replacement test: none; the feature is deleted.
- `navigation-4-52` (1 main case; `split_button`). Main promised: Touch form shows a trailing chevron hint. Deleted: CAND SplitButton touch form is a plain UI.Button trigger with no disclosure/chevron (nav_menu.luau UI.SplitButton); api.md makes no hint promise.. Replacement test: none; the feature is deleted.
- `paint-05` (3 main cases; `control_vocabulary`). Main promised: Rung-to-metric-path indirection and fill minima. Deleted: src/controls/control_vocabulary.luau (deferred solver metric paths) and solver fill minima deleted; ctx.controlSize reads the number directly.. Replacement test: none; the feature is deleted.
- `paint-12` (7 main cases; `control_vocabulary`). Main promised: Retired Button authoring refusals (name, semantic icon, construction-time, appearance words, circle trailingIcon, empty content). Deleted: Candidate accepts icon-only without name, raw rbxassetid icons (tested in native_inputs circle case), unknown appearance words and circle+trailingIcon; api.md no longer lists these refusals.. Replacement test: a candidate test covers the surviving part (see reason).
- `paint-17` (11 main cases; `control_vocabulary`). Main promised: Chip rung/appearance vocabulary and live appearance-family checks. Deleted: Chip has no controlSize/appearance (unknown option error); Button appearance values are no longer validated. api.md Chip lists only label/selected/onRemove/onToggle/removeLabel/removeFocusFallback/leading/trailing.. Replacement test: none; the feature is deleted.

### Value controls

0 class b and 15 class c contracts, 28 main cases.

- `apps-241` (1 main case; `adaptive_defaults`). Main promised: LevelPicker touch run wider than pointer run. Deleted: Facet environment (app.environment size class, interaction classes, distance profile, discoverable per-core environment, client host) deleted; controls read native AbsoluteSize and UserInputService.PreferredInput dire.... Replacement test: none; the feature is deleted.
- `inputs-103` (1 main case; `level_picker`). Main promised: Read-only Memo refused. Deleted: Legacy readonly rejection retired; api.md State: a callback makes value controls controlled.. Replacement test: none; the feature is deleted.
- `inputs-118` (4 main cases; `level_picker`). Main promised: High-count diagnostics. Deleted: Density diagnostics removed with the solver.. Replacement test: none; the feature is deleted.
- `inputs-149` (1 main case; `rating`). Main promised: Read-only Memo refused. Deleted: Legacy readonly rejection retired.. Replacement test: none; the feature is deleted.
- `inputs-217` (1 main case; `value_controls`). Main promised: Rejects non-settable value. Deleted: Readonly value allowed with onChange (api.md State).. Replacement test: none; the feature is deleted.
- `inputs-54` (4 main cases; `chip`). Main promised: Per-input-class structural affordance idioms. Deleted: Input-class idiom classifier removed with the presenter.. Replacement test: none; the feature is deleted.
- `navigation-3-65` (4 main cases; `picker_segments`). Main promised: Badge plate, decoration-only, no tap interception, absent without badges. Deleted: Badge plate node (surface 'badge', Count child) deleted; CAND appends ' (n)' to the segment label text.. Replacement test: none; the feature is deleted.
- `paint-20` (1 main case; `display_controls`). Main promised: Progress semantic value text and dump. Deleted: Facet dump/semanticText surface deleted; api.md ProgressView promises no accessible value text.. Replacement test: none; the feature is deleted.
- `paint-25` (1 main case; `display_controls`). Main promised: Text over-media classification. Deleted: UI.Text and over-media tag deleted.. Replacement test: none; the feature is deleted.
- `themes-P1-113` (2 main cases; `status_indicator`). Main promised: Retains last valid paint/count across invalid updates. Deleted: Last-valid-seal quarantine and record.api removed; bindings assert and error.. Replacement test: none; the feature is deleted.
- `themes-P1-119` (1 main case; `status_indicator`). Main promised: False form/status refused. Deleted: Refusal removed: media.luau reads `read(form) or 'dot'` and `or 'neutral'`, so false publishes defaults; api.md makes no refusal promise.. Replacement test: none; the feature is deleted.
- `themes-P1-55` (1 main case; `badge`). Main promised: Rejects empty/non-string names and false appearance. Deleted: Strict name/appearance validation removed: media.luau uses spec.name or caption and read(appearance) or 'standard'. api.md makes no such promise.. Replacement test: none; the feature is deleted.
- `themes-P1-94` (1 main case; `skeleton`). Main promised: Informational plate without a clock. Deleted: Internal skeleton.build without a motion clock deleted; every CAND Skeleton acquires the shared timeline.. Replacement test: none; the feature is deleted.
- `themes-P5-32` (3 main cases; `theme_value_displays`). Main promised: Stepper plate falls back to the control recipe. Deleted: chrome_slots.resolveSlot fallback is gone: inputs.luau calls ctx.skin(node, 'stepperPlate') only, so a package without stepperPlate leaves steppers unskinned. 10-rich-skinning lists slots without a fallback promise.. Replacement test: none; the feature is deleted.
- `themes-P5-36` (2 main cases; `theme_value_displays`). Main promised: Toggle live-found load and syncTags details. Deleted: Renderer decoration load reporting and the syncTags classification pass are deleted; Compose owns tags directly.. Replacement test: none; the feature is deleted.

### Text input

5 class b and 7 class c contracts, 49 main cases.

- `apps-169` (2 main cases; `keyboard_navigation`). Main promised: Text editing owns arrows; Tab commits then advances. Engine or Compose owns it: Roblox TextBox focus/FocusLost and engine keyboard handling. Replacement test: yes: native_navigation::native navigation controls::validates combo custom commits and cancels editable text.
- `inputs-122` (11 main cases; `multiline_input`). Main promised: Multiline frame, scroll viewport, border insets, keyboard shrink and caret reveal. Engine or Compose owns it: Native TextBox MultiLine editing, internal scrolling and caret reveal (creator-docs TextBox Text Editing).. Replacement test: yes: native_inputs::native input controls::ends a multiline edit when a native ancestor disables without late mutation (asserts MultiLine=true only).
- `inputs-124` (10 main cases; `native_caret_scroll`). Main promised: Asynchronous caret measurement ownership. Engine or Compose owns it: Native TextBox caret reveal.. Replacement test: no test.
- `inputs-137` (3 main cases; `paradigm_textinput`). Main promised: Edit carries or commits across device arrivals and OSK loss. Engine or Compose owns it: TextBox Focused/FocusLost across devices.. Replacement test: no test.
- `inputs-181` (2 main cases; `text_input`). Main promised: Activate enters editing; second Return idempotent. Engine or Compose owns it: TextBox focus capture by GuiService selection/engine.. Replacement test: no test.
- `apps-244` (1 main case; `adaptive_defaults`). Main promised: TextInput lifts off the on-screen keyboard. Deleted: Keyboard-avoidance lift deleted; Roblox TextBox/engine own on-screen keyboard behavior (api.md TextInput).. Replacement test: none; the feature is deleted.
- `inputs-08` (1 main case; `auto_input`). Main promised: Field geometry sync and keyboard keep-visible offset. Deleted: keepVisibleOffset / occlusion transform removed (no occlusion code in CAND src/ui/inputs.luau TextInput); api.md TextInput section does not promise keep-visible.. Replacement test: none; the feature is deleted.
- `inputs-139` (1 main case; `paradigm_textinput`). Main promised: Keep-visible offset reset on dock. Deleted: Occlusion keep-visible removed.. Replacement test: none; the feature is deleted.
- `inputs-189` (14 main cases; `text_input`). Main promised: keyboardType/submitLabel adapter maps and keyboard occlusion keep-visible. Deleted: Invented keyboardType/submitLabel maps and occlusion transform removed; api.md TextInput lists neither (native TextBox properties remain available).. Replacement test: none; the feature is deleted.
- `inputs-192` (2 main cases; `text_input`). Main promised: Value must be a writable cell. Deleted: Writable-only restriction retired; api.md State lets onChange make a read-only value controlled (NI 'edits a controlled map field').. Replacement test: none; the feature is deleted.
- `paint-111` (1 main case; `sheet_model`). Main promised: Editing yield transparent. Deleted: TextBox chrome lift deleted.. Replacement test: none; the feature is deleted.
- `paint-65` (1 main case; `native_style_scenario`). Main promised: Editing yield of lifted text. Deleted: TextBox chrome text lift deleted.. Replacement test: none; the feature is deleted.

### Menus and pickers

0 class b and 12 class c contracts, 38 main cases.

- `apps-238` (1 main case; `adaptive_defaults`). Main promised: Authored size class wins over the surface. Deleted: Facet environment (app.environment size class, interaction classes, distance profile, discoverable per-core environment, client host) deleted; controls read native AbsoluteSize and UserInputService.PreferredInput dire.... Replacement test: none; the feature is deleted.
- `apps-239` (1 main case; `adaptive_defaults`). Main promised: PopupButton touch resolves a sheet. Deleted: PopupButton removed.. Replacement test: none; the feature is deleted.
- `collections-80` (7 main cases; `row_actions_input`). Main promised: Action menu placement flip. Deleted: RowActions no longer places its own menu. It uses UI.Menu (nav_menu.luau), which owns the placement. The row-level flip code is gone.. Replacement test: none; the feature is deleted.
- `collections-83` (3 main cases; `row_actions_input`). Main promised: Outside-tap swallow, pseudo-loc menu rows, icon in tray and word in menu. Deleted: UI.Menu owns these. The menu item now gets both label and icon.. Replacement test: none; the feature is deleted.
- `navigation-2-25` (7 main cases; `menu`). Main promised: Menu authoring lints and depth diagnostics. Deleted: Half-iconned group lint, group-size and destructive-position advice, depth>=2 diagnostics and the unreachable-trigger refusal were deleted (no lint/diagnostics in nav_menu.luau; core diagnostics gone). Arbitrary nesti.... Replacement test: none; the feature is deleted.
- `navigation-2-43` (5 main cases; `menu`). Main promised: Shared menu row recipe with popup_button (resolvePresentation alias, shared tokens, compactLabel shape, Option icon/divider, shared lint). Deleted: menu_recipe and popup_button deleted (no resolvePresentation/popup in CAND src); rows are UI.Button 44px built in nav_menu.luau.. Replacement test: none; the feature is deleted.
- `navigation-2-52` (2 main cases; `menu_scenario`). Main promised: Scenario lint/advice (icons per group, destructive last, depth advice). Deleted: Menu lints and diagnostics deleted (see menu contract); the scenario still contains the content but no advice is reported.. Replacement test: none; the feature is deleted.
- `navigation-3-79` (1 main case; `picker_style`). Main promised: Check edge follows live interaction class. Deleted: No check glyph; chosen menu rows use the Button selected fill (facet-selected).. Replacement test: none; the feature is deleted.
- `navigation-3-84` (1 main case; `picker_style`). Main promised: valueAlignment moves a labelled menu value. Deleted: Picker valueAlignment was removed from src and api.md (f2929ef1). The foundation parity port restored valueAlignment (start and end) for a labelled menu; see post-port26-018 in the verification parity guide.. Replacement test: native_foundation_parity::native start-aligned Picker menu.
- `navigation-4-59` (1 main case; `tab_customization`). Main promised: Picker section headings only on the vertical axis. Deleted: Picker sectionTitle deleted (not in CAND src/api). TabView sections exist and are covered for sidebar by "renders sidebar sections and collapsed tab destinations through the shared menu".. Replacement test: none; the feature is deleted.
- `paint-34` (8 main cases; `display_controls`). Main promised: PopupButton adaptive presentation. Deleted: PopupButton deleted (not in src/ui, api.md or guide catalog); Picker style=menu and Menu replace it.. Replacement test: none; the feature is deleted.
- `paint-61` (1 main case; `native_regions_feedback`). Main promised: Composite Button as Menu trigger. Deleted: Menu takes label/icon only (api.md Menu); custom Button triggers deleted.. Replacement test: none; the feature is deleted.

### Navigation containers

1 class b and 2 class c contracts, 3 main cases.

- `navigation-4-62` (1 main case; `tab_customization`). Main promised: Shared indicator moves when keyed options reorder. Engine or Compose owns it: Indicator is a child of the selected option; UIListLayout LayoutOrder moves it with the option. Replacement test: yes: Candidate shows the indicator follows the selected option; no reorder case..
- `navigation-1-09` (1 main case; `adaptive_presentation`). Main promised: Ornate sidebar badge seal reserves space beside label. Deleted: The separate badge seal node (ZStack 'Badge') is gone; CAND nav_pages.luau appends the badge to the tab label text ('Label (n)'), so there is no separate node that could overlap. api.md does not promise a badge seal.. Replacement test: none; the feature is deleted.
- `paint-59` (1 main case; `native_regions_feedback`). Main promised: navBar. Deleted: navBar deleted; not in api.md/catalog.. Replacement test: none; the feature is deleted.

### Presented surfaces

3 class b and 16 class c contracts, 163 main cases.

- `apps2-45` (2 main cases; `examples_gallery`). Main promised: Confirm dialog: gamepad to Cancel; card centred. Engine or Compose owns it: GuiService selection within the Alert's SelectionGroup; native AnchorPoint/Position centring. Replacement test: no test (none example-specific).
- `apps2-94` (2 main cases; `gallery_chrome`). Main promised: The open Settings panel stays over the demo. Engine or Compose owns it: UI.Sheet presentation layer and native ZIndex. Replacement test: yes: parity_blockers.json G18 resolution: 'Tab chrome now has explicit native ZIndex 2 above page content' with gap-closure-live.json G18; not specific to the Settings sheet..
- `navigation-2-64` (1 main case; `modal_dismissal`). Main promised: Scrim layer sits beneath the top modal. Engine or Compose owns it: Native ZIndex (DismissOutside 100, panel 101) within the modal portal. Replacement test: no test.
- `collections-56` (1 main case; `row_actions`). Main promised: _settleTo private spring retarget. Deleted: The private engine methods are gone. runtime.spring owns the retarget.. Replacement test: none; the feature is deleted.
- `inputs-92` (18 main cases; `help`). Main promised: Activation dismissal, single-plate retirement, disclosure precedence, dump, construction-only string, reachability checker and arrow tail. Deleted: help metadata scanner/reachability diagnostics, plate tail and dump removed; src/ui/help.luau is a 21-line registry and nav_modal shows a tail-less 'Help' panel. api.md accepts a sentence and states only dwell/selecti.... Replacement test: none; the feature is deleted.
- `mech1-14` (18 main cases; `app_chrome_insets`). Main promised: Host chrome band insets for content vs modal, plates and popovers. Deleted: app chromeInsets/host band and inset policies (deviceSafeContent, presentCritical) deleted; api.md: native safe-area properties belong to caller targets. Alert full-screen presentation is tested separately (native_nav.... Replacement test: a candidate test covers the surviving part (see reason).
- `mech2-105` (21 main cases; `presenter`). Main promised: Presenter options: comparisons, enum opts, raise, transition resolution, duplicate ids. Deleted: presenter/presentModal/presentAnchored deleted; Alert/Sheet own presentation. Replacement test: none; the feature is deleted.
- `mech2-82` (2 main cases; `native_toast`). Main promised: Toast owns its component body. Deleted: toast composition service deleted; toast exists only as gallery example (native_gallery_parity toast cases). Replacement test: a candidate test covers the surviving part (see reason).
- `mech3-56` (20 main cases; `surface_lifecycle_seam`). Main promised: Surface lifecycle seam. Deleted: presenter surface lifecycle deleted. Replacement test: none; the feature is deleted.
- `mech3-57` (33 main cases; `surface_overlap`). Main promised: Cross-surface overlap diagnostics. Deleted: controller.diagnostics() and surface layer bands deleted. Replacement test: none; the feature is deleted.
- `mech3-86` (13 main cases; `toast_layer_seam`). Main promised: Toast layer seam. Deleted: presenter.presentToast and toast_layer deleted; toasts are now game-owned in examples/gallery/scenarios/sponsor_toast.luau. Replacement test: none; the feature is deleted.
- `mech3-88` (13 main cases; `toast_presentation`). Main promised: Toast band stacking, CanvasGroup text rule, placement validation, reduced-motion transitions. Deleted: presenter display bands and toast transition options deleted with presentToast; gallery example does not promise them. Replacement test: none; the feature is deleted.
- `mech3-90` (1 main case; `toast_schedule`). Main promised: Ratified toast defaults. Deleted: presentToast defaults deleted. Replacement test: none; the feature is deleted.
- `navigation-1-36` (6 main cases; `alert`). Main promised: Mark size relative to heading; header row existence. Deleted: Theme metrics (iconSizes, type.heading.size) and the Header row are gone. CAND renders AlertIcon at a fixed 28x28 as a column sibling of Title. api.md makes no mark-size promise.. Replacement test: none; the feature is deleted.
- `navigation-1-46` (7 main cases; `anchored_surface`). Main promised: presentAnchored presenter API (source path, raw rect, stack, modal path, key/policy refusal, dump). Deleted: Presenter presentAnchored and anchoredSurfaces() dump are deleted; no presentAnchored in CAND src or api.md. Menu/Callout use native overlays (nav_modal.overlay).. Replacement test: none; the feature is deleted.
- `navigation-2-60` (1 main case; `modal_dismissal`). Main promised: Invisible fullscreen root contributes nothing to the no-dismiss zone. Deleted: Painted-extent Zone A computation deleted; forgiveness is computed from the panel Instance rect only (nav_modal.luau DismissOutside.Activated).. Replacement test: none; the feature is deleted.
- `navigation-2-62` (2 main cases; `modal_dismissal`). Main promised: outsideTapCancel=false barrier (no dismiss, no click-through). Deleted: outsideTapCancel/presentModal options deleted (only historical docs/superpowers plan mentions them). Click-through prevention is now the native full-screen DismissOutside TextButton sinking input.. Replacement test: none; the feature is deleted.
- `navigation-4-13` (1 main case; `popup_catcher_paint`). Main promised: Catcher sits beneath its owner DisplayOrder. Deleted: Separate presenter roots with DisplayOrder are deleted; overlays are in-tree Frames (Modal ZIndex 100, panel 101) under the same ScreenGui.. Replacement test: none; the feature is deleted.
- `navigation-5-10` (1 main case; `tab_view`). Main promised: Nesting never touches the presenter back() stack. Deleted: Presenter and its back()/depth stack are deleted (no src/present in CAND). TabView binds only PreviousTab/NextTab InputActions; api.md has no presenter.. Replacement test: none; the feature is deleted.

### Virtual collections

1 class b and 14 class c contracts, 55 main cases.

- `collections-25` (3 main cases; `collection_self_measure`). Main promised: Windowing uses the native box and re-windows on resize, for lists and grids. Engine or Compose owns it: ScrollingFrame.AbsoluteWindowSize, observed by collections.luau into the Compose viewport. Replacement test: yes: NC1 and NC3 set AbsoluteWindowSize and assert the retained window and placements. No test resizes after the first geometry. There is no live Studio evidence (parity_blockers G02-G11 say 'Engine geometry still requires....
- `apps-134` (19 main cases; `paradigm_cards`). Main promised: Adaptive card rail paradigm (cards per view, peek, snap, minWidth, compact answer). Deleted: CardRail control and cards/snap-compact facts removed; api.md has no CardRail. examples/gallery/scenarios/card_rail.luau now composes a native ScrollingFrame/VirtualList.. Replacement test: none; the feature is deleted.
- `apps2-111` (8 main cases; `gallery_demo_picker`). Main promised: Host delivers present opts and binds the native-scroll mirror. Deleted: The presenter present() opts and Table/VirtualList bindNativeScroll mirror were deleted; collections own their native ScrollingFrame directly.. Replacement test: none; the feature is deleted.
- `collections-10` (3 main cases; `collection_list`). Main promised: clearSelection handle and selectionPaint option. Deleted: The imperative clearSelection and the selectionPaint option are gone. The selection is the caller's writable cell (api.md). Paint comes from the StyleSheet through the FacetSelected attribute.. Replacement test: none; the feature is deleted.
- `collections-209` (1 main case; `table_virtualized`). Main promised: First-frame over-mount seed. Deleted: Nothing mounts before geometry.. Replacement test: none; the feature is deleted.
- `collections-211` (2 main cases; `table_virtualized`). Main promised: Flowing-mode agreement. Deleted: There is no flowing mode. Block tables use Compose mode 'all' with the same placements.. Replacement test: none; the feature is deleted.
- `collections-22` (2 main cases; `collection_list`). Main promised: Illegal follow refused; live follow flip without remount. Deleted: api.md: 'follow is a static Compose option. To change its policy, replace the collection owner through Compose.keyed.' The live flip was removed on purpose. A probe showed that follow='start' is accepted silently. api.... Replacement test: none; the feature is deleted.
- `collections-224` (1 main case; `virtual_grid`). Main promised: Per-line extent function builds nothing. Deleted: There is no per-line function.. Replacement test: none; the feature is deleted.
- `collections-26` (2 main cases; `collection_self_measure`). Main promised: viewportExtent seed-from-screen and numeric/Readable viewportExtent. Deleted: viewportExtent is gone. CAND does the opposite of the seed: Compose.show(viewport.size > 0) mounts no rows until native geometry exists (collections.luau).. Replacement test: none; the feature is deleted.
- `collections-27` (3 main cases; `collection_self_measure`). Main promised: Nested-scroller measurement diagnostic. Deleted: The solver diagnostic for a list nested in another scroller is gone.. Replacement test: none; the feature is deleted.
- `collections-275` (1 main case; `virtual_list_measured_extents`). Main promised: Fractional rounding and the zero floor. Deleted: CAND ignores extent 0 and does not round.. Replacement test: none; the feature is deleted.
- `collections-28` (2 main cases; `collection_self_measure`). Main promised: itemExtent = 'cards' paints nothing before measurement. Deleted: The 'cards' extent mode is gone. Every CAND list mounts nothing before it has geometry, so the negative control (a plain list over-fills) no longer holds.. Replacement test: none; the feature is deleted.
- `collections-280` (1 main case; `virtual_list_measured_extents`). Main promised: Equal measured rows converge to uniform. Deleted: The differential against the removed uniform index is gone.. Replacement test: none; the feature is deleted.
- `collections-293` (3 main cases; `virtual_list_row_actions_identity`). Main promised: Mount-path identity differential. Deleted: Scene paths and the overlay are gone.. Replacement test: none; the feature is deleted.
- `collections-304` (4 main cases; `virtual_list_variable_extents`). Main promised: Guard per row, index rebuild minimality, uniform equivalence. Deleted: The index internals and the guard are gone.. Replacement test: none; the feature is deleted.

### Tables

1 class b and 16 class c contracts, 59 main cases.

- `inputs-05` (1 main case; `auto_input`). Main promised: Gamepad A on a focused table row selects it. Engine or Compose owns it: GuiButton.Activated from GuiService selection (engine ButtonA).. Replacement test: yes: native_collections::Compose native collections::selects ranges across unmounted rows while honoring row capability policies.
- `apps-243` (1 main case; `adaptive_defaults`). Main promised: Table row box and column reservation read one environment. Deleted: Facet environment (app.environment size class, interaction classes, distance profile, discoverable per-core environment, client host) deleted; controls read native AbsoluteSize and UserInputService.PreferredInput dire.... Replacement test: none; the feature is deleted.
- `apps2-176` (4 main cases; `playlist_columns`). Main promised: Grip hit floor overlaps its own header button; divider forwards a press that did not become a drag; guide line. Deleted: The grip is now a separate TextButton at the column's right edge with a UIDragDetector (collection_table.luau:332-360); the hit-floor model, press forwarding to Sort, promotion gate and resize guide line were removed..... Replacement test: none; the feature is deleted.
- `collections-135` (1 main case; `table`). Main promised: Selection-mark gutter in edit mode. Deleted: There is no selection mark. FacetSelected is painted by the StyleSheet.. Replacement test: none; the feature is deleted.
- `collections-141` (11 main cases; `table`). Main promised: Override clamp arithmetic, gap reservation, path-magic ids, control dump. Deleted: The DIR-4 clamp rule, the pure reservation function, path routing and dump() are gone. There is a default floor of 48, so 'no floor' does not exist.. Replacement test: none; the feature is deleted.
- `collections-143` (8 main cases; `table`). Main promised: Column alignment, sort mark reserve, width sugar, build-time column asserts. Deleted: There is no alignment option and no width band sugar. The sort mark is a text suffix. The table now sorts the data itself (the 'table never sorts data' contract is inverted: api.md 'sort ... `onSortChange` controlled').. Replacement test: none; the feature is deleted.
- `collections-152` (2 main cases; `table`). Main promised: Grip guide-line preview and the focus wrap cycle. Deleted: Width updates live during the drag, and there is no guide line. There is no Facet focus cycle.. Replacement test: none; the feature is deleted.
- `collections-157` (5 main cases; `table`). Main promised: Card clearance, mouse body-drag reorder, auto Edit/Done toggle. Deleted: Reorder is handle-only. There is no auto toggle.. Replacement test: none; the feature is deleted.
- `collections-193` (1 main case; `table_input`). Main promised: A collapsed column's sort from the plate. Deleted: The plate shows values only.. Replacement test: none; the feature is deleted.
- `collections-200` (6 main cases; `table_input`). Main promised: Edit-mode selection mark, gutters and toggle gating. Deleted: There is no selection mark and no auto toggle.. Replacement test: none; the feature is deleted.
- `collections-202` (3 main cases; `table_input`). Main promised: Focus-driven disclosure plate and heading disclosure. Deleted: Disclosure opens only through the Details action.. Replacement test: none; the feature is deleted.
- `collections-204` (2 main cases; `table_input`). Main promised: Collapse dump; a floorless table never collapses. Deleted: dump() is gone. There is a default 48 px floor.. Replacement test: none; the feature is deleted.
- `collections-217` (2 main cases; `table_virtualized`). Main promised: Row height derived rather than content-sized. Deleted: Behavior changed. Rows grow to fit cell TextBounds (NC19).. Replacement test: none; the feature is deleted.
- `collections-38` (4 main cases; `paradigm_table`). Main promised: Comma/Period, L1/R1 and the A-select resize model. Deleted: Those bindings and the select-then-adjust model are gone. collection_table.luau binds only Left/Right and DPadLeft/DPadRight while the grip is selected.. Replacement test: none; the feature is deleted.
- `collections-41` (3 main cases; `paradigm_table`). Main promised: The control-owned Edit/Done toggle and editing reset on a class flip. Deleted: The auto Edit/Done toggle is gone. In CAND, `editing` is a caller-owned value (api.md Table: '`editing` is a writable cell').. Replacement test: none; the feature is deleted.
- `collections-43` (1 main case; `paradigm_table`). Main promised: Auto-toggle focus exit into the rows. Deleted: There is no auto toggle (see above).. Replacement test: none; the feature is deleted.
- `inputs-70` (4 main cases; `compact_label`). Main promised: Table Edit/Done toggle as first consumer. Deleted: Table has no built-in Edit/Done button in CAND (collection_table.luau takes an 'editing' model option).. Replacement test: none; the feature is deleted.

### Row actions

0 class b and 14 class c contracts, 99 main cases.

- `collections-13` (1 main case; `collection_list`). Main promised: toggleGrab imperative verb. Deleted: The imperative list handle (toggleGrab) is gone. Handle activation arms and commits (collection_reorder.luau activate()).. Replacement test: none; the feature is deleted.
- `collections-14` (4 main cases; `collection_list`). Main promised: Rows as drop targets for foreign payloads. Deleted: The drop-target registry (accepts/onDrop, reject verdicts) is gone. CAND src has no onDrop or accepts, and api.md documents none.. Replacement test: none; the feature is deleted.
- `collections-146` (4 main cases; `table`). Main promised: Chip cap, invalid outside drop, shift+Navigate reorder. Deleted: The drag label is 'N selected'. There is no shift+Navigate verb. Behavior changed: a pointer outside the body clamps to the first or last slot, so the drop commits.. Replacement test: none; the feature is deleted.
- `collections-16` (4 main cases; `collection_list`). Main promised: Re-sort or removal mid-drag keeps the drag session. Deleted: Behavior changed. CAND cancels the move when the source changes (collection_reorder.luau watch on rows/sort), and NC10 asserts this. Reorder presentation motion is also gone. The ledger records this as retired.. Replacement test: none; the feature is deleted.
- `collections-169` (8 main cases; `table`). Main promised: Table rowActions internals (unknown keys, Grip, body drag, tray closes before reorder/grab, tap-to-close, rect identity). Deleted: These paths are gone. Reorder does not touch the coordinator. There is no tap-to-close.. Replacement test: none; the feature is deleted.
- `collections-172` (3 main cases; `table`). Main promised: Page scroll closes trays in a block table; path binding errors. Deleted: There are no published scroll paths. A block table does not observe page scrolling (it only closes on its own Body CanvasPosition).. Replacement test: none; the feature is deleted.
- `collections-283` (13 main cases; `virtual_list_row_actions`). Main promised: Lazy hosted engines, mount identity, scroll close, pen, dropSurface, re-present. Deleted: RowActions mounts eagerly per mounted row. There is no hosted engine, overlay, dropSurface or list scroll-close.. Replacement test: none; the feature is deleted.
- `collections-289` (8 main cases; `virtual_list_row_actions`). Main promised: Tab rank, activate seam, author exits, skipped rows, scroll during commit, outside tap, d-pad walk laziness. Deleted: The presenter rank, the activation seam and the hosted laziness are gone.. Replacement test: none; the feature is deleted.
- `collections-290` (10 main cases; `virtual_list_row_actions`). Main promised: Swipe release Activate arming. Deleted: The activation-artifact arming is gone.. Replacement test: none; the feature is deleted.
- `collections-62` (2 main cases; `row_actions`). Main promised: Minus opens the leading tray for leading-only destructive; the editing type is refused. Deleted: Behavior changed. The minus runs the destructive action directly on either edge (delete() -> execute). There is no Readable type check on editing.. Replacement test: none; the feature is deleted.
- `collections-63` (17 main cases; `row_actions`). Main promised: Tray and minus geometry details (theme gutters, trayGap, 44px hit floor, rigid flight, z-order, prefer grammar, device-round insets, glyph fallback). Deleted: The theme metrics editAffordance/rowGutter/trayGap, the compact prefer grammar, the framework fallback glyphs and the device-round inset arithmetic are gone. CAND uses a measured 72 px minimum edit gutter and bandWidt.... Replacement test: none; the feature is deleted.
- `collections-66` (11 main cases; `row_actions_hosted`). Main promised: Hosted engine plumbing (paths, onSlide, outsideDismiss, labels, keys context). Deleted: buildHosted, paths, onSlide/onCommitHeight and outsideDismiss are not in CAND src.. Replacement test: none; the feature is deleted.
- `collections-68` (5 main cases; `row_actions_hosted`). Main promised: Hosted minus path routing, refusals and theme gutter. Deleted: Path routing is gone.. Replacement test: none; the feature is deleted.
- `collections-87` (9 main cases; `row_actions_input`). Main promised: composeWithReorder gesture routing. Deleted: Handler composition is gone. Reorder uses a separate Move handle with its own UIDragDetector.. Replacement test: none; the feature is deleted.

### HUD and world targets

7 class b and 8 class c contracts, 118 main cases.

- `apps-207` (3 main cases; `hud_chrome_rotation`). Main promised: Anchored content clears lateral edges by the gutter; real safe-area inset wins. Engine or Compose owns it: ScreenGui ScreenInsets/SafeAreaCompatibility and GuiService inset properties (docs/guide/15-adaptive-recipes.md 'Use the ScreenGui inset and safe-area properties'). Replacement test: no test.
- `apps-208` (3 main cases; `hud_chrome_rotation`). Main promised: Theme swap, preferred text and theme metrics re-run give-way. Engine or Compose owns it: StyleSheet theming + TextSize/AutomaticSize reflow observed through native AbsoluteSize. Replacement test: yes: native_hud::native HUD::uses native body bounds after chrome layout and disconnects on disposal (AbsoluteSize-driven re-evaluation only).
- `apps-216` (5 main cases; `hud_composition`). Main promised: HUD clears platform controls at measured viewports. Engine or Compose owns it: ScreenGui ScreenInsets/SafeAreaCompatibility and GuiService inset properties (docs/guide/15-adaptive-recipes.md 'Use the ScreenGui inset and safe-area properties'). Replacement test: no test.
- `apps-222` (2 main cases; `hud_reservations`). Main promised: D-pad reaches HUD actions at 390 and 1920 widths. Engine or Compose owns it: Engine gamepad selection. Replacement test: live Studio evidence `classb_keyboard/hud-dpad-reaches-actions-390`, `classb_keyboard/hud-dpad-reaches-actions-1920`.
- `apps2-235` (4 main cases; `spatial`). Main promised: World SurfaceGui render target. Engine or Compose owns it: Callers mount a native SurfaceGui with Compose constructors (api.md "Native targets and boundaries"); src/client/surface_target.luau and target_contract are deleted.. Replacement test: yes: native_outpost_terminal asserts ClassName SurfaceGui, Face Back, CanvasSize 640, cleanup; AlwaysOnTop=false is set in examples/gallery/scenarios/outpost_terminal.luau but not asserted..
- `mech2-90` (22 main cases; `overflow_sweep`). Main promised: HUD clears showcase chrome and holds its boxes at 11 viewports. Engine or Compose owns it: native layout, ScreenInsets, measured AbsoluteSize. Replacement test: yes: native_hud::native HUD::recovers whole simplified zones in overflow and shares their live model; native_hud::native HUD::uses native body bounds after chrome layout and disconnects on disposal (no multi-viewport sweep).
- `mech3-59` (9 main cases; `surface_target`). Main promised: World-fixed SurfaceGui target. Engine or Compose owns it: Native SurfaceGui mounted by the caller with Compose constructors (api.md 597). Replacement test: yes: native_outpost_terminal::native outpost terminal::mounts a real SurfaceGui on the caller runtime and owns cleanup (Face, CanvasSize, cleanup); refusal messages for parent/adornee/face are gone with client.surface_target.
- `apps-209` (22 main cases; `hud_chrome_rotation`). Main promised: App-chrome column reservations, ladder may not buy chrome, beside-chrome band, riding/appChromeRects inputs, coverage ledger. Deleted: Framework HUD preset (nine-anchor partition, holdsLane, topbar span, rank ladder, app-chrome reservations, collision reports, deviceSafeContent) deleted with the solver; HUD is now the gallery example examples/gallery.... Replacement test: none; the feature is deleted.
- `apps-213` (1 main case; `hud_chrome_sequence`). Main promised: Process-global width store restored. Deleted: Width store deleted with the solver/measure cache.. Replacement test: none; the feature is deleted.
- `apps-217` (22 main cases; `hud_composition`). Main promised: Nine-anchor partition, holdsLane, topbar span, collision reports/findings, expand roles, strip riding, pseudo-locale, deviceSafeContent rung. Deleted: Framework HUD preset (nine-anchor partition, holdsLane, topbar span, rank ladder, app-chrome reservations, collision reports, deviceSafeContent) deleted with the solver; HUD is now the gallery example examples/gallery.... Replacement test: none; the feature is deleted.
- `apps-220` (5 main cases; `hud_reservations`). Main promised: HUD reservation API (surface coordination, toast reservations, insets, disposed owner, invalid toast). Deleted: HUD reservation API deleted; not in api.md.. Replacement test: none; the feature is deleted.
- `apps-223` (6 main cases; `world_anchor`). Main promised: client.world_anchor projection, padding, target tracking, near-plane rejection, validation. Deleted: world_anchor client module deleted (CAND src has only ui/ and vendor/); docs/guide/14-choosing-controls.md now says 'project the object with the engine camera. Then bind the resulting screen anchor to the control'.. Replacement test: none; the feature is deleted.
- `apps2-250` (5 main cases; `sponsor_scenarios`). Main promised: Sponsor markers track u,v without remount, burst arrange-only, keyed enter/exit, corner centring, exempt hue. Deleted: sponsor_markers fixture deleted. The different world_markers scenario is tested by native_gallery_workflows 'moves and hides native map markers without remounting their controls'.. Replacement test: a candidate test covers the surviving part (see reason).
- `apps2-251` (5 main cases; `sponsor_scenarios`). Main promised: Sponsor billboard: engine-seam degrade, depleting ring timer, reduced motion, record-vs-render, scope remount. Deleted: sponsor_billboard fixture deleted; BillboardGui target remains in api.md ('a BillboardGui into an applicable world target') but its timer/omen behaviors were fixture-specific.. Replacement test: none; the feature is deleted.
- `apps2-284` (4 main cases; `world_markers`). Main promised: worldMarkers layout: edge clamp, priority, HUD exclusions, reasons. Deleted: Facet.layout.worldMarkers and src/rect are deleted (no Facet.layout in CAND src or api.md). The gallery world_markers scenario now owns clamping/crowding/HUD band as game code, covered by the cited gallery parity case.... Replacement test: none; the feature is deleted.

### Adaptive environment

7 class b and 27 class c contracts, 385 main cases.

- `apps2-279` (1 main case; `virtual_monitors_input`). Main promised: Phone lays out Avatar/Assistant with controls selectable and no diagnostics. Engine or Compose owns it: Native selection over Selectable GuiObjects; layout via UIListLayout/AutomaticSize. Replacement test: live Studio evidence `classb_showcase/vm-phone-avatar-and-assistant`.
- `apps2-96` (7 main cases; `gallery_chrome`). Main promised: Chip strip and every edge sit inside the television overscan. Engine or Compose owns it: ScreenGui.ScreenInsets (default CoreUISafeInsets) and GuiService safe-area insets. Replacement test: no test (none; the gallery ScreenGui relies on the default ScreenInsets and nothing asserts it.).
- `inputs-141` (10 main cases; `platform_env_binding`). Main promised: Pointer vs pad primacy. Engine or Compose owns it: UserInputService.PreferredInput.. Replacement test: yes: native_inputs::native input controls::cancels slider pointer ownership on native gamepad handoff and reports bounded semantics; native_navigation::native navigation controls::shows button help after a pointer rests on....
- `inputs-142` (8 main cases; `platform_env_binding`). Main promised: Safe-area inset coherence. Engine or Compose owns it: ScreenGui ScreenInsets/SafeAreaCompatibility on the caller's target (api.md 'Native targets and boundaries').. Replacement test: no test.
- `mech1-09` (15 main cases; `adaptive`). Main promised: Topbar band / platformChrome L-shape / impossible-inset discard. Engine or Compose owns it: GuiService.TopbarInset, ScreenGui.ScreenInsets/SafeAreaCompatibility (caller-owned targets). Replacement test: no test (none: Facet no longer reads or sets these (only type declarations in src/ui/engine_types.luau); api.md 'The native safe-area and sizing properties belong to those targets'.).
- `mech3-16` (6 main cases; `renderer`). Main promised: Safe-area root policy (core/device safe content, edgeToEdge). Engine or Compose owns it: Native ScreenGui ScreenInsets/SafeAreaCompatibility owned by the caller's target (api.md 599; guide 15-adaptive-recipes 'Use the ScreenGui inset and safe-area properties'). Replacement test: no test (none (Facet sets no safe-area property; no test or live evidence)).
- `navigation-3-44` (2 main cases; `page_view`). Main promised: Class flip and resize during travel keep the page. Engine or Compose owns it: UIPageLayout re-lays pages on resize without rebuilding. Replacement test: live Studio evidence `classb_navigation/pageview-resize-during-travel`.
- `apps-132` (6 main cases; `preview`). Main promised: Preview device profiles, console overscan inset, notch profile, live-environment override preservation. Deleted: Facet preview/environment API (Facet.new environment, device profiles, overscan inset) removed; api.md has no preview or profile entry. Native viewport/GuiService inset facts are read directly.. Replacement test: none; the feature is deleted.
- `apps-236` (8 main cases; `adaptive_defaults`). Main promised: Environment discovery from its core (publish, ambiguity, GC, unpublish). Deleted: Facet environment (app.environment size class, interaction classes, distance profile, discoverable per-core environment, client host) deleted; controls read native AbsoluteSize and UserInputService.PreferredInput dire.... Replacement test: none; the feature is deleted.
- `apps-246` (8 main cases; `adaptive_defaults`). Main promised: Controls refuse without an environment and name the host; client host registration; api.md refusal scan. Deleted: Facet environment (app.environment size class, interaction classes, distance profile, discoverable per-core environment, client host) deleted; controls read native AbsoluteSize and UserInputService.PreferredInput dire.... Replacement test: none; the feature is deleted.
- `apps2-04` (2 main cases; `consumer_standalone`). Main promised: Consumer re-solves: button stack differs compact vs wide; viewport change without rebuild. Deleted: Consumer screen.luau is a single vertical UIListLayout with no viewport-dependent arrangement; the Facet re-solve path was deleted.. Replacement test: none; the feature is deleted.
- `apps2-227` (2 main cases; `showcase_tabs`). Main promised: Toolbar action hidden for compact/distant/unrelated demos; unobscured desktop layout choice. Deleted: The showcase toolbar actions and desktop layout choice were removed; screen.luau's toolbar has only the title and Settings.. Replacement test: none; the feature is deleted.
- `apps2-233` (4 main cases; `spatial`). Main promised: Presentation-space facts and spatialPointer capability. Deleted: Facet environment (src/env/environment.luau: presentationSpace, presentationProfile, interactionClasses) is deleted; CAND src has only src/ui and vendor, and api.md has no environment facts.. Replacement test: none; the feature is deleted.
- `apps2-75` (1 main case; `examples_games`). Main promised: Match-3 board reflows across viewports without remounting and names no device. Deleted: 07_match3.luau now uses a fixed 6x48px board (boardView Size UDim2.new(0, COLS*48, 0, ROWS*48)) with no viewport dependency; the reflow behavior was removed.. Replacement test: none; the feature is deleted.
- `apps2-90` (8 main cases; `gallery_chrome`). Main promised: Two targets forever, stepping to icon-only; chip strip keeps a left gutter at phone width. Deleted: The chip strip was replaced by UI.TabView style='sidebarAdaptable' (screen.luau), which switches to a scrolling bar at 390px (asserted in native_gallery 'switches the entire gallery catalogue'); the icon-only stepping.... Replacement test: a candidate test covers the surviving part (see reason).
- `inputs-140` (3 main cases; `platform_env_binding`). Main promised: Platform adapter binds OSK/camera connections. Deleted: Synthetic environment binding removed (ledger retiredMechanisms).. Replacement test: none; the feature is deleted.
- `inputs-160` (4 main cases; `rating`). Main promised: Star spacing per input paradigm. Deleted: Input-class spacing classifier removed.. Replacement test: none; the feature is deleted.
- `mech1-02` (36 main cases; `adaptive`). Main promised: Facet.adaptive size/height/orientation classes, conditions readables, nav-bar policy, effectiveDisplaySize. Deleted: adaptive module / app.environment deleted (no sizeClass, conditions, navPlacement, effectiveDisplaySize in CAND src or docs). Guide 15-adaptive-recipes now tells authors to read native AbsoluteSize; TabView sidebarAda.... Replacement test: none; the feature is deleted.
- `mech1-06` (9 main cases; `adaptive`). Main promised: Hidden ViewThatFits candidate requests no hit geometry / expander retraction. Deleted: Hit expanders and minimum-target expander adapter deleted; native GuiObject hit testing.. Replacement test: none; the feature is deleted.
- `mech1-109` (12 main cases; `edge_floor`). Main promised: edgeFloor inset knob. Deleted: Root inset policies deleted.. Replacement test: none; the feature is deleted.
- `mech1-43` (12 main cases; `band_policy`). Main promised: bandSafeContent root policy and topbar-region band inset. Deleted: Root inset policies and composition topbar region deleted; ScreenInsets on caller targets.. Replacement test: none; the feature is deleted.
- `mech2-38` (31 main cases; `layout_defaults`). Main promised: Default Grid lanes, AdaptiveStack refusal, overflow findings, ten-foot measure cap. Deleted: environment/adaptive module and Grid/AdaptiveStack/Composition deleted; guide 15-adaptive-recipes tells authors to read native bounds. Replacement test: none; the feature is deleted.
- `mech2-93` (25 main cases; `paradigm_tenfoot`). Main promised: Ten-foot typography, focus strengthening, overscan, table rung, handheld classing. Deleted: environment distance classes deleted; api.md/guides make no ten-foot promise. Replacement test: none; the feature is deleted.
- `mech3-05` (79 main cases; `region_expand`). Main promised: Region form ladders, expand affordance and expand plate. Deleted: UI.Region/ViewThatFits form ladders, expand/recover declarations and the expand plate are deleted; api.md/guide list no region or expand API. Nearest surviving concept is Table priority collapse to a shared Collapsibl.... Replacement test: none; the feature is deleted.
- `mech3-11` (7 main cases; `renderer`). Main promised: Environment per-key isolation and derived facts. Deleted: Facet environment (sizeClass, interactionClasses, displaySize, distanceProfile, overscanInsets) deleted; api.md exposes no environment; controls read native PreferredInput/AbsoluteSize directly. Replacement test: none; the feature is deleted.
- `mech3-62` (48 main cases; `ten_foot_metrics`). Main promised: Ten-foot metric ladder, hit floor, paint family and overscan. Deleted: distance profile, ten-foot metric transform, 66px focus floor and overscan margins deleted; api.md/guides contain no ten-foot/distance promise. Gallery keeps a UIScale 1.5 'Distant TV' preview only (native_gallery_she.... Replacement test: none; the feature is deleted.
- `navigation-1-15` (1 main case; `alert`). Main promised: Ten-foot distance stacks two actions full width. Deleted: env viewingDistance and alert.resolveStacked(…, 'ten-foot', …) are deleted. The CAND stacked rule is #actions>2, offer<480 or measured label width (nav_surfaces.luau). No ten-foot promise in api.md.. Replacement test: none; the feature is deleted.
- `navigation-2-10` (1 main case; `combo_box`). Main promised: ComboBox forwards an explicit environment with multiple surfaces. Deleted: Facet.newEnvironment / env option deleted; ComboBoxSpec has no env and api.md documents no environment object.. Replacement test: none; the feature is deleted.
- `navigation-2-21` (1 main case; `distance_gamepad`). Main promised: Authored viewing distance independent of capabilities/input. Deleted: environment viewingDistance/distanceProfile/effectiveDisplaySize deleted; no references in CAND src or docs.. Replacement test: none; the feature is deleted.
- `navigation-2-55` (2 main cases; `menu_scenario`). Main promised: Scenario idiom via interaction classes; compact forces sheet over forced panel. Deleted: Interaction-class environment deleted. The candidate scenario's Idiom picker is now inert (see bugs).. Replacement test: none; the feature is deleted.
- `navigation-4-46` (1 main case; `sheet`). Main promised: Panel at the bottom nearby and centered at ten-foot distance. Deleted: viewingDistance/ten-foot environment fact deleted (no viewingDistance in CAND src/api); Sheet always anchors (0.5,1) at the bottom. Bottom anchoring itself is untested.. Replacement test: none; the feature is deleted.
- `paint-56` (2 main cases; `native_regions_feedback`). Main promised: Stepped-down regions and composition refusals. Deleted: Regions/composition deleted; no api.md or guide promise.. Replacement test: none; the feature is deleted.
- `themes-P3-36` (13 main cases; `theme_selectby`). Main promised: selectBy: package chosen from primary interaction class at install and swapped once per settled profile change; manual swap latch. Deleted: MAIN src/client/theme_controller.luau (install/swapPackage/uninstall/onChange/inspect/dumpTokens) has no CAND counterpart; CAND src contains only src/ui/*.luau and grep for installTheme/swapPackage/onChange/dumpTokens.... Replacement test: none; the feature is deleted.
- `themes-P3-37` (10 main cases; `theme_selectby`). Main promised: selectBy subscription lifetime and adversarial rollback. Deleted: MAIN src/client/theme_controller.luau (install/swapPackage/uninstall/onChange/inspect/dumpTokens) has no CAND counterpart; CAND src contains only src/ui/*.luau and grep for installTheme/swapPackage/onChange/dumpTokens.... Replacement test: none; the feature is deleted.

### Public surface

0 class b and 58 class c contracts, 277 main cases.

- `apps-116` (8 main cases; `api_surface`). Main promised: Deprecation ledger, api.md deprecated markers, property schema, breaking-shape register. Deleted: Deprecation ledger, generated blueprint property schema and pinned public shape register removed by the 0.12.0 hard cutover (CONTRIBUTING.md 'Versioning': no migration shims); api.md no longer mentions deprecations or.... Replacement test: none; the feature is deleted.
- `apps-133` (30 main cases; `foreign`). Main promised: UI.Foreign strict authoring, foreignHost seam, host authority, layout, containment. Deleted: UI.Foreign and the foreignHost render-target seam removed (no Foreign in CAND src/ui or api.md); callers parent their own Instances as native children (api.md 'Constructor contract': numeric children forwarded to the .... Replacement test: none; the feature is deleted.
- `apps-141` (1 main case; `ref_contract`). Main promised: PopupButton ref record. Deleted: PopupButton removed (not in CAND UI registry or api.md).. Replacement test: none; the feature is deleted.
- `apps-142` (2 main cases; `ref_contract`). Main promised: Ref record exposes no blueprint/dispose. Deleted: The ref record type is gone; ref receives the Instance itself.. Replacement test: none; the feature is deleted.
- `collections-227` (2 main cases; `virtual_grid`). Main promised: Grid dump cross extent and zero clamp. Deleted: dump() is gone.. Replacement test: none; the feature is deleted.
- `collections-24` (1 main case; `collection_list`). Main promised: Control dump schema. Deleted: dump() is gone. CAND controls return native Instances (api.md Constructor contract).. Replacement test: none; the feature is deleted.
- `collections-241` (1 main case; `virtual_hgrid`). Main promised: Horizontal grid dump. Deleted: dump() is gone.. Replacement test: none; the feature is deleted.
- `collections-248` (8 main cases; `virtual_list_axis`). Main promised: Axis vocabulary, extent names, deprecated aliases, rowActions/width refusals. Deleted: axis/itemExtent/rowHeight and Facet.DEPRECATIONS entries are replaced by direction/itemSize (api.md). No refusals.. Replacement test: none; the feature is deleted.
- `collections-298` (3 main cases; `virtual_list_variable_extents`). Main promised: Per-item extent function acceptance, dump and refusal. Deleted: The per-item function is replaced by the `measured` map (api.md).. Replacement test: none; the feature is deleted.
- `inputs-09` (2 main cases; `auto_input`). Main promised: Presenter opts.onActivate and navigationGroups overrides. Deleted: Presenter/present opts (onActivate, navigationGroups) do not exist; api.md 'Mounting' states there is no application/presenter.. Replacement test: none; the feature is deleted.
- `inputs-106` (2 main cases; `level_picker`). Main promised: dump(). Deleted: dump() removed.. Replacement test: none; the feature is deleted.
- `inputs-136` (1 main case; `paradigm_input_axis`). Main promised: Presenter opts.onAdjust. Deleted: Presenter options removed.. Replacement test: none; the feature is deleted.
- `inputs-153` (1 main case; `rating`). Main promised: dump(). Deleted: dump() removed.. Replacement test: none; the feature is deleted.
- `inputs-191` (2 main cases; `text_input`). Main promised: dump() and handleActivate. Deleted: dump/handleActivate removed.. Replacement test: none; the feature is deleted.
- `inputs-218` (2 main cases; `value_controls`). Main promised: dump(). Deleted: dump() removed.. Replacement test: none; the feature is deleted.
- `inputs-56` (1 main case; `chip`). Main promised: dump(). Deleted: dump() removed with the scene renderer.. Replacement test: none; the feature is deleted.
- `inputs-66` (7 main cases; `compact_label`). Main promised: Closed compactLabel grammar and non-reactivity. Deleted: Union grammar removed; api.md: 'compactLabel: an alternative string or readable'.. Replacement test: none; the feature is deleted.
- `mech1-102` (24 main cases; `controls_conformance`). Main promised: Deleted primitives conformance and binding-authority declarations. Deleted: AdaptiveStack, Anchor, Box, Composition, Divider, ErrorBoundary, ForEach, Foreign, Grid, GridRow, Grip, HStack, Image, Path, Region, Screen, ScrollView, Spacer, Text, VStack, ViewThatFits, When, ZStack deleted (native.... Replacement test: none; the feature is deleted.
- `mech1-104` (6 main cases; `controls_namespace`). Main promised: Anonymous When/ForEach region dispatch and ForEach key field. Deleted: UI.When/UI.ForEach deleted; Compose.show/Compose.keyed used directly.. Replacement test: none; the feature is deleted.
- `mech1-28` (6 main cases; `authoring`). Main promised: Dimension grammar, padding sides, When.condition, leaf children, read-once cells. Deleted: Facet dimension/padding schema, UI.When and leaf classes deleted; native UDim2/UIPadding. Note: a readable passed to static VirtualList `follow` is silently ignored (see bugs).. Replacement test: none; the feature is deleted.
- `mech1-30` (3 main cases; `authoring`). Main promised: Diagnosed Text.color/Text.font and deprecation ledger. Deleted: Text primitive and deprecation ledger deleted; native TextColor3/FontFace.. Replacement test: none; the feature is deleted.
- `mech1-31` (4 main cases; `authoring`). Main promised: Input-contribution extension seam exported. Deleted: Input contribution seam/meta channel deleted; controls own native InputContext/InputAction.. Replacement test: none; the feature is deleted.
- `mech1-33` (8 main cases; `authoring`). Main promised: Schema/property-parity/dirty-map model. Deleted: Schema, PROP_DIRTY and authority manifest deleted.. Replacement test: none; the feature is deleted.
- `mech1-35` (4 main cases; `authoring`). Main promised: Frozen schema tables and PROP_DIRTY. Deleted: Schema tables deleted; native_registration asserts the controls table itself is frozen.. Replacement test: a candidate test covers the surviving part (see reason).
- `mech1-36` (4 main cases; `authoring`). Main promised: Modifier error names the modifier. Deleted: UI.alignment/UI.frame modifiers deleted.. Replacement test: none; the feature is deleted.
- `mech1-40` (2 main cases; `authoring`). Main promised: Suggester knows reserved keys id/children. Deleted: Property suggester deleted.. Replacement test: none; the feature is deleted.
- `mech1-45` (2 main cases; `bound_enum_seam`). Main promised: Bound Box surface enum seam. Deleted: Box surface enum deleted.. Replacement test: none; the feature is deleted.
- `mech2-110` (6 main cases; `process_globals`). Main promised: Shared text-metric and motion registries. Deleted: text metrics and motion registries deleted. Replacement test: none; the feature is deleted.
- `mech2-50` (15 main cases; `leaf_helpers`). Main promised: num/paths/rect helpers and MenuBar prefix routing. Deleted: internal helpers and path routing deleted. Replacement test: none; the feature is deleted.
- `mech2-85` (11 main cases; `node_facts`). Main promised: paths.stripThenSegments, mountedPathOf, screenRectOf. Deleted: controller/paths facts deleted; native AbsolutePosition. Replacement test: none; the feature is deleted.
- `mech3-07` (15 main cases; `render_target_contract`). Main promised: RenderTargetAdapter contract, fake/live adapter prop parity, visibleOf, playbook roster. Deleted: RenderTargetAdapter, ScreenTarget and FakeTarget deleted; api.md 'There is no Facet ... render target' and the caller mounts ScreenGui/BillboardGui/SurfaceGui with Compose constructors (api.md 591-601). Replacement test: none; the feature is deleted.
- `mech3-27` (9 main cases; `screen_props_seam`). Main promised: screen_props seam structure. Deleted: screen_props module deleted. Replacement test: none; the feature is deleted.
- `mech3-42` (15 main cases; `sorted_entries`). Main promised: sortedEntries deterministic ordering and UI.ForEach dictionary signal. Deleted: Facet.sortedEntries and UI.ForEach deleted (native_public_surface asserts UI.ForEach is nil); Compose keyed owns keyed regions. Replacement test: a candidate test covers the surviving part (see reason).
- `mech3-72` (13 main cases; `text_distance`). Main promised: text_distance and specGuard facade. Deleted: text_distance/specGuard deleted; unknown-option error has no suggestion. Replacement test: none; the feature is deleted.
- `navigation-1-24` (1 main case; `alert`). Main promised: Dedup presentation, dump. Deleted: Imperative alert.present(presenter)/dump() handle deleted. The CAND Alert is a declarative Compose control (api.md Alert: isPresented/item only).. Replacement test: none; the feature is deleted.
- `navigation-1-51` (4 main cases; `anchored_surface`). Main promised: Shared solver precedents and Facet.layout.anchorPlacement export. Deleted: Facet.layout.anchorPlacement and the shared solver used by disclosure/row actions are deleted. CAND src/init.luau exports no layout table, and api.md does not mention it.. Replacement test: none; the feature is deleted.
- `navigation-1-60` (2 main cases; `callout`). Main promised: Callout and queue dumps. Deleted: Callout dump()/presenter callouts() dumps are deleted; the CAND Callout is a native Compose control with no dump API in api.md.. Replacement test: none; the feature is deleted.
- `navigation-2-47` (1 main case; `menu`). Main promised: Menu dump. Deleted: dump() handles deleted; controls return native Instances.. Replacement test: none; the feature is deleted.
- `navigation-3-10` (6 main cases; `nav_bar`). Main promised: Facet.navBar shape, trailing spacer and closed keys. Deleted: Facet.navBar / UI.navBar blueprint constructor absent from CAND src (no navBar anywhere in src/ or docs/reference/api.md). NavigationStack draws its own NavigationBar frame internally.. Replacement test: none; the feature is deleted.
- `navigation-3-11` (6 main cases; `nav_bar`). Main promised: navBar Back/Title rules (chevron icon, Back first, title truncation, titleSize). Deleted: Same removal; the surviving internal chrome (UI.Button 'Back' with icon 'chevron.leading', Title TextTruncate AtEnd in nav_pages.luau) is not a public navBar promise.. Replacement test: none; the feature is deleted.
- `navigation-3-26` (1 main case; `navigation_stack`). Main promised: Batched push/pop/popToRoot compose on the caller path. Deleted: The NavigationStack handle api (push/pop/popToRoot/back) and dump() were deleted; CAND exposes only the caller's path cell (api.md: 'To push, append an entry').. Replacement test: none; the feature is deleted.
- `navigation-3-56` (1 main case; `picker_segments`). Main promised: Half-iconned group refused. Deleted: No all-or-none icon rule in CAND Picker; api.md does not state it.. Replacement test: none; the feature is deleted.
- `navigation-3-61` (2 main cases; `picker_segments`). Main promised: dump() schema and api.segments. Deleted: Control handles, dump() and api.segments deleted; constructors return native Instances (api.md Constructor contract).. Replacement test: none; the feature is deleted.
- `navigation-3-80` (1 main case; `picker_style`). Main promised: Deprecated presentation='radio' spelling. Deleted: No `presentation` Picker key; lowercase unknown keys error (context.luau ctx.props).. Replacement test: none; the feature is deleted.
- `navigation-4-07` (1 main case; `popup_button`). Main promised: dump() snapshot of open/value/options. Deleted: Control handles and dump() are deleted; controls return native Instances (api.md Constructor contract). No dump promise remains.. Replacement test: none; the feature is deleted.
- `navigation-4-09` (1 main case; `popup_button`). Main promised: Option ids must be path-safe. Deleted: Path routing (/-joined scene paths) is deleted; option id becomes an Instance Name only. No api.md promise on id characters.. Replacement test: none; the feature is deleted.
- `navigation-4-24` (5 main cases; `selection_indicator`). Main promised: newSelectionIndicator authoring checks (closed spec, segment ids, When/ForEach, skin/axis, bare mount). Deleted: newSelectionIndicator public constructor deleted; not in CAND api.md or src.. Replacement test: none; the feature is deleted.
- `navigation-4-25` (2 main cases; `selection_indicator`). Main promised: state()/picker dump carries indicator state. Deleted: dump()/state() snapshots deleted with control handles.. Replacement test: none; the feature is deleted.
- `navigation-5-02` (5 main cases; `tab_view`). Main promised: Pure resolvers, dump schema and strip-is-a-Picker. Deleted: src/controls/tab_view.luau (resolvePlacement, resolveSizing, dump 'facet-tab_view-dump/1', newPicker strip, 'conditions' requirement) is deleted; CAND TabView lives in src/ui/nav_pages.luau, exports only UI.TabView, b.... Replacement test: none; the feature is deleted.
- `navigation-5-27` (2 main cases; `tab_view`). Main promised: Closed spec: unknown keys and non-factory content refuse. Deleted: The closed blueprint schema is retired: CAND passes unrecognized keys through ctx.props to the native Frame and accepts an Instance as tab content (tests pass UI.Button instances as content). api.md says content 'is a.... Replacement test: none; the feature is deleted.
- `navigation-5-33` (2 main cases; `tab_view_accessories`). Main promised: accessoryPlacements matrix is a pure copied list. Deleted: tab_view.accessoryPlacements no longer exists; the slot matrix is a local table in nav_pages.luau (and changed: trailing is now hosted by topBar, bottomBar and bottomBarCompact). api.md does not publish the matrix.. Replacement test: none; the feature is deleted.
- `paint-32` (2 main cases; `display_controls`). Main promised: Picker/Disclosure dump. Deleted: Facet dump/registry deleted; disposal is Compose ownership.. Replacement test: none; the feature is deleted.
- `paint-38` (2 main cases; `display_controls`). Main promised: Determinate dump invariance and dispose() order. Deleted: No dump and no control dispose() API; Compose owner disposal only.. Replacement test: none; the feature is deleted.
- `paint-58` (1 main case; `native_regions_feedback`). Main promised: Application modifiers set. Deleted: Modifier set deleted; native properties replace it.. Replacement test: none; the feature is deleted.
- `themes-P1-100` (5 main cases; `stage`). Main promised: stageHost seam: declared, nil cases, cached handle, malformed refusal, dead handle. Deleted: Renderer stageHost/target_contract seam deleted; content is content(runtime, world) and lighting is native ViewportFrame properties.. Replacement test: none; the feature is deleted.
- `themes-P1-96` (1 main case; `stage`). Main promised: Stage is a non-container leaf. Deleted: Blueprint schema (UI.schema.forClass) deleted; Stage is a native ViewportFrame that accepts numeric children.. Replacement test: none; the feature is deleted.
- `themes-P1-98` (1 main case; `stage`). Main promised: Eight-surface vocabulary. Deleted: `surface`/`tint` options deleted; Stage is tagged facet-panel.. Replacement test: none; the feature is deleted.
- `themes-P2-56` (1 main case; `theme_icons_applied`). Main promised: attachHint rejects an asset id where a semantic name belongs. Deleted: chrome_slots.attachHint and the hint channel are removed.. Replacement test: none; the feature is deleted.

### Docs, examples and tooling

0 class b and 49 class c contracts, 447 main cases.

- `apps-219` (50 main cases; `hud_paint_probe`). Main promised: Paint probe (engine-vs-framework paint verdicts, plate layout, latching, culprits, flags). Deleted: Paint probe fixture deleted (no probe/paintProbe in CAND examples or src); the framework paint model it compared against no longer exists.. Replacement test: none; the feature is deleted.
- `apps2-156` (5 main cases; `perf_lab`). Main promised: Guide workload table matches registry; live counters and capture row share one block. Deleted: docs/guide/12-performance-lab.md no longer contains a workload table or counter block; module-load marks and the live counter block were removed from the lab.. Replacement test: none; the feature is deleted.
- `apps2-157` (4 main cases; `perf_lab`). Main promised: Lab registry satisfies the shared runner contract. Deleted: The shared gallery runner (examples/gallery/scenarios/runner.luau) was deleted; the lab has no registry contract with it.. Replacement test: none; the feature is deleted.
- `apps2-237` (2 main cases; `spatial`). Main promised: Platform playbook gates and spatial module VR header. Deleted: src/input/spatial.luau is deleted; docs/extending/new-platform-mode.md was rewritten and no longer lists the focus/hover/occlusion/comfort/cancellation/performance gates or PENDING_PHYSICAL (it now says a world-fixed .... Replacement test: none; the feature is deleted.
- `apps2-77` (20 main cases; `extension_checker`). Main promised: Registration rules for namespaces, allowlist reasons, dump functions, runner registration, playbooks, inputProofs and affordanceProofs. Deleted: The registration checker and its control registry rows (dumpFunction, inputProofs, affordanceProofs, allowlist) were deleted. Spec discovery is now a glob with a census check in tools/lune/native_verify.py:122-124,214.... Replacement test: a candidate test covers the surviving part (see reason).
- `apps2-79` (5 main cases; `extension_checker`). Main promised: Scaffold paradigm axis, render-target adapter and conformance spec, standalone scenario, applyBump. Deleted: tools/lune/scaffold.luau refuses every kind but 'control' ('Only control scaffolding belongs to Facet'); adapters, paradigm stamps, scenarios and applyBump were removed. docs/extending/new-control.md documents only th.... Replacement test: none; the feature is deleted.
- `apps2-85` (5 main cases; `gallery_boot_mode`). Main promised: Bootstrap reads each selector once, branches on the decision, warns every refusal, runner throws are re-raised. Deleted: The boot decision module and scenario runner (scenarios/runner.luau) were deleted; the source-structure checks have no subject.. Replacement test: none; the feature is deleted.
- `collections-205` (10 main cases; `table_rows_seam`). Main promised: table_rows builder dependency seam. Deleted: The table_rows blueprint builder is gone.. Replacement test: none; the feature is deleted.
- `collections-261` (15 main cases; `virtual_list_hosted_seam`). Main promised: VirtualList hosted seam source scans. Deleted: The hosted rowActions half of the list is gone.. Replacement test: none; the feature is deleted.
- `collections-297` (13 main cases; `virtual_list_slot_guard`). Main promised: Lying-extent slot guard diagnostic. Deleted: The solver slot guard is gone. Measured content goes through AbsoluteSize (measure=true).. Replacement test: none; the feature is deleted.
- `collections-64` (9 main cases; `row_actions_dump_seam`). Main promised: row_actions dump/params source-scan seam. Deleted: The imperative row_actions engine and its Params and dump are gone. collection_row_actions.luau is a single native control.. Replacement test: none; the feature is deleted.
- `inputs-125` (1 main case; `native_caret_scroll`). Main promised: Harness detects stale owner. Deleted: Caret measurement harness deleted.. Replacement test: none; the feature is deleted.
- `inputs-51` (1 main case; `button_text_centre`). Main promised: Negative control for asymmetric recipes. Deleted: Solver-based centring checker deleted.. Replacement test: none; the feature is deleted.
- `mech1-07` (3 main cases; `adaptive`). Main promised: D-6 device-matrix assertion negative controls. Deleted: Device matrix fixtures deleted.. Replacement test: none; the feature is deleted.
- `mech1-116` (15 main cases; `example_drift`). Main promised: Example drift lint (px sizes, invented tokens, raw colours, platform branches). Deleted: Lint deleted; native properties with literal values are the current authoring model. Examples are compiled/executed by native_documentation and gallery specs.. Replacement test: none; the feature is deleted.
- `mech1-117` (5 main cases; `experiment_markers`). Main promised: src carries no experiment markers. Deleted: Scanner deleted; no replacement check.. Replacement test: none; the feature is deleted.
- `mech1-16` (12 main cases; `arrange_reports_seam`). Main promised: arrange_reports_seam: solver seam source scans. Deleted: Facet layout solver/renderer (src/layout, src/render) deleted; CAND/src has only src/ui + vendor. api.md: 'There is no Facet application, mounting service, render target, solver'. No doc promises it.. Replacement test: none; the feature is deleted.
- `mech1-17` (7 main cases; `boundary_analysis_seam`). Main promised: boundary_analysis_seam: solver seam source scans. Deleted: Facet layout solver/renderer (src/layout, src/render) deleted; CAND/src has only src/ui + vendor. api.md: 'There is no Facet application, mounting service, render target, solver'. No doc promises it.. Replacement test: none; the feature is deleted.
- `mech1-18` (12 main cases; `boundary_report_seam`). Main promised: boundary_report_seam: solver seam source scans. Deleted: Facet layout solver/renderer (src/layout, src/render) deleted; CAND/src has only src/ui + vendor. api.md: 'There is no Facet application, mounting service, render target, solver'. No doc promises it.. Replacement test: none; the feature is deleted.
- `mech1-19` (13 main cases; `composition_resolve_seam`). Main promised: composition_resolve_seam: solver seam source scans. Deleted: Facet layout solver/renderer (src/layout, src/render) deleted; CAND/src has only src/ui + vendor. api.md: 'There is no Facet application, mounting service, render target, solver'. No doc promises it.. Replacement test: none; the feature is deleted.
- `mech1-20` (9 main cases; `dirty_closure_seam`). Main promised: dirty_closure_seam: solver seam source scans. Deleted: Facet layout solver/renderer (src/layout, src/render) deleted; CAND/src has only src/ui + vendor. api.md: 'There is no Facet application, mounting service, render target, solver'. No doc promises it.. Replacement test: none; the feature is deleted.
- `mech1-21` (12 main cases; `geometry_facts_seam`). Main promised: geometry_facts_seam: solver seam source scans. Deleted: Facet layout solver/renderer (src/layout, src/render) deleted; CAND/src has only src/ui + vendor. api.md: 'There is no Facet application, mounting service, render target, solver'. No doc promises it.. Replacement test: none; the feature is deleted.
- `mech1-22` (4 main cases; `attach_opts_documented`). Main promised: renderer.attach options documented. Deleted: renderer.attach deleted. Replacement doc-consistency check: native_registration asserts every constructor is named in api.md (does not check options).. Replacement test: a candidate test covers the surviving part (see reason).
- `mech1-58` (17 main cases; `commit_walks_seam`). Main promised: commit_walks_seam: solver/commit incremental internals equal a full solve. Deleted: Facet layout solver/renderer (src/layout, src/render) deleted; CAND/src has only src/ui + vendor. api.md: 'There is no Facet application, mounting service, render target, solver'. No doc promises it.. Replacement test: none; the feature is deleted.
- `mech1-59` (4 main cases; `diagnostic_replay`). Main promised: diagnostic_replay: solver/commit incremental internals equal a full solve. Deleted: Facet layout solver/renderer (src/layout, src/render) deleted; CAND/src has only src/ui + vendor. api.md: 'There is no Facet application, mounting service, render target, solver'. No doc promises it.. Replacement test: none; the feature is deleted.
- `mech1-60` (12 main cases; `fake_target_ops_opt`). Main promised: fake_target_ops_opt: solver/commit incremental internals equal a full solve. Deleted: Facet layout solver/renderer (src/layout, src/render) deleted; CAND/src has only src/ui + vendor. api.md: 'There is no Facet application, mounting service, render target, solver'. No doc promises it.. Replacement test: none; the feature is deleted.
- `mech1-61` (3 main cases; `fuzz_layout`). Main promised: fuzz_layout: solver/commit incremental internals equal a full solve. Deleted: Facet layout solver/renderer (src/layout, src/render) deleted; CAND/src has only src/ui + vendor. api.md: 'There is no Facet application, mounting service, render target, solver'. No doc promises it.. Replacement test: none; the feature is deleted.
- `mech2-109` (7 main cases; `primitive_registration`). Main promised: Primitive registration checker. Deleted: primitive schema deleted; docs/extending/new-primitive.md says there is no primitive catalog. Replacement test: none; the feature is deleted.
- `mech2-27` (3 main cases; `inherited_subtree_scenario`). Main promised: Showcase registration, package sweep, pseudo-localization. Deleted: scenario registry/overflow sweep/pseudo-loc tooling deleted. Replacement test: none; the feature is deleted.
- `mech2-53` (20 main cases; `maintainer_map`). Main promised: Maintainer map rows point at real tree. Deleted: docs/MAINTAINERS.md rewritten as prose with no area table; checker deleted. Replacement test: none; the feature is deleted.
- `mech2-54` (16 main cases; `matrix_rows`). Main promised: Device matrix row selection. Deleted: matrix selection tooling deleted; guide 11-device-verification lists sizes to exercise manually. Replacement test: none; the feature is deleted.
- `mech2-91` (15 main cases; `overflow_sweep`). Main promised: Sweep axis/breadth/waiver/locale/theme ledgers and HUD controls. Deleted: sweep tooling, waiver and locale ledgers deleted. Replacement test: none; the feature is deleted.
- `mech2-99` (4 main cases; `prefix_tests`). Main promised: Prefix literal scans and facet-* tag ownership. Deleted: adapter tag classifier deleted. Replacement test: none; the feature is deleted.
- `mech3-09` (3 main cases; `render_target_contract`). Main promised: Skinned-control asset roots derived from manifest (MAINT-8d). Deleted: rung-3 skinned control manifest/check_docs derivation removed with the old theme-control pipeline. Replacement test: none; the feature is deleted.
- `mech3-100` (20 main cases; `world_substrate`). Main promised: Headless world substrate and case-owned harness world. Deleted: tests/lib world substrate replaced by tests/lib/native_engine. Replacement test: a candidate test covers the surviving part (see reason).
- `mech3-46` (5 main cases; `src_tree`). Main promised: src_tree enumeration helper. Deleted: tests/lib/src_tree deleted with the seam specs; import boundary is now checked by native_verify/check_public_allowlist. Replacement test: a candidate test covers the surviving part (see reason).
- `mech3-85` (7 main cases; `tier`). Main promised: Fast test tier derived from run.luau. Deleted: tests/run.luau suite list and fast-tier exclusions replaced by tools/lune/native_verify.py tiers (covered by native_verify_selftest.py). Replacement test: a candidate test covers the surviving part (see reason).
- `navigation-1-64` (1 main case; `callout`). Main promised: Standing 'use tips sparingly' warning in docs. Deleted: The verbatim warning was removed from CAND docs/reference/api.md (grep finds no 'tips sparingly'). api.md keeps only 'A callout is contextual teaching attached to a control. It is not a second application presenter.'. Replacement test: none; the feature is deleted.
- `navigation-3-71` (1 main case; `picker_segments`). Main promised: Changelog register row for one-turn activation. Deleted: Main changelog-register assertion; not a runtime contract in CAND.. Replacement test: none; the feature is deleted.
- `navigation-5-41` (1 main case; `tab_view_accessories`). Main promised: No accessories keeps every shipped home path. Deleted: Main's HOME_OF node paths (RailHome/TopHome/...) and p4_foyer path pins are gone with the presenter paths; CAND has a flat Tabs/Content structure.. Replacement test: none; the feature is deleted.
- `paint-137` (9 main cases; `theme_docs`). Main promised: Theme docs gate obligations. Deleted: tools/lune/check_docs deleted; docs correctness now via executable snippets and native_registration (api.md names every constructor).. Replacement test: none; the feature is deleted.
- `paint-141` (11 main cases; `theme_docs`). Main promised: Rich-skinning docs gate obligations. Deleted: check_docs v2 obligations deleted; rung-3 contribution contract no longer documented.. Replacement test: none; the feature is deleted.
- `paint-142` (7 main cases; `theme_docs`). Main promised: Comparison document sourcing. Deleted: docs/guide/14-choosing-a-ui-library.md no longer compares frameworks; sourcing gate deleted.. Replacement test: none; the feature is deleted.
- `paint-147` (11 main cases; `themed_containment`). Main promised: Overflow guard rules and HUD ledger. Deleted: overflow_guard and HUD plate ledger deleted.. Replacement test: none; the feature is deleted.
- `themes-P3-07` (6 main cases; `theme_controller`). Main promised: token_sync: typed token round-trip, generated Luau, freshness gate, marker sections, client dump. Deleted: MAIN src/themes token_sync + theme_sync CLI removed; no token_sync/theme_sync/dumpTokens in CAND src, tools (except a string in strip_comments) or docs.. Replacement test: none; the feature is deleted.
- `themes-P3-14` (8 main cases; `theme_drift`). Main promised: Source scan: no framework control hardcodes a theme-owned metric (with allowlist/justifications). Deleted: MAIN drift scanner over src/controls, renderer, presenter removed; no equivalent check in CAND tests/tools (grep hardcod/THEME-OPT-OUT/PROBE_EXEMPT: none). CAND AGENTS/constitution/extending no longer state the rule. .... Replacement test: none; the feature is deleted.
- `themes-P3-15` (17 main cases; `theme_drift`). Main promised: Teaching-corpus scan: docs/examples spell values through theme names; opt-out markers and probe-exempt regions. Deleted: Corpus scanner and its THEME-OPT-OUT / PROBE-EXEMPT vocabulary removed; spacing steps and typography role props on layouts no longer exist (09-custom-themes: 'Spacing values are theme package data for your native layo.... Replacement test: none; the feature is deleted.
- `themes-P4-09` (3 main cases; `theme_layer_application`). Main promised: Adapter source-order contracts (identity before return, paint before suppress, skinned tag after ladder). Deleted: src/client/screen_target.luau and its siblings no longer exist; ordering is now within one Compose.watch in themes.skin. The observable remainder (tag present once layers exist) is covered by facet-skinned assertions .... Replacement test: none; the feature is deleted.
- `themes-P5-08` (4 main cases; `theme_reference_packages`). Main promised: fantasy_parchment theme_sync metrics region matches compiled package. Deleted: src/themes/token_sync and its CLI are deleted; CAND docs no longer teach theme sync (only strip_comments.py preserves the marker). The '-- theme_sync:begin/end metrics' markers remain in examples/themes/fantasy_parchm.... Replacement test: none; the feature is deleted.

### Gallery and examples

3 class b and 18 class c contracts, 179 main cases.

- `inputs-121` (2 main cases; `level_picker`). Main promised: Showcase composition paints every tinted mark at every viewport. Engine or Compose owns it: Native layout.. Replacement test: live Studio evidence `classb_keyboard/level-picker-showcase-marks-have-area`.
- `mech2-89` (87 main cases; `overflow_sweep`). Main promised: Every gallery scenario, tutorial and reference proof fits at every viewport and text size. Engine or Compose owns it: native layout + TextBounds/AutomaticSize. Replacement test: no test (none headless for geometry (guide 11: headless double does not run engine layout); parity_blockers pendingLiveRisks still lists "Collection toolbar under phone/largest-text and compact landscape" and "Shell measured r...).
- `navigation-3-33` (24 main cases; `navigation_visuals`). Main promised: Themed action/tab scenarios contained at six viewports with largest text. Engine or Compose owns it: Engine layout and text measurement (UIListLayout/UIGridLayout/AutomaticSize/TextWrapped) replace solver containment diagnostics. Replacement test: yes: Mount-only: native_gallery::native gallery::mounts <demo> with the native runtime (default theme, one viewport). No pixel_quest/fantasy_ornate x viewport x preferred-text sweep, no step driving..
- `apps2-100` (3 main cases; `gallery_demo_host`). Main promised: sorted-entries picker; no-environment refusal; shipped host uses the demo_host module. Deleted: client/demo_host.luau, scenarios/sorted_entries.luau and the Facet environment were deleted; screen.luau builds demos directly.. Replacement test: none; the feature is deleted.
- `apps2-103` (2 main cases; `gallery_demo_picker`). Main promised: Blurb length budget; fixtures listed in scenario-registry order. Deleted: Blurbs are no longer displayed (TabView uses title only) and the scenario registry order was deleted with scenarios/init registry.. Replacement test: none; the feature is deleted.
- `apps2-122` (2 main cases; `nested_compositing_scenario`). Main promised: nested_compositing scenario registration. Deleted: examples/gallery/scenarios/nested_compositing.luau is deleted; canvas_group.luau replaces the opacity half. Registration of the remaining scenarios is not part of this contract.. Replacement test: none; the feature is deleted.
- `apps2-238` (8 main cases; `sponsor_scenarios`). Main promised: Sponsor fixtures module contract and long-lived-core churn for deleted fixtures. Deleted: examples/gallery/scenarios/sponsor_{motion,celebration,list,drop,avatars,markers,billboard}.luau and the sponsor registry/ledgerRows contract were deleted; only sponsor_toast.luau remains. No docs promise them.. Replacement test: none; the feature is deleted.
- `collections-100` (2 main cases; `row_actions_scenario`). Main promised: Unread marker paint and device-matrix step registry. Deleted: The scenario device-matrix step registry is gone.. Replacement test: none; the feature is deleted.
- `collections-103` (2 main cases; `row_actions_scenario`). Main promised: Deletion reflow animation records and bindScroll. Deleted: withAnimation records and bindScroll are gone.. Replacement test: none; the feature is deleted.
- `collections-105` (1 main case; `row_actions_scenario`). Main promised: Table double-click onPrimaryAction. Deleted: onPrimaryAction is gone. Table onActivate(item,key) fires on a single activation (api.md).. Replacement test: none; the feature is deleted.
- `collections-107` (6 main cases; `row_actions_scenario`). Main promised: A swipe's own release Activate is swallowed exactly once. Deleted: The activation-artifact arming is gone. RowHit/Button activation and the root UIDragDetector are independent native paths.. Replacement test: none; the feature is deleted.
- `collections-115` (2 main cases; `row_capabilities_scenario`). Main promised: Controller entry from Edit into sectioned rows. Deleted: The scenario focus-graph entry is gone. Native selection is used.. Replacement test: none; the feature is deleted.
- `collections-117` (7 main cases; `row_capabilities_scenario`). Main promised: Refusal diagnostics, registration, package sweep, pseudo-loc. Deleted: The refusal diagnostic lines, the diagnostic registry and the solver finding sweeps are gone.. Replacement test: none; the feature is deleted.
- `mech2-49` (1 main case; `leaf_capabilities`). Main promised: Leaf gallery scenario drives controls. Deleted: leaf_capabilities scenario deleted. Replacement test: none; the feature is deleted.
- `navigation-5-46` (8 main cases; `tab_view_scenario`). Main promised: tab_view diagnostic scenario: printed placements, build counters, hugging strip, solver sweep, copy table. Deleted: examples/gallery/scenarios/tab_view.luau does not exist in CAND (scenario list has demo_tabs/all_controls only); its placement labels, build counters, presenter depth, solver diagnostics and `copy` export are removed .... Replacement test: none; the feature is deleted.
- `navigation-5-57` (2 main cases; `transient_over_live`). Main promised: Gallery demo picker panel over the live screen (and its negative control). Deleted: showcase_chrome/settings_panel and the anchored demo picker panel are removed; CAND gallery switches demos through a UI.Sheet in examples/gallery/client/screen.luau, a modal with a scrim by design.. Replacement test: none; the feature is deleted.
- `paint-126` (5 main cases; `theme_authoring_scenario`). Main promised: Scenario steps, decode ledger, value/gauge fixtures, registry. Deleted: examples/gallery/scenarios/theme_authoring deleted; ornate_gauge/custom_control examples remain but are exercised by no spec.. Replacement test: none; the feature is deleted.
- `paint-132` (3 main cases; `theme_authoring_scenario`). Main promised: Operator package choice and live authoring/export seams. Deleted: Scenario runner seams (editMetricLive/exportDump) deleted.. Replacement test: none; the feature is deleted.
- `paint-136` (5 main cases; `theme_authoring_scenario`). Main promised: Unknown package name and paired evidence report. Deleted: Scenario report deleted.. Replacement test: none; the feature is deleted.
- `paint-144` (4 main cases; `themed_containment`). Main promised: Director surfaces stay inside themed plates (overflow sweeps). Deleted: tests/lib/overflow_guard + solver rect harness deleted; pendingLiveRisks in parity_blockers.json still lists themed/largest-text reservation.. Replacement test: none; the feature is deleted.
- `paint-62` (3 main cases; `native_style_scenario`). Main promised: native_style scenario conformance steps. Deleted: examples/gallery/scenarios/native_style and src/client/native_style deleted.. Replacement test: none; the feature is deleted.

### Reference apps

6 class b and 46 class c contracts, 175 main cases.

- `apps-102` (2 main cases; `reference/foyer_spec`). Main promised: Every placement traversable, including pad DPad/ButtonA. Engine or Compose owns it: GuiService selection / engine gamepad navigation. Replacement test: no test (none for Foyer).
- `apps-24` (10 main cases; `reference/glade_spec`). Main promised: Focus entry, placement-ordered ring, modal dismissal reachability, pad traversal of every nav placement. Engine or Compose owns it: GuiService.SelectedObject + engine gamepad selection over native GuiButtons (Selectable/NextSelection*). Replacement test: no test (none for reference apps).
- `apps-30` (3 main cases; `reference/glade_spec`). Main promised: Theme swap, preferred-text steps and expansion locale reflow without remount. Engine or Compose owns it: StyleSheet/StyleRule theming, TextSize/AutomaticSize text reflow. Replacement test: no test (none for reference apps (generic: native_text_preference::native text preference updates::keeps editing focus row identity and scroll position across text and theme changes)).
- `apps-63` (1 main case; `reference/cartwheel_spec`). Main promised: Mounts under both reference theme packages without source edit. Engine or Compose owns it: StyleSheet/StyleRule theming. Replacement test: no test (none for Cartwheel).
- `apps-88` (2 main cases; `reference/sipworks_spec`). Main promised: Theme packages mount; every verb focusable on every input class. Engine or Compose owns it: StyleSheet theming; GuiService selection of native GuiButtons. Replacement test: no test (none for Sipworks).
- `apps-90` (4 main cases; `reference/sipworks_spec`). Main promised: Gamepad traversal of every nav placement. Engine or Compose owns it: GuiService.SelectedObject/engine gamepad selection over TabView buttons. Replacement test: no test (none for Sipworks).
- `apps-02` (1 main case; `reference/glade_spec`). Main promised: Glade report publishes spec substitutions. Deleted: CAND examples/reference/p1_glade/init.luau report() returns only supplies/commerce/visits/complete; no substitutions field. Guide does not promise it.. Replacement test: none; the feature is deleted.
- `apps-05` (7 main cases; `reference/glade_spec`). Main promised: Played-task strip UX (row ticks, clock take-back message, one-reset completion, 'What this shows', strip line box, landscape yield). Deleted: MAIN examples/reference/p1_glade/ui/{overview,detail,keeper,scene,shop,task,parts}.luau deleted; CAND p1_glade/init.luau renders a flat view.page/view.column UI through examples/reference/view.luau; ui/task.luau strip.... Replacement test: none; the feature is deleted.
- `apps-08` (2 main cases; `reference/glade_spec`). Main promised: Rejection reasons have player-facing copy; picker card refuses while pending. Deleted: Picker purchase card deleted with ui/shop.luau; CAND Shop shows raw status..': '..reason id rather than strings 'reject.*' copy.. Replacement test: none; the feature is deleted.
- `apps-10` (2 main cases; `reference/glade_spec`). Main promised: Charm offer card leaves after purchase with toast. Deleted: MAIN examples/reference/p1_glade/ui/{overview,detail,keeper,scene,shop,task,parts}.luau deleted; CAND p1_glade/init.luau renders a flat view.page/view.column UI through examples/reference/view.luau; the S1 charm offer.... Replacement test: none; the feature is deleted.
- `apps-101` (5 main cases; `reference/foyer_spec`). Main promised: App-local nav placement homes and word->glyph ladders. Deleted: Foyer shell/ladder code deleted; examples/reference/p4_foyer/init.luau uses UI.TabView 'sidebarAdaptable'.. Replacement test: none; the feature is deleted.
- `apps-105` (1 main case; `reference/foyer_spec`). Main promised: No-match query empty state with clear button. Deleted: Empty-state view deleted with Foyer screens.. Replacement test: none; the feature is deleted.
- `apps-11` (1 main case; `reference/glade_spec`). Main promised: Wisp modal offers charm while gated. Deleted: Wisp sheet in CAND examples/reference/p1_glade/init.luau has no charm offer.. Replacement test: none; the feature is deleted.
- `apps-110` (1 main case; `reference/foyer_spec`). Main promised: Matrix pin diagnostic sweep. Deleted: Solver diagnostics deleted.. Replacement test: none; the feature is deleted.
- `apps-111` (4 main cases; `reference/foyer_spec`). Main promised: Tile meta row wraps with distribute. Deleted: Tile meta row and 'distribute' layout modifier deleted (native UIListLayout used instead).. Replacement test: none; the feature is deleted.
- `apps-112` (3 main cases; `reference/foyer_spec`). Main promised: Backdrop-finding sweep. Deleted: Backdrop finder deleted.. Replacement test: none; the feature is deleted.
- `apps-113` (3 main cases; `reference/foyer_spec`). Main promised: Badge bubble clears themed host border per axis. Deleted: badgeBubble placement against theme carved borders deleted; api.md Badge row no longer promises border clearance.. Replacement test: none; the feature is deleted.
- `apps-16` (3 main cases; `reference/glade_spec`). Main promised: Keyed refill toasts (per-glade subject, no doubling, state visible in place). Deleted: Toast layer use removed from Glade; CAND writes a single 'notice' Label (last message wins).. Replacement test: none; the feature is deleted.
- `apps-18` (3 main cases; `reference/glade_spec`). Main promised: Favourite star sibling, visit suggestion fills filter, dismissable charm offer. Deleted: MAIN examples/reference/p1_glade/ui/{overview,detail,keeper,scene,shop,task,parts}.luau deleted; CAND p1_glade/init.luau renders a flat view.page/view.column UI through examples/reference/view.luau; star, suggestions .... Replacement test: none; the feature is deleted.
- `apps-20` (5 main cases; `reference/glade_spec`). Main promised: Detail hosts (lane vs NavigationStack push, rotation), empty-visits state, chip form. Deleted: MAIN examples/reference/p1_glade/ui/{overview,detail,keeper,scene,shop,task,parts}.luau deleted; CAND p1_glade/init.luau renders a flat view.page/view.column UI through examples/reference/view.luau; detail is always U.... Replacement test: none; the feature is deleted.
- `apps-21` (8 main cases; `reference/glade_spec`). Main promised: Circle doctrine / plain surface paint items 11-12. Deleted: MAIN examples/reference/p1_glade/ui/{overview,detail,keeper,scene,shop,task,parts}.luau deleted; CAND p1_glade/init.luau renders a flat view.page/view.column UI through examples/reference/view.luau; supply rings repla.... Replacement test: none; the feature is deleted.
- `apps-22` (6 main cases; `reference/glade_spec`). Main promised: App-local nav placement shells (rail/bottom/inline/top), terse vocabulary, measured detail panes. Deleted: Glade shell deleted; CAND uses UI.TabView style='sidebarAdaptable' whose placement is owned by the TabView control.. Replacement test: none; the feature is deleted.
- `apps-26` (1 main case; `reference/glade_spec`). Main promised: Proof-owned single-unit remaining/relative times. Deleted: Time formatting views deleted with ui/detail.luau.. Replacement test: none; the feature is deleted.
- `apps-27` (2 main cases; `reference/glade_spec`). Main promised: Wisp fly-in once per visit; ring arc chases level. Deleted: Wisp fly-in and painted arc deleted; CAND wisp position is a sine function of now, ring is ProgressView.. Replacement test: none; the feature is deleted.
- `apps-29` (6 main cases; `reference/glade_spec`). Main promised: Forbidden-list scanner (no wall clock/random, no Instance/service reach-around, no device branch, no raw colour). Deleted: Scanner deleted; CAND reference apps intentionally build Host.Frame/ScrollingFrame instances directly and read game:GetService('RunService').. Replacement test: none; the feature is deleted.
- `apps-32` (3 main cases; `reference/glade_spec`). Main promised: Shop hero best-value card; solver diagnostic sweeps (five-view, open detail locale x width). Deleted: Hero card deleted; solver diagnostics deleted with the Facet solver.. Replacement test: none; the feature is deleted.
- `apps-36` (1 main case; `reference/cartwheel_spec`). Main promised: Stage content declared against Stage node path. Deleted: Presenter path addressing deleted; CAND uses UI.Stage 'MarketScene' content callback.. Replacement test: none; the feature is deleted.
- `apps-38` (3 main cases; `reference/cartwheel_spec`). Main promised: Shell opening section, NavigationStack detail reset on selection, compact stack Back. Deleted: MAIN examples/reference/p2_cartwheel/screens/{shell,brews,brew_detail,dashboard,gallery,workbench,ledger,chatter,plaza,signup,celebration,topfive,parts}.luau deleted; CAND examples/reference/p2_cartwheel/init.luau is .... Replacement test: none; the feature is deleted.
- `apps-42` (8 main cases; `reference/cartwheel_spec`). Main promised: Brew table/sectioned list modes, sort, bulk complete, selection mode, empty-search message. Deleted: MAIN examples/reference/p2_cartwheel/screens/{shell,brews,brew_detail,dashboard,gallery,workbench,ledger,chatter,plaza,signup,celebration,topfive,parts}.luau deleted; CAND examples/reference/p2_cartwheel/init.luau is .... Replacement test: none; the feature is deleted.
- `apps-45` (1 main case; `reference/cartwheel_spec`). Main promised: Countdown uses a depleting M:SS form. Deleted: CAND BrewDetail shows ' · N s'; M:SS formatter view deleted.. Replacement test: none; the feature is deleted.
- `apps-46` (1 main case; `reference/cartwheel_spec`). Main promised: ButtonB pops one compact level. Deleted: MAIN examples/reference/p2_cartwheel/screens/{shell,brews,brew_detail,dashboard,gallery,workbench,ledger,chatter,plaza,signup,celebration,topfive,parts}.luau deleted; CAND examples/reference/p2_cartwheel/init.luau is .... Replacement test: none; the feature is deleted.
- `apps-48` (2 main cases; `reference/cartwheel_spec`). Main promised: Celebration scripted beats and feedback-bus event. Deleted: screens/celebration.luau and feedback bus deleted.. Replacement test: none; the feature is deleted.
- `apps-50` (1 main case; `reference/cartwheel_spec`). Main promised: Tally pulse declared, not per-frame transform. Deleted: Dashboard tally footer deleted.. Replacement test: none; the feature is deleted.
- `apps-52` (2 main cases; `reference/cartwheel_spec`). Main promised: Composite sort and grid/table shared selection. Deleted: screens/gallery.luau deleted.. Replacement test: none; the feature is deleted.
- `apps-56` (1 main case; `reference/cartwheel_spec`). Main promised: Two-state upsell disappears once entitled. Deleted: Upsell view deleted with screens/ledger.luau.. Replacement test: none; the feature is deleted.
- `apps-58` (1 main case; `reference/cartwheel_spec`). Main promised: Unmasked Guild Card disclosure caption. Deleted: screens/signup.luau variant B caption deleted.. Replacement test: none; the feature is deleted.
- `apps-59` (1 main case; `reference/cartwheel_spec`). Main promised: Signing out asks first. Deleted: CAND Guild button calls identity.signOut() directly; confirm modal deleted.. Replacement test: none; the feature is deleted.
- `apps-61` (3 main cases; `reference/cartwheel_spec`). Main promised: Plaza stage fallback plate, open-on-plaza, orbit via public handle. Deleted: screens/plaza.luau stageHost fallback and orbit handle deleted.. Replacement test: none; the feature is deleted.
- `apps-62` (4 main cases; `reference/cartwheel_spec`). Main promised: Solver verdict: no layout diagnostics, ten-foot factor, minimum fallback. Deleted: Facet solver and diagnostics deleted.. Replacement test: none; the feature is deleted.
- `apps-66` (1 main case; `reference/cartwheel_spec`). Main promised: Locale fact reaches the screens. Deleted: CAND examples/reference/p2_cartwheel/init.luau fixes strings.new('en'); no locale switch.. Replacement test: none; the feature is deleted.
- `apps-68` (6 main cases; `reference/cartwheel_spec`). Main promised: Five-view diagnostic sweep, wrapping rows (plaza summary, tag cloud, legend), backdrop sweep, namespace retirement. Deleted: Solver diagnostics, backdrop finder and app namespace registry deleted; those views deleted with MAIN examples/reference/p2_cartwheel/screens/{shell,brews,brew_detail,dashboard,gallery,workbench,ledger,chatter,plaza,s.... Replacement test: none; the feature is deleted.
- `apps-71` (5 main cases; `reference/sipworks_spec`). Main promised: App shell nav placement homes, tab band under content, no ellipsis, top bar hugs, compact detail route. Deleted: MAIN examples/reference/p3_sipworks/views/{shell,list,detail,rewards,book,task,parts}.luau and entry_compact.luau deleted; CAND examples/reference/p3_sipworks/init.luau is a flat TabView ('sidebarAdaptable') of view p.... Replacement test: none; the feature is deleted.
- `apps-74` (2 main cases; `reference/sipworks_spec`). Main promised: Botanical suggestions and all-unfavorited empty state with Browse. Deleted: MAIN examples/reference/p3_sipworks/views/{shell,list,detail,rewards,book,task,parts}.luau and entry_compact.luau deleted; CAND examples/reference/p3_sipworks/init.luau is a flat TabView ('sidebarAdaptable') of view p.... Replacement test: none; the feature is deleted.
- `apps-76` (3 main cases; `reference/sipworks_spec`). Main promised: Adaptive header candidates and flip card faces with Cancel ordering. Deleted: MAIN examples/reference/p3_sipworks/views/{shell,list,detail,rewards,book,task,parts}.luau and entry_compact.luau deleted; CAND examples/reference/p3_sipworks/init.luau is a flat TabView ('sidebarAdaptable') of view p.... Replacement test: none; the feature is deleted.
- `apps-80` (2 main cases; `reference/sipworks_spec`). Main promised: Once-per-visit stamp pop and plural caption. Deleted: Rewards view deleted; CAND caption is a fixed English 'N stamps · ...'.. Replacement test: none; the feature is deleted.
- `apps-84` (5 main cases; `reference/sipworks_spec`). Main promised: Compact-link entry flow (deep link, hidden nav, pay-shaped CTA, shared row blueprint, enterFull). Deleted: entry_compact.luau deleted in CAND.. Replacement test: none; the feature is deleted.
- `apps-86` (20 main cases; `reference/sipworks_spec`). Main promised: Solver five-view sweeps (20 device/locale/text combinations). Deleted: Facet solver diagnostics deleted.. Replacement test: none; the feature is deleted.
- `apps-89` (6 main cases; `reference/sipworks_spec`). Main promised: Stamps tab presentation vs destination, compact navigation focus/Cancel, matrix pin sweep. Deleted: MAIN examples/reference/p3_sipworks/views/{shell,list,detail,rewards,book,task,parts}.luau and entry_compact.luau deleted; CAND examples/reference/p3_sipworks/init.luau is a flat TabView ('sidebarAdaptable') of view p.... Replacement test: none; the feature is deleted.
- `apps-91` (1 main case; `reference/sipworks_spec`). Main promised: Ten-foot structural factor. Deleted: Facet ten-foot scale factor deleted with solver.. Replacement test: none; the feature is deleted.
- `apps-92` (1 main case; `reference/sipworks_spec`). Main promised: Backdrop-finding sweep. Deleted: Backdrop finder deleted.. Replacement test: none; the feature is deleted.
- `apps-95` (4 main cases; `reference/sipworks_spec`). Main promised: Task strip UX ('What this shows', compact-link no task, landscape yield, one line box). Deleted: MAIN examples/reference/p3_sipworks/views/{shell,list,detail,rewards,book,task,parts}.luau and entry_compact.luau deleted; CAND examples/reference/p3_sipworks/init.luau is a flat TabView ('sidebarAdaptable') of view p.... Replacement test: none; the feature is deleted.
- `apps-98` (1 main case; `reference/sipworks_spec`). Main promised: App namespace retirement. Deleted: App namespace registry deleted.. Replacement test: none; the feature is deleted.

### Performance

3 class b and 60 class c contracts, 708 main cases.

- `paint-88` (2 main cases; `path_stroke_authority`). Main promised: Path2D geometry uploaded only on real change. Engine or Compose owns it: Scale-relative Path2D control points rasterized by the engine on resize; SetControlPoints only in a progress/diameter watch. Replacement test: yes: native_themes_media::native themes and media::keeps circular progress endpoints bounded and theme metrics reactive (real change uploads; no-upload-on-resize unasserted).
- `themes-P1-64` (1 main case; `progress_circular`). Main promised: Value change is paint-only. Engine or Compose owns it: Native Path2D:SetControlPoints on a retained Path2D; no Facet solver.. Replacement test: yes: ENDPOINTS reuses the same RingFill across values; no write-count assertion..
- `themes-P1-66` (1 main case; `progress_circular`). Main promised: Rotation writes points without re-solve. Engine or Compose owns it: Compose timeline -> Path2D control points; native layout.. Replacement test: yes: ROTATE asserts arc instance retained; no op count..
- `apps2-136` (4 main cases; `perf_lab`). Main promised: Resize fact-group flushes, resize-vs-value cost ratio, incremental layout boundary sizing. Deleted: Facet environment adapter fact-groups, reactive flush counting and the incremental layout solver no longer exist in CAND src; guide 12 disclaims comparing removed layout phases.. Replacement test: none; the feature is deleted.
- `apps2-138` (4 main cases; `perf_lab`). Main promised: Workload selectors: settings key=value parsing, unknown scenario/setting refused, declared passes. Deleted: The selector/settings-string parser and per-workload pass declarations were removed; perf_lab.luau now selects via UI.Picker onChange and probe.select(id,count) (examples/performance/lab/perf_lab.luau:4-140).. Replacement test: none; the feature is deleted.
- `apps2-140` (2 main cases; `perf_lab`). Main promised: Exactly one implementation mounts; native reference refuses without host seam. Deleted: The implementation registry/host-seam refusal was removed; the native reference is now just the 'dense-scroll-native' workload id selected like any other.. Replacement test: none; the feature is deleted.
- `apps2-143` (3 main cases; `perf_lab`). Main promised: Scenario cycler wraps and reports position; overlay reservation dropped on viewport change. Deleted: The cycler/overlay (examples/performance/lab/overlay.luau) was deleted; the workload is chosen from a UI.Picker.. Replacement test: none; the feature is deleted.
- `apps2-146` (2 main cases; `perf_lab`). Main promised: Passes recorded separately; clean capture dismisses overlay. Deleted: Per-pass sequences and the overlay are gone; capture rows hard-code cleanCapture=false (perf_lab.luau:81).. Replacement test: none; the feature is deleted.
- `apps2-150` (1 main case; `perf_lab`). Main promised: Mounted-window rule is arithmetic over the viewport. Deleted: rows.luau no longer exports a window-size formula; native VirtualList computes its window internally (covered generally by native_collections).. Replacement test: a candidate test covers the surviving part (see reason).
- `apps2-152` (11 main cases; `perf_lab`). Main promised: profileWindow targeting, profile pass selection, readout unmeasured, loop stoppable. Deleted: profileWindow and the profiling loop were removed: no match for profileWindow in CAND examples/performance or bench.. Replacement test: none; the feature is deleted.
- `apps2-153` (11 main cases; `perf_lab`). Main promised: Three named levers and host-move lever measure solves, memo and structural sync. Deleted: examples/performance/lab/levers.luau deleted; the levers measured Facet solver solves/measure memo/structural sync, which no longer exist.. Replacement test: none; the feature is deleted.
- `apps2-154` (6 main cases; `perf_lab`). Main promised: Overlay arm selector and readable status line. Deleted: examples/performance/lab/overlay.luau deleted.. Replacement test: none; the feature is deleted.
- `apps2-160` (3 main cases; `perf_principles`). Main promised: Resize may cost the tree but one value <25%; adapter elides layout containers; recycling/incremental layout on by default. Deleted: Facet's solver (arranged counts, incremental layout lastSkipped) and render adapter (elision, ensureInstance) were deleted; CAND src/ has only ui/ + vendor/. Resize is native UIListLayout/AutomaticSize work outside th.... Replacement test: none; the feature is deleted.
- `mech1-11` (13 main cases; `anchor_arrange`). Main promised: anchor_arrange: incremental anchor re-offer/skip equals full solve. Deleted: Facet layout solver/renderer (src/layout, src/render) deleted; CAND/src has only src/ui + vendor. api.md: 'There is no Facet application, mounting service, render target, solver'. No doc promises it.. Replacement test: none; the feature is deleted.
- `mech1-110` (4 main cases; `elided_handlers`). Main promised: Elided inert containers materialise for drag/hit. Deleted: Container elision deleted; every Host node is a real Instance.. Replacement test: none; the feature is deleted.
- `mech1-12` (14 main cases; `anchor_skip`). Main promised: anchor_skip: incremental anchor re-offer/skip equals full solve. Deleted: Facet layout solver/renderer (src/layout, src/render) deleted; CAND/src has only src/ui + vendor. api.md: 'There is no Facet application, mounting service, render target, solver'. No doc promises it.. Replacement test: none; the feature is deleted.
- `mech1-125` (1 main case; `fine_grained_reactivity`). Main promised: Known gap: one change re-lays-out the whole tree. Deleted: Facet layout solver/renderer (src/layout, src/render) deleted; CAND/src has only src/ui + vendor. api.md: 'There is no Facet application, mounting service, render target, solver'. No doc promises it.. Replacement test: none; the feature is deleted.
- `mech1-23` (5 main cases; `authored_hidden_stale_dirt`). Main promised: authored hidden flip inside a solve reaches the adapter. Deleted: Facet layout solver/renderer (src/layout, src/render) deleted; CAND/src has only src/ui + vendor. api.md: 'There is no Facet application, mounting service, render target, solver'. No doc promises it. Native Visible is.... Replacement test: none; the feature is deleted.
- `mech1-46` (21 main cases; `build_children_reuse`). Main promised: build_children_reuse: solver/commit incremental internals equal a full solve. Deleted: Facet layout solver/renderer (src/layout, src/render) deleted; CAND/src has only src/ui + vendor. api.md: 'There is no Facet application, mounting service, render target, solver'. No doc promises it.. Replacement test: none; the feature is deleted.
- `mech1-47` (21 main cases; `build_index_patch`). Main promised: build_index_patch: solver/commit incremental internals equal a full solve. Deleted: Facet layout solver/renderer (src/layout, src/render) deleted; CAND/src has only src/ui + vendor. api.md: 'There is no Facet application, mounting service, render target, solver'. No doc promises it.. Replacement test: none; the feature is deleted.
- `mech1-48` (27 main cases; `container_aggregate`). Main promised: container_aggregate: solver/commit incremental internals equal a full solve. Deleted: Facet layout solver/renderer (src/layout, src/render) deleted; CAND/src has only src/ui + vendor. api.md: 'There is no Facet application, mounting service, render target, solver'. No doc promises it.. Replacement test: none; the feature is deleted.
- `mech1-49` (25 main cases; `container_memo`). Main promised: container_memo: solver/commit incremental internals equal a full solve. Deleted: Facet layout solver/renderer (src/layout, src/render) deleted; CAND/src has only src/ui + vendor. api.md: 'There is no Facet application, mounting service, render target, solver'. No doc promises it.. Replacement test: none; the feature is deleted.
- `mech1-50` (5 main cases; `container_relative_incremental`). Main promised: container_relative_incremental: solver/commit incremental internals equal a full solve. Deleted: Facet layout solver/renderer (src/layout, src/render) deleted; CAND/src has only src/ui + vendor. api.md: 'There is no Facet application, mounting service, render target, solver'. No doc promises it.. Replacement test: none; the feature is deleted.
- `mech1-51` (3 main cases; `commit_arrange_sibling`). Main promised: commit_arrange_sibling: solver/commit incremental internals equal a full solve. Deleted: Facet layout solver/renderer (src/layout, src/render) deleted; CAND/src has only src/ui + vendor. api.md: 'There is no Facet application, mounting service, render target, solver'. No doc promises it.. Replacement test: none; the feature is deleted.
- `mech1-52` (10 main cases; `commit_descend_cache`). Main promised: commit_descend_cache: solver/commit incremental internals equal a full solve. Deleted: Facet layout solver/renderer (src/layout, src/render) deleted; CAND/src has only src/ui + vendor. api.md: 'There is no Facet application, mounting service, render target, solver'. No doc promises it.. Replacement test: none; the feature is deleted.
- `mech1-53` (2 main cases; `commit_descend_predicate`). Main promised: commit_descend_predicate: solver/commit incremental internals equal a full solve. Deleted: Facet layout solver/renderer (src/layout, src/render) deleted; CAND/src has only src/ui + vendor. api.md: 'There is no Facet application, mounting service, render target, solver'. No doc promises it.. Replacement test: none; the feature is deleted.
- `mech1-54` (23 main cases; `commit_dirt_classes`). Main promised: commit_dirt_classes: solver/commit incremental internals equal a full solve. Deleted: Facet layout solver/renderer (src/layout, src/render) deleted; CAND/src has only src/ui + vendor. api.md: 'There is no Facet application, mounting service, render target, solver'. No doc promises it.. Replacement test: none; the feature is deleted.
- `mech1-55` (16 main cases; `commit_probe_filter`). Main promised: commit_probe_filter: solver/commit incremental internals equal a full solve. Deleted: Facet layout solver/renderer (src/layout, src/render) deleted; CAND/src has only src/ui + vendor. api.md: 'There is no Facet application, mounting service, render target, solver'. No doc promises it.. Replacement test: none; the feature is deleted.
- `mech1-56` (12 main cases; `commit_scope`). Main promised: commit_scope: solver/commit incremental internals equal a full solve. Deleted: Facet layout solver/renderer (src/layout, src/render) deleted; CAND/src has only src/ui + vendor. api.md: 'There is no Facet application, mounting service, render target, solver'. No doc promises it.. Replacement test: none; the feature is deleted.
- `mech1-57` (11 main cases; `commit_translate`). Main promised: commit_translate: solver/commit incremental internals equal a full solve. Deleted: Facet layout solver/renderer (src/layout, src/render) deleted; CAND/src has only src/ui + vendor. api.md: 'There is no Facet application, mounting service, render target, solver'. No doc promises it.. Replacement test: none; the feature is deleted.
- `mech2-111` (15 main cases; `profile_scopes`). Main promised: Profiler scopes. Deleted: profile scopes deleted (no debug.profilebegin in CAND src). Replacement test: none; the feature is deleted.
- `mech2-114` (8 main cases; `rect_cow`). Main promised: Rect map/pass/reads internals. Deleted: Facet layout solver/measure/renderer (src/layout, src/render, src/client adapter) deleted; CAND/src has only src/ui + vendor/compose; api.md "There is no Facet application, mounting service, render target, solver, rea.... Replacement test: none; the feature is deleted.
- `mech2-115` (3 main cases; `rect_pass_constant`). Main promised: Rect map/pass/reads internals. Deleted: Facet layout solver/measure/renderer (src/layout, src/render, src/client adapter) deleted; CAND/src has only src/ui + vendor/compose; api.md "There is no Facet application, mounting service, render target, solver, rea.... Replacement test: none; the feature is deleted.
- `mech2-116` (10 main cases; `rect_reads_seam`). Main promised: Rect map/pass/reads internals. Deleted: Facet layout solver/measure/renderer (src/layout, src/render, src/client adapter) deleted; CAND/src has only src/ui + vendor/compose; api.md "There is no Facet application, mounting service, render target, solver, rea.... Replacement test: none; the feature is deleted.
- `mech2-14` (11 main cases; `host_move_write_cost`). Main promised: Moving host costs one engine write. Deleted: Facet layout solver/measure/renderer (src/layout, src/render, src/client adapter) deleted; CAND/src has only src/ui + vendor/compose; api.md "There is no Facet application, mounting service, render target, solver, rea.... Replacement test: none; the feature is deleted.
- `mech2-19` (6 main cases; `incremental_layout`). Main promised: Partial solve equals full solve. Deleted: Facet layout solver/measure/renderer (src/layout, src/render, src/client adapter) deleted; CAND/src has only src/ui + vendor/compose; api.md "There is no Facet application, mounting service, render target, solver, rea.... Replacement test: none; the feature is deleted.
- `mech2-56` (15 main cases; `measure_facts_seam`). Main promised: Measure facts seam. Deleted: Facet layout solver/measure/renderer (src/layout, src/render, src/client adapter) deleted; CAND/src has only src/ui + vendor/compose; api.md "There is no Facet application, mounting service, render target, solver, rea.... Replacement test: none; the feature is deleted.
- `mech2-57` (12 main cases; `measure_memo`). Main promised: Measure memo verdicts. Deleted: Facet layout solver/measure/renderer (src/layout, src/render, src/client adapter) deleted; CAND/src has only src/ui + vendor/compose; api.md "There is no Facet application, mounting service, render target, solver, rea.... Replacement test: none; the feature is deleted.
- `mech2-58` (21 main cases; `measure_publish_settle`). Main promised: Measure/publish settle cycle. Deleted: Facet layout solver/measure/renderer (src/layout, src/render, src/client adapter) deleted; CAND/src has only src/ui + vendor/compose; api.md "There is no Facet application, mounting service, render target, solver, rea.... Replacement test: none; the feature is deleted.
- `mech2-59` (11 main cases; `measure_reuse`). Main promised: Cross-solve measure reuse. Deleted: Facet layout solver/measure/renderer (src/layout, src/render, src/client adapter) deleted; CAND/src has only src/ui + vendor/compose; api.md "There is no Facet application, mounting service, render target, solver, rea.... Replacement test: none; the feature is deleted.
- `mech2-60` (26 main cases; `measure_serve`). Main promised: Measure serve of clean rows. Deleted: Facet layout solver/measure/renderer (src/layout, src/render, src/client adapter) deleted; CAND/src has only src/ui + vendor/compose; api.md "There is no Facet application, mounting service, render target, solver, rea.... Replacement test: none; the feature is deleted.
- `mech2-61` (7 main cases; `measure_split`). Main promised: Arrange-only split. Deleted: Facet layout solver/measure/renderer (src/layout, src/render, src/client adapter) deleted; CAND/src has only src/ui + vendor/compose; api.md "There is no Facet application, mounting service, render target, solver, rea.... Replacement test: none; the feature is deleted.
- `mech2-62` (8 main cases; `measure_stamp_seam`). Main promised: Measure stamp seam. Deleted: Facet layout solver/measure/renderer (src/layout, src/render, src/client adapter) deleted; CAND/src has only src/ui + vendor/compose; api.md "There is no Facet application, mounting service, render target, solver, rea.... Replacement test: none; the feature is deleted.
- `mech2-79` (7 main cases; `nameplates_baseline`). Main promised: Nameplate perf baseline. Deleted: Facet layout solver/measure/renderer (src/layout, src/render, src/client adapter) deleted; CAND/src has only src/ui + vendor/compose; api.md "There is no Facet application, mounting service, render target, solver, rea.... Replacement test: none; the feature is deleted.
- `mech2-87` (20 main cases; `node_reuse`). Main promised: Cross-solve layout node store. Deleted: Facet layout solver/measure/renderer (src/layout, src/render, src/client adapter) deleted; CAND/src has only src/ui + vendor/compose; api.md "There is no Facet application, mounting service, render target, solver, rea.... Replacement test: none; the feature is deleted.
- `mech2-96` (4 main cases; `plan_cache`). Main promised: Plan verdict cache. Deleted: Facet layout solver/measure/renderer (src/layout, src/render, src/client adapter) deleted; CAND/src has only src/ui + vendor/compose; api.md "There is no Facet application, mounting service, render target, solver, rea.... Replacement test: none; the feature is deleted.
- `mech3-06` (9 main cases; `render_stats_seam`). Main promised: Render-stats seam. Deleted: controller.stats()/render_stats deleted; no stats API in api.md. Replacement test: none; the feature is deleted.
- `mech3-103` (14 main cases; `zorder_bounded`). Main promised: Bounded z-order walk. Deleted: renderer z-order walk deleted. Replacement test: none; the feature is deleted.
- `mech3-50` (39 main cases; `stack_replay`). Main promised: Stack replay incremental arrange and oracles. Deleted: solver replay deleted. Replacement test: none; the feature is deleted.
- `mech3-51` (10 main cases; `stack_replay_candidates`). Main promised: Indexed stack replay candidates. Deleted: solver replay deleted. Replacement test: none; the feature is deleted.
- `mech3-53` (17 main cases; `structural_incremental`). Main promised: Bounded structural sync and drag retirement census. Deleted: scene structural sync deleted. Replacement test: none; the feature is deleted.
- `mech3-54` (18 main cases; `structural_scope`). Main promised: Boundary-rooted structural solves. Deleted: solver P5 plan deleted. Replacement test: none; the feature is deleted.
- `mech3-81` (37 main cases; `text_settle_closure`). Main promised: Learned-word settle closure and oracle. Deleted: word settle closure deleted. Replacement test: none; the feature is deleted.
- `mech3-82` (17 main cases; `text_settle_probe`). Main promised: Over-cap push probe. Deleted: settle probe deleted. Replacement test: none; the feature is deleted.
- `mech3-92` (30 main cases; `translate_arm`). Main promised: Translate arm. Deleted: solver translate arm deleted. Replacement test: none; the feature is deleted.
- `mech3-93` (17 main cases; `translate_arm_seam`). Main promised: Translate arm seam. Deleted: solver translate arm deleted. Replacement test: none; the feature is deleted.
- `mech3-94` (23 main cases; `translate_host`). Main promised: Translate host coordinate spaces. Deleted: renderer translate hosts deleted. Replacement test: none; the feature is deleted.
- `mech3-95` (19 main cases; `translate_lane`). Main promised: Translate lane. Deleted: renderer translate lane deleted. Replacement test: none; the feature is deleted.
- `navigation-4-57` (1 main case; `tab_customization`). Main promised: Indicators do not reread geometry on idle refresh. Deleted: syncGeometry/contribution bundles deleted; indicators are native children.. Replacement test: none; the feature is deleted.
- `paint-84` (1 main case; `path`). Main promised: Path points change is one write, no re-solve. Deleted: UI.Path and the solver deleted.. Replacement test: none; the feature is deleted.
- `themes-P1-68` (1 main case; `progress_circular`). Main promised: No Path2D or point writes when no circular indicator mounted. Deleted: Facet op log/paths accounting (fake_target ops) deleted; only circular presentation constructs Path2D per media.luau.. Replacement test: none; the feature is deleted.
- `themes-P4-03` (2 main cases; `theme_layer_application`). Main promised: Layer census: layers vs instances published and counted. Deleted: No census/metrics publication for skins in CAND src/ui/themes.luau; not promised in api.md.. Replacement test: none; the feature is deleted.
- `themes-P4-22` (5 main cases; `theme_layers`). Main promised: Layer census per kind, flat package pays nothing, per-target instance counts, whole-stack failover counted once. Deleted: No census in CAND src/ui/themes.luau.. Replacement test: none; the feature is deleted.

### Error handling

1 class b and 23 class c contracts, 140 main cases.

- `mech1-68` (2 main cases; `compose_contract`). Main promised: Formula/watch failure containment and reporting. Engine or Compose owns it: Compose runtime error handling (vendored). Replacement test: yes: Partial: native_compose_contract asserts a failed formula is not hidden and recovers; watch-failure containment (never reaches the writer) is not asserted..
- `apps2-113` (1 main case; `gallery_demo_picker`). Main promised: canvas_group refusal is the framework's own sentence at mount. Deleted: The framework refusal for nested group opacity came from the deleted solver/presenter.. Replacement test: none; the feature is deleted.
- `apps2-84` (6 main cases; `gallery_boot_mode`). Main promised: Unhonourable selectors (empty, wrong type, out of range, two reasons) refuse loudly; registered scenario names. Deleted: examples/gallery/client/boot_mode.luau and its refusal reporting were deleted. init.client.luau now silently falls back: a non-string Facet_Scenario is ignored and an out-of-range Facet_Example indexes nil and boots t.... Replacement test: none; the feature is deleted.
- `collections-08` (2 main cases; `collection_gap_tokens`). Main promised: Unresolvable gutter name and negative gap are refused. Deleted: There is no gap validation. A probe showed that a negative itemSize is accepted silently. api.md makes no refusal promise for values.. Replacement test: none; the feature is deleted.
- `collections-119` (4 main cases; `row_capability_optouts`). Main promised: Capability construction refusals and the flow-wrap refusal. Deleted: rowSelectable/rowMovable/rowDeletable are replaced by selectable/movable/action.enabled. There are no construction refusals and no wrap option.. Replacement test: none; the feature is deleted.
- `collections-163` (4 main cases; `table`). Main promised: The disclose column key and strict column keys. Deleted: Disclosure is automatic. Column keys are not validated (a probe showed an unknown column key is accepted). api.md promises no column-key refusal.. Replacement test: none; the feature is deleted.
- `collections-218` (2 main cases; `table_virtualized`). Main promised: Refusals for virtualized with scrolls=false and virtualized with rowActions. Deleted: scrolls=false forces mode 'all'. rowActions is allowed in every mode.. Replacement test: none; the feature is deleted.
- `collections-220` (3 main cases; `virtual_grid`). Main promised: Grid columns, itemExtent and per-line validation. Deleted: columns is clamped with math.max(1, floor(n)). There is no per-line function. itemSize is a number or readable.. Replacement test: none; the feature is deleted.
- `collections-239` (2 main cases; `virtual_hgrid`). Main promised: Axis refusal and the width refusal. Deleted: direction is only 'horizontal' or anything else. There is no width refusal.. Replacement test: none; the feature is deleted.
- `collections-273` (3 main cases; `virtual_list_measured_extents`). Main promised: Measured-mode refusals. Deleted: measure is a boolean. itemSize is always the estimate. There is no 'measured' string.. Replacement test: none; the feature is deleted.
- `collections-295` (2 main cases; `virtual_list_row_gap`). Main promised: rowGap refusal and dump. Deleted: gap is not validated. dump() is gone.. Replacement test: none; the feature is deleted.
- `collections-52` (4 main cases; `row_actions`). Main promised: Empty action list, unknown ActionSpec keys, coordinator id rules. Deleted: The closed ActionSpec validation and the coordinator id requirement are gone. A probe showed that unknown keys inside an action table are accepted. The coordinator is a plain cell (api.md).. Replacement test: none; the feature is deleted.
- `collections-58` (1 main case; `row_actions`). Main promised: A throwing tray action is contained and still closes. Deleted: execute() in collection_row_actions.luau calls onActivate directly, with no pcall. The tray closes before the call. No containment is promised.. Replacement test: none; the feature is deleted.
- `collections-71` (5 main cases; `row_actions_input`). Main promised: Throwing commits, _commitFirst, pen press, flick momentum frames. Deleted: There is no error containment around onActivate. The private _commitFirst is gone. UIDragDetector owns input-type filtering. The hand-stepped momentum is gone.. Replacement test: none; the feature is deleted.
- `mech2-20` (3 main cases; `inert_placement_warning`). Main promised: Inert placement prop warning. Deleted: placement audit deleted with blueprint props. Replacement test: none; the feature is deleted.
- `mech2-95` (28 main cases; `placement_audit`). Main promised: Placement audit of inert props. Deleted: placement props/audit deleted. Replacement test: none; the feature is deleted.
- `mech3-25` (5 main cases; `runtime_quarantine`). Main promised: Motion clock quarantines throwing callbacks and deferred dispose. Deleted: motion.newClock deleted; Compose animation runtime (vendor) owns scheduling and its error handling. Replacement test: none; the feature is deleted.
- `mech3-45` (42 main cases; `spec_guard_sweep`). Main promised: Guard sweep for deleted boundaries, did-you-mean and non-string keys. Deleted: PopupButton, valueModel, newResourceProvider, adaptive.conditions, inputHint, autoscroll/drag, renderer.attach, motion.newClock and presenter entry points are deleted; the candidate message has no did-you-mean and non.... Replacement test: none; the feature is deleted.
- `navigation-2-42` (2 main cases; `menu`). Main promised: presentation plain-string refusal at construction and out-of-domain reactive refusal on core.lastError. Deleted: Candidate accepts plain strings for presentation (Value<...>) and does not validate the domain; core.lastError no longer exists.. Replacement test: none; the feature is deleted.
- `navigation-5-32` (1 main case; `tab_view`). Main promised: railWidth on a rail-less placement refuses. Deleted: CAND accepts railWidth with any placement and simply does not use it off the sidebar; no refusal in nav_pages.luau and none promised in api.md.. Replacement test: none; the feature is deleted.
- `navigation-5-43` (3 main cases; `tab_view_accessories`). Main promised: Sorted-first refusal, nested rail refusal, accessories-must-be-table. Deleted: CAND iterates accessories with pairs order, does not refuse head/foot on a nested (automatic) TabView (it just never mounts them), and has no explicit table check. api.md promises none of these.. Replacement test: none; the feature is deleted.
- `paint-151` (3 main cases; `typography_weight_roles`). Main promised: Runtime refusal of unknown type roles. Deleted: Runtime refusal retired; media_types TextRole is a typed singleton union checked by tools/check_types.py. (Runtime accepts 'bolder' – see gap above.). Replacement test: none; the feature is deleted.
- `paint-40` (6 main cases; `display_controls`). Main promised: Retired ProgressView refusals (spinner value, height, circular showValue, options without value, capability registry). Deleted: height option removed (unknown option error); circular showValue is now supported and tested ('centers circular readouts when native text bounds fit'); api.md lists no such refusals.. Replacement test: none; the feature is deleted.
- `themes-P4-07` (6 main cases; `theme_layer_application`). Main promised: Asset failure: per-layer grace timer, stack fails/recovers as a unit, fallback hide beats disabled dim, re-sync on asset state. Deleted: No asset-failure/fallback machinery in CAND themes.skin (the `fallback` field in theme_types is vestigial and unread). Not promised in api.md/guides.. Replacement test: none; the feature is deleted.

### Replication and server state

0 class b and 3 class c contracts, 24 main cases.

- `apps2-162` (6 main cases; `platform_adapters_recovery`). Main promised: Replication resnapshot retry and patch.remove. Deleted: src/replication/adapters (collection, ingestPatch, gapDiagnostics) is deleted; api.md has no replication API. Outpost uses full snapshots (native_outpost_terminal convergence cases), which is not the same contract.. Replacement test: a candidate test covers the surviving part (see reason).
- `apps2-223` (1 main case; `replication`). Main promised: Two clients converge on one semantic snapshot with independent presentation state. Deleted: Replication module and Facet environment removed; no candidate equivalent.. Replacement test: none; the feature is deleted.
- `apps2-81` (17 main cases; `replication`, `fuzz_replication`). Main promised: ReplicatedSnapshot/ReplicatedCollection revision, gap, resnapshot rules and Mutation optimistic/confirm/reject/reset. Deleted: Facet's replication module (ReplicatedSnapshot, ReplicatedCollection, Mutation) is gone from CAND src (no match for replicat/mutation in src/ or api.md). docs/guide/06-client-server.md now teaches a hand-rolled reques.... Replacement test: none; the feature is deleted.

## Parts of covered contracts that are retired

These contracts are class a: a candidate test covers them. While the tests were made stronger, one part of each promise was found to be no longer promised. The note gives the evidence. The source is the result file in the parity work folder.

- `apps-125` (Loads headless and a public-API slice mounts, binds and tears down). Status after strengthening: full. Note: VERSION equals the newest changelog entry; Button and Label slice binds, activates and tears down to the census baseline. The Phase-1 Facet.new API is retired. Source: `weakC/surface.result.json`.
- `apps-131` (Mount/unmount laps, scrolled pooled surface and ramp laps return instance count to baseline). Status after strengthening: partial. Note: Three full ramp laps after warm-up return instances, connections and owners to the floor, and teardown returns to baseline. Retired: fake_target parked-handle pool (no recycle pool in the native renderer). The ramp laps use a short ladder of 40, 80 and 120 rows. The declared ceiling rungs (10000 and 40000 rows) take more than a minute headless, so the suite does not run them. Source: `weakC/lifetime.result.json`.
- `apps-140` (Every composite's ref identifies its root once (34 controls)). Status after strengthening: full. Note: native_conformance now also asserts that ref runs exactly once per build and not again on a property update, for every public control. The frozen-record shape is retired: ref receives the native root. Source: `weakC/surface.result.json`.
- `apps-147` (Removing the focused item falls to the nearest surviving neighbor). Status after strengthening: full. Note: Nearest-survivor fallback after removing a general focused node is no longer a live promise. docs/guide/07-input.md:36-39 promises only restoration when the requested collection row exists. docs/reference/api.md:224 promises removeFocusFallback only for Chip, and the candidate covers it. The Compose focus scope clears a removed key (src/vendor/compose/core/focus-scope.luau, the source watch). The new case proves that a removed focused row never leaves selection stranded on a destroyed node. Source: `weakB/result-focus-selection.json`.
- `apps-159` (Entry focus shown for controller, not painted for mouse/touch). Status after strengthening: full. Note: Screen-level entry focus from a presenter is retired: the native GuiService owns screen selection, and docs/guide/07-input.md:17-24 promises entry selection only for modal controls. The new case extends the modal-entry proof to Sheet and Menu for Touch, KeyboardAndMouse and Gamepad. Source: `weakB/result-focus-selection.json`.
- `apps-167` (Stepper is driven by Activate and the Adjust keys). Status after strengthening: full. Note: Space/Return Activate on the selected Decrease/Increase button steps both ways; Period/Comma and bumpers step from a descendant or the root; no Adjust action while focus is outside. Main's claim that arrows only move between the step buttons is retired: the branch binds Left/Right as bounded adjust actions that yield to navigation at the limits (src/ui/inputs.luau adjustment()); api.md says only that keyboard and gamepad adjustment use the input actions of the control. Source: `weakB/result-controls-values.json`.
- `apps-185` (Replacing order keeps focus or lands on nearest survivor). Status after strengthening: full. Note: Focus stays on its key through a VirtualList reorder and replacement and through a Table sort change, and the next Down moves from the new position. For a removed TabView tab, the survivor case already exists in native_navigation. Nearest survivor for collection rows is not promised (see apps-147). Source: `weakB/result-focus-selection.json`.
- `apps-190` (Phone gamepad keeps compact flow through input switches; ten-foot larger geometry). Status after strengthening: full. Note: The input-class switch is asserted. The ten-foot distanceProfile environment fact is retired (no src or api.md reference). Ten-foot geometry is now the gallery viewing-distance UIScale 1.5, which the gallery preview case and native_gallery_shell assert. Source: `weakB/result-navigation-containers.json`.
- `apps-195` (iconOnly renders glyph and refuses incompatible options). Status after strengthening: full. Note: Glyph equals themes.resolveIcon(checkmark), title text empty, FacetLabel kept; absent icon refused for segmented, inline, radioGroup and menu, static and live, and a live replacement that drops the icon errors. Refusing radio + iconOnly is retired: api.md:279 lists iconOnly as a plain Picker option and src/ui/nav_menu.luau:754 only requires an icon on each option. Source: `weakB/result-menus-pickers.json`.
- `apps2-11` (Reset mid-cascade stops the machine and leaves no retiring tile; dispose mid-cascade releases the hook). Status after strengthening: full. Note: Reset mid-cascade: phase idle, empty log, not resolving, no tick hook, score/moves 0, retiring tile destroyed after settle, only live tile ids mounted, machine still plays; dispose mid-swap drops the hook; full census baseline. Source: `weakC/lifetime.result.json`.
- `apps2-129` (Every exit reason lands in the same state; exited terminal sends nothing and re-engages; no transport is a state). Status after strengthening: full. Note: All eight exit reasons from a pending request: exited, reason recorded, request cancelled, onExit once, second exit refused, stale confirm ignored; 20 engage/cancel cycles then send; no-transport gives status rejected (bug fixed). Retired: exit result table {reason, already} is now a boolean. Source: `weakC/lifetime.result.json`.
- `apps2-134` (Console ten-foot profile applies ten-foot facts to the environment). Status after strengthening: full. Note: The profile facts are applied to the native PreferredInput and the ten-foot gamepad policies engage (44 px table rows, inline automatic picker) and revert with desktop facts. The derived type floor (typographyScale 1.5) and overscan are retired with src/env: Facet has no environment module and src/ui has no ten-foot, overscan or text-scale derivation; the engine owns overscan and PreferredTextSize. Source: `weakB/result-adaptive-environment.json`.
- `apps2-145` (Run all sweeps every workload, stop halts between workloads, ramp climbs and stops, reset returns to idle). Status after strengthening: full. Note: An injected failing workload does not end Run all; the final line names the capture count and the JSON output; stop halts between workloads; reset destroys the workload node. The main 'says where it stopped' text is retired: the lab has no such message. Source: `weakC/textperf.result.json`.
- `apps2-155` (Motion-flight pass profiles an animation flight and cleans up). Status after strengthening: full. Note: Motion-flight moves all 24 springs every frame, reports 30 control samples in a valid capture, the idle control reports none, and the next selection destroys the chips and returns heartbeat listeners to baseline. Retired: withAnimation position/size/paint arms no longer exist. Source: `weakC/motion.result.json`.
- `apps2-163` (PreloadAsync receives Instances). Status after strengthening: full. Note: PreloadAsync gets a one-item list holding the retained ImageLabel (an Instance, Image = key); no instance is made or destroyed during preload; ready and failed paths; retired: the temporary per-key target and its destroy, because the ImageLabel is now retained. Source: `weakC/icons.result.json`.
- `apps2-179` (A selected column is released on Cancel, second Activate, or when focus leaves the table). Status after strengthening: full. Note: The Cancel and second-Activate release of a held column is retired: there is no held-column mode. src/ui/collection_table.luau:373-397 gates NarrowColumn/WidenColumn only on the grip's native selection, and docs/reference/api.md (Table, lines 495-530) promises no held mode. The new case proves release when selection moves to another grip and when the table unmounts; the candidate already proves SelectionLost. Source: `weakB/result-focus-selection.json`.
- `apps2-186` (Exit/branch replacement retains fading geometry, then releases). Status after strengthening: partial. Note: Opacity continuity and monotone recovery on reopen mid-exit, retained stroke thickness and points while fading, directional focus never selects the retiring button, release after the fade. Engine refusal of selection on a non-interactable retiring button needs the real engine. Stroke thinning during retirement is retired: api.md does not promise it. Source: `weakC/motion.result.json`.
- `apps2-197` (buttonSurface="none" omits plate with per-item override). Status after strengthening: full. Note: Default keeps the .facet-button plate (resolved BT 0, control/controlSelected fill); none writes BackgroundTransparency 1 on every item, which wins per 05-styling; invalid value throws. Retired: per-item buttonSurface (RadialItem has no such field; api.md does not promise it). Source: `weakC/paint.result.json`.
- `apps2-199` (Empty center puts Back in the ring). Status after strengthening: full. Note: SW (225 deg) is asserted for compass distribution, where src/ui/nav_radial.luau assigns the first free slot from SW. For even distribution the navigation item is appended as the last even sector, so main's 225 deg under 'even' is no longer promised (api.md RadialMenu does not name a slot). The corner launcher-position Back is retired: an empty center puts Back or Close in the ring (nav_radial.luau append()), which satisfies docs/guide/14-choosing-controls.md 'Keep a clear Back path'. The new case asserts that the ring Back stays inside the 90 degree corner arc. Source: `weakB/result-navigation-containers.json`.
- `apps2-210` (Analog preview without commit; semantic Cancel/B closes). Status after strengthening: full. Note: Tab traversal within the ring is retired: Button refuses Tab, and the ring uses directional Radial-* actions (src/ui/nav_radial.luau). The new case proves an analog preview with no commit, ModalBack bound to ButtonB and Escape closing from the root with onClose, and Back returning one branch level before it closes. Source: `weakB/result-input-actions.json`.
- `apps2-226` (Toasts offered in Indicators and retired when leaving the page). Status after strengthening: full. Note: Main retired toasts at once on leaving the page. That promise is retired: tools/lune/parity_blockers.json:229 records that toasts keep expiring on their timers while away, and sponsor_toast.luau does not read ctx.pageVisible. The new case proves the toast layer is hidden while away, the Beneath control still works while a toast shows, and the toasts are gone (visible 0, queued 0, no cards) after their timer while away and on return. Source: `weakB/result-presented-surfaces.json`.
- `apps2-247` (Toasts: content-fit, no focus theft, passthrough, burst cap, read floor, supersede, reverse, edge, dismissal cap, reduced motion). Status after strengthening: live. Note: Burst now asserts exactly one drop and which toast (lowest priority, newest); dismissal cap checked at 0.5 s with counters and instance count back to baseline; reduced motion compares the visible/queued sequence every 0.1 s over 8 s against the animated run. Painted geometry (toast body rect wider than 200 px and title rect with real width) needs real engine layout. Per-dismissal reason codes are not a live promise: examples/gallery/scenarios/sponsor_toast.luau reports only dropped and superseded counters. Source: `weakB/result-presented-surfaces.json`.
- `apps2-263` (Genre/sort/search filtering opens chosen game and clears back to full feed). Status after strengthening: full. Note: Genre chips are replaced by the Genre picker; 'All genres' is the way back to the full feed (examples/virtual_monitors/screens.luau:780). Press-again chip toggle-off is not a live promise. Source: `weakB/result-controls-text-input.json`.
- `apps2-270` (About this rating popover anchors and dismisses). Status after strengthening: full. Note: Outside tap (Touch InputBegan outside panel and anchor) retires the popover; a tap inside the panel does not; placement below the anchor asserted. Source: `weakB/result-presented-surfaces.json`.
- `collections-112` (Gesture rules: axis lock, ties go vertical, half-tray settle, velocity, full swipe, per-edge off, linear tracking). Status after strengthening: full. Note: Asserts ties go vertical, linear tracking inside and past the tray, half-tray settle both edges, full swipe at 70 percent (209 px opens, 210 px commits on a 300 px row), per-edge opt-out and an absent edge staying at zero. The 70 percent threshold is the current src rule (collection_row_actions.luau finish); api.md states no number. Rubber-banding past a tray is not promised by api.md; the drag clamps at the row width. Source: `weakB/result-pointer-touch-drag.json`.
- `collections-17` (Disabled rows are skipped by keyboard navigation). Status after strengthening: full. Note: The tap half ('a skipped row still activates on a tap') is retired. docs/reference/api.md:451-453 lists disabled(item) only as a collection focus option. src/ui/collections.luau makes RowHit non-Interactable, and select() returns early for disabled rows. The new case proves that Table keyboard navigation skips disabled rows and that a tap on one does nothing. Source: `weakB/result-focus-selection.json`.
- `inputs-10` (Build/mount/interact/dispose returns registries to baseline). Status after strengthening: full. Note: TextInput plus reorderable selectable Table: row select, rows change, edit mode, typing, then dispose while the field is focused returns to the full census baseline. Retired: keyboardOcclusionRect environment fact (no Facet environment). Source: `weakC/lifetime.result.json`.
- `inputs-195` (Invalid feedback restarts, respects reduced motion, disposes cleanly and requires a readable). Status after strengthening: full. Note: Rising-edge pulse without geometry change, exact retrace of fresh frames on a mid-pulse restart, reduced motion never pulses, disposal leaves nothing armed, non-readable declarations refused. Retired: the sideways shake is now a UIScale pulse. Source: `weakC/motion.result.json`.
- `inputs-203` (Switch paints its initial value immediately). Status after strengthening: full. Note: Initial on/off paint is immediate and a change repaints the same Mark in place with no frame subscription. The slide animation is retired: api.md Toggle promises no motion and src/ui/inputs.luau updateIndicator writes the Mark position directly. Source: `weakB/result-controls-values.json`.
- `inputs-53` (Chip selection toggles from pointer/touch/keyboard/gamepad). Status after strengthening: full. Note: Probe from the contract asserted for all four inputs: onToggle receives true and the cell stays unchanged; a writable chip toggles in place with facet-selected; the 44 px floor is asserted. Main's hover preview and ten-foot focus profile belonged to the retired adapter; native GuiState and selection images own them now and no doc promises a Facet-side idiom. Source: `weakB/result-controls-values.json`.
- `inputs-76` (Buttons get a shared press haptic by default). Status after strengthening: full. Note: The Facet-owned default 'contact' waveform is retired: docs/reference/api.md:112-114 makes pressHaptic an application-supplied HapticEffect. The case asserts one shared effect on buttons, segments and toggles, and no effect when pressHaptic is absent. Source: `weakB/result-controls-actions.json`.
- `mech1-05` (A candidate whose single-line label would be cut does not fit (DIR-7, M1a/b)). Status after strengthening: full. Note: Exact fit threshold and an author-pinned label that is never replaced. The nested solver ladder is retired with the solver. Source: `weakC/textperf.result.json`.
- `mech1-121` (Locale, accessibility-fact and preferred-input storms stay legal and never remount). Status after strengthening: full. Note: Garbage clamping into size and typography classes is retired with the environment module. The new case storms every PreferredInput value (with mid-scrub input), garbage PreferredTransparency, ReducedMotionEnabled and garbage window extents over 20 seeds and asserts legal row heights, a legal scrim, unchanged node identity and a working radial menu. Source: `weakB/result-adaptive-environment.json`.
- `mech1-32` (Every public constructor is registered and visible to conformance). Status after strengthening: full. Note: The candidate registration and conformance cases cover every public constructor. Grip and hit-floor registration are retired with the primitives. Source: `weakC/surface.result.json`.
- `mech1-42` (Drag-to-edge autoscroll: dwell, innermost host, chain rule, opt-out, disarm). Status after strengthening: full. Note: Asserts that a middle pointer and no drag never scroll, the edge band scrolls, retraction stops at once, the top band scrolls back and clamps at 0, the bottom clamps at the content end, and release stops scrolling. Dwell-before-arm, the nested-scroller chain, pinned-inner transparency and the autoscroll=false opt-out are retired: api.md has no autoscroll option and collection_reorder.luau onFrame scrolls only the collection body with no dwell. Source: `weakB/result-pointer-touch-drag.json`.
- `mech1-83` (Native bindings change only the bound property; disabled inherits through ancestors). Status after strengthening: full. Note: Disabled inheritance through ancestors is covered. Tint is retired: api.md has no tint option, and paint comes from the StyleSheet. Source: `weakC/surface.result.json`.
- `mech1-88` (Text-size preference, touch targets not shrinking, effective target by input). Status after strengthening: full. Note: Sweeps every visible interactive target of 18 public controls under Touch and Gamepad with zero, large and small text bounds. Exposed and fixed undersized Stepper and TextInput clear buttons (see bugs). The pointer factor (0.727) of main is retired: the native floor is a fixed 44 for all inputs (themes.luau controlSizes.regular.height = 44). Source: `weakB/result-adaptive-environment.json`.
- `mech1-91` (Contrast token pairs and focus). Status after strengthening: full. Note: Token pairs >= 4.5 and accent-on-surface >= 3 for neutral and every shipped palette, with the sheet painting those roles. Retired: Facet focus ring; the native GuiService selection highlight draws focus. Source: `weakC/paint.result.json`.
- `mech1-94` (No leaked connection when an instance is reused or rewired). Status after strengthening: full. Note: Reused VirtualList row containers keep the same per-container connection count, fire only the current item's handler once, and keep the census flat across scroll cycles. Retired: 16 named *Conn fields of the deleted screen_target handle adapter. Source: `weakC/lifetime.result.json`.
- `mech2-104` (Modal traps navigation, Cancel/Back restores focus, re-entrant dismiss, churn neutral). Status after strengthening: live. Note: Base-screen document-order initial focus is retired: docs/guide/07-input.md lists GuiService selection as a native mechanism and promises an initial item only for modal controls. New case covers Activate routing to the top modal only and a re-entrant dismiss from an action callback with base activation afterwards. Selection movement and wrap inside the trapped modal (Down twice) needs real engine gamepad selection. Source: `weakB/result-presented-surfaces.json`.
- `mech2-11` (Disposing a core stops watches, child cleanups, is idempotent). Status after strengthening: full. Note: Stopping the mount stops its watches and runs child cleanups once; stop and runtime:dispose are idempotent; mount count returns. Retired: runtime:dispose tearing down live mounts (api.md: stop mounts before dispose; no host-built core). Source: `weakC/lifetime.result.json`.
- `mech2-12` (Shutdown releases pending exit animations immediately (RM on/off)). Status after strengthening: full. Note: With reduced motion off and on, two animated Alerts (one exiting) and a presence region with a pending exit are all released at shutdown: region destroyed, cleanup once, no heartbeat, census baseline. Retired: app presenter toast queue. Source: `weakC/lifetime.result.json`.
- `mech2-63` (Reduced motion snaps state-driven motion). Status after strengthening: full. Note: A live reduced switch lands pop, sheet, row, alert, tab, trail and shimmer on their terminus in the same frame and they stay there. Retired: Facet clock settle events. Source: `weakC/motion.result.json`.
- `mech2-66` (Reduced motion snaps decorative values). Status after strengthening: full. Note: Decorative values snap and hold; informational indeterminate progress keeps stepping quantized under reduced motion. Retired: Facet clock settle-event parity. Source: `weakC/motion.result.json`.
- `mech2-73` (Reduced motion places tween terminus). Status after strengthening: full. Note: Reduced motion places each control at its terminus on the switch frame. Retired: Facet clock:tween informational timers. Source: `weakC/motion.result.json`.
- `mech3-12` (Token schema compiles with contrast report and rejects low contrast). Status after strengthening: full. Note: Asserts all 7 report.contrast entries (pair, measured ratio, ok), per-pair refusal naming field and pair, and refusal of a missing identity, empty or malformed palette, and bad channels. Retired: the renderer property-authority manifest; api.md now says explicit Instance paint overrides stylesheet paint on purpose. Source: `weakC/themepkg.result.json`.
- `mech3-13` (Button padding reaches the engine inset and follows theme metrics). Status after strengthening: full. Note: Per-side contentInsets reach .facet-button::UIPadding and its > UIPadding twin with the 12 px floor, described/busy/disclosure follow; equal packages cost zero SetProperties, metric edits write only the changed rule. Retired: a per-Button padding prop (api.md has none). Source: `weakC/paint.result.json`.
- `mech3-19` (A different node at the same position sweeps the old subtree). Status after strengthening: full. Note: Compose.show and a keyed row put a different Slot at the same position 20 times: old slot and child destroyed, only the new occupant remains, census flat. Retired: weak-table source check of the deleted renderer. Source: `weakC/lifetime.result.json`.
- `mech3-37` (Resource provider lifetime). Status after strengthening: full. Note: One acquire for three consumers, release only on the last, fresh acquire after release, in-flight loader cancel exactly once, late resolve/reject inert, census back to baseline after dispose; retired: newResourceProvider counters and give-up ledger (deleted API). Source: `weakC/icons.result.json`.
- `mech3-48` (A declared compact form is spent before the string is truncated). Status after strengthening: full. Note: The compact form is used at every width below the fit threshold. Sibling priority is retired: Roblox layout distributes the siblings. Source: `weakC/textperf.result.json`.
- `mech3-60` (State tags are removed when state changes; game tags untouched). Status after strengthening: full. Note: Game tags (including a facet- prefixed one) survive role, appearance, selected, busy, disabled, toggle, level, picker and tab changes while state tags follow. Retired: surface-tag swap. Source: `weakC/paint.result.json`.
- `mech3-67` (Compact form rung precedes truncation). Status after strengthening: full. Note: Compact before truncation at every narrower width. The sibling order and minMax floor are retired with the solver. Source: `weakC/textperf.result.json`.
- `mech3-69` (Pointer dwell engagement and touch behavior of explanatory panel). Status after strengthening: full. Note: Touch long-press disclosure is retired: docs/reference/api.md:151-154 says help does not show on touch. Gamepad selection is covered by the native_parity_gaps case. Source: `weakB/result-controls-actions.json`.
- `mech3-87` (Gallery toast continuity, content-fit width, passthrough, edge docking and lifetime). Status after strengthening: live. Note: Retired with the presenter toast service (tools/lune/coverage_mechanisms.json:1139-1146): DisplayOrder bands, safe-area inset, enter slide offsets, transition merging, invalid position/width validation, timeout/manual reason events, and text outside CanvasGroup (examples/gallery/scenarios/sponsor_toast.luau paints cards through a CanvasGroup). Layer ordering is now ZIndex 10 between the page and modal ZIndex 100. Absolute geometry (short and long hug toasts centred at x=195, wrapped long toast taller than the short one) needs real engine layout and TextBounds. Source: `weakB/result-presented-surfaces.json`.
- `mech3-89` (Toast schedule: maxVisible, priority, read floor, supersede, capacity, dismissal). Status after strengthening: full. Note: Retired with the framework scheduler (tools/lune/coverage_mechanisms.json:1139-1146): configurable maxVisible/queueCap, reason strings, boolean dismiss result, and a readFloor default of 2.5 s. The gallery scheduler (examples/gallery/scenarios/sponsor_toast.luau) uses 3 visible, 8 queued, duration 4, readFloor 0.5 and replaces a showing same-subject toast in place at once (asserted by the existing gallery case). Unknown ids are a no-op. Source: `weakB/result-presented-surfaces.json`.
- `mech3-98` (Animated change re-targets on interruption and is immediate under reduced motion). Status after strengthening: full. Note: Interruption retargets from painted values for sheet, row reveal, alert and radial; reduced motion is immediate. Retired: the general withAnimation layout API. Source: `weakC/motion.result.json`.
- `navigation-1-01` (TabView sidebarPreference binding and default persistent sidebar). Status after strengthening: full. Note: The ten-foot viewing-distance override and the collapseSidebar/expandSidebar handle are retired. api.md TabView names neither, and src/ui/nav_pages.luau resolvePlacement has no distance input. Source: `weakB/result-navigation-containers.json`.
- `navigation-1-08` (Menu adapts hierarchy presentation (single panel vs cascade)). Status after strengthening: full. Note: Main's gamepad -> single panel rule is retired: src/ui/nav_menu.luau:292 asSheet is Touch or a narrow extent; gamepad and pointer keep the cascade. Asserted: gamepad and KeyboardAndMouse cascade, Touch sheet at 1280 px, live flips, and explicit presentation overrides in both directions. Source: `weakB/result-menus-pickers.json`.
- `navigation-1-13` (Safe initial selection, trapped navigation, B cancels). Status after strengthening: full. Note: The ten-foot focus-ring profile is retired: no focus profile exists in src or docs. Escape is no longer reserved for Roblox: nav_modal binds ModalBack to Escape and ButtonB by design ('owns the Back input', docs/guide/07-input.md:23). The Tab and Escape reservation is proved by the Button shortcut refusal. The safe cancel-role initial selection is asserted when a destructive action comes first. Source: `weakB/result-focus-selection.json`.
- `navigation-1-33` (Enter fades in, exit removes; card carries panel chrome). Status after strengthening: full. Note: Monotone fade in from 1 to 0 over the declared time, panel tag and fill, monotone fade out, removal at the declared time, immediate without transition. Retired: exit faster than enter (api.md declares one seconds value). Source: `weakC/motion.result.json`.
- `navigation-1-57` (Never blocks: focus unchanged, anchor operable, non-modal, tap-away retires). Status after strengthening: full. Note: The catcher z-order check is retired because the callout has no catcher. Non-modality is asserted directly: no ModalBack, no DismissOutside, no SelectionGroup Stop trap, background selection accepted, and a background shortcut stays enabled and fires. Source: `weakB/result-focus-selection.json`.
- `navigation-2-14` (Disclosure caret reflects expanded state). Status after strengthening: full. Note: The animated 0-to-90 caret rotation is no longer promised: docs/reference/api.md:401-406 names no caret motion, and src/ui/nav_surfaces.luau:61-63 swaps chevron.trailing/chevron.down. The case asserts the caret state for leading and trailing positions through real header activation. Source: `weakB/result-controls-actions.json`.
- `navigation-2-17` (Header vocabulary: appearances, divider only with content, description). Status after strengthening: live. Note: controlSize is not a DisclosureGroup option (src/ui/navigation_types.luau:122-132), so main's rung geometry is not a live promise. Header height growth with a description needs engine AutomaticSize layout; headless asserts AutomaticSize XY, the described padding tag and the Description caption only. Source: `weakB/result-controls-actions.json`.
- `navigation-2-35` (Panel idiom stacks submenus beside the parent). Status after strengthening: full. Note: Fixed defect: the current cascade panel was placed at trigger x + depth*(panelWidth+8) while ancestor levels step 288 px, so a 220 px panel overlapped its 280 px parent by 52 px. Now depth*288. Anchoring to the originating row is retired: every level shares the trigger's top (src/ui/nav_menu.luau:311 and AncestorMenu Position). Source: `weakB/result-menus-pickers.json`.
- `navigation-2-65` (Transparent catcher still dismisses). Status after strengthening: full. Note: The public scrim='none' option is retired (not in docs/reference/api.md); the menu catcher is transparent through backgroundTransparency=1 in src/ui/nav_menu.luau. Source: `weakB/result-presented-surfaces.json`.
- `navigation-2-67` (Default Cancel dismisses the modal). Status after strengthening: full. Note: The HUD-resign half is retired: no passive or engaged responder, engage or resign exists in src or docs. Source: `weakB/result-input-actions.json`.
- `navigation-3-01` (Push/Back mounts routes and releases page-owned resources). Status after strengthening: full. Note: 'Exactly one live page' is retired. api.md NavigationStack says Compose LayerStack owns the retained pages, and nav_pages.luau uses retention='all'. Therefore the test asserts that the covered root is retained (built once) and each popped page is released. Source: `weakB/result-navigation-containers.json`.
- `navigation-3-12` (Caller-owned path push/pop releases every page). Status after strengthening: full. Note: The handle api push/pop/popToRoot/back is retired. api.md says the caller writes path. The equivalent path writes and NavigationBack are asserted. Source: `weakB/result-navigation-containers.json`.
- `navigation-3-18` (Destination fault releases partial content, keeps Back and a sticky error). Status after strengthening: full. Note: Partial content released, FacetError holds the failure text, Back returns to root, census back to baseline. The main sticky error after Back is retired: api.md makes no promise, and the stack clears FacetError when the path becomes valid. Source: `weakC/surface.result.json`.
- `navigation-3-25` (Remount does not accumulate observers; disposal ignores later path writes). Status after strengthening: full. Note: Three stop/remount cycles keep the unmounted and mounted census equal; path writes after disposal build nothing and change no census. Retired: imperative nav.api.push. Source: `weakC/lifetime.result.json`.
- `navigation-3-66` (Badge on unselected option stays live and reactive). Status after strengthening: full. Note: An empty or nil badge now hides with no ' ()' suffix (src/ui/nav_menu.luau already guards badge ~= ''). Main's separate surface='badge' plate node is retired: the branch shows the badge as label text and api.md does not document an option badge plate. Source: `weakB/result-controls-values.json`.
- `navigation-4-04` (Dismissing the popup (tap away / re-tap / Cancel) leaves the selection unchanged). Status after strengthening: full. Note: Picker menu tap away and Back leave the selection and onChange count unchanged. The explicit Cancel row is retired: the native menu panel has no Cancel row and api.md 'Menu and SplitButton' names native Back behavior instead. Source: `weakB/result-menus-pickers.json`.
- `navigation-4-27` (Indicator is present on the initial selection at first paint and follows selection changes). Status after strengthening: full. Note: Underline bar Visible and geometry at first paint and after changes; pill and automatic selected paint (facet-selected, facet-segment-current) on the selected option only. The sliding chip motion of main is retired; the branch pill is the selected option paint. Source: `weakB/result-controls-values.json`.
- `navigation-4-38` (Grabber Resize button cycles detents via tap/Return/ButtonA). Status after strengthening: live. Note: Return is retired: docs/reference/api.md (Sheet) promises the grabber only 'for touch taps, the mouse and the gamepad'. Gamepad ButtonA reaches the grabber only through native engine activation of the selected Grabber TextButton, which needs a live check. Tap, mouse and gamepad-shaped Activated inputs, the detent wrap, Selectable and the single-detent state are asserted headless. Source: `weakB/result-input-actions.json`.
- `navigation-4-41` (Back/ButtonB dismisses and restores focus to the opener). Status after strengthening: full. Note: The ten-foot centered placement is retired: no viewing-distance placement exists in src, and the api.md Sheet section promises none. Source: `weakB/result-focus-selection.json`.
- `navigation-4-49` (Bound presentation reopens and disposal returns to baseline). Status after strengthening: full. Note: Animated Sheet opens, closes with Done (content released, closed census), reopens with fresh content, changes detent, then disposes to baseline with no heartbeat. Retired: dispose writing isPresented false (not in api.md). Source: `weakC/lifetime.result.json`.
- `navigation-5-29` (Dispose is registry-neutral). Status after strengthening: full. Note: Sidebar TabView with badge, sections and accessories: 20 switch round trips after warm-up keep the full census; dispose returns to baseline and retires every accessory. Source: `weakC/lifetime.result.json`.
- `navigation-5-44` (Re-homing disposes the accessory owner). Status after strengthening: full. Note: At the moment of re-home the head accessory cleanup has run once and its node is destroyed; returning builds a new one; all retired on stop. Source: `weakC/lifetime.result.json`.
- `navigation-5-48` (Declared fade crossfades the content layers). Status after strengthening: full. Note: Simultaneous complementary crossfade with { seconds }; false or absent means no fade; old spellings are refused by name. Retired: the { enter = 'fade' } spelling. Source: `weakC/motion.result.json`.
- `paint-09` (Vocabulary tags minted per authored word). Status after strengthening: full. Note: Exact facet tag sets for each appearance word, static and bound, standard mints none; control size is an attribute. Retired: facet-size-* and facet-over-media tags. Source: `weakC/paint.result.json`.
- `paint-101` (State rules ordered after base rules). Status after strengthening: full. Note: Every :Hover/:Press rule outranks its base, Press outranks Hover, text < surface < states < selected < disabled, roles after base, skinned last. Retired: selected below hover and pointer-live gating (native selection outranks pointer states). Source: `weakC/paint.result.json`.
- `paint-103` (Transitions use shared known timing). Status after strengthening: full. Note: For Neutral and every shipped package, under full, reduced and transition=false, exactly the Color3 and Transparency properties transition, on one shared Quad Out timing with the package's normal time or 0. Retired: state-rules-only semantics. Source: `weakC/motion.result.json`.
- `paint-115` (Selected content color is readable and authored). Status after strengthening: full. Note: Content without onSelected, authored onSelected on plate, title and icon of a mounted selected Button, report ratio, and rejection of an unreadable onSelected or plate. Auto-switching to a partner is retired. Source: `weakC/paint.result.json`.
- `paint-135` (Incompatible package refused, screen keeps working). Status after strengthening: full. Note: A package that fails define, one whose build throws, the test-only stub and a missing name are all refused; theme, palette, sheet paint, rule instances, the demo node and its activation stay intact, and a later valid swap works. Retired: schema-version refusal; define no longer reads schemaVersion and api.md does not promise it. Source: `weakC/themepkg.result.json`.
- `paint-19` (Determinate ProgressView maps, clamps, reacts and disposes). Status after strengthening: full. Note: Asserts facet-track/facet-fill tags, Fill1 identity through value changes, clamping at both ends, min/max mapping and disposal. Main's semanticText is not promised by api.md for ProgressView and the branch publishes none. Source: `weakB/result-controls-values.json`.
- `paint-31` (Picker/Disclosure declaration refusals). Status after strengthening: full. Note: Read-only Picker selection, read-only DisclosureGroup expansion, duplicate values and unknown style are refused by name. Refusal of empty options is retired: the candidate accepts an empty options list. Source: `weakC/surface.result.json`.
- `paint-39` (Spinner dots pulse by paint only). Status after strengthening: full. Note: Eight dots (retired: main's five), no Segment9 or bar, fixed square size equal to the spinnerDotSize metric and reactive to it, brightest dot travels, every dot stays visible. Source: `weakC/motion.result.json`.
- `paint-50` (Preferred compact hint art keeps its square across themes). Status after strengthening: full. Note: Preferred ShortcutHint glyph keeps identity and stays iconSize square (20, 28, 16, 20) across live theme swaps; key form height is iconSize + 8; retired: Button compactLabel icon hint (compactLabel is text only now). Source: `weakC/icons.result.json`.
- `paint-85` (Theme-metric stroke thickness). Status after strengthening: full. Note: Ring thickness is max(metric, 8% of diameter) and follows a metric edit, bar track and spinner dots follow their metrics, authored thickness wins, ring paint resolves accent/control. Ten-foot scaling retired. Source: `weakC/paint.result.json`.
- `themes-P1-05` (Shared request renders completion and releases with owner (+dimmed tint)). Status after strengthening: full. Note: Shared resource pending then ready renders completion in both consumers; bound dimmed sets ImageTransparency 0 -> 0.35 -> 0 on its own image only; release after the last owner; retired: the tint-blend-toward-surface form of dim (now ImageTransparency). Source: `weakC/icons.result.json`.
- `themes-P1-21` (Presence mark uses a shared status cutout). Status after strengthening: full. Note: Presence child of the Avatar has the Cutout inner border stroke (tag, thickness 1) and sits in the bottom-right corner at 12px; retired: the separate cutout rect geometry (the cutout is now a stroke). Source: `weakC/icons.result.json`.
- `themes-P1-33` (Refuses bad members and malformed container policies). Status after strengthening: full. Note: Fifteen malformed specs refused (missing or false items, non-dense, empty name or id, bad presence, max 0/1.5/huge, grid, bad overflow, bad form, onOverflow=true, huge rung, unknown option); empty group has no faces or chip; fixed missing form and onOverflow validation; retired: member key field (provider API deleted). Source: `weakC/icons.result.json`.
- `themes-P1-39` (Bound-nil presence survives source replacement). Status after strengthening: full. Note: Keyed face keeps identity through nil -> online -> refused invalid -> nil -> image replacement -> busy; label and mark follow; chip activates; empty roster destroys the face; retired: per-member presenceLabel (not in AvatarMember). Source: `weakC/icons.result.json`.
- `themes-P1-44` (Distinct identities; repeated user ids share one request). Status after strengthening: full. Note: Distinct images keep distinct ImageLabels; repeated userId with one caller shared resource loads once and releases when both leave; image change keeps the keyed face; userId gives the rbxthumb URL; retired: automatic dedupe without a caller resource. Source: `weakC/icons.result.json`.
- `themes-P1-46` (Neutral plate with no focus stop). Status after strengthening: full. Note: Resolved control fill, opaque plate, control corner, padding and content text/icon in every shipped palette, no status tag, nothing selectable. Retired: an explicit neutral status now uses the shared neutral status pair (asserted). Source: `weakC/paint.result.json`.
- `themes-P1-51` (Requires meaningful content and icon-only name). Status after strengthening: full. Note: Fixed: an invalid iconPosition was silently leading (see bugs). Main's refusal of iconPosition without an icon and of a raw asset id as icon are not promised by api.md. Source: `weakB/result-controls-values.json`.
- `themes-P1-65` (Indeterminate head travels at fixed sweep). Status after strengthening: full. Note: Head travels while the sweep stays exactly 90 degrees on every frame. Retired: 'no backing ring' (api.md is silent; RingTrack stays). Source: `weakC/motion.result.json`.
- `themes-P1-77` (Indeterminate view reports animating under reduced motion). Status after strengthening: full. Note: Under reduced motion the cycle keeps a heartbeat subscription, the readout says Loading and the indicator steps. Retired: record.dump().animating. Source: `weakC/motion.result.json`.
- `themes-P2-06` (A nine-slice skin paints the package's own image values). Status after strengthening: full. Note: Parchment panel skin: exact Image, ScaleType Slice, SliceCenter Rect(40,40,104,104), SliceScale; the art has no tags so no StyleRule can tint it, ImageColor3 white and ImageTransparency 0; target skinned; retired: the $ChromeTint sheet token. Source: `weakC/icons.result.json`.
- `themes-P2-40` (Semantic icon map with state variants and namespaced custom names resolves per state). Status after strengthening: full. Note: Undeclared pressed/selected/disabled fall back to default for a framework and a namespaced name, including on a mounted Button; retired: the compiled iconArt sibling (resolveIcon reads the map directly). Source: `weakC/icons.result.json`.
- `themes-P2-44` (The settings gear resolves to the archived asset). Status after strengthening: full. Note: Exact rbxassetid://78513807849201 from an empty registry, the neutral package, a defined package, the ART row, and a mounted Picker LeadingIcon; retired: the ASCII fallback glyph. Source: `weakC/icons.result.json`.
- `themes-P2-49` (Pixel Quest keeps its own pixel art and fills missing action icons from the framework). Status after strengthening: full. Note: menu, flag, facet:search and close resolve; close is the package cross, not framework art; unmapped trash is framework art; retired: ASCII glyph floor for buildWithoutIcons (it now resolves framework art). Source: `weakC/icons.result.json`.
- `themes-P2-50` (Shipped themes draw the real magnifying glass, and package art overrides it). Status after strengthening: full. Note: Exact rbxassetid://85188541706347 on neutral, pixel_quest, compact_pointer and glossy_touch; package override reaches a mounted icon; retired: the ASCII floor. Source: `weakC/icons.result.json`.
- `themes-P2-60` (The icon element resolves, sizes, tags and pixel-samples, and dies with its owner). Status after strengthening: full. Note: ctx.icon resolves and re-resolves, 20x20, transparent, facet-icon tag, Active false, Default then Pixelated resampling, unknown name refused, destroyed on stop and census back to baseline; retired: the ICON_Z_INDEX ladder. Source: `weakC/icons.result.json`.
- `themes-P2-66` (Changing icon content re-renders mounted icons). Status after strengthening: full. Note: Zero Image writes on palette and map-order changes; content change, state-map change and declaration removal re-render the same instances; retired: asset tintRole (paint uses the .facet-icon rule). Source: `weakC/icons.result.json`.
- `themes-P3-01` (Package compiles to a caller-linked native StyleSheet; teardown destroys its rules). Status after strengthening: full. Note: One caller-parented named sheet and exactly one StyleLink referencing it; controls read the same package metrics; stop destroys sheet, every rule, link and button with an instance/connection census back to the pre-install level, while a pre-existing sheet and link stay intact and linked. Retired: theme_controller install/uninstall API. Source: `weakC/themepkg.result.json`.
- `themes-P3-31` (Default/compiled theme data is immutable and cannot be corrupted by callers). Status after strengthening: full. Note: neutralPackage copies are independent at every level; define output is recursively frozen, writes throw, later definition edits do not leak, and a derived package is frozen too. Retired: the shared frozen neutral() snapshot; api.md says neutralPackage returns a mutable copy. Source: `weakC/themepkg.result.json`.
- `themes-P3-33` (Named palette selection). Status after strengthening: full. Note: Selects each declared palette by name on one reactive sheet (3 two-palette packages), the default when unnamed, keeps rule identity. Retired: rejecting an undeclared name; api.md does not promise it, and the sheet falls back to a declared palette (asserted). Source: `weakC/themepkg.result.json`.
- `themes-P4-01` (Layer box geometry: full-bleed fill/frame/tile, four corner ornaments, per-side edge rails, plaque overhang and text insets). Status after strengthening: full. Note: Pins every box: full-bleed fill/frame/tile, four anchored 24px corners with per-corner art, top/bottom and left/right rails stretching on the other axis, plaques overhanging on all four edges, plaque caption padding from textInsets, ZIndex and array order. Retired: canonical edge order; slots are now retained and named per side. Source: `weakC/themepkg.result.json`.
- `themes-P4-16` (A layered recipe using the full vocabulary compiles and renders). Status after strengthening: full. Note: Fantasy Ornate's six-kind panel renders 10 images with each layer's asset, scale type, slice centre, tile size, rail box, distinct corner art, plaque box and caption padding, plus its raised shadow. Retired: define-time layer shape validation; api.md and the skinning guide do not promise it. Source: `weakC/themepkg.result.json`.
- `themes-P4-20` (Per-layer output carries the package's values and suppresses the native plate). Status after strengthening: full. Note: Every layer carries its own asset, scale type, slice centre, slice scale and tile size in array order; per-state maps swap only their layers; facet-skinned suppresses the plate and outranks every fill rule; the tag goes with the art. Retired: corner and outline suppression and per-layer tint rules of the deleted sheet model. Source: `weakC/themepkg.result.json`.
- `themes-P4-24` (v2 value-display and toggle slots are consumed (bar track/fill/cap/center, stepper plate, toggle track/knob)). Status after strengthening: full. Note: Stretchers (barTrack, barFill, toggleTrack, toggleKnob, stepperPlate) fill their part and follow the asset slice; barCap and barCenter keep their declared fixed box when the value changes. A per-slot whole-image classification is retired: docs/guide/10-rich-skinning.md makes slicing follow the asset slice rectangle. Source: `weakB/result-controls-values.json`.
- `themes-P4-48` (Per-state map compiles and a layer takes the same grammar). Status after strengthening: full. Note: A full and a partial state map give the same art on a recipe and on a layer for all five states (unstated states use default); a Button drives selected, hover, pressed and disabled, and disabled beats pressed. Retired: the canonical normalized map and the error-state tag. Source: `weakC/themepkg.result.json`.
- `themes-P5-02` (Reference packages compile to a complete native rule set). Status after strengthening: full. Note: Two compiles of each reference package give identical selector, priority and property lists (over 30 rules); the unnamed sheet paints the default palette; each palette paints distinct colors with the same rule count. Retired: the sheet_model stamp and schema. Source: `weakC/themepkg.result.json`.
- `themes-P5-11` (checkCoverage: covering/uncovering packages, silent compile of namespaced roles). Status after strengthening: full. Note: custom_control and ornate_gauge needs against glossy_mobile, classic_desktop, fantasy_ornate, pixel_quest and glossy_touch, documented color and fields needs on all packages, custom_control.check messages name the package id and role, silent define of namespaced roles. Retired: framework checkCoverage messages naming the package id; api.md does not promise it. Source: `weakC/themepkg.result.json`.
- `themes-P5-14` (Success and warning pairs clear 4.5:1 in every shipped variant). Status after strengthening: full. Note: All 13 shipped variants declare their own success/warning pairs, clear 4.5:1 with the worst pinned (fantasy_parchment/Daylight warning 5.27), are not the accent/content/each other, report entries are ok, and status rules paint them. Retired: the Facet Neutral Light variant, which is no longer shipped. Source: `weakC/themepkg.result.json`.
- `themes-P5-18` (Package icons resolve to art; unmapped names fall to framework art). Status after strengthening: full. Note: Ornate: 6 mapped + 1 namespaced resolve to its own asset content, unmapped menu and more to exact framework art; glossy_touch and compact_pointer declare only facet:search and take exact framework art for increment, decrement and chevron.trailing; retired: ASCII glyph floor. Source: `weakC/icons.result.json`.

## Retirement reasons in the coverage ledgers

The `tools/lune/coverage_*.json` files are the older, spec-level ledger. They give one reason for each retired spec or group of specs.

- `coverage_architecture.json`: 34 retired assertions inside replacement entries (`replacements.*.retiredAssertions`).
- `coverage_collections.json` spec `row_actions_dump_seam`: Dump/dependency-schema source scans described the deleted imperative RowActions engine and its diagnostic facade.
- `coverage_collections.json` spec `row_actions_state`: The separate pointer-state machine, probe radius transport and Facet capture arbitration are replaced with engine UIDragDetector plus local control policy.
- `coverage_collections.json` spec `table_rows_seam`: Source-level dependency surface checks described the deleted table_rows blueprint builder.
- `coverage_collections.json` spec `virtual_extents`: Facet prefix sums, extent indexes and window arithmetic are deleted; Compose.OrderedCollection owns these algorithms.
- `coverage_collections.json` spec `virtual_list_hosted_seam`: Removed controller/contribution attachment plumbing; native controls mount directly into the Compose Roblox runtime.
- `coverage_collections.json` spec `virtual_list_slot_guard`: The Facet blueprint/solver fill-slot refusal is removed. Native instances own sizing; measured content is observed through AbsoluteSize.
- `coverage_collections.json`: 75 retired assertions inside replacement entries (`replacements.*.retiredAssertions`).
- `coverage_gallery.json`: 26 retired assertions inside replacement entries (`replacements.*.retiredAssertions`).
- `coverage_hud.json`: 20 retired assertions inside replacement entries (`replacements.*.retiredAssertions`).
- `coverage_inputs.json` spec `activation_gate`: Generic activation-interception metadata and nested gate traversal were removed with scene composition. Applications express wake/unlock state using ordinary native button callbacks and Compose-controlled presentation.
- `coverage_inputs.json` spec `interaction_tokens`: Deleted shared gesture-classifier/token/velocity facade. Native engine owns gesture transport; the few retained control-specific thresholds and velocity decisions are private policies exercised by native collections tests.
- `coverage_inputs.json` spec `platform_env_binding`: Deleted synthetic environment binding, raw-input primacy overrides, camera/inset coherence belt and platform probe subscriptions. Consumers bind native services and properties directly through Compose.
- `coverage_inputs.json` spec `tap_arm_seam`: All assertions inspect deleted renderer tap-arm dependency records, echo timestamps, metadata dispatch and private armed-session storage. Native controls do not retain that renderer seam.
- `coverage_inputs.json`: 61 retired assertions inside replacement entries (`replacements.*.retiredAssertions`).
- `coverage_mechanisms.json` group `third-reviewed-extraction-and-adapter-mechanisms` (11 specs): Root approved exact thirdReview case inventory; old renderer extraction seams, adapter/style switches, core harness and schema registration were deleted. Native registration/documentation consistency requires separate native contract checks.
- `coverage_mechanisms.json` group `removed-layout-solver-policy-and-diagnostic-contracts` (12 specs): Assertions invoke the deleted Facet solver/environment/blueprint tree, solved rect diagnostics, partial-arrangement counters, waiver sweeps, region negotiation and distance-scale policies. Native Roblox layout and Insets/TextSize replace these mechanisms; this retirement does not claim native pixel geometry equivalence.
- `coverage_mechanisms.json` group `removed-pure-focus-graph-algorithms` (2 specs): Assertions address the removed Facet FocusGraph string-path/group topology API and its pure traversal oracle. Native GuiService selection and Compose collection focus use actual instances and separate behavioral cases.
- `coverage_mechanisms.json` group `removed-old-maintainer-and-authoring-scanners` (2 specs): Deleted scanners require semantic blueprint sizing, token/layout catalogs, old source module inventories and their maintainer obligations. The new native registration/docs contract, import graph, package purity and example builds replace these structural checks.
- `coverage_mechanisms.json` group `removed-text-modifier-facades` (4 specs): Deleted generic UI.textReveal/UI.valueReveal blueprint modifiers, grapheme-state renderer annotation and delayed overflow disclosure manager. Native TextBox/TextLabel properties, caller Compose animation and control-specific full-value disclosure are the current surface; no compatibility facade or old timing manager remains.
- `coverage_mechanisms.json` group `removed-toast-composition-service` (4 specs): Deleted application/presenter toast scheduler, global layer seam and blueprint presentation options. Compose/Engine own presentation lifetimes; Facet no longer owns a global toast composition service.
- `coverage_mechanisms.json` group `removed-theme-inference-and-glyph-shims` (2 specs): Deleted icon ASCII namespace glyph derivation and custom asset decode/inference/failure-ledger mechanisms. Native ImageLabel content and explicit theme asset data replace them.
- `coverage_mechanisms.json` group `removed-primitive-and-host-policy-shims` (3 specs): Divider was a Facet primitive facade (native Frame/style now); matrix_rows selected old simulator presets and asserted deleted Facet environment distance/size classes; mouse_scroll implemented deleted custom scroller pointer/paging shim (native ScrollingFrame/UIPageLayout now). No old facade is preserved.
- `coverage_native_paint.json`: 22 retired assertions inside replacement entries (`replacements.*.retiredAssertions`).
- `coverage_navigation.json` spec `backdrop_fade`: Retires the deleted scene/presenter backdrop-opacity diagnostic; native Alert presence, modal scrim ownership and interrupted transitions are exercised by native_navigation source-transition and dismissal cases.
- `coverage_navigation.json` spec `nav_bar`: Retires the deleted NavigationBar scaffolding constructor and scene geometry assertions; native NavigationStack and TabView own their control chrome, with retained destinations, native Back routing and responsive accessory geometry covered by native_navigation.
- `coverage_navigation.json`: 98 retired assertions inside replacement entries (`replacements.*.retiredAssertions`).
- `coverage_performance.json`: 4 retired assertions inside replacement entries (`replacements.*.retiredAssertions`).
- `coverage_radial.json`: 8 retired assertions inside replacement entries (`replacements.*.retiredAssertions`).
- `coverage_reference_apps.json`: 27 retired assertions inside replacement entries (`replacements.*.retiredAssertions`).
- `coverage_surface.json`: 147 retired assertions inside replacement entries (`replacements.*.retiredAssertions`).
- `coverage_themes_media.json` spec `theme_controller`: Facet install/swap/uninstall controller and its adapter/core settlement lifecycle are deleted. Native StyleLink and reactive StyleSheet are caller-owned Compose nodes.
- `coverage_themes_media.json` spec `theme_drift`: Source-scanning the removed theme-controller and solver constants is obsolete; new architecture boundary checks inspect the native surface.
- `coverage_themes_media.json` spec `theme_paint_repaint`: Renderer dirty/paint operation ordering and adapter fallback repaint paths are deleted. Native StyleRule properties are now materialized directly.
- `coverage_themes_media.json` spec `theme_pixel_content`: Solver-wide snapping of application content and renderer decoration hints are removed. Native image resampling remains a control/theme concern.
- `coverage_themes_media.json` spec `theme_roles`: Custom solver/renderer role expansion, calibrated text-metrics keys and exact Facet geometry fixtures are removed. Native styles and engine text/layout are authoritative.
- `coverage_themes_media.json` spec `theme_selectby`: Imperative theme-controller selection arbitration is removed. Callers select package readables through Compose formulas.
- `coverage_themes_media.json` spec `theme_snapshot`: Frozen environment metric snapshots, solver scaling and metric path resolution façade are removed. Controls read package policy directly; engine computes geometry.
- `coverage_virtual_monitors.json`: 15 retired assertions inside replacement entries (`replacements.*.retiredAssertions`).

## Use this page

- To accept a retirement, remove the promise from `docs/reference/api.md` and the guides, and record it in `CHANGELOG.md`.
- To restore a behavior, add the candidate case id to the contract in `tools/lune/verification_parity.json` and change its class to `a`.
- See [Verification parity](20-verification-parity.md) for the method and the class meanings.
